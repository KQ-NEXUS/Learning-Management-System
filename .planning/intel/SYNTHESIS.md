# Synthesis Summary

Entry point for downstream consumers (`gsd-roadmapper`). Generated by `gsd-doc-synthesizer` in `new` mode.

## Source classifications consumed

6 of 6 classification files in `.planning/intel/classifications/` were consumed. Breakdown by type:

| Type | Count | Docs |
|---|---|---|
| PRD | 1 | `docs/reference/Professional-Training-LMS-PRD-Revision-3-Multi-Gateway-Payments.md` |
| SPEC | 2 | `docs/reference/Professional-Training-LMS-PXR-Revision-3-Multi-Gateway-Payments.md`, `docs/superpowers/specs/2026-09-01-track-a-foundation-design.md` |
| DOC | 3 | `docs/superpowers/plans/2026-09-01-auth-and-courses-slice.md`, `docs/superpowers/plans/2026-09-01-track-a-first-three-hours.md`, `docs/TRACK-A-TASKS.md` |
| ADR | 0 | — |
| UNKNOWN | 0 | — |

Precedence used was the **manifest override**, not the default `ADR > SPEC > PRD > DOC` ordering (explicitly instructed not to let the PXR SPEC outrank the PRD):
0 PRD → 1 PXR (SPEC) → 2 foundation-design (SPEC) → 3 auth-and-courses-slice (DOC) → 4 track-a-first-three-hours (DOC) → 5 TRACK-A-TASKS (DOC).

## Cycle detection

Cross-ref graph: `auth-and-courses-slice.md` → foundation-design.md; `track-a-first-three-hours.md` → foundation-design.md; `TRACK-A-TASKS.md` → PRD, PXR. No cycles found. Traversal depth well under the cap of 50.

## Decisions locked

0. No ADR-type document was present in this ingest batch, so `.planning/intel/decisions.md` contains no entries (placeholder note only).

## Requirements extracted

109 requirement entries in `.planning/intel/requirements.md`, all sourced from the PRD (precedence 0):
IAM-01–06, RBAC-01–08, CAT-01–08, COH-01–07, REG-01–05, PAY-01–07 (§11.6) + PAY-08–14 (§19.3), LRN-01–07, ATT-01–04, ASM-01–07, CRD-01–06, COM-01–04, SUP-01–06, RPT-01–05, NFR-01–14, LIC-01–08.

Note: REQ-PAY-01 ("one approved online payment gateway") is marked superseded by REQ-PAY-08 onward — this is a document-internal revision within the PRD itself (Revision 3 adds multi-gateway support and §19 explicitly says it supersedes the earlier assumption), not a cross-document conflict.

## Constraints extracted

21 constraint entries in `.planning/intel/constraints.md`, extracted at section granularity from the two SPECs (row-by-row extraction was impractical given PXR's length — ~466 lines of UI/workflow tables): 3 entries from `track-a-foundation-design.md` (D1–D4 decisions, file structure, tech/version constraints + success criteria) and 12 entries from the PXR (§1 through §12, including the licence module and multi-gateway checkout sections, and the "decision required" workflow-review flags that mirror PRD §15.3).

## Context topics

4 topic entries in `.planning/intel/context.md`, from the 3 DOC-classified plans: the "no Auth.js" decision, the auth-and-courses implementation plan, the foundation scaffold implementation plan, and the 7-day lead-developer task list.

## Conflicts

**0 blockers, 1 warning, 1 auto-resolved (info).** See `.planning/INGEST-CONFLICTS.md` for full detail.

- WARNING: ambiguous auth-library decision (SPEC folder label "Auth.js" vs. DOC's reasoned "no Auth.js" decision tied to PRD IAM-03) — needs explicit user confirmation before routing.
- INFO: Next.js 15 (SPEC, precedence 2) auto-resolved as winner over Next.js 16 (DOC, precedence 3) per manifest precedence; flagged because the live `package.json` actually pins 16.3.4, contradicting the auto-resolved value.

## STATUS

AWAITING USER — one competing/ambiguous item (the auth-library decision) needs explicit resolution before this intel should be treated as final for ROADMAP.md/REQUIREMENTS.md/PROJECT.md generation. No blockers prevent proceeding, but the WARNING should be reviewed first.

## Files

- `.planning/intel/decisions.md`
- `.planning/intel/requirements.md`
- `.planning/intel/constraints.md`
- `.planning/intel/context.md`
- `.planning/INGEST-CONFLICTS.md`

## Conflict Detection Report

### BLOCKERS (0)

None. No ADR-type documents were present in this ingest batch (0 of 6 classifications), so no LOCKED-vs-LOCKED contradiction is possible. No cycles were detected in the cross-reference graph. No document was classified `UNKNOWN` or `low` confidence.

### WARNINGS (1)

[WARNING] Ambiguous auth-library decision — SPEC folder label vs. DOC's explicit "no Auth.js" decision
  Found: `docs/superpowers/specs/2026-09-01-track-a-foundation-design.md` (SPEC, manifest precedence 2) lists `src/server/auth/` in its file-structure section as "Auth.js configuration, database sessions." `docs/superpowers/plans/2026-09-01-auth-and-courses-slice.md` (DOC, manifest precedence 3) states "Decision: no Auth.js" and gives a reasoned justification: "PRD IAM-03 requires selective and global session revocation. Auth.js's Credentials provider does not support the database session strategy — it forces JWT, and a signed token cannot be withdrawn before it expires." The DOC frames itself as extending the SPEC ("Spec: ...track-a-foundation-design.md — extended by the decision recorded below"), not contradicting it, but the SPEC's own text does name Auth.js specifically.
  Impact: Applying the manifest's literal precedence (SPEC=2 outranks DOC=3) would mechanically favor the SPEC's "Auth.js" folder label — but the DOC's reasoning ties directly to PRD IAM-03 (a MUST requirement for selective/global session revocation) and the SPEC does not address that requirement at all. A downstream roadmapper or builder picking the SPEC's implied library choice over the DOC's reasoned rejection could re-introduce a design that cannot satisfy IAM-03. This was not auto-resolved because doing so mechanically would likely produce a technically incorrect outcome.
  → Confirm explicitly: does the "no Auth.js, hand-rolled database sessions" approach in auth-and-courses-slice.md stand as the actual auth architecture (recommended, since it is the only one of the two that reasons from a specific MUST requirement), or should track-a-foundation-design.md be revised to drop the Auth.js folder label? Either way, update whichever doc is stale before routing this intel into ROADMAP.md/REQUIREMENTS.md.
  → **RESOLVED (2026-09-01, user-confirmed):** "No Auth.js, hand-rolled database sessions" stands as the actual auth architecture. Verified against the live working tree, not just the docs: `src/server/auth/current-actor.ts` has already been edited to remove the Auth.js-named session cookie (`authjs.session-token`) in favor of the hand-rolled `SESSION_COOKIE` exported from `src/server/auth/lockout.ts`, and `src/server/services/auth-service.ts` implements `signIn` against the `Session` model directly. `track-a-foundation-design.md`'s "Auth.js configuration" folder label is stale and should be treated as superseded wherever it's read downstream.

### INFO (1)

[INFO] Auto-resolved by manifest precedence: Next.js 15 (SPEC) over Next.js 16 (DOC)
  Note: `docs/superpowers/specs/2026-09-01-track-a-foundation-design.md` (SPEC, precedence 2) and `docs/superpowers/plans/2026-09-01-track-a-first-three-hours.md` (DOC, precedence 4) both specify "Next.js 15." `docs/superpowers/plans/2026-09-01-auth-and-courses-slice.md` (DOC, precedence 3) specifies "Next.js 16" / "Next.js 16.3.4" in its Tech Stack and Global Constraints sections. Per the manifest's explicit precedence integers (lower wins; 2 beats 3 and 4), Next.js 15 is recorded as the winning value in `.planning/intel/constraints.md`. For transparency: the repository's actual `package.json` currently pins `"next": "16.3.4"`, matching the lower-precedence DOC rather than the higher-precedence SPEC. This is recorded as INFO, not a BLOCKER/WARNING, because it is a version-string disagreement rather than a scope or safety issue — but the roadmapper should reconcile it against the live codebase before locking a tech-stack decision.
  → **RESOLVED (2026-09-01, user-confirmed):** the live codebase is authoritative here — `package.json` pins `"next": "16.3.4"` and the app runs on it. Treat Next.js 16.3.4 as the locked tech-stack value; the SPEC's "Next.js 15" is stale.

---
phase: 04-catalogue-authoring-programmes-courses-modules-lessons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.mts
  - tests/support/harness.ts
  - src/lib/sanitize.ts
  - tests/sanitize.test.ts
autonomous: false
requirements: [CAT-04]

must_haves:
  truths:
    - "Every dependency this phase needs is installed at the version the research verified"
    - "A staff member's lesson HTML is stripped to the D-30 allow-list before it can be stored or rendered"
    - "Component tests written as .tsx files can run without breaking the existing node-mode suite"
  artifacts:
    - path: "src/lib/sanitize.ts"
      provides: "The single sanitisation allow-list for D-30, used on save and on render"
      exports: ["sanitizeLessonBody", "LESSON_BODY_ALLOWED_TAGS"]
    - path: "tests/sanitize.test.ts"
      provides: "Proof that javascript:, data:, protocol-relative, script, h1 and style are stripped"
      min_lines: 40
    - path: "tests/support/harness.ts"
      provides: "Shared grant builder + injectable withPermission for service tests"
      exports: ["grant", "createTestWithPermission"]
    - path: "vitest.config.mts"
      provides: "Two Vitest projects — node (tests/**/*.test.ts) and components (jsdom, tests/components/**/*.test.tsx)"
  key_links:
    - from: "vitest.config.mts"
      to: "tests/components/**/*.test.tsx"
      via: "projects[] entry with jsdom environment"
      pattern: "tests/components"
    - from: "src/lib/sanitize.ts"
      to: "sanitize-html"
      via: "import"
      pattern: "from \"sanitize-html\""
---

<objective>
Install and verify every dependency Phase 4 needs, stand up the test infrastructure the rest of the
phase writes against, and build the HTML sanitiser that D-30 requires on both write and render.

Purpose: every other plan in this phase imports from here. The sanitiser in particular is a
correctness-under-adversity control (stored XSS via `Lesson.body`), so it ships first with its own
test file rather than being bolted onto a UI plan later.
Output: a package set, a two-project Vitest config, a shared test harness, and `src/lib/sanitize.ts`.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-CONTEXT.md
@.planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-RESEARCH.md
@AGENTS.md
@package.json
@vitest.config.mts
@tests/resource-service.test.ts
</context>

<phase_decisions>
Decisions made at planning time that this plan records for the whole phase:

- **Testing strategy (RESEARCH A9, D-32):** adopt the narrow component-test project. A second Vitest
  project running jsdom over `tests/components/**/*.test.tsx` exists solely to prove the D-19 keyboard
  reorder path and the D-26 three-state readiness rendering. Everything else stays node-mode.
- **Integration DB (D-32):** `testcontainers` + `@testcontainers/postgresql` against `postgres:16-alpine`.
  The harness lands in plan 04-05, its first consumer.
- **Sanitiser (Claude's discretion, resolved):** `sanitize-html` >= 2.17.7. DOMPurify is rejected because
  it needs jsdom in the RSC render path, contradicting D-30's own NFR-02 reasoning.
</phase_decisions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for unaudited and flagged packages</name>
  <what-built>Nothing yet. This gate runs BEFORE any install.</what-built>
  <read_first>
    - .planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-RESEARCH.md (section "## Package Legitimacy Audit")
  </read_first>
  <action>
Do not run `npm install` and do not edit `package.json` in this task. Perform only the verification
below, then hand the outcome to the developer and wait. Task 2 installs nothing until this gate returns.
  </action>
  <how-to-verify>
    Nine packages are either `[ASSUMED]` (never audited by slopcheck) or carry a maintenance WARNING.
    Open each of these on npmjs.com and confirm the package page exists, the repository link resolves,
    and the weekly download count is non-trivial:

    Note before you start: `@aws-sdk/s3-request-presigner` is now LOAD-BEARING, not optional. The
    research's assumption A2 planned to stream downloads through the app and left the presigner unused;
    D-36 reverses that — downloads 302 to a presigned URL (60s for files and images, 4h for video per
    D-37) because production storage is
    Cloudflare R2 (D-35), which has a browser-resolvable host. It was in the original slopcheck audit
    (14 OK / 0 SLOP / 0 SUS) so it needs no extra verification here, but do not drop it from the install
    as an unused dependency.

    UNAUDITED — added after the research ran, so slopcheck never saw them:
      1. https://npmjs.com/package/testcontainers
      2. https://npmjs.com/package/@testcontainers/postgresql
      3. https://npmjs.com/package/@testing-library/react
      4. https://npmjs.com/package/jsdom
      5. https://npmjs.com/package/@vitejs/plugin-react
      6. https://npmjs.com/package/@types/sanitize-html
      7. https://npmjs.com/package/@types/clamscan

    FLAGGED WITH A WARNING by the research audit:
      8. https://npmjs.com/package/clamscan — last publish 2024-10-21 (23 months stale). Historical
         advisory GHSA-5v25-xr56-phph was in binary invocation; this phase uses TCP clamd mode only.
      9. https://npmjs.com/package/sanitize-html — GitHub repo archived 2026-02-26, development moved
         into the apostrophecms/apostrophe monorepo; npm releases continued monthly through 2026-08-13.
         Confirm the latest published version is >= 2.17.7.

    Then run, for the postinstall check the research left open (RESEARCH A12):
      npm view sanitize-html clamscan testcontainers @testcontainers/postgresql jsdom scripts.postinstall
    Confirm no package declares a postinstall script you do not recognise.
  </how-to-verify>
  <acceptance_criteria>
    - Developer has stated in their resume message that all nine npm pages resolved and none looked
      like typosquats or AI-hallucinated names.
    - Developer has confirmed `sanitize-html` latest >= 2.17.7.
    - Developer has confirmed the `npm view ... scripts.postinstall` output is acceptable.
  </acceptance_criteria>
  <resume-signal>Type "approved" or name the packages to substitute</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and split Vitest into node and component projects</name>
  <files>package.json, package-lock.json, vitest.config.mts, tests/support/harness.ts</files>
  <read_first>
    - package.json
    - vitest.config.mts
    - tests/resource-service.test.ts (the `harness()` idiom being extracted)
    - tests/with-permission.test.ts
    - .planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-RESEARCH.md (sections "## Standard Stack" and "## Validation Architecture")
  </read_first>
  <action>
Install runtime dependencies at the versions the research verified, using exact-minor ranges:
`@hello-pangea/dnd@^18.0.1`, `@tiptap/react@^3.31.0`, `@tiptap/pm@^3.31.0`, `@tiptap/starter-kit@^3.31.0`,
`sanitize-html@^2.17.7`, `zod@^4.5.4`, `@aws-sdk/client-s3@^3.1124.0`, `@aws-sdk/lib-storage@^3.1124.0`,
`@aws-sdk/s3-request-presigner@^3.1124.0`, `pg-boss@^12.29.0`, `clamscan@^2.4.0`.

Install dev dependencies: `@types/sanitize-html`, `@types/clamscan`, `testcontainers`,
`@testcontainers/postgresql`, `@testing-library/react`, `jsdom`, `@vitejs/plugin-react`.

Do NOT install `react-beautiful-dnd`, `@dnd-kit/*`, `isomorphic-dompurify`, or `minio` — each was
evaluated and rejected in the research's Alternatives table.

Rewrite `vitest.config.mts` to declare two projects. Read the installed Vitest 4.1.11 types
(`node_modules/vitest/dist/config.d.ts` or the package's own docs) to confirm the exact projects API
before writing — do not assume the Vitest 3 `workspace` shape. Project one keeps the current behaviour
verbatim: name `node`, `environment: "node"`, `include: ["tests/**/*.test.ts"]`. Project two: name
`components`, `environment: "jsdom"`, `include: ["tests/components/**/*.test.tsx"]`, plugins
`[react()]` from `@vitejs/plugin-react`. Both projects keep the `@` -> `src` alias that exists today.
The node project must EXCLUDE `tests/components/**` so a `.tsx` file is never picked up twice.

Create `tests/support/harness.ts` exporting two helpers, generalising the private `grant()` and
`harness()` idioms currently duplicated inside `tests/resource-service.test.ts`:
`grant(permission, scopeType = "GLOBAL", scopeId = null): RawGrant` and
`createTestWithPermission(grants: RawGrant[], opts?: { userId?: string })` which returns
`{ withPermission, audits }` built from `createWithPermission` with `getActor` resolving
`{ userId: opts?.userId ?? "user-1" }`, `loadGrants` returning `grants`, and `audit` pushing into
`audits`. Do NOT refactor the existing test files to use it — they pass today and rewriting them is
churn this plan does not need.

Do not add `serverExternalPackages` to `next.config.ts`: `@aws-sdk/client-s3` is already on Next 16's
auto-externalized list.
  </action>
  <verify>
    <automated>npx vitest run tests/resource-service.test.ts tests/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');['@hello-pangea/dnd','@tiptap/react','@tiptap/pm','@tiptap/starter-kit','sanitize-html','zod','@aws-sdk/client-s3','@aws-sdk/lib-storage','@aws-sdk/s3-request-presigner','pg-boss','clamscan'].forEach(d=>{if(!p.dependencies[d])throw new Error('missing '+d)})"` exits 0.
    - `node -e "const p=require('./package.json');['@types/sanitize-html','@types/clamscan','testcontainers','@testcontainers/postgresql','@testing-library/react','jsdom','@vitejs/plugin-react'].forEach(d=>{if(!p.devDependencies[d])throw new Error('missing '+d)})"` exits 0.
    - `grep -c 'react-beautiful-dnd\|@dnd-kit\|isomorphic-dompurify' package.json` outputs `0`.
    - `grep -v '^\s*//' vitest.config.mts | grep -c 'jsdom'` is at least 1.
    - `grep -v '^\s*//' vitest.config.mts | grep -c 'tests/components/\*\*/\*.test.tsx'` is at least 1.
    - `npx vitest run tests/resource-service.test.ts tests/boundary.test.ts` passes — the config split did not break the existing node suite.
    - `tests/support/harness.ts` exports both `grant` and `createTestWithPermission`.
  </acceptance_criteria>
  <done>All phase dependencies are installed, `npm test` still green, and `.tsx` component tests are runnable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The D-30 sanitisation allow-list</name>
  <files>src/lib/sanitize.ts, tests/sanitize.test.ts</files>
  <read_first>
    - .planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-CONTEXT.md (D-29, D-30)
    - .planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-RESEARCH.md (section "### Pitfall 2")
    - node_modules/@types/sanitize-html/index.d.ts (the installed `IOptions` shape — read it, do not assume)
    - src/server/permissions/catalogue.ts (house comment style: explain the WHY, cite the requirement)
  </read_first>
  <behavior>
    - `sanitizeLessonBody("<h1>x</h1>")` returns text with no `<h1>` element.
    - `sanitizeLessonBody('<a href="javascript:alert(1)">x</a>')` returns an anchor with no `href`, or no anchor.
    - `sanitizeLessonBody('<a href="data:text/html,<script>">x</a>')` strips the href.
    - `sanitizeLessonBody('<a href="//evil.com">x</a>')` strips the href (protocol-relative disallowed).
    - `sanitizeLessonBody('<a href="https://ok.example">x</a>')` keeps the href and carries `rel="noopener noreferrer nofollow"`.
    - `sanitizeLessonBody('<p style="x">t</p>')` returns `<p>t</p>` — no `style` attribute survives.
    - `sanitizeLessonBody("<script>alert(1)</script>")` returns a string containing neither `<script` nor `alert(1)`.
    - `sanitizeLessonBody("<h2>a</h2><h3>b</h3><ul><li>c</li></ul><ol><li>d</li></ol><strong>e</strong><em>f</em><p>g</p>")` round-trips every one of those eight tags.
    - Idempotence: `sanitizeLessonBody(sanitizeLessonBody(x)) === sanitizeLessonBody(x)` for every case above — this is what makes D-30's second pass on render safe.
  </behavior>
  <action>
Write `tests/sanitize.test.ts` FIRST, covering every case in `<behavior>`. Run it and watch it fail.

Then create `src/lib/sanitize.ts` as the single source of truth for D-30. Export a frozen
`LESSON_BODY_ALLOWED_TAGS` equal to exactly `["h2","h3","p","ul","ol","li","strong","em","a"]` — nine
entries, no `h1` (D-29: the page title owns H1). Export `sanitizeLessonBody(dirty: string): string`
calling `sanitizeHtml` with: `allowedTags` set to that constant, `allowedAttributes` of
`{ a: ["href","rel","target"] }`, `allowedSchemes` of `["http","https","mailto"]`,
`allowedSchemesAppliedToAttributes: ["href"]`, `allowProtocolRelative: false`,
`disallowedTagsMode: "discard"`, and a `transformTags` entry using
`sanitizeHtml.simpleTransform("a", { rel: "noopener noreferrer nofollow", target: "_blank" })`.

The exported options object must NOT be mutable from outside the module — do not export it.

Head the file with a comment stating: this function is called on save AND again on render (D-30); the
allow-list is the reason four of the five 2026 `sanitize-html` advisories are unreachable here; growing
it past nine tags requires re-reading the advisory analysis in `04-RESEARCH.md` Pitfall 2.
  </action>
  <verify>
    <automated>npx vitest run tests/sanitize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sanitize.test.ts` passes with at least 9 assertions covering every `<behavior>` bullet.
    - `grep -c '"h1"' src/lib/sanitize.ts` outputs `0`.
    - `grep -c 'noopener noreferrer nofollow' src/lib/sanitize.ts` is at least 1.
    - `grep -c 'allowProtocolRelative' src/lib/sanitize.ts` is at least 1.
    - `src/lib/sanitize.ts` contains no `export` of the raw options object (only `sanitizeLessonBody` and `LESSON_BODY_ALLOWED_TAGS` are exported).
    - `npx eslint src/lib/sanitize.ts` reports no errors.
  </acceptance_criteria>
  <done>Lesson HTML cannot carry a script, a style attribute, an H1, or a non-http(s)/mailto href, proven by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repository | Third-party code enters the build. A typosquat or hijacked package executes at install and at runtime. |
| staff browser → `Lesson.body` → learner browser | Staff-authored HTML is stored and re-rendered to other users. Stored XSS crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | npm install of 18 packages | mitigate | Task 1 blocking human gate over the 9 unaudited/flagged packages; `npm view <pkg> scripts.postinstall` inspected before install |
| T-04-02 | Elevation of Privilege | `sanitizeLessonBody` | mitigate | Nine-tag allow-list, `allowedSchemes` restricted to http/https/mailto, `allowProtocolRelative: false`, idempotence test proving the render-time second pass is safe |
| T-04-03 | Information Disclosure | anchor `target="_blank"` in lesson body | mitigate | `rel="noopener noreferrer nofollow"` forced by `simpleTransform` — prevents reverse tabnabbing on the opener window |
| T-04-SC | Tampering | supply chain | mitigate | slopcheck audit inherited from `04-RESEARCH.md` (14 OK / 0 SLOP / 0 SUS) plus the Task 1 blocking-human checkpoint for the 9 packages that audit did not cover |
</threat_model>

<verification>
- `npx vitest run tests/sanitize.test.ts` passes.
- `npm test` still passes the pre-existing 84 tests (the node project's include glob is unchanged).
- `npx eslint .` is clean.
</verification>

<success_criteria>
- All 18 packages present in `package.json` at the researched versions; no rejected alternative installed.
- `vitest.config.mts` declares a `node` and a `components` project; `.tsx` under `tests/components/` runs under jsdom.
- `src/lib/sanitize.ts` is the only place in the repository that configures `sanitize-html`.
- `tests/support/harness.ts` exports `grant` and `createTestWithPermission`.
</success_criteria>

<output>
Create `.planning/phases/04-catalogue-authoring-programmes-courses-modules-lessons/04-01-SUMMARY.md` when done
</output>

---
phase: 07-multi-gateway-payments-paystack-manual-refunds
plan: 11
type: execute
wave: 7
depends_on: ["07-09", "07-10"]
files_modified:
  - tests/checkout-phase-invariants.test.ts
  - tests/support/cohort-fixtures.ts
  - prisma/schema.prisma
  - prisma/migrations/20260912150000_remove_legacy_cohort_price/migration.sql
  - prisma/seed.ts
  - .env.example
  - README.md
autonomous: false
requirements: [COH-02, PAY-08, PAY-14, PAY-16]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No runtime code path reads Cohort.priceMinor or Cohort.currency — checkout, cohort administration, publication readiness, the public catalogue and receipts all read the dual-price columns or the Order's own snapshot (COH-02)."
    - "An AST-based invariant fails the build if a legacy Cohort price read is reintroduced, and it does not flag Order.amountMinor, which intentionally remains the learner total."
    - "The legacy columns are dropped only after the invariant passes against application code and every Cohort row carries at least one migrated price — a precondition checked by the migration itself, not assumed."
    - "Deployment configuration documentation names every variable this phase added: Paystack test key, subaccount code, Stripe connected account id, both webhook URLs, and the fee-schedule seeding step (PAY-14)."
    - "No credential value appears in .env.example, README.md, a test fixture, or a migration file — only placeholder shapes (PAY-14, D-02)."
  artifacts:
    - "tests/checkout-phase-invariants.test.ts — a third AST invariant covering legacy Cohort price reads"
    - "prisma/migrations/20260912150000_remove_legacy_cohort_price/migration.sql — the guarded cleanup migration"
    - "README.md and .env.example — the full multi-gateway deployment configuration section"
  key_links:
    - "The legacy-reference invariant -> the cleanup migration's go/no-go: the columns are dropped only once no application code reads them"
    - "The migration's precondition check -> every Cohort row, so a deployment with an unmigrated Cohort fails the migration rather than silently losing its price"
---

<objective>
Remove the transitional single-price columns, and prove mechanically that nothing reads them, before
dropping anything. Document the full deployment configuration this phase introduced.

Purpose: the dual-price model has been the source of truth since 07-05, but the legacy
`Cohort.priceMinor`/`currency` pair is still written and could still be read. Dropping columns is the
one genuinely irreversible action in this phase, so it goes last, behind an executable proof and an
explicit decision.
Output: a clean legacy-free codebase, a guarded cleanup migration, and complete configuration docs.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-RESEARCH.md
@tests/checkout-phase-invariants.test.ts
@prisma/schema.prisma
</context>

<global_constraints>
- **Never run `git commit`.** Stage with `git add` and report a suggested commit message in the
  SUMMARY. The repository owner creates every commit explicitly.
- Integer minor units only; no floating-point money math (D-24).
- The invariant is AST-based, never a raw-text scan — the file's own header comment explains that a
  regex false-positives on a comment or a doc string and is exactly why a gate like this gets deleted
  six months later.
</global_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Legacy-reference invariant and the last remaining legacy reads</name>
  <files>tests/checkout-phase-invariants.test.ts, tests/support/cohort-fixtures.ts</files>
  <read_first>
    - `tests/checkout-phase-invariants.test.ts` in full — the two existing AST invariants, their `ts.isPropertyAssignment` / `ts.isStringLiteral` node checks, the directory-prefix exemption pattern, and both fixture tests that prove each invariant can actually fail
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-RESEARCH.md` — the Task 9 annotation on following the AST-based, not regex-based, discipline
  </read_first>
  <behavior>
    - The invariant reports a violation for a property access of `priceMinor` or `currency` on a value whose declared or inferred shape is a Cohort, anywhere under `src/`.
    - It does not flag `Order.amountMinor` — the learner total intentionally keeps that name and meaning.
    - It does not flag `Order.currency` or `PaymentAttempt.currency`, which are snapshot columns this phase keeps.
    - It does not flag a mention of the legacy field name inside a comment or a string literal, because the walk is AST-based.
    - A fixture test proves the invariant fails, naming the offending file, when a legacy read is deliberately introduced.
    - A fixture test proves an allow-listed compatibility site, if any remains, is exempt by directory prefix rather than by filename.
    - Run against the real tree, the invariant reports zero violations.
  </behavior>
  <action>
Extend `tests/checkout-phase-invariants.test.ts` with a third invariant following the same AST-walk
shape the two existing ones use. Reuse `walkSourceFiles` and `parse`; do not add a second file-walk
implementation, for the same reason 07-RESEARCH.md's Pitfall 5 gives for the provider-isolation scan.

Because `currency` is a common property name across several models, scope the check narrowly: flag a
read only where the object expression resolves to a Cohort-shaped value — a `cohort.` / `cohorts[i].`
member access, a destructure from a `cohort.findUnique`/`findMany` select, or an explicitly
Cohort-typed parameter. Narrow deliberately and document the narrowing in a comment: a broader check
that flagged every `.currency` in the codebase would be deleted the first time it blocked a legitimate
Order read.

Add both fixture tests, mirroring the existing ones, so the new invariant is proven capable of failing
rather than trivially green.

Run it against the real tree and fix whatever it finds. Expect remaining reads in Phase 5/6 fixtures
and any compatibility fallback 07-05 deliberately left in place. Update `tests/support/cohort-fixtures.ts`
so fixtures construct the dual-price columns; keep the fixture builder's existing call signature
working for every pre-existing caller rather than forcing a repo-wide rewrite.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/checkout-phase-invariants.test.ts</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for `tests/checkout-phase-invariants.test.ts`, or the real-tree case reports one or more legacy Cohort price reads.</fails_when>
  </verify>
  <acceptance_criteria>
    - The new invariant is added to the existing file, sharing `walkSourceFiles` and `parse`; no second walk implementation exists.
    - Both fixture tests exist and demonstrate the invariant failing and exempting correctly.
    - The real-tree run reports zero violations.
    - `Order.amountMinor`, `Order.currency` and `PaymentAttempt.currency` are proven unflagged by an explicit fixture case.
    - The two pre-existing invariants still pass unchanged.
  </acceptance_criteria>
  <done>Nothing under `src/` reads a legacy Cohort price, and a regression would fail the build.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve dropping the legacy Cohort price columns</name>
  <read_first>
    - Task 1's invariant output against the real tree
    - `prisma/schema.prisma` — `Cohort.priceMinor` (non-nullable) and `Cohort.currency`
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md` — D-08
  </read_first>
  <reversibility rating="one-way">
Dropping a column destroys its data. Restoring it means a new migration plus reconstructing every
row's value from a backup — there is no revert commit that brings the data back. Everything else in
this phase was additive.
  </reversibility>
  <decision>Drop `Cohort.priceMinor` and `Cohort.currency` now, or defer the drop to a later phase?</decision>
  <context>
Before presenting this checkpoint, Claude has already gathered the two facts the decision turns on and
must state both as numbers:

1. Task 1's legacy-reference invariant result against the real tree — expected: zero legacy Cohort
   price reads anywhere under `src/`.
2. The count of Cohort rows where both `priceNgnMinor` and `priceUsdMinor` are null — the set that
   would lose its price entirely. This must be zero before the drop is safe at all; if it is not,
   the decision is defer regardless of preference, and the affected cohort ids are listed.

07-CONTEXT.md's D-08 backfill already copied every legacy value into its matching currency rail, so on
a correctly-migrated deployment the drop removes nothing that is not already duplicated. Every other
schema change in this phase was additive and revertible; this one is not.
  </context>
  <options>
    <option id="drop-now">
      <name>Drop the legacy columns in Task 3</name>
      <pros>The schema states one truth about a Cohort's price; no future contributor can reintroduce a read of a column that no longer exists; the transitional dual-write in `cohort-service.ts` and `prisma/seed.ts` goes away with it</pros>
      <cons>Irreversible — the data is gone, and restoring it means a new migration plus a backup restore. Any consumer outside this repository reading those columns breaks immediately</cons>
    </option>
    <option id="defer">
      <name>Leave the legacy columns in place, keep writing them, revisit later</name>
      <pros>Costs nothing but schema clutter; keeps a recovery path open if anything outside this repository still reads the old shape</pros>
      <cons>The dual-write stays in `cohort-service.ts` and the seed indefinitely; the invariant is the only thing stopping a read from creeping back, and dead-but-populated columns invite exactly that</cons>
    </option>
  </options>
  <resume-signal>Reply "drop-now" or "defer". On "defer", Task 3 performs only its documentation half and records the deferral as a carried-forward item — it does not proceed with the migration anyway.</resume-signal>
  <acceptance_criteria>
    - The decision is recorded as approve or defer, never as "proceed unless told otherwise".
    - Both evidence counts are stated as numbers in the SUMMARY.
    - If any Cohort has neither rail priced, the decision is recorded as defer regardless of preference, and the affected cohort ids are listed.
  </acceptance_criteria>
  <done>The one irreversible action in this phase is explicitly approved or explicitly deferred.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Guarded cleanup migration and deployment configuration documentation</name>
  <files>prisma/schema.prisma, prisma/migrations/20260912150000_remove_legacy_cohort_price/migration.sql, prisma/seed.ts, .env.example, README.md</files>
  <read_first>
    - Task 2's recorded decision
    - `prisma/migrations/20260912120000_dual_currency_split_settlement/migration.sql` (07-02) — the backfill this cleanup depends on
    - `README.md` — the existing configuration/setup section's structure and depth
    - `.env.example` — the entries 07-01 added
  </read_first>
  <precondition>Task 2 recorded an explicit approval. If the decision was defer, perform only the documentation half of this task and record the migration as deliberately not run.</precondition>
  <action>
**If Task 2 approved the drop.** Remove `priceMinor` and `currency` from `model Cohort` in
`schema.prisma`, remove the legacy write from `cohort-service.ts`'s create/update path and from
`prisma/seed.ts`, and create the cleanup migration. Open the migration with a guard that fails the
migration rather than proceeding when any Cohort has neither rail priced — for example a `DO $$ ...
RAISE EXCEPTION ... $$` block counting such rows. A migration that silently drops the only price a
Cohort had is exactly the outcome this guard exists to prevent, and a comment in the file should say
so. Then run `npx prisma migrate dev --name remove_legacy_cohort_price` (explicit `--name` so Prisma
does not prompt) and `npx prisma generate`.

This step is blocking: `npx tsc --noEmit` and `npx next build` both type against `schema.prisma`
rather than the live database, so leaving the migration unapplied would report green while the live
database still carries the columns.

**Documentation, regardless of the decision.** Add a multi-gateway configuration section to
`README.md` covering: the Paystack test secret key and subaccount code, the Stripe connected account
id, both webhook URLs (`/api/webhooks/stripe` and `/api/webhooks/paystack`) and where each is
registered, the `reconcile-payments` scheduled function and its cadence, and the fee-schedule seeding
step with a pointer to `prisma/seed.ts` as the place a deployment's own approved rates are configured
(D-11 — the seeded public rates are examples, not permanent truth). Confirm `.env.example` lists every
variable with a placeholder shape only.

Write no credential value into any of these files.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/checkout-phase-invariants.test.ts tests/schema-cohort.test.ts tests/schema-payment-split.test.ts</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for any of the three files, or a schema-delta assertion still finds `priceMinor` on `Cohort` after an approved drop.</fails_when>
  </verify>
  <acceptance_criteria>
    - On approval: `npx prisma migrate status` reports nothing pending, and `Cohort` no longer declares `priceMinor` or `currency`.
    - The cleanup migration opens with a guard that raises rather than proceeding when any Cohort has neither rail priced.
    - On deferral: no migration was created, the legacy write path is intact, and the SUMMARY records the deferral as a carried-forward item.
    - `README.md` documents both webhook URLs, all three new environment variables, the scheduled function and its cadence, and the fee-schedule seeding step.
    - `git diff .env.example README.md` contains no value resembling a real key, subaccount code or account id.
  </acceptance_criteria>
  <done>The legacy columns are gone (or explicitly deferred), and a new deployment can be configured from the documentation alone.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**Removed Prisma columns** (on approval)
- `Cohort.priceMinor`, `Cohort.currency`

**New files**
- `prisma/migrations/20260912150000_remove_legacy_cohort_price/migration.sql`

**New test invariants**
- a third AST invariant in `tests/checkout-phase-invariants.test.ts` covering legacy Cohort price reads

**Documentation**
- `README.md` multi-gateway configuration section
- `.env.example` confirmed complete for this phase

**New route handlers, React components, env vars:** none.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration -> production data | A column drop destroys data irreversibly |
| documentation -> source control | Configuration docs are a common accidental credential-leak surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-14 | Denial of Service / Tampering | dropping columns a code path still reads | medium | mitigate | The AST invariant must report zero violations against the real tree before the migration is written, and a migration-level guard raises rather than proceeding when any Cohort has neither rail priced |
| T-07-47 | Repudiation | an irreversible drop performed without a recorded decision | high | mitigate | A `checkpoint:decision` gate with the two evidence counts recorded in the SUMMARY; the deferral branch is a real branch, not a formality |
| T-07-05 | Information Disclosure | `README.md`, `.env.example` | high | mitigate | Placeholder shapes only; an explicit diff-review acceptance criterion; no credential value in any migration, fixture or doc (PAY-14) |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run tests/checkout-phase-invariants.test.ts tests/schema-cohort.test.ts tests/schema-payment-split.test.ts` exits 0.
- The legacy-reference invariant reports zero violations against the real tree.
- On approval, `npx prisma migrate status` reports nothing pending and `Cohort` carries only the dual-price columns.
</verification>

<success_criteria>
- No application code reads a legacy Cohort price, enforced mechanically.
- The irreversible drop happened only behind an explicit recorded approval and a data-safety guard, or was explicitly deferred.
- A new deployment can be configured for both gateways from the documentation alone, with no credential in source control.
</success_criteria>

<output>
Create `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-11-SUMMARY.md` when done.
</output>

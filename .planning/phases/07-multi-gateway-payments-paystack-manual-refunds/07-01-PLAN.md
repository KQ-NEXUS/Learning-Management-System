---
phase: 07-multi-gateway-payments-paystack-manual-refunds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - src/server/payments/settlement-config.ts
  - tests/payments-settlement-config.test.ts
autonomous: false
requirements: [PAY-13, PAY-14, PAY-17]
user_setup:
  - service: paystack
    why: "NGN split settlement to the school's subaccount (D-02, D-03, PAY-17). No LMS code can create a Paystack account or subaccount — D-05 puts onboarding outside the LMS."
    env_vars:
      - name: PAYSTACK_SECRET_KEY
        source: "Paystack Dashboard -> Settings -> API Keys & Webhooks -> Test Secret Key (sk_test_...)"
      - name: PAYSTACK_SUBACCOUNT_CODE
        source: "Paystack Dashboard -> Settings -> Subaccounts -> create a test subaccount, copy the returned subaccount_code (ACCT_...)"
    dashboard_config:
      - task: "Register the webhook URL so charge.success reaches this deployment"
        location: "Paystack Dashboard -> Settings -> API Keys & Webhooks -> Test Webhook URL -> {BASE_URL}/api/webhooks/paystack"
      - task: "Record the subaccount's creation-time percentage_charge value (read-only for the LMS, D-05) so the recorded settlement expectation can be sanity-checked"
        location: "Paystack Dashboard -> Settings -> Subaccounts -> the created subaccount"
  - service: stripe
    why: "USD Connect destination charge to the school's connected account (D-02, D-04, PAY-17). The stripe npm package is already pinned at 22.6.1 and human-approved in Phase 6 P01 — only the connected account is new."
    env_vars:
      - name: STRIPE_CONNECTED_ACCOUNT_ID
        source: "Stripe Dashboard (test mode) -> Connect -> Accounts -> create/onboard a test connected account, copy its acct_... id"
    dashboard_config:
      - task: "Record the COUNTRY registered on the KQ NEXUS platform account and on the new connected account — these two values decide whether payment_intent_data.on_behalf_of is required (07-RESEARCH.md Pitfall 3 / Open Question 2)"
        location: "Stripe Dashboard -> Settings -> Business details (platform), and Connect -> Accounts -> the connected account -> Country"

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every provider secret and school account identifier this phase needs (PAYSTACK_SECRET_KEY, PAYSTACK_SUBACCOUNT_CODE, STRIPE_CONNECTED_ACCOUNT_ID) is read only through one server-side module and never reaches a client bundle, an export, an audit detail row, or a learner-facing page (D-02, PAY-14)."
    - "When a required school settlement identifier is absent or blank, the config reader raises a typed, named error rather than returning a placeholder — checkout fails closed with a support route instead of initiating a payment that cannot settle (D-05)."
    - "The Stripe refund reversal policy (reverse_transfer true or false) and the Paystack split-refund corrective path are recorded as an explicit decision with a rationale before any refund code is written (D-22, D-23, PAY-13, 07-RESEARCH.md Open Question 1)."
    - "Whether payment_intent_data.on_behalf_of is required for this deployment is decided from the two real registered account countries, not assumed either way (07-RESEARCH.md Open Question 2, COVERAGE.md connect_on_behalf_of)."
  artifacts:
    - "src/server/payments/settlement-config.ts — the single server-side reader for provider secrets and school settlement identifiers"
    - "tests/payments-settlement-config.test.ts — proves the fail-closed behaviour and the absence of any fallback placeholder"
    - ".env.example — documents every new variable with a test-mode-only example shape"
  key_links:
    - "settlement-config.ts -> process.env only; no @prisma/client import, no provider SDK import, so it can be imported from both provider adapter directories without breaking PAY-09 isolation"
    - "The recorded refund-reversal decision -> 07-08's refund-service.ts, which must pass reverse_transfer explicitly rather than relying on Stripe's implicit default"
---

<objective>
Provision the provider settlement credentials this phase cannot create for itself, resolve the two
one-way policy questions 07-RESEARCH.md left open, and land the single fail-closed server-side
module every later plan reads those values through.

Purpose: 07-RESEARCH.md's Environment Availability table names three credentials with **no fallback**
(`PAYSTACK_SECRET_KEY`, `PAYSTACK_SUBACCOUNT_CODE`, `STRIPE_CONNECTED_ACCOUNT_ID`) and two open
questions whose wrong default is expensive to unwind after real settlements exist. Phase 6 solved the
identical shape in 06-01 with a human credentials gate before any provider code was written; this
plan is that gate for Phase 7.
Output: a working `.env.local` for the executor's environment, two recorded decisions, and
`src/server/payments/settlement-config.ts`.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-RESEARCH.md
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/COVERAGE.md
@src/server/payments/providers/stripe/client.ts
</context>

<global_constraints>
- **Never run `git commit`.** Stage work with `git add` and report a suggested commit message in the
  SUMMARY. The repository owner creates every commit explicitly. This overrides any default
  autonomous-commit behaviour in the execute-plan workflow.
- Before editing any Next.js-framework-touching code (route handlers, server actions, scheduled
  function wiring), read the relevant guide under `node_modules/next/dist/docs/` per `AGENTS.md` —
  this repository tracks a customized Next.js fork whose APIs may differ from upstream.
- All monetary arithmetic is integer minor units with deterministic rounding. No floating-point
  money math anywhere in domain or provider code (D-24).
</global_constraints>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Provision Paystack and Stripe Connect test-mode settlement accounts</name>
  <read_first>
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-RESEARCH.md` — "Environment Availability" table and "Pitfall 3"
    - `.env.example` (current contents, to see the established documentation convention for Phase 6's Stripe variables)
  </read_first>
  <action>Provision the Paystack test-mode account plus school subaccount, and the Stripe test-mode connected account, then report both Stripe account countries.</action>
  <instructions>
Claude cannot create a Paystack merchant account, a Paystack subaccount, or a Stripe Connect connected
account. D-05 puts school onboarding and capability verification outside the LMS entirely, and no API
this codebase may call substitutes for it — so this is genuinely human-only work, not automatable
work being delegated.

1. Paystack test mode: open the KQ NEXUS Paystack account and copy the **Test Secret Key**
   (`sk_test_...`) into `.env.local` as `PAYSTACK_SECRET_KEY`.
2. Paystack test mode: create a subaccount representing this deployment's one school (business name,
   settlement bank, account number, `percentage_charge`). Copy the returned `subaccount_code`
   (`ACCT_...`) into `.env.local` as `PAYSTACK_SUBACCOUNT_CODE`. Per D-05 the LMS never calls the
   subaccount-creation endpoint, so this value can only come from the dashboard.
3. Paystack test mode: set the Test Webhook URL to `{BASE_URL}/api/webhooks/paystack`.
4. Stripe test mode: create/onboard a test connected account under the KQ NEXUS platform account and
   copy its `acct_...` id into `.env.local` as `STRIPE_CONNECTED_ACCOUNT_ID`.
5. Report two facts Task 2 needs: the **country registered on the KQ NEXUS Stripe platform account**
   and the **country registered on the new connected account**.

Record in the SUMMARY which items came back provisioned and which (if any) were deferred. A deferred
credential is a recorded blocker for the plan that needs it (07-04 for Paystack, 07-06 for Stripe
Connect), never a value invented here.
  </instructions>
  <verification>Claude will confirm each variable is present and non-blank in the environment once Task 3's config reader exists, and will record both country values verbatim in the SUMMARY. No credential value is echoed back.</verification>
  <resume-signal>Type "done" when the three variables are in `.env.local`, and state the two Stripe account countries. Type "defer {service}" to record a blocker instead.</resume-signal>
  <acceptance_criteria>
    - `PAYSTACK_SECRET_KEY`, `PAYSTACK_SUBACCOUNT_CODE` and `STRIPE_CONNECTED_ACCOUNT_ID` are present in `.env.local` with test-mode values, or their absence is recorded in the SUMMARY as a named blocker against the specific downstream plan.
    - The platform-account country and connected-account country are both written down verbatim in the SUMMARY.
    - No credential value is written into `.env.example`, a test fixture, a comment, or the SUMMARY body.
  </acceptance_criteria>
  <done>The three credentials exist in the executor's environment (or are recorded as blocked), and both Stripe account countries are known.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Lock the refund-reversal policy and the on_behalf_of requirement</name>
  <read_first>
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-RESEARCH.md` — "Open Questions" 1 and 2, "Pitfall 3", "Pitfall 4", Assumptions A4 and A6
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md` — D-22, D-23, D-04
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/COVERAGE.md` — the `refund_reverse_transfer` and `connect_on_behalf_of` rows
  </read_first>
  <reversibility rating="one-way">
Both choices become visible in real settlement history the moment a live refund or a live destination
charge exists. Reversing the refund-reversal policy afterwards means manually correcting the school's
balance for every refund already processed under the old rule; changing `on_behalf_of` afterwards
changes the settlement merchant and statement descriptor on charges already made. Neither is a code
edit you can simply revert.
  </reversibility>
  <decision>Who absorbs a refunded base price on each provider (PAY-13, D-22, D-23), and whether this deployment's Stripe destination charges require `payment_intent_data.on_behalf_of` (D-04)?</decision>
  <context>
07-CONTEXT.md's D-22 and D-23 describe the learner-facing refund amount and its component allocation
in detail, but deliberately do not say which side — KQ NEXUS's platform balance or the school's
settled base price — absorbs a refunded base component. 07-RESEARCH.md raises this as Open Question 1
and Pitfall 4, with Assumption A4 recording that no source consulted confirmed Paystack's own
split-refund behaviour at all.

Separately, Stripe's own documentation states that a destination charge across differing
platform/connected-account regions requires `on_behalf_of`, and 07-RESEARCH.md's Assumption A6 records
that this cannot be resolved without the real accounts. Task 1 has now produced both country values.

All three answers are inputs to 07-06 and 07-08. Recorded here so neither plan re-derives or assumes
them, and so 07-08's Stripe refund passes `reverse_transfer` explicitly rather than by omission
(COVERAGE.md `refund_reverse_transfer` records this capability as INTEGRATE for exactly that reason).
  </context>
  <options>
    <option id="A1">
      <name>Stripe refunds always pass reverse_transfer: true</name>
      <pros>The school's base-price transfer is pulled back in proportion to the refund, so a refunded enrolment does not leave the school over-paid; KQ NEXUS absorbs only the non-recoverable gateway component</pros>
      <cons>The school sees clawbacks against its balance, which needs to be understood commercially before the first real refund</cons>
    </option>
    <option id="A2">
      <name>Stripe refunds always pass reverse_transfer: false</name>
      <pros>The school's settlement is never clawed back — simplest possible school-side experience</pros>
      <cons>KQ NEXUS's platform balance absorbs the entire refund including the school's base price; a refund-heavy cohort is a direct loss to KQ NEXUS</cons>
    </option>
    <option id="A3">
      <name>reverse_transfer: true for a full refund, false for a partial refund</name>
      <pros>A cancelled enrolment reverses cleanly while small goodwill adjustments stay on KQ NEXUS's side</pros>
      <cons>Two behaviours to reason about at reconciliation time; the partial case still costs KQ NEXUS the base component</cons>
    </option>
    <option id="B1">
      <name>Confirm Paystack's split-refund behaviour before 07-08 ships its Paystack refund path</name>
      <pros>Removes Assumption A4 entirely before any code depends on it</pros>
      <cons>Blocks 07-08's Paystack refund path on a provider-support answer or a sandbox experiment</cons>
    </option>
    <option id="B2">
      <name>Ship 07-08 recording exactly what Paystack reports, raising a variance when the school's settlement does not move</name>
      <pros>Unblocks 07-08 immediately and matches D-22's own closing rule — record the actual outcome rather than assume a fee or transfer return</pros>
      <cons>The first real Paystack refund is where the behaviour is learned, with a reconciliation variance as the signal</cons>
    </option>
    <option id="C">
      <name>on_behalf_of, resolved from Task 1's two country values</name>
      <pros>Stated as a fact ("the countries are X and Y, therefore on_behalf_of is / is not required"), so 07-06 reads an answer instead of re-deriving a conditional</pros>
      <cons>Must be revisited if either account's registered country ever changes — which is why the two values are recorded alongside the answer</cons>
    </option>
  </options>
  <resume-signal>Reply with one of A1/A2/A3, one of B1/B2, and the resolved C answer stated as "countries are X and Y, therefore on_behalf_of is/is not required" — then Claude writes all three, with your rationale, into the SUMMARY under `## Locked settlement policy decisions`.</resume-signal>
  <acceptance_criteria>
    - Decision A names exactly one of A1/A2/A3 and states that 07-08 passes `reverse_transfer` explicitly either way.
    - Decision B names exactly one of B1/B2 and, if B1, records who will confirm and by when.
    - Decision C states the two country values and the resulting yes/no for `on_behalf_of` as a resolved fact.
    - No decision is recorded as "to be decided during implementation".
  </acceptance_criteria>
  <done>All three decisions are locked and written into the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-closed settlement-config reader and .env.example documentation</name>
  <files>src/server/payments/settlement-config.ts, tests/payments-settlement-config.test.ts, .env.example</files>
  <read_first>
    - `src/server/payments/providers/stripe/client.ts` — the existing lazy, server-only env-reading convention this module mirrors
    - `.env.example` — the existing documentation style for Phase 6's `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`SUPPORT_CONTACT_EMAIL` entries
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md` — D-02, D-05, PAY-14
  </read_first>
  <behavior>
    - Reading a present, non-blank `PAYSTACK_SUBACCOUNT_CODE` returns that exact string.
    - Reading an absent `PAYSTACK_SUBACCOUNT_CODE` raises `MissingSettlementAccountError` naming the
      provider, and does NOT return an empty string, a placeholder, or `undefined`.
    - Reading a whitespace-only value is treated identically to absent — a blank is not a configuration.
    - The same three behaviours hold for `STRIPE_CONNECTED_ACCOUNT_ID` and `PAYSTACK_SECRET_KEY`.
    - The error message contains the variable name and the word "deployment configuration", and does
      NOT contain any part of a secret value, even a truncated one.
    - The module reads `process.env` lazily inside each accessor, so importing it in a test or a build
      step with no env set does not throw at import time (the `getStripe()` precedent).
  </behavior>
  <action>
Create `src/server/payments/settlement-config.ts` exporting a `MissingSettlementAccountError` class
and four lazy accessors — `paystackSecretKey()`, `paystackSubaccountCode()`,
`stripeConnectedAccountId()`, and `paystackWebhookSecretKey()` (Paystack signs webhooks with the same
secret key, so the last may alias the first rather than introduce a second variable). Each accessor
trims the value, treats a blank as absent, and throws `MissingSettlementAccountError` naming the
variable when absent.

Model the lazy-read structure on `src/server/payments/providers/stripe/client.ts`. This module must
import nothing from `stripe`, nothing from `@prisma/client`, and nothing under `providers/` — it sits
above both provider directories and is imported by both, so any provider-namespaced import here would
break the PAY-09 isolation scan the moment 07-04 generalizes it.

Add a `on_behalf_of` note to the module's header comment recording Task 2's Decision C as a resolved
fact, so 07-06 reads the answer rather than re-deriving it.

Extend `.env.example` with `PAYSTACK_SECRET_KEY`, `PAYSTACK_SUBACCOUNT_CODE` and
`STRIPE_CONNECTED_ACCOUNT_ID`, each with a commented one-line description and a clearly
non-functional shape example (`sk_test_xxx`, `ACCT_xxx`, `acct_xxx`). Follow the existing file's
convention exactly — a real credential value must never be written here (PAY-14).

Write `tests/payments-settlement-config.test.ts` first, covering every case in `<behavior>` by
setting and restoring `process.env` around each case.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/payments-settlement-config.test.ts</automated>
    <fails_when>`tsc` prints a `error TS` diagnostic line, or the vitest summary reports a non-zero `failed` count for `tests/payments-settlement-config.test.ts`, or the process exits non-zero.</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/payments-settlement-config.test.ts` asserts a thrown `MissingSettlementAccountError` for each of the absent and whitespace-only cases, for all three variables.
    - A test asserts the thrown message names the variable and carries no fragment of the value that was set in a sibling case.
    - Importing `src/server/payments/settlement-config.ts` with an empty `process.env` does not throw.
    - `.env.example` gains all three variables with placeholder shapes only; `git diff .env.example` shows no `sk_test_` or `acct_` value longer than the documented placeholder.
    - `src/server/payments/settlement-config.ts` contains no import from `stripe`, `@prisma/client`, or any path under `src/server/payments/providers/`.
  </acceptance_criteria>
  <done>The config reader exists, is proven fail-closed for every variable, and `.env.example` documents all three without leaking a value.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New files**
- `src/server/payments/settlement-config.ts`
- `tests/payments-settlement-config.test.ts`

**New exported symbols**
- `MissingSettlementAccountError` (class)
- `paystackSecretKey(): string`
- `paystackSubaccountCode(): string`
- `paystackWebhookSecretKey(): string`
- `stripeConnectedAccountId(): string`

**New environment variables**
- `PAYSTACK_SECRET_KEY`
- `PAYSTACK_SUBACCOUNT_CODE`
- `STRIPE_CONNECTED_ACCOUNT_ID`

**New Prisma models/columns:** none.
**New route handlers / React components / CLI entries:** none.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deployment secret store -> server process | Provider secrets and school account identifiers enter the process only through `process.env`, never through a form, a database row, or a request |
| server process -> client bundle | Any accidental import of this module from a client component would ship secret reads into browser JavaScript |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Information Disclosure | `settlement-config.ts`, `.env.example`, audit/error paths | high | mitigate | One server-side reader; `MissingSettlementAccountError` messages name the variable only, never a value fragment (asserted by a test); `.env.example` carries placeholder shapes only |
| T-07-15 | Denial of Service / Tampering | absent or blank school settlement identifier | medium | mitigate | Fail closed with a typed named error (D-05) rather than returning a blank that a provider adapter would happily send as an empty `subaccount`, silently settling the school's money to the platform account |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | 07-RESEARCH.md's Package Legitimacy Audit confirms this phase installs **no new packages** — Paystack is a raw `fetch()` wrapper and `stripe@22.6.1` is the already-human-approved Phase 6 pin. If any plan in this phase proposes a third-party Paystack SDK instead, the full Package Legitimacy Gate protocol must run against that package name first and a blocking human checkpoint must precede the install |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run tests/payments-settlement-config.test.ts` exits 0.
- The SUMMARY contains `## Locked settlement policy decisions` with answers to A, B and C.
- `.env.example` documents the three new variables; no real credential appears anywhere in the diff.
</verification>

<success_criteria>
- Three provider credentials are provisioned in the executor's environment or recorded as a named blocker against a specific downstream plan.
- Stripe's `on_behalf_of` requirement is a resolved fact, not a conditional.
- The refund-reversal policy is locked before any refund code exists.
- `settlement-config.ts` fails closed for every variable and leaks no value in any error path.
</success_criteria>

<output>
Create `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-01-SUMMARY.md` when done.
</output>

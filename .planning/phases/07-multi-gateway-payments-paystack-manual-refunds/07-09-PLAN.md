---
phase: 07-multi-gateway-payments-paystack-manual-refunds
plan: 09
type: execute
wave: 6
depends_on: ["07-05", "07-06"]
files_modified:
  - src/server/services/public-catalogue-service.ts
  - src/app/(public)/CohortCards.tsx
  - src/app/(checkout)/checkout/[orderId]/page.tsx
  - src/app/(checkout)/checkout/[orderId]/PolicyConsentForm.tsx
  - src/app/orders/[reference]/page.tsx
  - src/server/services/checkout-service.ts
  - tests/components/cohort-cards.test.tsx
  - tests/components/checkout-summary.test.tsx
  - tests/components/cohort-detail-actions.test.tsx
  - tests/components/order-confirmation.test.tsx
  - tests/public-catalogue-service.test.ts
autonomous: true
requirements: [PAY-08, PAY-15, PAY-16, PAY-17]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An offer card names its currency and its fixed provider on every CTA — \"Pay in NGN with Paystack\" and \"Pay in USD with Stripe\" — using the administrator-entered base prices, so the learner chooses a currency and never a gateway (D-16, D-07)."
    - "The order-summary page shows four separate rows — School fee, KQ NEXUS platform fee (1.5%), Estimated payment-processing fee, Total charged — followed by a currency/provider line, and the breakdown is the last thing read before the policy checkboxes (D-16)."
    - "The summary discloses that the processing fee comes from the configured expected schedule and that the actual gateway fee may differ without changing the total charged (D-17)."
    - "The receipt renders the identical four-line breakdown read from the Order's own immutable snapshot columns, never recomputed from the Cohort's current price or the active fee schedule, so a later price or schedule edit cannot change what a past receipt shows (D-18, D-13)."
    - "No provider credential, subaccount code or connected-account id appears in any learner-facing page, form field, or HTML payload (D-02, PAY-14)."
    - "Currency rails the learner cannot actually pay on are never rendered as a selectable option; an unusable rail shows an explicit, recoverable support route instead (D-19, D-05)."
    - "An offer card with both rails priced renders two CTAs side by side at 640px and above, stacked full-width below it. (UI-SPEC §8 — populated: offer/cohort card with both rails priced)"
    - "An offer card with exactly one rail priced renders a single CTA in the position the previous single CTA occupied, with no empty second slot and no layout shift relative to the two-rail case. (UI-SPEC §8 — partial: offer/cohort card with exactly one rail priced)"
    - "The offer card handles zero, one and two available currency rails as three explicit branches, and two is a hard cap because Paystack and Stripe are the only rails this system has. (UI-SPEC §8 — zero-one-many: offer/cohort card currency rails)"
    - "The offer, order-summary and receipt pages remain server-rendered with no client-side loading state, unchanged from Phase 6. (UI-SPEC §8 — loading: cohort/offer/order-summary/receipt pages)"
  artifacts:
    - "src/app/(public)/CohortCards.tsx — the zero/one/two-rail CTA branches and the unavailable-rail notice"
    - "src/app/(checkout)/checkout/[orderId]/page.tsx — the four-line breakdown card and the fee-variance disclosure"
    - "src/app/orders/[reference]/page.tsx — the same breakdown, read from the Order snapshot"
    - "tests/components/checkout-summary.test.tsx, tests/components/cohort-cards.test.tsx — the exact-label and state-branch proofs"
  key_links:
    - "Order.baseAmountMinor/platformFeeMinor/gatewayFeeEstimateMinor/amountMinor (07-04 snapshot) -> the four rendered rows on both the summary and the receipt, with no recomputation between them"
    - "PublicCohort.priceNgnMinor/priceUsdMinor (07-04) -> the per-rail CTA branches"
    - "SUPPORT_CONTACT_EMAIL (existing Phase 6 env placeholder) -> the unavailable-rail notice's mailto link, reused verbatim rather than a second contact string"
---

<objective>
Give the learner an honest, complete view of what they are being charged: a currency choice that
names its provider, a four-line breakdown disclosed before consent, and a receipt that preserves that
breakdown permanently.

Purpose: the tracer showed one NGN CTA and left the money display on Phase 6's single-price fact.
This plan expands that to the full learner-facing contract in 07-UI-SPEC — every state branch of the
offer card, the four-row breakdown with its estimate disclosure, and the immutable receipt.
Output: the complete learner surface for both rails, on the existing design system.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md
@src/app/(public)/CohortCards.tsx
@src/app/(checkout)/checkout/[orderId]/page.tsx
@src/app/orders/[reference]/page.tsx
</context>

<global_constraints>
- **Never run `git commit`.** Stage with `git add` and report a suggested commit message in the
  SUMMARY. The repository owner creates every commit explicitly.
- Before editing these pages or the Server Action form wiring, read the relevant guide under
  `node_modules/next/dist/docs/` per `AGENTS.md`.
- No change to `globals.css`, the four primitives' contracts, `LearnerShell`, or `StaffShell` —
  this phase is a pure consumer of the 04.1/06 design system (07-UI-SPEC §0.2).
- `Intl.NumberFormat` is for display only, after all integer calculation is complete (D-24).
- No new icon: every new banner reuses an inventoried icon or Phase 6's text-only warning-banner
  convention (07-UI-SPEC §1).
</global_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offer-card currency rails — zero, one and two, with the unavailable-rail notice</name>
  <files>src/server/services/public-catalogue-service.ts, src/app/(public)/CohortCards.tsx, tests/components/cohort-cards.test.tsx, tests/public-catalogue-service.test.ts</files>
  <read_first>
    - `src/app/(public)/CohortCards.tsx` in full — the single form-POST CTA the tracer extended, the `seatsAvailable === 0` "Full" branch, and the file's own rationale for a form POST rather than a `<Link>`
    - `src/server/services/public-catalogue-service.ts` — `PublicCohort` (already carrying `priceNgnMinor`/`priceUsdMinor` from 07-04) and the anonymous-payload rule that keeps `seatsTaken`/`capacity` off it
    - `src/app/orders/[reference]/page.tsx` — the existing `SUPPORT_CONTACT_EMAIL` mailto pattern to reuse verbatim
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md` §6.1 (exact CTA and notice copy) and §7.2 (the four branches and their responsive behaviour)
  </read_first>
  <behavior>
    - Both rails priced: two buttons render, labelled "Pay in NGN with Paystack" and "Pay in USD with Stripe", each inside its own form POST to `enrollAction` carrying its own hidden `currency` value.
    - Both rails priced at 640px and above: the two buttons sit side by side; below 640px they stack full width, following the card's existing responsive convention.
    - Exactly one rail priced: a single button renders in the position the previous single CTA occupied, with no empty second slot.
    - Neither rail priced: no button renders; the "Payment is temporarily unavailable for this cohort. Contact support to enrol." notice renders inline in the same card with a `mailto:` link built from the existing `SUPPORT_CONTACT_EMAIL` variable.
    - `seatsAvailable === 0`: the "Full" label renders and no button renders, regardless of how many rails are priced — a full cohort and an unpriced cohort are distinct conditions and both can be true at once without either implying the other.
    - Each card shows each priced rail's own formatted base price; a null rail renders no price at all rather than a zero.
    - No rendered `href` anywhere in this component targets the `/enrol/` resumption path, preserving Phase 6's prefetch invariant.
  </behavior>
  <action>
Replace the tracer's single NGN CTA with the full branch set from 07-UI-SPEC §7.2. Keep the form-POST
mechanism exactly as it is — the existing file documents why a `<Link>` would let a prefetch create an
order, and adding a second button must not quietly reintroduce that.

Replace the legacy `formatPrice(cohort.priceMinor, cohort.currency)` display with per-rail formatted
prices derived from `priceNgnMinor`/`priceUsdMinor`. Keep `formatPrice`'s existing
`Intl.NumberFormat` shape; it is display-only and runs after all integer work is done.

Model the unavailable-rail notice on Phase 6's own text-only warning-toned banner convention —
`--warning-surface` background, no new icon (07-UI-SPEC §1). This is a configuration gap, not a
capacity gap, so it must never reuse the "Full" copy.

Extend `PublicCohort`'s consumers as needed but keep the anonymous-payload rule intact: no
`seatsTaken`, no `capacity`, and nothing about the deployment's provider configuration reaches this
payload. Whether a rail is usable at all — the D-05 "school settlement account absent" case — is
resolved server-side; the card receives a boolean-shaped fact, never a reason string that would
describe the deployment's configuration to an anonymous visitor.

Extend both test files with every case in `<behavior>`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/components/cohort-cards.test.tsx tests/public-catalogue-service.test.ts</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for either file, or a render case finds a rendered price of zero for an unpriced rail.</fails_when>
  </verify>
  <acceptance_criteria>
    - All three rail-count branches plus the `seatsAvailable === 0` branch have render cases.
    - Both CTA labels match 07-UI-SPEC §6.1 exactly, character for character.
    - The unavailable-rail notice renders the §6.1 copy and a `mailto:` link built from `SUPPORT_CONTACT_EMAIL`.
    - A render case asserts no anchor in the component targets the resumption path.
    - `PublicCohort` still carries no `seatsTaken`, `capacity`, or provider-configuration detail.
  </acceptance_criteria>
  <done>Every offer-card rail state renders honestly, and no unusable rail is offered as a choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The four-line breakdown card and the fee-variance disclosure on the order summary</name>
  <files>src/app/(checkout)/checkout/[orderId]/page.tsx, src/app/(checkout)/checkout/[orderId]/PolicyConsentForm.tsx, src/server/services/checkout-service.ts, tests/components/checkout-summary.test.tsx</files>
  <read_first>
    - `src/app/(checkout)/checkout/[orderId]/page.tsx` lines 100-140 — the existing four-fact `<dl>` grid and where the Price fact sits relative to the verification/decline banners and the consent form
    - `src/app/(checkout)/checkout/[orderId]/PolicyConsentForm.tsx` — unchanged as a component; only its position relative to the breakdown matters
    - `src/server/services/checkout-service.ts` — `OrderSnapshot`, the shape the page reads
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md` §4 (the Total-charged weight rule and the estimate-qualifier tone), §6.1 (the four row labels, the currency/provider line, the disclosure sentence, all verbatim) and §7.3 (card position, divider placement, and what stays unchanged)
  </read_first>
  <behavior>
    - The breakdown card renders exactly four rows labelled "School fee", "KQ NEXUS platform fee (1.5%)", "Estimated payment-processing fee" and "Total charged", in that order.
    - A fifth line renders "{Currency} via {Provider}", for example "NGN via Paystack".
    - The four rendered amounts equal the Order's `baseAmountMinor`, `platformFeeMinor`, `gatewayFeeEstimateMinor` and `amountMinor` respectively, formatted for display only.
    - The three component rows sum exactly to the total row — asserted from the rendered values, so a formatting bug that drops a digit is caught.
    - The fee-variance disclosure sentence renders verbatim beneath the Estimated row, in muted text, and does not use the warning colour.
    - The breakdown card renders after the Cohort/Dates/Mode fact grid and before the policy checkboxes, so it is the last thing read before consent.
    - The hold countdown, verification banner, decline banner and hold-expired panel render exactly as they did in Phase 6, with unchanged triggers and copy.
    - The page renders no provider credential, subaccount code or connected-account id in any element or attribute.
  </behavior>
  <action>
Move the single "Price" fact out of the existing four-fact `<dl>` grid into its own breakdown card
positioned where that fact was — immediately below the fact grid and above the verification/decline
banners — leaving the Cohort, Dates and Mode facts in the grid untouched. Build the card as a `<dl>`
of four rows plus the currency/provider line inside the same card shell every other learner-facing
panel on this page already uses, with a 1px border divider between the Estimated row and the Total
row per 07-UI-SPEC §7.3.

Type the rows at the weights 07-UI-SPEC §4 specifies: labels at Body, amounts at Label weight with
`font-mono tabular-nums`, and the Total row's label and amount both at Label weight. This introduces
no fifth type size — it is the same 14px at a heavier weight, which is how an invoice total is
conventionally distinguished.

Render the disclosure sentence in muted text, not warning-toned. Nothing is wrong with an estimate
being an estimate, and borrowing the warning colour here would teach the learner to distrust a correct
number.

Extend `OrderSnapshot` in `checkout-service.ts` with the four snapshot values and the provider so the
page reads a single shaped object rather than assembling money from several sources. Do not change
`getOwnOrder`'s ownership contract: "not mine" and "does not exist" remain the same answer.

`PolicyConsentForm.tsx` itself is unchanged — only its position relative to the now-larger breakdown
card matters, and that ordering is already what Phase 6 does.

Extend `tests/components/checkout-summary.test.tsx` with every case in `<behavior>`, including the
sum-of-rendered-values assertion and a case asserting none of the three secret-bearing environment
values appears anywhere in the rendered output.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/components/checkout-summary.test.tsx tests/checkout-service.test.ts</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for either file, or the sum-of-rendered-values assertion reports a total that does not equal the three components.</fails_when>
  </verify>
  <acceptance_criteria>
    - All four row labels and the disclosure sentence match 07-UI-SPEC §6.1 verbatim.
    - A render case asserts the three component values sum to the total value as rendered.
    - A render case asserts the breakdown card appears before the policy checkboxes in document order.
    - The Phase 6 countdown, verification, decline and hold-expired cases in this file all still pass unchanged.
    - A render case asserts no provider secret or account identifier appears in the output.
  </acceptance_criteria>
  <done>The learner sees the full four-line charge, with its estimate honestly labelled, before consenting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The permanent receipt breakdown, read from the Order's own snapshot</name>
  <files>src/app/orders/[reference]/page.tsx, tests/components/order-confirmation.test.tsx, tests/components/cohort-detail-actions.test.tsx</files>
  <read_first>
    - `src/app/orders/[reference]/page.tsx` lines 40-125 — the file's own header comment about `amountMinor`/`currency` immutability, the single "Amount" fact, the Order reference/Cohort/Payment-pill/Enrolment-pill facts, and the existing `SUPPORT_CONTACT_EMAIL` usage
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md` §7.4 — the same breakdown in the position the Amount fact occupied, and what stays unchanged
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md` — D-18, D-13
  </read_first>
  <behavior>
    - The receipt renders the same four row labels, the same currency/provider line and the same disclosure sentence as the order summary, verbatim.
    - Every rendered amount comes from the Order row's own snapshot columns; the page performs no fee calculation and reads no Cohort price and no `GatewayFeeSchedule`.
    - Editing the Cohort's price after the Order was paid leaves the receipt's rendered values unchanged — asserted by a case that mutates the Cohort fixture between renders.
    - Editing the active `GatewayFeeSchedule` after the Order was paid leaves the receipt's rendered values unchanged.
    - The Phase 6 exception sub-state ("Payment received — finishing up" with its warning-toned Enrolment pill) renders alongside the breakdown, unchanged.
    - The existing Order reference, Cohort, Payment pill and Enrolment pill facts are unchanged.
  </behavior>
  <action>
Replace the single "Amount" fact with the same breakdown card built in Task 2, in the position the
Amount fact occupied. Extract the card into one shared component both pages render rather than
duplicating the markup — two copies of a four-row money layout is how the summary and the receipt
start disagreeing about a label, and 07-UI-SPEC §7.4 requires them to be identical.

Read every value from the Order row. Extend the page's existing header comment — which already states
the immutability rule for `amountMinor`/`currency` — to cover all four snapshot values, because that
comment is the thing standing between a future contributor and a well-meaning "just recompute it from
the cohort" refactor.

Extend `tests/components/order-confirmation.test.tsx` with the mutate-the-fixture-between-renders
cases for both the Cohort price and the fee schedule. These are the executable form of D-18, and a
prose assertion would not survive the refactor it exists to prevent.

Check `tests/components/cohort-detail-actions.test.tsx` for any assertion tied to the single-price
offer display and update it to the dual-rail shape without weakening its existing coverage.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/components/order-confirmation.test.tsx tests/components/checkout-summary.test.tsx tests/components/cohort-detail-actions.test.tsx</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for any of the three files, or the mutate-between-renders case reports a changed rendered amount.</fails_when>
  </verify>
  <acceptance_criteria>
    - One shared breakdown component is rendered by both the summary and the receipt; the markup is not duplicated.
    - A case mutates the Cohort price between renders and asserts every rendered receipt amount is identical.
    - A case mutates the fee schedule between renders and asserts the same.
    - The receipt page imports nothing from `pricing.ts` — it renders a snapshot, it does not price anything.
    - The Phase 6 exception sub-state case still passes unchanged.
  </acceptance_criteria>
  <done>A paid receipt shows the same four lines forever, regardless of what changes afterwards.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New React components**
- a shared four-line charge-breakdown component rendered by both `/checkout/[orderId]` and `/orders/[reference]`
- the unavailable-rail notice branch inside `CohortCards`

**Changed public shapes**
- `OrderSnapshot` (`checkout-service.ts`) — gains `baseAmountMinor`, `platformFeeMinor`, `gatewayFeeEstimateMinor`, `selectedProvider`
- `CohortCards` — renders zero, one or two currency CTAs plus the unavailable-rail notice

**New files, Prisma models/columns, route handlers, env vars, CLI entries:** none.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render -> anonymous visitor | The offer card's HTML is public; anything placed on it is disclosed to everyone |
| server render -> authenticated learner | The summary and receipt render another party's commercial arrangement (school settlement, KQ allocation) if not narrowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-12 | Information Disclosure | learner-facing pages | high | mitigate | No provider credential, subaccount code or connected-account id is read by any of these components; a render case asserts none of the three environment values appears in the rendered output (D-02, PAY-14) |
| T-07-41 | Information Disclosure | expected school/KQ settlement values | medium | mitigate | The learner-facing breakdown shows only the four charged rows and the currency/provider line; expected and actual settlement figures are Finance-only and render on `/staff/payments/[orderId]` under `payments.view` (D-16, D-18) |
| T-07-42 | Tampering | offer-card CTA creating an order by prefetch | medium | mitigate | Both CTAs stay form POSTs to the Server Action; a render case asserts no anchor targets the side-effecting resumption path, preserving Phase 6's documented invariant |
| T-07-43 | Spoofing | a rendered breakdown that does not match what was charged | medium | mitigate | Every rendered value reads the Order's own snapshot columns; the receipt imports no pricing module, and mutate-between-renders cases prove a later price or schedule edit cannot change it (D-13, D-18) |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run tests/components/cohort-cards.test.tsx tests/components/checkout-summary.test.tsx tests/components/order-confirmation.test.tsx tests/components/cohort-detail-actions.test.tsx tests/public-catalogue-service.test.ts` exits 0.
- The three component rows sum to the total as rendered on both the summary and the receipt.
- A Cohort price edit and a fee-schedule edit both leave a paid receipt's rendered values unchanged.
</verification>

<success_criteria>
- The learner picks a currency, never a gateway, and every offered rail is actually payable.
- The four-line breakdown and its estimate disclosure are visible before consent.
- The receipt preserves that breakdown permanently, from the Order's own snapshot.
- Nothing about the deployment's provider configuration reaches a learner-facing page.
</success_criteria>

<output>
Create `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-09-SUMMARY.md` when done.
</output>

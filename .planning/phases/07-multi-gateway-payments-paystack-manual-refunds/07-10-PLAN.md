---
phase: 07-multi-gateway-payments-paystack-manual-refunds
plan: 10
type: execute
wave: 6
depends_on: ["07-08"]
files_modified:
  - src/server/services/payment-read-service.ts
  - src/app/staff/payments/page.tsx
  - src/app/staff/payments/[orderId]/page.tsx
  - src/app/staff/payments/actions.ts
  - src/app/staff/payments/ManualPaymentDialog.tsx
  - src/app/staff/payments/RefundDialog.tsx
  - src/app/staff/layout.tsx
  - tests/components/payment-detail.test.tsx
  - tests/payment-read-service.test.ts
  - tests/staff-payments-actions.test.ts
autonomous: true
requirements: [PAY-03, PAY-04, PAY-05, PAY-13, PAY-14]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Finance staff holding payments.view see Paystack, Stripe and manual records in one list, distinguishable by provider, with the learner charge, payment state and settlement state per row (PAY-12 groundwork, D-18)."
    - "Finance payment detail shows expected school settlement and expected KQ gross alongside the actual gateway charge, actual school settlement and actual KQ net, and distinguishes an unresolved actual value from a zero one (D-14, D-18)."
    - "A reconciliation mismatch renders a visible exception banner on the detail page rather than being silently absorbed (D-18, PAY-07)."
    - "The manual-confirmation action renders only while the actor holds payments.confirm and the Order can still be acted on; against an already-paid Order it renders the existing transaction and a corrective path instead of a second attempt (PAY-03, PAY-04)."
    - "The refund action renders only while the actor holds refunds.manage and eligible captured value remains, and the dialog states the remaining eligible amount before the amount field rather than only rejecting after submit (PAY-05, D-22)."
    - "No gateway credential, subaccount code or connected-account id renders on any staff payments screen; staff may see which providers are enabled but never a secret (PAY-14, D-02)."
    - "The Finance payments list renders a \"No payments yet\" empty state with a phase-specific body and no create affordance, because every row originates from checkout or a manual confirmation. (UI-SPEC §8 — empty: Finance payments list)"
    - "The refunds sub-list on the detail page renders no placeholder text when empty; the \"Record a refund\" button is the only affordance. (UI-SPEC §8 — empty: refunds sub-list)"
    - "The Finance list and detail use ResourceTable's and DetailLayout's existing loading skeletons unmodified. (UI-SPEC §8 — loading: Finance list / detail)"
    - "Both dialogs disable their confirm button while a submission is in flight, via ConfirmModal's existing pending contract. (UI-SPEC §8 — loading: ManualPaymentDialog / RefundDialog submit)"
    - "Both dialogs render the verbatim \"Action not applied\" copy on failure. (UI-SPEC §8 — error: ManualPaymentDialog / RefundDialog failure)"
    - "A duplicate or conflicting confirmation attempt renders the \"This order is already paid\" banner with the existing transaction visible, never a silent duplicate. (UI-SPEC §8 — error: PAY-04 duplicate/conflicting confirmation attempt)"
    - "A reconciliation exception renders a warning-toned, non-alarming banner above the Settlement section. (UI-SPEC §8 — error: reconciliation exception)"
    - "The Finance list at typical volume uses ResourceTable's existing pagination, sort and filter with no new volume assumption. (UI-SPEC §8 — populated: Finance list at typical volume)"
    - "Long learner names, emails and refund reasons wrap using the existing break-words / max-w-prose treatment on fact cards and DetailFacts rows. (UI-SPEC §8 — overflow: long learner name/email, long refund reason)"
    - statement: "The reconciliation-exception banner body stays legible at a very small or very large variance value, because the banner does not state the variance inline and points instead to the Settlement section's own mono/tabular-nums fields."
      verification: backstop
    - "PLANNER ASSUMPTION (UI-SPEC §8 unresolved — ManualPaymentDialog evidence field): neither 07-CONTEXT.md nor the source implementation plan specifies a maximum length or control type for PAY-03's evidence/note field. This plan assumes a multi-line <textarea> with a 2000-character maximum, matching ConfirmModal's own reason-field control type for consistency within the same dialog family. This is a planner assumption, not a verified fit — confirm or override it during UAT before treating the ceiling as decided."
  artifacts:
    - "src/app/staff/payments/page.tsx — the ResourceTable Finance list"
    - "src/app/staff/payments/[orderId]/page.tsx — the stacked DetailLayout with four sections"
    - "src/app/staff/payments/ManualPaymentDialog.tsx, RefundDialog.tsx — the two PublishDialog-precedent dialogs"
    - "src/server/services/payment-read-service.ts — the payments.view-scoped list and detail reads"
  key_links:
    - "payment-read-service.ts -> withPermission(payments.view) -> both pages; the server boundary is the authorization, not the rendered button"
    - "confirmManualPayment / recordRefund (07-08) -> staff/payments/actions.ts -> the two dialogs"
    - "PaymentAttempt actual columns (07-07) -> the Settlement section's expected/actual pairs and the exception banner"
---

<objective>
Build the Finance-facing payments surface: a provider-distinguishing list, a detail page pairing
expected against actual settlement, and the two permission-gated dialogs that drive manual
confirmation and refunds.

Purpose: 07-08 built the two staff money paths as services with no way to invoke them. This plan is
that surface, and it is also where D-18's "Finance sees expected versus actual, plus a visible
exception" and PAY-04's "show staff the existing transaction and a corrective path" actually become
things a person can see.
Output: `/staff/payments` and `/staff/payments/[orderId]` on the existing primitives.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md
@.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md
@src/components/primitives/ResourceTable.tsx
@src/components/primitives/DetailLayout.tsx
@src/components/catalogue/PublishDialog.tsx
</context>

<global_constraints>
- **Never run `git commit`.** Stage with `git add` and report a suggested commit message in the
  SUMMARY. The repository owner creates every commit explicitly.
- Before editing these pages, the Server Actions, or the staff layout, read the relevant guide under
  `node_modules/next/dist/docs/` per `AGENTS.md`.
- No change to `globals.css`, the four primitives' contracts, or `StaffShell`'s chrome — this phase
  consumes the 04.1 system (07-UI-SPEC §0.2).
- Authorization lives at the server boundary. A hidden button is a presentation detail, never the
  access control (RBAC-06).
- No new icon; reuse an inventoried one or Phase 6's text-only warning-banner convention.
</global_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Permission-scoped payments read service and the Finance list</name>
  <files>src/server/services/payment-read-service.ts, src/app/staff/payments/page.tsx, src/app/staff/layout.tsx, tests/payment-read-service.test.ts</files>
  <read_first>
    - `src/components/primitives/ResourceTable.tsx` in full — the column config, `primaryColumnKey`, the `search`/`select` filter kinds, the `isSegmented` threshold for four-or-fewer options, the default empty/error copy and the `emptyBody` override, and the sub-640px card-list fallback
    - an existing `ResourceTable`-based staff list page (for example `src/app/staff/cohorts/page.tsx`) — the page/service/permission wiring convention
    - `src/app/staff/layout.tsx` lines 20-28 — the flat `StaffNavItem[]` array
    - `src/server/permissions/catalogue.ts` — `payments.view`
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md` §6 (list copy), §6.1 (page title, column headers, filters) and §7.5 (columns, tones, the discretionary Settlement view-model and its rule)
  </read_first>
  <behavior>
    - A caller without `payments.view` in matching scope gets the standard denied panel and no row data, and the service performs no read that could confirm a record exists.
    - The list renders columns Order, Learner, Amount, Provider, Payment, Settlement.
    - Provider renders as plain text — "Paystack", "Stripe", "Manual" — not as a status pill.
    - Payment renders a `StatusPill` toned per 07-UI-SPEC §5's `OrderStatus` mapping.
    - Settlement renders a three-state derived pill: "Estimated only" while every actual field is null, "Reconciled" once actuals are present and within the schedule's rounding tolerance, "Exception" once actuals are present and outside it.
    - With no payments, the empty state reads "No payments yet" with the §6 `emptyBody` override, and no create affordance renders.
    - The Provider filter has exactly four options and therefore renders segmented through `ResourceTable`'s existing threshold, with no new segmented-versus-select logic written.
    - Below 640px the existing card-list fallback renders with `primaryColumnKey` set to the order column.
    - The staff nav gains one "Payments" entry after "Enrolments".
  </behavior>
  <action>
Create `src/server/services/payment-read-service.ts` with a `withPermission`-wrapped list read
against `payments.view` in the Order's cohort scope, returning a flat row view-model: order reference,
cohort title, learner name and email, learner total and currency, provider, order status, and the
derived settlement state. Derive the settlement state in this service, not in the page — 07-UI-SPEC
§7.5 flags the three-state label as a view-model under CONTEXT.md's Claude's-discretion allowance,
and keeping the derivation server-side means the page cannot drift from the detail page's version of
the same fact.

Build `/staff/payments` from `ResourceTable` with the §7.5 columns, a search filter over learner
name/email/order reference, a `select` Status filter drawn from `OrderStatus`, and a four-option
Provider filter that segments automatically. Pass no `onCreate` — there is no blank-slate payment to
create — and override `emptyBody` with the §6 copy, because the primitive's default ("Create one to
get started") is wrong for a record staff never create directly.

Add `{ label: "Payments", href: "/staff/payments" }` to the staff nav array after "Enrolments",
keeping the existing commerce-adjacent grouping.

Write `tests/payment-read-service.test.ts` for the permission and scope cases and the settlement-state
derivation table.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/payment-read-service.test.ts tests/with-permission.test.ts</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for either file, or a denied-caller case returns row data.</fails_when>
  </verify>
  <acceptance_criteria>
    - The denied case asserts the standard denied panel and asserts the service read nothing that could confirm a record exists.
    - The settlement-state derivation has a case for each of the three states, including the all-null "Estimated only" case.
    - The list passes no `onCreate` and overrides `emptyBody` with the §6 copy verbatim.
    - The Provider column renders plain text, asserted by a case that finds no status pill in that cell.
    - `src/app/staff/layout.tsx` gains exactly one nav entry.
  </acceptance_criteria>
  <done>Finance can see every payment across all three providers, correctly scoped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finance payment detail — expected versus actual, and the exception banner</name>
  <files>src/app/staff/payments/[orderId]/page.tsx, src/server/services/payment-read-service.ts, tests/components/payment-detail.test.tsx</files>
  <read_first>
    - `src/components/primitives/DetailLayout.tsx` in full — the `mode` prop, `DetailFacts`, breadcrumbs, badges, the loading skeleton and the default error copy
    - `src/app/staff/cohorts/[id]/page.tsx` — an existing `DetailLayout` consumer and its conditional-fact convention
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md` §4 (the null-actual typography rule), §6.1 (every section heading and banner string verbatim) and §7.6 (the four sections in order, and the already-paid guard's behaviour)
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-CONTEXT.md` — D-14, D-18
  </read_first>
  <behavior>
    - The page renders `DetailLayout` in stacked mode with breadcrumbs "Workspace" / "Payments" / the order reference, and the Payment and Settlement pills as badges.
    - Section 1 "Learner charge" renders the same four breakdown values plus currency/provider, learner name and email, and the order reference in mono — read from the Order's own snapshot, identical to the learner's receipt.
    - Section 2 "Settlement" renders expected/actual pairs: expected school settlement, actual school settlement, expected KQ gross, actual KQ gross, actual gateway fee, actual KQ net.
    - Every unresolved actual value renders the literal "— (pending reconciliation)" in muted text — never "0" and never an empty cell.
    - When the settlement state is "Exception", the §6.1 reconciliation-exception banner renders above the Settlement section in warning tone, not danger tone.
    - Section 4 "Refunds" lists existing refunds with reference, amount, a `RefundStatus` pill, actor and date; with none, it renders no rows and no placeholder text.
    - A `RECORDED_MANUALLY` refund renders the accent-toned pill, distinguishing how it was processed from whether it succeeded.
    - Long learner names, emails and refund reasons wrap rather than overflowing, using the existing break-words / max-w-prose treatment.
    - A caller without `payments.view` gets the standard denied panel and no facts.
  </behavior>
  <action>
Build `/staff/payments/[orderId]` from `DetailLayout` with `mode="stacked"` and the four sections in
07-UI-SPEC §7.6's order. Extend `payment-read-service.ts` with a detail read, `withPermission`-wrapped
against `payments.view` in the Order's scope, returning the snapshot values, the expected/actual
settlement pairs, the refund list and the derived settlement state.

Render the "Learner charge" section from the same Order snapshot the receipt reads, so Finance and the
learner are looking at one number and not two independently computed ones.

For every actual value, render "— (pending reconciliation)" when it is null. This is the single most
important rendering rule on this page: D-14 makes null and zero different facts, and a blank cell or a
zero would tell Finance that the gateway charged nothing when in truth nothing is known yet.

Render the reconciliation-exception banner in warning tone above the Settlement section. It is a
bookkeeping flag for Finance to investigate, not a claim that the learner was wronged or that money
was lost — 07-UI-SPEC §5 and §7.6 both call this out, and danger tone would misreport the severity.

Write `tests/components/payment-detail.test.tsx` covering every case in `<behavior>`, including an
explicit case asserting a null actual renders the em-dash string and asserting the character "0" is
not rendered in that cell.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/components/payment-detail.test.tsx</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for `tests/components/payment-detail.test.tsx`, or the null-actual case finds a rendered zero in a settlement cell.</fails_when>
  </verify>
  <acceptance_criteria>
    - All four section headings match 07-UI-SPEC §6.1 verbatim.
    - A case asserts a null actual renders "— (pending reconciliation)" and asserts no zero renders in that cell.
    - A case asserts the exception banner uses the warning surface, not the danger surface.
    - The empty refunds sub-list renders zero rows and no placeholder text.
    - A denied-caller case renders the standard denied panel with no facts.
    - The detail page reads the Order snapshot and does not import `pricing.ts`.
  </acceptance_criteria>
  <done>Finance can see what was charged, what was expected, what actually settled, and where they disagree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Manual-confirmation and refund dialogs with their PAY-04 guard</name>
  <files>src/app/staff/payments/ManualPaymentDialog.tsx, src/app/staff/payments/RefundDialog.tsx, src/app/staff/payments/actions.ts, src/app/staff/payments/[orderId]/page.tsx, tests/staff-payments-actions.test.ts, tests/components/payment-detail.test.tsx</files>
  <read_first>
    - `src/components/catalogue/PublishDialog.tsx` in full — the structured-fields-plus-`MIN_REASON` precedent, how it reuses `ConfirmModal`'s focus trap, ESC handling and return-focus, and its pending/failure handling
    - `src/components/primitives/ConfirmModal.tsx` in full — the `pending` prop contract, the `tone` contract, and the "Action not applied" failure copy
    - `src/server/services/manual-payment-service.ts` and `src/server/services/refund-service.ts` (07-08) — the exact inputs, the `ALREADY_PAID` result shape and the eligible-value cap error
    - `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-UI-SPEC.md` §0.3 (why two purpose-built dialogs rather than a bare `ConfirmModal`), §6 and §6.1 (every label, hint and failure string verbatim), §7.6 sections 3 and 4
  </read_first>
  <behavior>
    - The "Confirm manual payment" button renders only when the actor holds `payments.confirm` and the Order is not already PAID.
    - When the Order is already PAID, section 3 renders the "This order is already paid" banner in warning tone with the existing successful attempt's provider, date and reference inline, and no confirm button.
    - `ManualPaymentDialog` collects amount, currency, date, channel, reference and evidence/note, plus a reason of at least 10 characters, and its confirm button stays disabled until the reason threshold is met.
    - `RefundDialog` renders the remaining-eligible-amount hint above the amount field, uses `tone="danger"`, and gates its confirm on a reason of at least 10 characters.
    - Both dialogs disable confirm while a submission is in flight, through `ConfirmModal`'s existing `pending` contract.
    - Both dialogs render the verbatim "Action not applied" copy on failure.
    - Both dialogs trap focus, close on ESC and return focus to their trigger, inheriting `ConfirmModal`'s behaviour rather than re-deriving it.
    - The Server Action refuses a caller lacking the matching permission even when the request is posted directly without the button ever rendering.
    - A refund request above the eligible cap is refused by the action with the eligible figure named.
  </behavior>
  <action>
Build both dialogs on the `PublishDialog.tsx` precedent: a purpose-built dialog collecting structured
fields, gating its own confirm behind a `MIN_REASON = 10` reason, and reusing `ConfirmModal`'s
focus-trap / ESC / return-focus behaviour verbatim rather than re-implementing it. A bare
`ConfirmModal` is not used anywhere in this phase — neither action here is reason-only.

**Planner assumption to surface, not silently resolve.** 07-UI-SPEC §8 records the evidence/note
field's maximum length and control type as unresolved: PAY-03 requires the field but neither
07-CONTEXT.md nor the source implementation plan specifies either. Implement it as a multi-line
`<textarea>` with a 2000-character maximum, matching `ConfirmModal`'s own reason-field control type
for consistency within the dialog family, and write a comment at the field naming this as a planner
assumption pending confirmation at UAT. Do not present the number as decided.

Create `src/app/staff/payments/actions.ts` with two Server Actions wrapping `confirmManualPayment` and
`recordRefund`. Validate every field server-side with zod; the dialogs' own validation is a courtesy
to the person typing, never the boundary. The actions must refuse a direct post from a caller lacking
the permission, with the button's absence being irrelevant to that refusal (RBAC-06).

Wire section 3's already-paid branch to `confirmManualPayment`'s `ALREADY_PAID` result: render the
banner and the existing transaction's provider, date and reference. PAY-04's required corrective path
is *visibility into the existing transaction*, not a second attempt at the same action — so no
"confirm anyway" affordance renders on that branch.

Wire section 4's refund button to render only when `refunds.manage` is held and eligible captured
value remains, and pass that remaining value into the dialog so the cap is visible before the staff
member types a number rather than only as a rejection afterwards.

Extend `tests/components/payment-detail.test.tsx` with the dialog render cases and
`tests/staff-payments-actions.test.ts` with the direct-post permission refusals and the cap refusal.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/components/payment-detail.test.tsx tests/staff-payments-actions.test.ts tests/components/confirm-modal.test.tsx tests/components/publish-dialog.test.tsx</automated>
    <fails_when>`tsc` prints an `error TS` diagnostic, or the vitest summary reports a non-zero `failed` count for any of the four files, or a direct-post refusal case reports a write having occurred.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every label, hint, confirm label and failure string in both dialogs matches 07-UI-SPEC §6.1 verbatim.
    - The already-paid branch renders the banner plus the existing attempt's provider, date and reference, and renders no confirm affordance.
    - Both dialogs gate confirm on a reason of at least 10 characters and disable confirm while pending.
    - The direct-post cases prove the Server Actions refuse an unauthorized caller with zero writes.
    - The refund cap refusal names the eligible figure.
    - The evidence-field assumption is recorded in a comment at the field and repeated in the SUMMARY.
    - The existing `confirm-modal` and `publish-dialog` component tests still pass unchanged.
  </acceptance_criteria>
  <done>Finance can confirm a manual payment or record a refund safely, and a duplicate attempt shows the existing transaction instead.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

**New route segments**
- `/staff/payments` (list)
- `/staff/payments/[orderId]` (detail)

**New React components**
- `ManualPaymentDialog`
- `RefundDialog`

**New files**
- `src/server/services/payment-read-service.ts`
- `src/app/staff/payments/page.tsx`, `src/app/staff/payments/[orderId]/page.tsx`, `src/app/staff/payments/actions.ts`
- `tests/components/payment-detail.test.tsx`, `tests/payment-read-service.test.ts`, `tests/staff-payments-actions.test.ts`

**New exported symbols**
- `listPaymentsForStaff()`, `getPaymentDetailForStaff()`, `derivedSettlementState()` (`payment-read-service.ts`)
- `confirmManualPaymentAction()`, `recordRefundAction()` (`staff/payments/actions.ts`)

**Changed configuration**
- `src/app/staff/layout.tsx` — one new `StaffNavItem`, "Payments"

**New Prisma models/columns, env vars, CLI entries:** none.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser -> payments Server Actions | Manual confirmation and refund requests cross here, including direct posts that never rendered a button |
| database -> staff render | Settlement figures, learner identity and provider references are read into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Elevation of Privilege | `staff/payments/actions.ts` | high | mitigate | Both actions delegate to the `withPermission`-wrapped services from 07-08; direct-post cases assert refusal with zero writes, so the hidden button is never the access control (RBAC-06, PAY-03, PAY-05) |
| T-07-13 | Information Disclosure | `/staff/payments*` | high | mitigate | `payments.view` scoping on both reads; no provider credential, subaccount code or connected-account id is read by any component on these pages; the denied panel shows no cue keyed to record existence (PAY-14, D-02) |
| T-07-44 | Tampering | duplicate manual confirmation from the UI | high | mitigate | The already-paid branch renders no confirm affordance and shows the existing transaction; the service's own PAY-04 guard refuses a direct post regardless of what rendered (PAY-04) |
| T-07-45 | Tampering | refund above eligible value submitted directly | high | mitigate | The cap is enforced in `recordRefund` (07-08); the dialog's hint is informational only, and a direct post above the cap is refused with the eligible figure named (PAY-05, D-22) |
| T-07-46 | Repudiation | manual confirmation without an attributable reason | medium | mitigate | Both dialogs and both actions require a reason of at least 10 characters, stored on the `PaymentAttempt`/`Refund` row with the acting staff id, matching the codebase's existing correction-reason convention (PAY-03, PAY-05) |
</threat_model>

<verification>
- `npx tsc --noEmit && npx vitest run tests/components/payment-detail.test.tsx tests/payment-read-service.test.ts tests/staff-payments-actions.test.ts tests/components/confirm-modal.test.tsx tests/components/publish-dialog.test.tsx` exits 0.
- A null actual settlement value renders the em-dash string and never a zero.
- Direct posts to both Server Actions from an unauthorized caller are refused with zero writes.

**Outstanding human verification** (carried into `07-UAT.md`): the reconciliation-exception banner at
a very large variance magnitude needs a rendered check — 07-UI-SPEC §8 records this as a backstop, and
absent explicit evidence it resolves to human-needed rather than a silent pass. The
`ManualPaymentDialog` evidence-field ceiling assumed here also needs confirmation or override.
</verification>

<success_criteria>
- Finance sees all three providers in one list, with payment and settlement state per row.
- Expected and actual settlement are shown side by side, with null distinguishable from zero.
- A reconciliation mismatch is visible and correctly toned.
- Manual confirmation and refunds are invocable, permission-gated at the server, and duplicate-safe.
</success_criteria>

<output>
Create `.planning/phases/07-multi-gateway-payments-paystack-manual-refunds/07-10-SUMMARY.md` when done.
</output>

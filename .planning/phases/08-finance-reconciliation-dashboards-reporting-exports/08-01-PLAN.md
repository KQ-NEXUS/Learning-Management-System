---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260915120000_reconciliation_cases/migration.sql
  - src/server/services/reconciliation-case-service.ts
  - src/app/staff/reconciliation/[caseId]/page.tsx
  - tests/reconciliation-case.integration.test.ts
autonomous: true
requirements: [PAY-06, PAY-12, RPT-02]
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "A permitted Finance user can open a payment or refund reconciliation case and see provider, currency, expected/actual integer-minor-unit amounts, the existing payment timeline, evidence, and history (D-05, D-09)."
    - "A case has durable current state plus append-only events and can preserve an earlier resolution when contradictory evidence reopens it (D-02, D-08)."
    - "Manual gateway-only values are represented as not applicable, online unknown actuals as pending/null, and numeric zero only as genuine zero (D-10)."
    - "[PAY-12 boundary/precision] All money persists as integer minor units and NGN/USD are never aggregated together (D-23)."
  artifacts:
    - { path: "prisma/schema.prisma", provides: "Payment-or-refund ReconciliationCase and ReconciliationCaseEvent models" }
    - { path: "src/server/services/reconciliation-case-service.ts", provides: "Scoped case synchronization/detail projection" }
    - { path: "src/app/staff/reconciliation/[caseId]/page.tsx", provides: "Production case-detail tracer route" }
  key_links:
    - { from: "src/app/staff/reconciliation/[caseId]/page.tsx", to: "src/server/services/reconciliation-case-service.ts", via: "authorized server-side detail read" }
    - { from: "src/server/services/reconciliation-case-service.ts", to: "prisma/schema.prisma", via: "case/event transaction" }
    - { from: "src/server/services/reconciliation-case-service.ts", to: "/staff/payments/[orderId] and /staff/cohorts/[cohortId]", via: "server-derived links emitted only after current payment/refund/enrolments permission and trusted case-ancestry checks (D-07)" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] An investigation resolution must never mutate payment, refund, settlement, expected-fee, or enrolment facts (D-07)."
    - "[FLAGGED-UNVERIFIED] Manual gateway-only values must never be represented as zero or pending (D-10)."
---

<objective>Deliver the production reconciliation tracer: one scoped case from durable evidence through a server service to a dedicated Finance detail page (D-05, D-08, D-09). Purpose: prove the phase's database/authorization/UI path before expanding the queue. Output: applied case ledger schema, service, page, and live-Postgres proof.</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-CONTEXT.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-RESEARCH.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-PATTERNS.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: End-to-end scoped reconciliation case detail</name>
  <files>prisma/schema.prisma, prisma/migrations/20260915120000_reconciliation_cases/migration.sql, src/server/services/reconciliation-case-service.ts, src/app/staff/reconciliation/[caseId]/page.tsx, tests/reconciliation-case.integration.test.ts</files>
  <read_first>
    - prisma/schema.prisma (`PaymentAttempt`, `Refund`, `AuditEvent`, `User` relations)
    - src/server/services/payment-reconciliation-service.ts (conditional transaction and evidence projection)
    - src/server/services/payment-read-service.ts (factory/live binding and finance-safe detail projection)
    - src/app/staff/payments/[orderId]/page.tsx (dedicated detail/denial pattern)
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (promised dynamic params)
  </read_first>
  <behavior>
    - Verified conflicting payment or refund evidence opens one subject-specific case and one OPENED event; replay does not duplicate either.
    - Detail authorization derives scope from the case's trusted Order/Cohort ancestry and denies absent/out-of-scope IDs identically.
    - Resolved-case evidence fingerprint change appends REOPENED and retains prior RESOLVED history (D-08).
    - The projection distinguishes expected, actual, pending, not-applicable, and zero values and includes the established payment timeline plus permission-gated links to existing payment, refund, and cohort/enrolment corrective workflows (D-05/D-07/D-10).
  </behavior>
  <action>Add controlled enums for subject (`PAYMENT`, `REFUND`), risk (`CAPTURED_MONEY`, `SETTLEMENT_VARIANCE`, `MISSING_PROVIDER_DATA`), case status, event type, and resolution reason. Add `ReconciliationCase` with nullable `paymentAttemptId`/`refundId`, a database CHECK requiring exactly one subject, and partial unique indexes that permit exactly one case per payment attempt or refund; include assignment/resolution/fingerprint timestamps and append-only `ReconciliationCaseEvent` carrying actor, safe evidence, fingerprint and time. Create the named additive migration. Implement `createReconciliationCaseService` with narrow injected delegates, payment/refund `syncEvidenceAsSystem` using conditional/unique writes, and `getCaseDetail` wrapped by `payments.view` with trusted cohort ancestry per D-05/D-08/D-09. Its projection must reuse the existing payment-detail timeline, identify the case subject, and return only server-derived corrective links the actor can currently use: `/staff/payments/[orderId]` for payment facts; the existing refund control on that same detail page only after `refunds.manage` authorization; and `/staff/cohorts/[cohortId]` as the enrolment corrective destination only after both `cohorts.view` and `enrolments.manage` authorize the case's database-derived cohort. Never accept an order/cohort/enrolment target from browser input when constructing these links. Define the detail-history projection seam so later payment/refund/enrolment operational AuditEvents can be correlated by trusted case subject/order/cohort/enrolment identifiers and correlation ID without rewriting case events. Render the detail tracer as an async Server Component with promised params, existing tokens, non-enumerating denial, separate expected/actual money, ordered payment timeline and case history, corrective-workflow links, and exact D-10 labels. Case resolution must never invoke those corrective actions or mutate financial facts.</action>
  <verify>
    <automated>npx prisma validate &amp;&amp; npx vitest run tests/reconciliation-case.integration.test.ts</automated>
    <fails_when>Command exits non-zero, zero tests pass, a replay adds another case/event, scope denial leaks existence, or resolution mutates a payment-domain row.</fails_when>
  </verify>
  <acceptance_criteria>
    - Prisma validation exits 0 and the integration suite passes against Testcontainers PostgreSQL.
    - Concurrent identical payment and refund synchronization each yield exactly one subject case and one winning transition event.
    - The detail page obtains all displayed facts, timeline entries, and corrective-workflow links from the authorized service projection, not client input.
    - Link tests prove payment/refund/enrolment destinations are absent without their current permission or matching trusted scope, and a captured-money-without-enrolment case exposes the authorized cohort/enrolment workflow rather than a reconciliation mutation.
    - Order, PaymentAttempt, Refund, and Enrolment rows remain byte-identical during case-only transitions.
  </acceptance_criteria>
  <done>One real scoped reconciliation case can be synchronized and rendered end to end with honest payment semantics.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the reconciliation migration to a live database</name>
  <files>prisma/migrations/20260915120000_reconciliation_cases/migration.sql</files>
  <read_first>
    - package.json (`db:migrate` uses `prisma migrate dev`)
    - prisma/migrations/20260912120000_dual_currency_split_settlement/migration.sql
    - prisma/schema.prisma as changed by Task 1
  </read_first>
  <action>Run `npm run db:migrate` in an interactive local shell and then `npx prisma generate`. This is BLOCKING: if Prisma reports non-TTY restrictions, database unavailability, or authentication failure, stop and report it; do not substitute `db push`, generate-only proof, or hand-applied SQL. Rerun the live integration suite only after `npx prisma migrate status` reports no pending migration.</action>
  <verify>
    <automated>npx prisma migrate status &amp;&amp; npx vitest run tests/reconciliation-case.integration.test.ts</automated>
    <fails_when>Migration status is not up to date, the live-database suite exits non-zero, or generated types exist while the database lacks either case table.</fails_when>
  </verify>
  <acceptance_criteria>
    - Migration status explicitly reports the database is up to date.
    - The named migration contains both case tables, relations, the exactly-one-subject constraint, and partial unique payment-attempt/refund identities.
    - Integration tests execute database writes/reads rather than schema-text-only assertions.
  </acceptance_criteria>
  <done>The live database and generated client both expose the reconciliation ledger.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser → case page/service | Case IDs are untrusted and scope is re-derived from stored ancestry. |
| provider evidence → case ledger | Evidence is normalized/redacted before durable history. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Information Disclosure | case detail | high | mitigate | `payments.view` plus trusted cohort/programme/course scope; identical not-found/denial response. |
| T-08-02 | Tampering | case synchronization | high | mitigate | deterministic fingerprint, unique identity, conditional transaction, append-only events. |
| T-08-03 | Repudiation | case history | medium | mitigate | actor/system identity, correlation, before/after evidence and timestamps. |
| T-08-SC | Tampering | package installs | low | accept | This plan installs no package. |
</threat_model>

## Artifacts this phase produces
- `ReconciliationCase`, `ReconciliationCaseEvent`, and controlled enums in `prisma/schema.prisma`.
- `createReconciliationCaseService`, `syncEvidenceAsSystem`, and `getCaseDetail` in `src/server/services/reconciliation-case-service.ts`.
- `/staff/reconciliation/[caseId]` tracer page and `tests/reconciliation-case.integration.test.ts`.

<verification>`npx prisma migrate status`; focused integration suite; `npx tsc --noEmit`.</verification>
<success_criteria>The reconciliation ledger is applied to a live database and one permission/scope-safe detail path works end to end.</success_criteria>
<output>Create `.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-01-SUMMARY.md`.</output>

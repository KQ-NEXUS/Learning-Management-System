---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 12
type: execute
wave: 4
depends_on: [08-02, 08-05]
files_modified:
  - src/app/staff/reconciliation/ReconciliationWorkspace.tsx
  - src/app/staff/reconciliation/actions.ts
  - tests/components/ReconciliationWorkspace.test.tsx
autonomous: true
requirements: [PAY-06, RPT-02, RPT-03]
estimate: { tokens: 14000, raw_tokens: 14000, tasks: 1, confidence: low }
must_haves:
  truths:
    - "Authorized Finance staff can queue a refund reconciliation CSV from the reconciliation workspace using its current provider/currency/date/status filters (PAY-06, D-15, D-24)."
    - "Stripe, Paystack, and Manual refund exports contain exactly the scoped matching refund rows; failed requests retain filters/options and never imply a download."
  artifacts:
    - { path: "src/app/staff/reconciliation/ReconciliationWorkspace.tsx", provides: "Provider-filtered refund export control and feedback" }
    - { path: "src/app/staff/reconciliation/actions.ts", provides: "Strict reconciliation refund export Server Action" }
  key_links:
    - { from: "src/app/staff/reconciliation/ReconciliationWorkspace.tsx", to: "src/app/staff/reconciliation/actions.ts", via: "normalized refund filters, columns, reason, and frozen asOf" }
    - { from: "src/app/staff/reconciliation/actions.ts", to: "src/server/services/export-service.ts", via: "`reconciliation-refunds` immutable export request" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Refund export must not broaden provider filters, accept client scope/rows, or expose synchronous file bytes/storage keys."
---

<objective>Expose PAY-06 refund reconciliation CSV from the scoped reconciliation workspace using the immutable export lifecycle (D-15, D-20, D-24). Output: export control, strict action, and provider-parity component coverage.</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-CONTEXT.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-02-SUMMARY.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-05-SUMMARY.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Queue provider-filtered refund reconciliation CSV from the workspace</name>
  <files>src/app/staff/reconciliation/ReconciliationWorkspace.tsx, src/app/staff/reconciliation/actions.ts, tests/components/ReconciliationWorkspace.test.tsx</files>
  <read_first>
    - src/app/staff/reconciliation/ReconciliationWorkspace.tsx and actions.ts from 08-02
    - src/app/staff/reports/actions.ts and `requestExport` contract from 08-05
    - src/server/services/report-registry.ts (`reconciliation-refunds` export definition)
    - node_modules/next/dist/docs/01-app/02-guides/server-actions.md
    - 08-UI-SPEC.md export dialog and reconciliation state contracts
  </read_first>
  <behavior>
    - Export control is shown only with current view/export capability and submits the normalized provider/currency/date/status filters and server-projected asOf currently represented by the workspace.
    - Strict action accepts only filters, selected column IDs, operational reason, and asOf; the export service reauthorizes and queries trusted refund rows.
    - Stripe, Paystack, and Manual filter cases queue metadata/rows equal to the visible authorized refund projection; failure retains inputs and success links to Export History.
  </behavior>
  <action>Add `Export refunds CSV` to the reconciliation workspace without changing the assignment/resolution controls. Build its accessible options dialog from the server-owned `reconciliation-refunds` column definition and submit normalized provider/currency/date/status filters, selected allowlisted columns, operational reason, and frozen asOf. Add strict `requestReconciliationRefundExportAction`; delegate to `requestExport` with the constant `reconciliation-refunds` ID, re-read current actor/scope in the service, and return job reference/status only. Preserve filters/options/reason on safe failure; announce Queued and link to Export History on success.</action>
  <verify><automated>npx vitest run tests/components/ReconciliationWorkspace.test.tsx tests/export-service.integration.test.ts tests/report-scope.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, any provider filter broadens rows, displayed/exported refund parity fails, unauthorized control appears, failure loses state, or action exposes rows/file/storage data.</fails_when></verify>
  <acceptance_criteria>
    - Component and integration assertions cover All, Stripe, Paystack, Manual, empty, failure-retention and queued-success states.
    - The queued snapshot metadata and rows match the same authorized refund request for provider/currency/date/status/asOf.
  </acceptance_criteria>
  <done>PAY-06 refund reconciliation has a real scoped, provider-filtered asynchronous CSV entry point.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reconciliation browser → Server Action | Filters/columns/reason/asOf are untrusted; actor and scope are server-derived. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-34 | Information Disclosure | refund export action | high | mitigate | Constant dataset ID, strict schema, current permission/scope query, provider-parity tests. |
| T-08-35 | Tampering | filters/asOf/columns | medium | mitigate | Registry allowlist and normalized server validation against the rendered request. |
| T-08-SC | Tampering | installs | low | accept | This plan installs no package. |
</threat_model>

## Artifacts this phase produces
- Provider-filtered refund reconciliation export control, action, and parity tests.

<verification>Reconciliation component, export integration, and scope integration suites.</verification>
<success_criteria>Finance can export exactly the scoped refund reconciliation rows represented by current provider filters.</success_criteria>
<output>Create `.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-12-SUMMARY.md`.</output>

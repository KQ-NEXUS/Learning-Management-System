---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/server/services/reconciliation-case-service.ts
  - src/server/services/payment-reconciliation-service.ts
  - src/app/staff/reconciliation/page.tsx
  - src/app/staff/reconciliation/ReconciliationWorkspace.tsx
  - src/app/staff/reconciliation/[caseId]/page.tsx
  - src/app/staff/reconciliation/[caseId]/ReconciliationCaseDetail.tsx
  - src/app/staff/reconciliation/actions.ts
  - tests/reconciliation-case-service.test.ts
  - tests/components/ReconciliationWorkspace.test.tsx
autonomous: true
requirements: [PAY-06, PAY-12, RPT-02]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "The reconciliation landing order is unresolved exceptions, separate-currency summaries, then All transactions (D-01, D-23)."
    - "One queue has All/Stripe/Paystack/Manual tabs, provider/currency text, risk-then-age ordering, and scoped totals equal its rows (D-04, D-06, D-09)."
    - "Cases start shared, allow optional single/bulk assignment, never bulk resolution, and retain selection on failure (D-03)."
    - "Resolution requires a controlled reason and case note, changes investigation state only, and later contradictory evidence reopens it (D-02, D-07, D-08)."
    - "Empty, loading, error, populated, partial settlement, overflow, 0/1/many, and long-history states follow UI-SPEC §11 without replacing failures by zero."
    - "[FLAGGED ASSUMPTION PAY-06] Reconciliation parity is defined as summary totals equalling the complete scoped row set for the identical normalized provider/date/currency/status filters."
  artifacts:
    - { path: "src/app/staff/reconciliation/ReconciliationWorkspace.tsx", provides: "Exception-first provider-tab queue" }
    - { path: "src/app/staff/reconciliation/actions.ts", provides: "Validated assignment and resolution boundaries" }
    - { path: "tests/reconciliation-case-service.test.ts", provides: "Lifecycle, concurrency, and isolation tests" }
  key_links:
    - { from: "src/app/staff/reconciliation/actions.ts", to: "src/server/services/reconciliation-case-service.ts", via: "validated IDs/reason/note only" }
    - { from: "src/server/services/payment-reconciliation-service.ts", to: "src/server/services/reconciliation-case-service.ts", via: "post-evidence synchronization" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Bulk resolution must not exist (D-03)."
    - "[FLAGGED-UNVERIFIED] Resolution must not alter money, payment, refund, settlement, or enrolment facts (D-07)."
---

<objective>Expand the tracer into the complete exceptions-first reconciliation workspace and audited investigation lifecycle (D-01–D-10). Purpose: Finance can find, own, investigate, resolve, and automatically reopen cases without changing source financial facts. Output: queue/detail UI, mutations, synchronization hooks, and regression coverage.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-CONTEXT.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md
@src/server/services/reconciliation-case-service.ts</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Complete case lifecycle, priority, scope, and evidence reopening</name>
  <files>src/server/services/reconciliation-case-service.ts, src/server/services/payment-reconciliation-service.ts, tests/reconciliation-case-service.test.ts</files>
  <read_first>
    - src/server/services/reconciliation-case-service.ts and tests/reconciliation-case.integration.test.ts from 08-01
    - src/server/services/payment-reconciliation-service.ts (winning settlement transition)
    - src/server/permissions/with-permission.ts and src/server/permissions/scope.ts
  </read_first>
  <behavior>
    - List uses active grant unions before query, filters provider/currency/status/date, and sorts captured-money then variance then missing-data, oldest/id tie-break (D-06/D-09).
    - Assign one/many cases only to a permitted Finance user; empty assignment returns them to shared queue (D-03).
    - Resolve requires controlled reason plus nonblank note and writes only case/event/audit rows (D-02/D-07).
    - New material fingerprint on a resolved case reopens exactly once under concurrent synchronization (D-08).
  </behavior>
  <action>Extend the service with `listCases`, `getReconciliationSummary`, `assignCases`, and `resolveCase`; define stable risk/reason labels; make all reads and filter options scope-aware before SQL. Wire every winning payment reconciliation evidence update to idempotent `syncEvidenceAsSystem`, including previously reconciled attempts when material evidence changes. Use transactions/conditional predicates so concurrent assign/resolve/reopen operations converge; emit one append-only case event and safe AuditEvent per winning mutation. Never update payment-domain models from resolve/assign.</action>
  <verify><automated>npx vitest run tests/reconciliation-case-service.test.ts tests/reconciliation-case.integration.test.ts tests/payment-reconciliation.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, broader totals than rows, duplicate reopen events, or forbidden domain-row changes.</fails_when></verify>
  <acceptance_criteria>
    - Tests cover multi-grant scope unions, out-of-scope IDs/options, deterministic stable ordering, empty sets, idempotent/concurrent reopen, and byte-identical financial rows.
    - Provider/manual differences and separate NGN/USD totals are asserted.
  </acceptance_criteria>
  <done>Case lifecycle is scope-safe, append-only, deterministic, and convergent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build exception-first queue with provider tabs and assignment-only bulk action</name>
  <files>src/app/staff/reconciliation/page.tsx, src/app/staff/reconciliation/ReconciliationWorkspace.tsx, src/app/staff/reconciliation/actions.ts, tests/components/ReconciliationWorkspace.test.tsx</files>
  <read_first>
    - src/app/staff/payments/page.tsx and PaymentsTable.tsx
    - src/app/staff/audit/AuditTable.tsx (URL-backed filters, desktop/mobile parity)
    - src/app/staff/payments/actions.ts (Zod/action error contract)
    - 08-UI-SPEC.md §§6.1, 9–11
    - node_modules/next/dist/docs/01-app/02-guides/server-actions.md
  </read_first>
  <behavior>
    - Page order and tabs implement D-01/D-04; every row has provider/currency/risk/age and links to its case.
    - Empty state retains tabs, summaries, and All transactions link; error retains filters and safe recovery controls.
    - Desktop table and sub-640px semantic cards expose equivalent fields; controls wrap without page scroll.
    - Bulk action is assignment only and failed action preserves selection/current assignments (D-03).
  </behavior>
  <action>Create async page parsing promised URL search params and loading scoped cases/summary/options. Create the client workspace using existing ResourceTable/StatusPill/tokens, exact UI-SPEC copy, URL-backed All/Stripe/Paystack/Manual tabs and filters, semantic mobile cards, and no automatic refresh. Create strict Zod Server Actions accepting only trusted IDs plus assignee/reason/note; re-read scope in the service; return narrow safe failures and revalidate queue/detail only after success. Do not expose bulk resolution.</action>
  <verify><automated>npx vitest run tests/components/ReconciliationWorkspace.test.tsx tests/reconciliation-case-service.test.ts</automated><fails_when>Non-zero exit, zero component tests, missing mobile parity, local-only filters, bulk-resolution control, or optimistic success on failure.</fails_when></verify>
  <acceptance_criteria>
    - Component tests assert empty/loading/error/populated/one/many/overflow states and exact recovery copy.
    - Metric totals and provider tabs preserve normalized URL filters and scope.
    - Assignment dialog traps/returns focus, preserves inputs on failure, and disables only submitted action.
  </acceptance_criteria>
  <done>Finance can operate the exception queue and assignment workflow with honest recoverable UI states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete dedicated case detail and reason-plus-note resolution</name>
  <files>src/app/staff/reconciliation/[caseId]/page.tsx, src/app/staff/reconciliation/[caseId]/ReconciliationCaseDetail.tsx, src/app/staff/reconciliation/actions.ts, tests/components/ReconciliationWorkspace.test.tsx</files>
  <read_first>
    - src/app/staff/reconciliation/[caseId]/page.tsx from 08-01
    - src/app/staff/payments/[orderId]/page.tsx and src/components/primitives/ConfirmModal.tsx
    - 08-UI-SPEC.md §6.2 and copywriting contract
  </read_first>
  <behavior>
    - Detail shows order, learner, provider references, expected/actual fields, assignment, notes, and chronological events (D-05/D-10).
    - Resolution requires category+note and shows the assurance that it changes investigation only (D-02/D-07).
    - Failed resolution keeps values/focus and does not imply state changed; history wraps long evidence without truncating its only investigative value.
  </behavior>
  <action>Extract a dedicated detail client component and add assign/resolve dialogs with controlled reason options, required note validation, focus/error-summary behavior, exact assurance/recovery copy, and ordered semantic history. Render manual fields as `Not applicable`, unknown online actuals as pending, and actual zero numerically. Display prior resolution plus reopen cause when D-08 occurs.</action>
  <verify><automated>npx vitest run tests/components/ReconciliationWorkspace.test.tsx tests/reconciliation-case.integration.test.ts</automated><fails_when>Non-zero exit, resolution can submit without both fields, history is overwritten, or manual/pending/zero states collapse.</fails_when></verify>
  <acceptance_criteria>
    - Tests assert required category/note, failed-form retention, chronological history, reopen evidence, manual/pending/zero labels, and no forbidden bulk resolve.
  </acceptance_criteria>
  <done>Each case has a complete audited investigation surface and resolution does not alter source facts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser → Server Actions | IDs, assignee, reason and note are untrusted. |
| grants → collection queries | Aggregates/options/rows must share the same authorized predicate. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04 | Elevation of Privilege | queue/actions | high | mitigate | Server reauthorization from trusted case/order ancestry for every row and mutation. |
| T-08-05 | Information Disclosure | totals/options | high | mitigate | Scope predicate applied before aggregate, rows and option selection; non-revealing denials. |
| T-08-06 | Tampering | assign/resolve/reopen | high | mitigate | Strict input, transactions, conditional state transitions, no financial delegates in resolution path. |
| T-08-07 | Repudiation | resolution | medium | mitigate | Required category/note plus immutable actor/time events and audit correlation. |
| T-08-SC | Tampering | package installs | low | accept | No package install in this plan. |
</threat_model>

## Artifacts this phase produces
- Complete reconciliation lifecycle exports in `reconciliation-case-service.ts`.
- `/staff/reconciliation` queue, `ReconciliationWorkspace`, `ReconciliationCaseDetail`, and validated actions.
- Lifecycle and UI state suites named in `files_modified`.

<verification>Focused reconciliation/component suites; `npx tsc --noEmit`.</verification>
<success_criteria>All D-01–D-10 workflows are usable, scoped, audited, and preserve original financial evidence.</success_criteria>
<output>Create `08-02-SUMMARY.md`.</output>

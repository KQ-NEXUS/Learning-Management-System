---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 10
type: execute
wave: 6
depends_on: [08-02, 08-04, 08-08, 08-09, 08-11, 08-12]
files_modified:
  - src/app/staff/StaffShell.tsx
  - tests/phase8-invariants.test.ts
  - tests/components/finance-reporting-ui.test.tsx
  - .planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-VALIDATION.md
autonomous: true
requirements: [PAY-06, PAY-12, RPT-01, RPT-02, RPT-03, RPT-04, RPT-05]
estimate: { tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Staff navigation reaches Reconciliation and Reports; active path and mobile shell behavior remain correct."
    - "UI-EMPTY-RECON: Empty reconciliation retains provider tabs, summary access and All transactions link with approved copy."
    - "UI-EMPTY-REPORT: A checked available-report zero uses report-zero copy and never the unavailable panel."
    - "UI-EMPTY-EXPORT: Empty Export History retains dataset/status filters and Refresh status."
    - "UI-LOADING-PAGES: Queue, reports and histories preserve chrome/filter dimensions with skeletons and polite announcements."
    - "UI-LOADING-MUTATION: Export submission disables only its action, exposes pending text and never implies synchronous download."
    - "UI-ERROR-PAGES: Reconciliation/report/history errors use safe recovery copy, preserve filters and retain independently successful sections."
    - "UI-ERROR-MUTATION: Resolve/assign/export failures retain form values or selection and never show optimistic success."
    - "UI-POPULATED-RECON: Queue rows are exception-first, risk/age ordered, text-label provider/currency, individually linked, with assignment-only bulk action."
    - "UI-POPULATED-REPORT: Hub/dashboard show concise overview, groups, trust frame, metrics, at most one accessible breakdown and matching rows."
    - "UI-POPULATED-EXPORT: History rows expose frozen as-of, dataset, status, availability, lineage and only currently authorized actions."
    - "UI-PARTIAL-AVAILABILITY: Each dataset independently shows Available or Not available yet; missing capability never becomes zero."
    - "UI-PARTIAL-SETTLEMENT: Unknown online actual is pending, manual gateway-only is Not applicable, actual zero is numeric zero."
    - "UI-PARTIAL-SECTION: A failed dashboard section degrades alone while definition, filters, timestamps and successful sections remain readable."
    - "UI-OVERFLOW-TABLE: Desktop cells wrap identifiers/notes; equivalent mobile cards may omit low-value columns without page overflow."
    - "UI-OVERFLOW-CONTROLS: Provider tabs/filter controls wrap; no page-level horizontal scrolling."
    - "UI-OVERFLOW-HISTORY: Long reconciliation notes/evidence wrap while chronology and actor/time remain visible."
    - "UI-ZERO-ONE-MANY-COLLECTION: Queue/report/history singular/plural and table/card rhythm work at 0, 1 and paginated many rows."
    - "UI-ZERO-ONE-MANY-CURRENCY: Only independently labelled NGN and USD totals render; no cross-currency grand total."
    - "UI-LONG-NAMES: Learner/offer/filter names and active chips wrap with full accessible names."
    - "UI-LONG-REFERENCES: Provider references use mono wrapping and safe job failures are bounded without ellipsizing the only investigative value."
    - "UI-LONG-NOTES: Resolution textarea grows to a bounded height and saved notes keep actor/category/time adjacent."
    - "UI-LONG-UNAVAILABLE: Owning-capability explanation reflows without truncation."
    - statement: "At 200% zoom, dense headline cards reflow to one column with no clipped currency amount or horizontal page scroll."
      verification: backstop
    - statement: "Large NGN/USD values, negative variances, pending and not-applicable labels render without overlap."
      verification: backstop
    - "Provider/risk/status/currency never rely on color; metrics/segments have text values/focus; desktop tables and mobile cards have semantic parity."
    - "[Phase edge audit] PAY-06/PAY-12/RPT-01..05 acceptance, precision, stable ordering, empty/adjacent boundaries, idempotency/concurrency and flagged assumptions have executable coverage."
  artifacts:
    - { path: "tests/phase8-invariants.test.ts", provides: "Cross-subsystem authorization/trust/prohibition gates" }
    - { path: "tests/components/finance-reporting-ui.test.tsx", provides: "Complete UI consideration/backstop matrix" }
  key_links:
    - { from: "src/app/staff/StaffShell.tsx", to: "/staff/reconciliation and /staff/reports", via: "the existing NAV registry consumed by the staff layout" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Phase 8 must not add shadcn, a chart library, raw hex UI colors, live FX conversion, bulk exception resolution, or public/permanent report links."
    - "[FLAGGED-UNVERIFIED] Denied UI/errors must not confirm existence of out-of-scope case/job/record/filter options."
---
<objective>Integrate Phase 8 navigation and seal every UI/trust/edge contract with cross-subsystem gates. Purpose: ensure no requirement or approved state disappears between slices. Output: nav links, invariant/UI matrix tests, completed validation map.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-VALIDATION.md
@src/app/staff/StaffShell.tsx</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Staff navigation and cross-subsystem trust invariants</name>
  <files>src/app/staff/StaffShell.tsx, tests/phase8-invariants.test.ts</files>
  <read_first>
    - src/app/staff/StaffShell.tsx (the existing `NAV` registry and active-route behavior)
    - src/app/staff/layout.tsx (consumer only; do not modify unless implementation proves navigation is not sourced from StaffShell)
    - tests/checkout-phase-invariants.test.ts and boundary.test.ts
    - all Phase 8 service/action/route entry points
  </read_first>
  <behavior>
    - NAV includes working Reconciliation and Reports entries without changing StaffShell auth/chrome.
    - Static/runtime gates find no client Prisma/runtime service import, FX combination, bulk resolution, raw secret/storage-key projection, unsafe raw SQL or public download.
  </behavior>
  <action>Add the two real routes to the existing `NAV` registry in `src/app/staff/StaffShell.tsx`; preserve its active-path and mobile-shell behavior and leave `layout.tsx` unchanged because it only consumes the shell. Create AST/source-boundary tests that enforce server-only services, permission entry points, parameterized queue SQL, no bulk resolve export, no cross-currency aggregator, no storageKey/signed URL in client projections, and no shadcn/chart dependency/raw UI hex additions. Include direct-request authorization tests where runtime assertions are required.</action>
  <verify><automated>npx vitest run tests/phase8-invariants.test.ts tests/boundary.test.ts</automated><fails_when>Non-zero exit, zero tests, nav route missing, or any prohibited boundary is detected.</fails_when></verify>
  <acceptance_criteria>
    - Both links render/activate through existing shell and all source-boundary assertions pass.
  </acceptance_criteria>
  <done>Phase 8 surfaces are reachable and architectural/security prohibitions are executable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Complete UI consideration and accessibility backstop suite</name>
  <files>tests/components/finance-reporting-ui.test.tsx</files>
  <read_first>
    - 08-UI-SPEC.md §§9–11 (all 22 covered rows and 2 backstops)
    - tests/components/audit-table.test.tsx and component tests from 08-02/03/04/08
  </read_first>
  <behavior>
    - Exercise every §11 category across reconciliation, reports and export history without duplicating only happy paths.
    - Assert responsive DOM parity, text-bearing status/focus names, live announcements, time dateTime, focus-return/value retention and long-value wrapping classes.
  </behavior>
  <action>Create a table-driven component contract suite covering each of the 22 explicit consideration rows and the two backstops. Use representative one/many/paginated rows, extremely long names/references/notes/failures, long formatted NGN/USD, negative variance, pending and not-applicable. Assert CSS/DOM reflow contracts at 200% proxy viewport, no page-level overflow class, semantic desktop/mobile content equivalence, and reduced-motion-compatible loading.</action>
  <verify><automated>npx vitest run tests/components/finance-reporting-ui.test.tsx tests/components/ReconciliationWorkspace.test.tsx tests/components/Reports.test.tsx tests/components/ExportHistory.test.tsx tests/components/audit-table.test.tsx</automated><fails_when>Non-zero exit, zero tests, any UI-SPEC row lacks a named case, clipped/ellipsized sole investigative value, or status depends on color alone.</fails_when></verify>
  <acceptance_criteria>
    - Test names map one-to-one to all 24 UI consideration rows; both backstops use large/zoom fixtures.
    - No unresolved UI state remains.
  </acceptance_criteria>
  <done>Approved UI contract has complete explicit and backstop regression evidence.</done>
</task>
<task type="auto">
  <name>Task 3: Finalize validation map and run Phase 8 regression gate</name>
  <files>.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-VALIDATION.md</files>
  <read_first>
    - 08-VALIDATION.md
    - all completed 08-01..08-09 and 08-11..08-12 summaries plus Phase 8 test files
  </read_first>
  <action>Update validation task IDs/plan/waves/file-exists/status from actual results and set `nyquist_compliant: true` only if every requirement has automated evidence. Run all Phase 8 tests plus inherited payment, permission/scope, audit, storage and boundary regressions; then typecheck, lint and build. Record the deployed Netlify background/storage walkthrough as manual-needed unless genuinely executed; never fabricate it.</action>
  <verify><automated>npm test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</automated><fails_when>Any command exits non-zero, the full inherited suite has 0 passed or omits configured projects, validation claims green without evidence, or manual deployment proof is fabricated.</fails_when></verify>
  <acceptance_criteria>
    - All focused and regression commands exit 0; every PAY/RPT row names its evidence.
    - Validation honestly distinguishes automated green from outstanding deployed/manual walkthrough.
  </acceptance_criteria>
  <done>Phase 8 is mechanically covered and ready for conversational UAT.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| all Phase 8 public entries → services | Cross-cutting direct-request bypass and leakage must remain impossible. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-28 | Elevation of Privilege | phase entry points | high | mitigate | Invariant/runtime tests enumerate pages/actions/routes and permission boundaries. |
| T-08-29 | Information Disclosure | UI/error/download | high | mitigate | Non-inference and secret/storage projection gates plus adversarial fixtures. |
| T-08-30 | Tampering | regression evidence | medium | mitigate | Full focused suite, typecheck, lint/build and honest validation status. |
| T-08-SC | Tampering | installs | low | accept | No package install in this plan. |
</threat_model>
## Artifacts this phase produces
- Reconciliation/Reports entries in the existing `StaffShell.tsx` navigation registry.
- `tests/phase8-invariants.test.ts`, `tests/components/finance-reporting-ui.test.tsx`, finalized `08-VALIDATION.md`.
<verification>Complete Phase 8 test command, typecheck, lint and build.</verification>
<success_criteria>All Phase 8 requirements, D-01–D-24, 22 explicit UI states, two backstops and safety prohibitions have traceable evidence.</success_criteria>
<output>Create `08-10-SUMMARY.md`.</output>

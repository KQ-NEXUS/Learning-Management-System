---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 07
type: execute
wave: 4
depends_on: [08-05, 08-06]
files_modified:
  - package.json
  - package-lock.json
  - src/server/services/storage-service.ts
  - src/server/services/export-worker-service.ts
  - src/server/scheduled/process-export-jobs-task.ts
  - src/server/scheduled/expire-export-jobs-task.ts
  - netlify/functions/process-export-jobs-background.ts
  - netlify/functions/expire-export-jobs.ts
  - tests/export-worker.integration.test.ts
  - tests/netlify-export-functions.test.ts
autonomous: true
requirements: [RPT-03, RPT-04]
estimate: { tokens: 43000, raw_tokens: 43000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Oldest queued jobs are claimed once, immutable rows are serialized/uploaded, and terminal/audit writes converge under concurrent workers and platform replay (D-16, D-20)."
    - "Succeeded files expire after 24 hours while job history remains; failed jobs retain only safe UI errors and remain retryable as linked attempts (D-17, D-18)."
    - "Generation uses snapshot rows only and preserves stable columns, filters, as-of, timezone and rowCount (D-20, D-24)."
    - "[RPT-04 edge] Two concurrent claims/process retries/stale recovery runs produce one terminal effect/object/completion audit per job."
  artifacts:
    - { path: "src/server/services/export-worker-service.ts", provides: "Idempotent claim/generate/fail/expire processor" }
    - { path: "netlify/functions/process-export-jobs-background.ts", provides: "Thin background runtime adapter" }
  key_links:
    - { from: "export-worker-service.ts", to: "csv-service.ts", via: "snapshot-row serialization" }
    - { from: "export-worker-service.ts", to: "storage-service.ts", via: "deterministic private managed upload" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] A public function invocation must not accept arbitrary dataset/filter/snapshot instructions."
    - "[FLAGGED-UNVERIFIED] Export objects must not use public/predictable URLs or permanent download links."
---
<objective>Run every export through an idempotent private background lifecycle and expire its file at 24 hours (D-16–D-18, D-20, D-24). Output: vetted dependency, storage/worker services, deployment adapters, and concurrency tests.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-RESEARCH.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-06-SUMMARY.md
@src/server/services/export-service.ts
@src/server/services/csv-service.ts</context>
<tasks>
<task type="auto">
  <name>Task 1: Install approved managed upload and add private export storage operations</name>
  <files>package.json, package-lock.json, src/server/services/storage-service.ts</files>
  <read_first>
    - 08-06-SUMMARY.md (explicit exact-version approval is a precondition)
    - package.json (AWS SDK family version)
    - src/server/services/storage-service.ts (request-agnostic S3 boundary)
  </read_first>
  <precondition>08-06 records explicit human approval of `@aws-sdk/lib-storage@3.1125.0`.</precondition>
  <action>Install exact `@aws-sdk/lib-storage@3.1125.0` without upgrading sibling packages. Extend storage with `buildExportStorageKey(jobId, datasetVersion)`, `uploadExportObject` using managed streaming/multipart upload with bounded concurrency/part size, `deleteExportObject`, and `presignExportObjectUrl` using a very short configured GET TTL and sanitized filename. Keys contain random/deterministic job identity only, never PII/raw filenames. Keep permission/session imports out of this low-level module.</action>
  <verify><automated>npm ls @aws-sdk/lib-storage &amp;&amp; npx tsc --noEmit</automated><fails_when>Non-zero exit, resolved version differs, sibling AWS packages unexpectedly upgrade, or storage imports request permissions.</fails_when></verify>
  <acceptance_criteria>
    - npm reports exactly 3.1125.0 and lockfile matches.
    - Export methods operate only on private configured bucket and never expose credentials.
  </acceptance_criteria>
  <done>Managed private export upload/sign/delete operations are available behind storage service.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Idempotent concurrent export processor and expiry sweep</name>
  <files>src/server/services/export-worker-service.ts, src/server/scheduled/process-export-jobs-task.ts, src/server/scheduled/expire-export-jobs-task.ts, tests/export-worker.integration.test.ts</files>
  <read_first>
    - src/server/services/payment-reconciliation-service.ts (conditional per-item processing)
    - src/server/scheduled/reconcile-payments-task.ts
    - src/server/services/export-service.ts and csv-service.ts from 08-05
    - prisma/schema.prisma (`ExportJob`, `ExportSnapshotRow`)
  </read_first>
  <behavior>
    - Parameterized `FOR UPDATE SKIP LOCKED` claims oldest queued jobs; concurrent claimers never share a job.
    - Worker reads only ordered snapshot rows, writes deterministic object, conditionally marks success/failure and audits once.
    - Stale PROCESSING recovery and repeated expiry converge; expiry deletes object and marks EXPIRED while retaining job/snapshot history.
  </behavior>
  <action>Create injected worker factory with bounded claim/process batches, parameterized SQL, deterministic ordering, claim timestamp/stale recovery, conditional terminal updates and per-item failure isolation. Stream serializer output from immutable rows into storage, record rowCount/storageKey/expiresAt=`completedAt + 24h`, safe error code/message separately from server diagnostics, and audit only winning transitions. Add bounded scheduled processing/expiry task factories; expiry clears storage capability while preserving history.</action>
  <verify><automated>npx vitest run tests/export-worker.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, duplicate claim/object/audit, live-domain requery, unsafe failure text, or expiry loses history.</fails_when></verify>
  <acceptance_criteria>
    - Real-Postgres tests cover 2+ claimers, background replay, stale claim, partial item failure, deterministic ordering, terminal idempotency and repeated expiry.
  </acceptance_criteria>
  <done>Background processing converges safely under concurrency/retries and enforces 24-hour object availability.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Thin Netlify background and scheduled expiry adapters</name>
  <files>netlify/functions/process-export-jobs-background.ts, netlify/functions/expire-export-jobs.ts, tests/netlify-export-functions.test.ts</files>
  <read_first>
    - netlify/functions/reconcile-payments.ts
    - src/server/scheduled/process-export-jobs-task.ts and expire-export-jobs-task.ts
    - 08-RESEARCH.md Open Question 3 and Netlify runtime findings
  </read_first>
  <behavior>
    - Background adapter invokes only server-owned queued job processing and accepts no dataset/filter instructions.
    - Scheduled expiry invokes bounded expiry task; wrapper failures propagate for platform retry/observability.
  </behavior>
  <action>Create injectable thin adapters following the tracked reconcile function. Name the background entry according to Netlify's background-function convention, return accepted behavior through framework adapter, and expose a conservative scheduled expiry config. Validate any invocation secret/shape needed by deployment but never accept job content from HTTP input.</action>
  <verify><automated>npx vitest run tests/netlify-export-functions.test.ts tests/export-worker.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, arbitrary instructions reach worker, wrapper swallows failure, or multiple task invocations occur per call.</fails_when></verify>
  <acceptance_criteria>
    - Wrapper tests prove exactly-once delegation per invocation and no public instruction surface.
  </acceptance_criteria>
  <done>Deployment adapters trigger only database-authoritative processing and expiry.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| database queue → worker | Platform concurrency/retries can replay claims. |
| worker → private S3 | Bytes/keys/errors cross external storage boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-18 | Tampering | claim/terminal writes | high | mitigate | Row locking, conditional transitions, deterministic keys, concurrency tests. |
| T-08-19 | Information Disclosure | object/error | high | mitigate | Private unpredictable keys, safe error fields, no PII key names. |
| T-08-20 | Denial of Service | worker | medium | mitigate | Bounded batches/stream upload/per-item isolation/stale recovery. |
| T-08-SC | Tampering | dependency | high | mitigate | 08-06 explicit approval plus exact install/lock verification. |
</threat_model>
## Artifacts this phase produces
- Export storage methods, `createExportWorkerService`, process/expiry task factories.
- Netlify background/expiry adapters and their integration/function suites.
<verification>Worker/function suites; `npm ls`; `npx tsc --noEmit`.</verification>
<success_criteria>Queued snapshots reliably become one private file or one safe failure, then expire after 24 hours without erasing history.</success_criteria>
<output>Create `08-07-SUMMARY.md`.</output>

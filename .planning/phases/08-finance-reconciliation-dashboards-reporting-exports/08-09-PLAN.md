---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 09
type: execute
wave: 5
depends_on: [08-05, 08-07]
files_modified:
  - src/server/services/audit-export-service.ts
  - src/app/staff/audit/page.tsx
  - src/app/staff/audit/AuditTable.tsx
  - src/app/staff/audit/actions.ts
  - tests/audit-export-service.test.ts
  - tests/audit-append-only.integration.test.ts
  - tests/components/audit-table.test.tsx
autonomous: true
requirements: [RPT-02, RPT-03, RPT-05]
estimate: { tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "Audit CSV uses existing actor/action/target/date filters, frozen request-time data and the same async lifecycle/history/download path (D-15, D-16, D-20)."
    - "Only an actor with global audit.view and audit.export can request it; sensitive columns additionally require authorization and operational reason (D-21, D-24)."
    - "Correlation and redacted investigative context remain useful while secrets, credentials, storage keys, raw provider payloads and stacks never export."
    - "Audit rows remain append-only before/after export requests, failures, retries and downloads."
    - "[FLAGGED ASSUMPTION RPT-05] Global audit export is intentionally not resource-scoped because the existing audit.view boundary requires a global grant; its filters and safe projection are the non-inference boundary."
  artifacts:
    - { path: "src/server/services/audit-export-service.ts", provides: "Global permission-gated redacted audit snapshot producer" }
    - { path: "src/app/staff/audit/actions.ts", provides: "Audit export public action" }
  key_links:
    - { from: "audit-export-service.ts", to: "export-service.ts", via: "immutable audit snapshot request" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Audit export must not expose secrets or credential material, even as selectable sensitive fields."
    - "[FLAGGED-UNVERIFIED] AuditEvent rows must not be updated or deleted by application export workflows."
---
<objective>Extend the existing audit view with a safe, global-permission-gated asynchronous CSV export (D-15, D-16, D-20, D-21, D-24). Output: audit snapshot service, action/UI control, redaction and append-only tests.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@src/server/services/audit-read-service.ts
@src/app/staff/audit/page.tsx
@src/app/staff/audit/AuditTable.tsx
@src/server/services/export-service.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Redacted append-only audit export snapshot</name>
  <files>src/server/services/audit-export-service.ts, tests/audit-export-service.test.ts, tests/audit-append-only.integration.test.ts</files>
  <read_first>
    - src/server/services/audit-read-service.ts and audit-service.ts
    - src/server/permissions/with-permission.ts (global empty-resource semantics)
    - src/server/services/export-service.ts and report-registry.ts
    - prisma/schema.prisma (`AuditEvent`)
  </read_first>
  <behavior>
    - Requires both global audit.view and audit.export; one without the other denies without counts/existence.
    - Snapshot matches normalized existing filters/asOf and includes stable safe actor/action/target/outcome/time/correlation fields.
    - Recursive redaction removes known secret/credential/token/storage/raw-payload keys; append-only rows are byte-identical.
  </behavior>
  <action>Create `requestAuditExport` service that independently enforces both global permissions, validates existing filters and permitted columns, shapes/redacts before passing immutable rows to export service, and uses dataset `audit` with a stable version. Sensitive actor identity requires `users.view` plus explicit reason; never offer secrets as a column. Audit the request with filter/column/reason/correlation metadata without embedding exported evidence.</action>
  <verify><automated>npx vitest run tests/audit-export-service.test.ts tests/audit-append-only.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, either permission is bypassed, secret fixture survives, correlation disappears, or an existing AuditEvent changes.</fails_when></verify>
  <acceptance_criteria>
    - Tests cover permission permutations, filters/as-of edges, safe/sensitive columns, required reason, nested redaction, stable columns, correlation and append-only byte equality.
  </acceptance_criteria>
  <done>Audit snapshots preserve investigative value without weakening global authorization or append-only evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Audit-page export options and queued feedback</name>
  <files>src/app/staff/audit/page.tsx, src/app/staff/audit/AuditTable.tsx, src/app/staff/audit/actions.ts, tests/components/audit-table.test.tsx</files>
  <read_first>
    - src/app/staff/audit/page.tsx and AuditTable.tsx
    - src/app/staff/reports/actions.ts (queued action result)
    - src/components/primitives/ConfirmModal.tsx
    - 08-UI-SPEC.md §§8.1, 8.3 and copy contract
  </read_first>
  <behavior>
    - Export control appears only when audit.view+audit.export are currently held; no disabled teaser otherwise.
    - Default queues safe columns/current filters; sensitive options are unchecked, gated, reason-required and audited.
    - Failure keeps filters/options/reason; success announces queued and links to Export History.
  </behavior>
  <action>Load export capability on server without leaking event existence; add `Export audit CSV` and authorized options dialog to existing table while preserving its filters/expanded evidence/mobile behavior. Create strict Zod action accepting filters, selected column IDs and reason only, delegate to audit-export service, and return safe discriminated results. Keep dialogs accessible and submitted-action-only pending.</action>
  <verify><automated>npx vitest run tests/components/audit-table.test.tsx tests/audit-export-service.test.ts</automated><fails_when>Non-zero exit, zero tests, unauthorized teaser, filters/options lost, missing reason accepted, or synchronous download implied.</fails_when></verify>
  <acceptance_criteria>
    - Tests assert permission visibility, default safe request, sensitive reason flow, failure retention, queued status/link and existing audit-table regressions.
  </acceptance_criteria>
  <done>Authorized reviewers can queue a safe filtered audit export from the existing audit view.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| audit evidence → CSV snapshot | Arbitrary JSON may contain secrets or hostile spreadsheet strings. |
| browser → audit export action | Filters/columns/reason are untrusted; global permission is server-owned. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-25 | Information Disclosure | audit export | high | mitigate | Dual global permissions, allowlisted columns, recursive redaction, sensitive reason gate. |
| T-08-26 | Tampering | audit rows | high | mitigate | Read/snapshot only; byte-equality append-only integration tests. |
| T-08-27 | Repudiation | sensitive export | medium | mitigate | Actor/reason/columns/filters/correlation audit record. |
| T-08-SC | Tampering | installs | low | accept | No package install. |
</threat_model>
## Artifacts this phase produces
- `requestAuditExport`, audit export action/control/options, redaction and append-only tests.
<verification>Audit export, append-only and component suites; `npx tsc --noEmit`.</verification>
<success_criteria>RPT-05 audit export is filterable, reproducible, permission-gated, correlated, redacted and append-only.</success_criteria>
<output>Create `08-09-SUMMARY.md`.</output>

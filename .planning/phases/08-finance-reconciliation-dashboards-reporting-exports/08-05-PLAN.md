---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 05
type: execute
wave: 3
depends_on: [08-01, 08-04]
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260915130000_export_snapshots/migration.sql
  - src/server/services/csv-service.ts
  - src/server/services/export-service.ts
  - src/app/staff/reports/actions.ts
  - src/app/staff/reports/[dataset]/ReportDashboard.tsx
  - tests/csv-service.test.ts
  - tests/export-service.integration.test.ts
  - tests/export-snapshot-load.test.ts
  - tests/components/Reports.test.tsx
autonomous: true
requirements: [PAY-06, RPT-02, RPT-03, RPT-04]
estimate: { tokens: 42000, raw_tokens: 42000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Every accepted export immediately creates a QUEUED job with immutable request-time rows, normalized filters, scope/columns/version/timezone and one frozen asOf; requests exceeding the measured 25,000-row or 25-MiB snapshot bound fail before job creation (D-15, D-16, D-20)."
    - "Safe stable columns are default; permitted sensitive fields require current additional authorization, a nonblank operational reason and audit evidence (D-21)."
    - "CSV headers/metadata are versioned and stable, cells are spreadsheet-safe, and exported rows reconcile to the same scoped request as dashboard rows (D-24)."
    - "Retry preserves the failed row and frozen snapshot in a linked new attempt; rerun after expiry makes a new current snapshot (D-17, D-18)."
    - "[FLAGGED ASSUMPTION RPT-03] Row-level reconciliation means dataset/version/filter/as-of metadata plus deterministic ordinal rows let reviewers map CSV rows back to the authorized report request."
    - "[RPT-02 edge] Duplicate/concurrent requests with the same idempotency key converge to one job/snapshot; empty exports retain headers and genuine rowCount 0."
  artifacts:
    - { path: "prisma/schema.prisma", provides: "Extended ExportJob and immutable ExportSnapshotRow" }
    - { path: "src/server/services/csv-service.ts", provides: "Stable formula-safe CSV serialization" }
    - { path: "src/server/services/export-service.ts", provides: "Request/retry/rerun snapshot lifecycle" }
    - { path: "src/app/staff/reports/[dataset]/ReportDashboard.tsx", provides: "Dashboard export request UI wired to the Server Action" }
  key_links:
    - { from: "src/server/services/export-service.ts", to: "src/server/services/report-query-service.ts", via: "same normalized authorized row projection" }
    - { from: "src/app/staff/reports/actions.ts", to: "src/server/services/export-service.ts", via: "strict public Server Action" }
    - { from: "src/app/staff/reports/[dataset]/ReportDashboard.tsx", to: "src/app/staff/reports/actions.ts", via: "normalized filters, selected columns, operational reason, and frozen asOf submission" }
    - { from: "src/server/services/export-service.ts", to: "trusted report/refund/audit services", via: "server-only TrustedExportProducer callback receiving server-derived scope/filters/asOf; public actions cannot supply rows, scope, or producers" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Background generation must not requery mutable domain rows for an existing job (D-20)."
    - "[FLAGGED-UNVERIFIED] Secrets, credentials, raw provider payloads, storage keys and stack traces must not enter snapshots or CSVs."
---
<objective>Build immutable asynchronous export requests and one safe CSV contract (D-15–D-21, D-23–D-24). Output: applied snapshot schema, serializer, request/retry/rerun service/actions, and concurrency/parity tests.</objective>
<scope_rationale>This plan intentionally keeps the 10 schema/migration/service/action/UI/test files together because snapshot persistence, the live migration, trusted-producer invocation, and CSV/action wiring form one transaction and compatibility boundary. Splitting after schema creation would duplicate ownership of `export-service.ts` and its integration suite or permit a browser action to target an unproven snapshot contract; the separate worker, storage, history, audit, and reconciliation-UI concerns are already isolated in 08-06 through 08-12.</scope_rationale>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-RESEARCH.md
@src/server/services/report-registry.ts
@src/server/services/report-query-service.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Immutable export snapshot schema and transactional request lifecycle</name>
  <files>prisma/schema.prisma, prisma/migrations/20260915130000_export_snapshots/migration.sql, src/server/services/export-service.ts, tests/export-service.integration.test.ts, tests/export-snapshot-load.test.ts</files>
  <read_first>
    - prisma/schema.prisma (`ExportJob`, `JobStatus`, `AuditEvent`)
    - src/server/services/report-query-service.ts and report-registry.ts from 08-03/04
    - src/server/services/payment-reconciliation-service.ts (conditional transaction pattern)
    - tests/payment-reconciliation.integration.test.ts (Testcontainers import/order)
  </read_first>
  <behavior>
    - Request authorization/query/snapshot/job/audit share one transaction and one server asOf; the 25,000-row/25-MiB bound is enforced before job creation and the maximum fixture completes within 10 seconds.
    - Snapshot rows are immutable ordered JSON projections; job stores datasetVersion, filters, scope, columns, timezone, rowCount and sensitive reason.
    - Retry/rerun semantics and concurrent idempotency follow D-17/D-18.
    - A `TrustedExportProducer` receives only the service's normalized filters, server-derived authorized scope, frozen asOf and selected allowlisted columns; public actions cannot submit a producer, scope, or rows.
  </behavior>
  <action>Extend `ExportJob` with datasetVersion, asOf, timezone, scopeSnapshot, columnSnapshot, sensitiveReason, startedAt, safe error code/message, idempotencyKey, self retry relation and updatedAt; add `ExportSnapshotRow` unique on `(jobId, ordinal)` with cascade ownership. Create additive migration and `createExportService` methods `requestExport`, `retryExport`, `rerunExpiredExport`; define an internal server-only `TrustedExportProducer` callback API whose input is exactly `{ datasetId, normalizedFilters, authorizedScope, selectedColumns, asOf, tx }` and whose output is immutable projected rows. `requestExport` authorizes the metadata-only `EXPORT_DATASET_REGISTRY` entry, derives permission/domain scope and matching-scope `users.view`, then invokes the trusted producer inside the same transaction; ordinary report and `reconciliation-refunds` wrappers supply producers backed by `report-query-service`. Public Server Actions accept only dataset/filter/column/reason/asOf values and call closed server wrappers, so neither producer callbacks, rows, scope predicates nor scope snapshots cross the browser boundary. Freeze producer output transactionally, audit chosen sensitive columns/reason, and count rows/serialized snapshot bytes before job creation; reject safely when rows exceed 25,000 or bytes exceed 25 MiB. Add a repeatable PostgreSQL load test at both limits with a 10-second budget and an over-limit test proving no partial job/snapshot/audit remains.</action>
  <verify><automated>npx prisma validate &amp;&amp; npx vitest run tests/export-service.integration.test.ts tests/export-snapshot-load.test.ts</automated><fails_when>Non-zero exit, zero tests, snapshot changes after source edits, duplicate job on replay/race, bound fixture exceeds 10 seconds, over-limit leaves a job, or sensitive request succeeds without permission/reason.</fails_when></verify>
  <acceptance_criteria>
    - Tests mutate/delete source rows after request and prove frozen snapshot remains byte-equivalent.
    - Retry is linked and copies frozen snapshot; rerun receives new asOf/current rows; original jobs remain unchanged.
    - Contract tests prove registry entries are metadata-only, trusted report/refund producers receive the exact server-derived scope/filters/asOf, and forged browser rows/scope/producer fields are rejected or ignored before producer invocation.
  </acceptance_criteria>
  <done>Export requests are immutable, scoped, audited and replay-safe.</done>
</task>
<task type="auto">
  <name>Task 2: [BLOCKING] Apply export migration to a live database</name>
  <files>prisma/migrations/20260915130000_export_snapshots/migration.sql</files>
  <read_first>
    - package.json (`db:migrate`)
    - prisma/schema.prisma as changed in Task 1
    - 08-01-SUMMARY.md (confirmed migration environment)
  </read_first>
  <action>Run `npm run db:migrate` interactively and `npx prisma generate`; stop on non-TTY/database/authentication errors and do not replace with `db push` or generate-only evidence. Only after `migrate status` is clean, run the live snapshot integration suite.</action>
  <verify><automated>npx prisma migrate status &amp;&amp; npx vitest run tests/export-service.integration.test.ts</automated><fails_when>Pending migration, missing live snapshot table/columns, or integration test non-zero exit.</fails_when></verify>
  <acceptance_criteria>
    - Database reports up to date and tests perform real ExportJob/ExportSnapshotRow transactions.
  </acceptance_criteria>
  <done>Live database and Prisma client expose the complete export snapshot contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Stable spreadsheet-safe CSV and validated dashboard export actions</name>
  <files>src/server/services/csv-service.ts, src/app/staff/reports/actions.ts, src/app/staff/reports/[dataset]/ReportDashboard.tsx, tests/csv-service.test.ts, tests/export-service.integration.test.ts, tests/components/Reports.test.tsx</files>
  <read_first>
    - src/app/staff/payments/actions.ts
    - src/server/services/report-registry.ts (column contracts)
    - node_modules/next/dist/docs/01-app/02-guides/server-actions.md
    - 08-UI-SPEC.md §8.1
  </read_first>
  <behavior>
    - Serializer emits stable ordered headers and metadata for dataset/version/filters/generated/request-as-of/timezone and safely encodes comma/quote/newline/formula prefixes.
    - Dashboard submits normalized current filters, selected columns, required sensitive reason when applicable, and the server-rendered frozen asOf to the action; action queues and returns immediately, while failure retains options and reports no download/success.
    - Provider-filtered `reconciliation-refunds` CSV headers/rows/totals match the authorized refund query for the same normalized filters.
  </behavior>
  <action>Create central streaming-capable CSV row serializer with explicit UTF-8/RFC quoting and a documented spreadsheet formula-prefix neutralization policy for `=`, `+`, `-`, `@`. Create strict `requestReportExportAction`, `retryExportAction`, and `rerunExportAction` accepting only export dataset/filter/column/reason/job identifiers plus the rendered request `asOf`; services re-read all ownership/scope and reject stale/tampered asOf values. Wire `ReportDashboard`'s Export CSV dialog to submit its normalized URL filters, selected allowlisted columns, operational reason, and server-projected asOf to `requestReportExportAction`; retain those values on failure and show queued feedback/history link only on success. Revalidate relevant routes only after success and return narrow safe action results. The reconciliation refund UI/action is delivered separately in 08-12 against this export contract.</action>
  <verify><automated>npx vitest run tests/csv-service.test.ts tests/export-service.integration.test.ts tests/components/Reports.test.tsx</automated><fails_when>Non-zero exit, zero tests, dashboard export does not call the action with normalized filters/columns/reason/asOf, refund row/CSV parity fails, formula input remains executable, columns reorder, action returns file bytes/storage keys, or failed values are discarded.</fails_when></verify>
  <acceptance_criteria>
    - Adversarial separators, quotes, CR/LF, Unicode, formula prefixes, null/pending/not-applicable, large and negative minor amounts pass.
    - Action result exposes job reference/status only, never snapshot rows or storage key.
    - Integration tests assert Stripe, Paystack, and Manual refund export filters produce exactly the authorized matching refund rows and metadata; 08-12 adds the reconciliation UI entry point.
  </acceptance_criteria>
  <done>All datasets share one safe stable CSV/request boundary.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser → export action | Dataset, filters, columns, reason and job ID are untrusted. |
| snapshot → spreadsheet | User/provider text can become executable spreadsheet content. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-14 | Information Disclosure | snapshot creation | high | mitigate | Current permission/scope before query; sensitive gate+reason; no client-supplied scope. |
| T-08-15 | Tampering | CSV cells | high | mitigate | Central formula neutralization and adversarial serializer tests. |
| T-08-16 | Tampering | request/retry race | high | mitigate | Unique idempotency and transactional immutable snapshot/job creation. |
| T-08-17 | Information Disclosure | errors/snapshot | high | mitigate | Allowlisted projections and separate safe error fields; exclude secrets/raw payloads. |
| T-08-SC | Tampering | installs | low | accept | No package install in this plan. |
</threat_model>
## Artifacts this phase produces
- Extended `ExportJob`, `ExportSnapshotRow`, migration.
- `createExportService`, request/retry/rerun exports, `serializeCsv` contracts, report Server Actions.
- CSV and snapshot integration suites.
<verification>Migration status, CSV and export integration tests, `npx tsc --noEmit`.</verification>
<success_criteria>Queued exports reproduce request-time scoped rows and stable safe columns without spreadsheet or authorization leakage.</success_criteria>
<output>Create `08-05-SUMMARY.md`.</output>

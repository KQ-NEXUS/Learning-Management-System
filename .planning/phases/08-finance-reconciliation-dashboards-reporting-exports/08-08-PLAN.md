---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 08
type: execute
wave: 5
depends_on: [08-05, 08-07]
files_modified:
  - src/server/services/export-read-service.ts
  - src/server/services/export-download-service.ts
  - src/app/staff/reports/exports/page.tsx
  - src/app/staff/reports/exports/ExportHistory.tsx
  - src/app/staff/reports/exports/actions.ts
  - src/app/api/staff/reports/exports/[jobId]/download/route.ts
  - tests/components/ExportHistory.test.tsx
  - tests/export-download-route.test.ts
autonomous: true
requirements: [RPT-02, RPT-04]
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "Central Export History shows only currently authorized scoped jobs, newest first, with frozen filters/as-of/version/row count/status/availability/lineage (D-15–D-18)."
    - "History uses explicit Refresh status without polling and covers empty/loading/error/populated/pending-row-count/0/1/many/long-failure states."
    - "Every download reauthenticates current permissions and dataset scope, rejects non-success/expired jobs without confirming existence, audits success, and issues private no-store short-lived redirect (D-17, D-21, D-24)."
    - "Failed retry and expired rerun preserve original history; action failure retains filters/choices and never shows a download (D-18)."
  artifacts:
    - { path: "src/app/staff/reports/exports/ExportHistory.tsx", provides: "Audited job lifecycle/history UI" }
    - { path: "src/app/api/staff/reports/exports/[jobId]/download/route.ts", provides: "Current-authorization download redirect" }
  key_links:
    - { from: "download/route.ts", to: "export-download-service.ts", via: "reauthorize then presign" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] History/page props must not expose storage keys or signed URLs."
    - "[FLAGGED-UNVERIFIED] Download permission at request time must not substitute for current authorization."
---
<objective>Deliver Export History and current-authorization downloads (D-15–D-18, D-21, D-24). Output: scoped read/download services, lifecycle UI/actions, route, and route/component tests.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md
@src/server/services/export-service.ts
@src/server/services/storage-service.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Scoped history projection and authorized download service</name>
  <files>src/server/services/export-read-service.ts, src/server/services/export-download-service.ts, tests/export-download-route.test.ts</files>
  <read_first>
    - src/server/services/audit-read-service.ts and payment-read-service.ts
    - src/server/services/export-service.ts and report-registry.ts
    - src/server/services/storage-service.ts
    - src/app/api/lesson-resources/[id]/download/route.ts
  </read_first>
  <behavior>
    - History options/rows apply requester visibility plus current dataset permission/scope before returning IDs/counts.
    - Download checks actor, job visibility, dataset export permission/scope, SUCCEEDED and unexpired state immediately before presign.
  </behavior>
  <action>Create deliberately shaped history projections with human filter chips and no storageKey/signed URL. Create download service that maps all absent/out-of-scope/revoked/unavailable cases to one non-revealing error, rechecks sensitive-column authorization too, audits successful authorization, and only then calls short-TTL presign.</action>
  <verify><automated>npx vitest run tests/export-download-route.test.ts tests/report-scope.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, revoked/out-of-scope actor gets URL, denial differs by existence, or storage key leaks.</fails_when></verify>
  <acceptance_criteria>
    - Tests cover permission/scope revocation after creation, ownership, expiry boundary, wrong status, sensitive gate and no-store result contract.
  </acceptance_criteria>
  <done>History and download data are reauthorized against current grants without inference leakage.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Export History lifecycle UI and refresh/retry/rerun actions</name>
  <files>src/app/staff/reports/exports/page.tsx, src/app/staff/reports/exports/ExportHistory.tsx, src/app/staff/reports/exports/actions.ts, tests/components/ExportHistory.test.tsx</files>
  <read_first>
    - src/app/staff/audit/page.tsx and AuditTable.tsx
    - src/components/primitives/ResourceTable.tsx (`ResourceTable` and `StatusPill`)
    - src/app/staff/reports/actions.ts
    - 08-UI-SPEC.md §§8.2, 9–11
  </read_first>
  <behavior>
    - URL-backed dataset/status/requested/search filters, newest stable order, responsive cards, and all lifecycle action rules.
    - Refresh status preserves filters/focus and politely announces changed statuses; no automatic polling.
    - Failed retry makes linked attempt; expired rerun warns of current snapshot; failures preserve UI state.
  </behavior>
  <action>Build promised-search-param server page and ResourceTable-style client with exact status/action/copy matrix, human chips, semantic ordered desktop/mobile views and no secrets. Strict Server Actions delegate to export service and revalidate only on success. Render Pending row count distinctly from genuine zero; bound and wrap safe failure prose.</action>
  <verify><automated>npx vitest run tests/components/ExportHistory.test.tsx tests/export-service.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, polling exists, filters/focus lost, failed row overwritten, or pending row count renders zero.</fails_when></verify>
  <acceptance_criteria>
    - Component suite covers empty/loading/error, all five statuses, actions, lineage, 0/1/many, long failures and mobile parity.
  </acceptance_criteria>
  <done>Staff can leave, return, refresh, retry/rerun, and understand each export without losing audit history.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: No-store authorized download Route Handler</name>
  <files>src/app/api/staff/reports/exports/[jobId]/download/route.ts, tests/export-download-route.test.ts</files>
  <read_first>
    - src/app/api/lesson-resources/[id]/download/route.ts
    - src/server/services/export-download-service.ts from Task 1
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md
  </read_first>
  <behavior>
    - Promised jobId params are awaited; success is 302 with Location and `Cache-Control: private, no-store`.
    - Missing, denied, expired and non-succeeded return the same non-revealing unavailable response and never mint URL.
  </behavior>
  <action>Create GET handler delegating all authorization/state checks to download service. Return a hand-built no-store redirect only on success; map expected authentication/authorization/unavailable errors identically and rethrow unexpected faults for logging. Never serialize signed URL or key as JSON.</action>
  <verify><automated>npx vitest run tests/export-download-route.test.ts</automated><fails_when>Non-zero exit, zero tests, missing no-store, divergent denial, or signer called on unsuccessful path.</fails_when></verify>
  <acceptance_criteria>
    - Route tests assert promised params, identical denials, current auth, exact expiry edge, audit-before-redirect and no URL on failure.
  </acceptance_criteria>
  <done>Downloads are short-lived capabilities minted only after current authorization.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/job ID → history/download | IDs must not enumerate scoped jobs. |
| app → signed storage URL | URL is minted only after fresh authorization and is not cached. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-21 | Information Disclosure | history/download IDOR | high | mitigate | Current permission/scope filters plus identical denial. |
| T-08-22 | Elevation of Privilege | stale authorization | high | mitigate | Reauthorize dataset/sensitive scope on every download/retry/rerun. |
| T-08-23 | Information Disclosure | signed URL expiry/cache | high | mitigate | Very short presign, 24h object gate, private no-store redirect, no page-prop URL. |
| T-08-24 | Repudiation | download | medium | mitigate | Append audit before successful redirect. |
| T-08-SC | Tampering | installs | low | accept | No package install. |
</threat_model>
## Artifacts this phase produces
- Export history/read/download services, `/staff/reports/exports`, `ExportHistory`, actions and download route.
- Export History component and download route suites.
<verification>History component, download route, export integration tests; `npx tsc --noEmit`.</verification>
<success_criteria>Authorized staff can track every job and download only currently authorized unexpired files.</success_criteria>
<output>Create `08-08-SUMMARY.md`.</output>

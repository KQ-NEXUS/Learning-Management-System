---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 04
type: execute
wave: 2
depends_on: [08-03]
files_modified:
  - src/server/services/report-query-service.ts
  - src/app/staff/reports/[dataset]/page.tsx
  - src/app/staff/reports/[dataset]/ReportDashboard.tsx
  - src/app/staff/reports/[dataset]/loading.tsx
  - tests/report-scope.integration.test.ts
  - tests/components/Reports.test.tsx
autonomous: true
requirements: [PAY-12, RPT-01, RPT-02]
estimate: { tokens: 38000, raw_tokens: 38000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "All ten routes state definition, filters, date basis, scope and refresh/as-of; four query real rows and six explicitly say Not available yet (D-11, D-14, D-19)."
    - "Metric/segment links open matching rows with identical URL filters and authorized scope (D-13, D-24)."
    - "Manual values, pending actuals, zero, large/negative amounts, and separate NGN/USD remain semantically distinct (D-10, D-23)."
    - "Refresh is manual, preserves URL filters, and no failed section becomes zero (D-22)."
    - "[RPT-01 edge] Empty/one/many datasets and adjacent date boundaries use inclusive documented semantics and stable business-date/id ordering."
    - "Available drill-down empty, loading, error, populated, partial-section, mobile-card, overflow, long-text states satisfy UI-SPEC §11."
  artifacts:
    - { path: "src/app/staff/reports/[dataset]/page.tsx", provides: "Ten fixed dashboard route" }
    - { path: "src/app/staff/reports/[dataset]/ReportDashboard.tsx", provides: "Trust frame, metrics, filters, breakdown, rows" }
  key_links:
    - { from: "ReportDashboard.tsx", to: "report-query-service.ts", via: "server projection from one normalized request" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] A failed or unavailable dataset must not render as numeric zero."
    - "[FLAGGED-UNVERIFIED] No chart may rely on hover/color/geometry alone or replace row drill-down."
---
<objective>Deliver the ten fixed dashboard routes with four authoritative datasets and six explicit unavailable states (D-11–D-14, D-19, D-22–D-24). Output: shared dashboard UI/query expansion and coverage.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md
@src/server/services/report-registry.ts
@src/server/services/report-query-service.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Available dashboard aggregates, rows, options, and drill-down parity</name>
  <files>src/server/services/report-query-service.ts, tests/report-scope.integration.test.ts</files>
  <read_first>
    - src/server/services/report-query-service.ts from 08-03
    - src/server/services/registration-service.ts, enrolment-service.ts, attendance-service.ts, payment-read-service.ts
    - prisma/schema.prisma (business event timestamp fields)
    - 08-UI-SPEC.md §7.2 metric table
  </read_first>
  <behavior>
    - Registrations use registration date; payments confirmation date; enrolments activation/transition date; attendance session date (D-19).
    - Aggregates equal the complete filtered row projection and drill-down URL round-trips to exactly those rows (D-13/D-24).
    - Payment projection exposes every PAY-12 component, refunds, safe exception context and provider/status breakdown.
  </behavior>
  <action>Add dataset-specific query adapters for registrations, payments, enrolments, and attendance behind the registry contract. Each returns definition/date basis/scope/asOf/filters/metrics/optional accessible breakdown/rows/options from one normalized server request. Define inclusive date boundaries in Africa/Lagos then compare instants; stable sort by business date then id. Preserve minor units and null/not-applicable distinctions.</action>
  <verify><automated>npx vitest run tests/report-scope.integration.test.ts tests/payment-read-service.test.ts</automated><fails_when>Non-zero exit, zero tests, aggregate/row mismatch, boundary exclusion, unstable order, scope leak, or cross-currency total.</fails_when></verify>
  <acceptance_criteria>
    - Tests cover 0/1/paginated many, exact date edges, repeated reads, all PAY-12 fields, filter/metric drill-down parity and out-of-scope inference.
  </acceptance_criteria>
  <done>Four available datasets share trustworthy query semantics.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Fixed dashboard trust frame, explicit refresh, and all UI states</name>
  <files>src/app/staff/reports/[dataset]/page.tsx, src/app/staff/reports/[dataset]/ReportDashboard.tsx, src/app/staff/reports/[dataset]/loading.tsx, tests/components/Reports.test.tsx</files>
  <read_first>
    - src/app/staff/audit/page.tsx and AuditTable.tsx
    - src/components/primitives/ResourceTable.tsx (`ResourceTable` and `StatusPill`)
    - node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md
    - 08-UI-SPEC.md §§7.2, 9–11
  </read_first>
  <behavior>
    - Unknown dataset is non-revealing; available route renders trust frame/filters/2–5 metrics/table/export CTA; unavailable has no refresh/export CTA.
    - Metric/breakdown interactions are keyboard links and preserve filters; equivalent rows exist.
    - A failed independent section keeps successful sections and timestamps visible.
  </behavior>
  <action>Create promised-param/search-param server route, stable loading skeleton and client presentation. Use URL filters, manual `Refresh data` via router refresh preserving query, semantic desktop table/mobile cards, at most one CSS breakdown with text/value controls, local/exact time elements, exact zero/unavailable/error copy, and no polling/caching/chart dependency. Keep large values intact and wrap long content.</action>
  <verify><automated>npx vitest run tests/components/Reports.test.tsx tests/report-registry.test.ts tests/report-scope.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, unavailable CTA, inert segment, page scroll at mobile fixtures, failure-as-zero, or filters lost on refresh.</fails_when></verify>
  <acceptance_criteria>
    - Tests assert all ten routes/labels, four available/six unavailable, empty/loading/error/partial/populated states, mobile parity and exact copy.
  </acceptance_criteria>
  <done>Every required dashboard communicates trustworthy availability, scope, date and row evidence.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| URL filters → report service | Dataset/filter values are untrusted and cannot choose arbitrary columns or scope. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-11 | Information Disclosure | filters/aggregates/drill-down | high | mitigate | Registry schemas plus same pre-query scope predicate and parity tests. |
| T-08-12 | Tampering | date/currency filters | medium | mitigate | Zod normalization, server timezone boundaries, exact currency grouping. |
| T-08-13 | Denial of Service | report queries | medium | mitigate | Bounded pagination, indexed business-date predicates, no unbounded client payload. |
| T-08-SC | Tampering | installs | low | accept | No package install. |
</threat_model>
## Artifacts this phase produces
- Available dataset query adapters.
- `/staff/reports/[dataset]`, `ReportDashboard`, route loading skeleton, expanded report tests.
<verification>Report/component suites; `npx tsc --noEmit`.</verification>
<success_criteria>All ten dashboards satisfy RPT-01/02 and available totals reconcile exactly with matching scoped rows.</success_criteria>
<output>Create `08-04-SUMMARY.md`.</output>

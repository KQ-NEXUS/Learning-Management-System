---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 11
type: execute
wave: 5
depends_on: [08-07]
files_modified:
  - src/server/scheduled/dispatch-export-jobs-task.ts
  - netlify/functions/dispatch-export-jobs.ts
  - netlify/functions/process-export-jobs-background.ts
  - netlify/functions/expire-export-jobs.ts
  - tests/netlify-export-functions.test.ts
autonomous: true
requirements: [RPT-04]
user_setup:
  - service: netlify
    why: "Authenticate scheduled server-to-server export dispatch"
    env_vars:
      - name: EXPORT_DISPATCH_SECRET
        source: "Generate a high-entropy value and configure the same deployment-managed secret for the dispatcher and Background Function"
estimate: { tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low }
must_haves:
  truths:
    - "A one-minute scheduled dispatcher invokes the Background Function after QUEUED jobs are committed, so every accepted export has a production dispatch path independent of the browser request (D-16)."
    - "The Background Function accepts only a valid deployment-managed dispatch secret and an empty/constant request; public callers cannot supply dataset, filters, rows, job IDs, storage keys, or processing limits."
    - "The database queue remains authoritative, Background Function replay delegates to the idempotent bounded worker, and scheduled expiry delegates to the idempotent expiry task (D-17, D-20)."
  artifacts:
    - { path: "src/server/scheduled/dispatch-export-jobs-task.ts", provides: "Trusted zero-instruction server-to-server dispatch" }
    - { path: "netlify/functions/process-export-jobs-background.ts", provides: "Authenticated database-authoritative Background Function" }
    - { path: "tests/netlify-export-functions.test.ts", provides: "Dispatch, injection-resistance, replay, and expiry adapter tests" }
  key_links:
    - { from: "netlify/functions/dispatch-export-jobs.ts", to: "netlify/functions/process-export-jobs-background.ts", via: "scheduled authenticated POST with no export instructions" }
    - { from: "netlify/functions/process-export-jobs-background.ts", to: "src/server/scheduled/process-export-jobs-task.ts", via: "database-authoritative bounded processing" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Browser actions and public callers must not invoke processing with caller-selected export instructions."
    - "[FLAGGED-UNVERIFIED] Missing or invalid dispatch authentication must not disclose queue state or invoke the worker."
---

<objective>Connect committed export jobs to Netlify production execution through an authenticated scheduled dispatcher and thin Background Function, while keeping all job instructions in the database (D-16, D-17, D-20). Output: dispatch task, Netlify adapters, and injection/replay tests.</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-RESEARCH.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-07-SUMMARY.md
@src/server/scheduled/process-export-jobs-task.ts
@src/server/scheduled/expire-export-jobs-task.ts
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: End-to-end scheduled dispatch into authenticated background processing</name>
  <files>src/server/scheduled/dispatch-export-jobs-task.ts, netlify/functions/dispatch-export-jobs.ts, netlify/functions/process-export-jobs-background.ts, tests/netlify-export-functions.test.ts</files>
  <read_first>
    - netlify/functions/reconcile-payments.ts (tracked thin scheduled adapter)
    - src/server/scheduled/process-export-jobs-task.ts from 08-07
    - 08-RESEARCH.md Resolved Design Question 3
    - Netlify Background and Scheduled Functions documentation cited in 08-RESEARCH.md
  </read_first>
  <behavior>
    - The one-minute scheduled handler issues one POST to the deployed Background Function endpoint with `x-export-dispatch-secret` and an empty JSON object, then requires a 202 response.
    - Background handler rejects absent/incorrect secret and rejects any body with keys before worker delegation; denials reveal no queue state.
    - Valid constant invocation delegates exactly once to the bounded database queue task; repeated platform invocations are safe because claim/terminal idempotency remains in the worker.
  </behavior>
  <action>Create an injected `createDispatchExportJobsTask` that builds the deployment-local Background Function URL from trusted Netlify environment configuration, sends only `{}` with `x-export-dispatch-secret`, applies a short request timeout, and throws on non-202 so scheduled failures remain observable/retryable. Create `dispatch-export-jobs.ts` with a one-minute schedule and no public input mapping. Create `process-export-jobs-background.ts` using Netlify's background naming/runtime contract; constant-time compare the deployment-managed secret, require an empty JSON object, reject request query/body instructions, then call `runProcessExportJobsTask()` with no caller arguments. Never inspect or pass dataset, filters, row IDs, job IDs, storage keys, or batch limits from HTTP.</action>
  <verify><automated>npx vitest run tests/netlify-export-functions.test.ts tests/export-worker.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, no scheduled-to-background 202 path, missing/invalid secret invokes processing, arbitrary body/query instructions reach the worker, or wrapper failure is swallowed.</fails_when></verify>
  <acceptance_criteria>
    - Tests prove post-commit jobs are discoverable through the database-authoritative task after scheduled dispatch.
    - Public unauthenticated, wrong-secret, query-injection, body-injection, and replay fixtures cannot select or alter processing instructions.
  </acceptance_criteria>
  <done>QUEUED jobs have a secure production dispatch path that cannot be programmed by public callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thin scheduled expiry deployment adapter</name>
  <files>netlify/functions/expire-export-jobs.ts, tests/netlify-export-functions.test.ts</files>
  <read_first>
    - netlify/functions/reconcile-payments.ts
    - src/server/scheduled/expire-export-jobs-task.ts from 08-07
  </read_first>
  <behavior>
    - Scheduled expiry delegates once with no caller-controlled job input.
    - Task failures propagate for Netlify retry/observability; repeated invocation converges in the expiry service.
  </behavior>
  <action>Create the thin Netlify scheduled expiry adapter using a conservative recurring schedule and direct no-argument delegation to `runExpireExportJobsTask`. Keep authorization, selection, limits, and state transitions in the database service/task; propagate failures rather than returning false success.</action>
  <verify><automated>npx vitest run tests/netlify-export-functions.test.ts tests/export-worker.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, wrapper accepts job instructions, invokes more than once, or swallows a task failure.</fails_when></verify>
  <acceptance_criteria>
    - Function tests prove exactly-once no-argument delegation, failure propagation, and repeated-expiry convergence.
  </acceptance_criteria>
  <done>Deployment expiry invokes only the bounded database-authoritative sweep.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| scheduled dispatcher → public Background Function URL | Only a deployment-managed secret and constant request may cross. |
| Background Function → database worker | HTTP input must not control queue selection or processing instructions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-31 | Spoofing | background invocation | high | mitigate | High-entropy deployment secret, constant-time comparison, non-revealing denial tests. |
| T-08-32 | Tampering | request instructions | high | mitigate | Empty-body/query contract and no-argument worker delegation with adversarial tests. |
| T-08-33 | Denial of Service | scheduled/background replay | medium | mitigate | Short dispatcher timeout plus bounded idempotent database claims and terminal transitions. |
| T-08-SC | Tampering | installs | low | accept | This plan installs no package. |
</threat_model>

## Artifacts this phase produces
- Trusted dispatch task, one-minute scheduled adapter, authenticated Background Function, expiry adapter, and function-boundary tests.

<verification>Netlify function and worker integration suites; deployed test-environment dispatch smoke test remains manual.</verification>
<success_criteria>Every committed QUEUED export can reach production background processing without exposing a caller-programmable worker endpoint.</success_criteria>
<output>Create `.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-11-SUMMARY.md`.</output>

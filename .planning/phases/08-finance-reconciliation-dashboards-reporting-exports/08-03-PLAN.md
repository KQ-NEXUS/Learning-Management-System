---
phase: 08-finance-reconciliation-dashboards-reporting-exports
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/permissions/collection-scope.ts
  - src/server/services/report-registry.ts
  - src/server/services/report-query-service.ts
  - src/app/staff/reports/page.tsx
  - src/app/staff/reports/ReportsHub.tsx
  - tests/report-registry.test.ts
  - tests/report-scope.integration.test.ts
  - tests/components/Reports.test.tsx
autonomous: true
requirements: [RPT-01, RPT-02]
estimate: { tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low }
must_haves:
  truths:
    - "The central Reports hub groups exactly ten fixed dashboard datasets into Admissions & Finance, Learning Delivery, and Outcomes & Support, while a separate export-dataset registry may additionally contain audit and reconciliation-refunds (D-11, PAY-06, RPT-05)."
    - "The hub shows trusted headlines, separately labelled NGN/USD totals, availability, and last-refreshed/as-of context—not every detailed chart (D-12, D-23)."
    - "Registrations/payments/enrolments/attendance are AVAILABLE; six future-domain reports are NOT_AVAILABLE_YET rather than zero (D-14)."
    - "Permission scope is applied before aggregates, rows, filter options, identifiers, and URLs, including empty and adjacent scope unions (D-24)."
    - "Hub empty/loading/error/populated/mixed-availability/long-label states follow UI-SPEC §11."
    - "[RPT-02 edge] Stable ordering uses registry order and deterministic database tie-breakers; repeated/concurrent reads are side-effect-free and return the same authorized projection."
  artifacts:
    - { path: "src/server/permissions/collection-scope.ts", provides: "Active-grant union to Prisma collection predicate" }
    - { path: "src/server/services/report-registry.ts", provides: "Exactly-ten dashboard registry plus metadata-only export-dataset registry" }
    - { path: "src/app/staff/reports/page.tsx", provides: "Central report hub" }
  key_links:
    - { from: "src/server/services/report-query-service.ts", to: "src/server/permissions/collection-scope.ts", via: "authorize before every query branch" }
    - { from: "src/app/staff/reports/page.tsx", to: "src/server/services/report-registry.ts", via: "fixed ordered registry" }
    - { from: "src/server/services/report-registry.ts", to: "src/server/services/export-service.ts", via: "metadata contract only; trusted server producers are supplied through the internal TrustedExportProducer API, never registry-held or browser-supplied rows" }
  prohibitions:
    - "[FLAGGED-UNVERIFIED] Unavailable operational data must not be represented as zero (D-14)."
    - "[FLAGGED-UNVERIFIED] Financial headlines must not combine currencies (D-23)."
---
<objective>Create the shared authorization/report contract and central Reports hub (D-11, D-12, D-14, D-23, D-24). Purpose: establish one trustworthy definition seam for dashboards and exports. Output: collection scope gate, registry/query services, hub UI, and inference-resistance tests.</objective>
<execution_context>@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-CONTEXT.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-RESEARCH.md
@.planning/phases/08-finance-reconciliation-dashboards-reporting-exports/08-UI-SPEC.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Collection-aware authorization and fixed report registry</name>
  <files>src/server/permissions/collection-scope.ts, src/server/services/report-registry.ts, tests/report-registry.test.ts, tests/report-scope.integration.test.ts</files>
  <read_first>
    - src/server/permissions/with-permission.ts and scope.ts (active grants and exact scope semantics)
    - src/server/services/audit-read-service.ts (authorized read factory)
    - prisma/schema.prisma (`Assignment`, Cohort/Course/Programme ancestry)
    - 08-RESEARCH.md Patterns 1–2; 08-UI-SPEC.md §7
  </read_first>
  <behavior>
    - GLOBAL/PROGRAMME/COURSE/COHORT grant unions produce deny-by-default predicates and scope-filtered options.
    - `REPORT_REGISTRY` has exactly ten stable dashboard IDs/versions/groups/definitions/date labels/permissions/availability/column contracts; `EXPORT_DATASET_REGISTRY` is a distinct closed contract containing those ten plus `reconciliation-refunds` and `audit`.
    - Four current datasets are available; six named future datasets are unavailable independent of empty tables.
  </behavior>
  <action>Create `authorizeCollection(permission)` around current actor/grant loading and translate only trusted active grants into Prisma OR predicates that cover cohort ancestry; expose no caller-controlled scope. Create typed immutable `REPORT_REGISTRY`/`ReportDataset` owning exactly the ten D-11 dashboard definitions and a separate metadata-only `EXPORT_DATASET_REGISTRY`/`ExportDataset` that includes those ten plus non-dashboard `reconciliation-refunds` and `audit` definitions. Each export definition owns only ID, version, permission/scope policy, filter schema, and safe/sensitive column contracts; it must not hold rows, a browser-callable producer, or a mutable-data query callback. Plan 08-05 defines the server-only `TrustedExportProducer` interface consumed after authorization, with ordinary report/refund producers bound from trusted report services and the audit producer supplied explicitly by 08-09's audit-export service. Only report definitions own dashboard availability/group/drill-down metadata. Use matching-scope `users.view` as the additional sensitive-identity permission per resolved research question 2 while retaining dataset permission/scope.</action>
  <verify><automated>npx vitest run tests/report-registry.test.ts tests/report-scope.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, malformed/empty grants widen access, a future dataset reports available, or option IDs exceed authorized scope.</fails_when></verify>
  <acceptance_criteria>
    - Tests assert exactly ten ordered dashboard definitions, exactly twelve compatible metadata-only export definitions, all prescribed business-date labels, type/runtime rejection of unknown dashboard or export IDs, and absence of row/producer/browser-scope fields from registry entries.
    - Tests cover empty grants, adjacent and overlapping multi-grant unions, malformed scope, out-of-scope identifiers, stable ordering, repeated and concurrent reads.
  </acceptance_criteria>
  <done>The registry and collection authorization form one typed, deny-by-default contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Scoped hub headline query</name>
  <files>src/server/services/report-query-service.ts, tests/report-scope.integration.test.ts</files>
  <read_first>
    - src/server/services/report-registry.ts and src/server/permissions/collection-scope.ts from Task 1
    - src/server/services/payment-read-service.ts (integer-money projection)
    - prisma/schema.prisma (`User`, `Order`, `Enrolment`, attendance models)
  </read_first>
  <behavior>
    - One normalized request carries registry version, validated filters, authorized scope and request `asOf`.
    - Hub counts and NGN/USD learner totals apply identical scope/date cutoff; null does not become zero.
  </behavior>
  <action>Implement injectable `createReportQueryService` with `getHubOverview`, `getScopedFilterOptions`, and normalized request types. Set `asOf` once per load, apply scope predicates inside every Prisma count/aggregate/options query, group money by exact currency, and return deliberately shaped projections. No client filtering establishes totals or scope.</action>
  <verify><automated>npx vitest run tests/report-scope.integration.test.ts</automated><fails_when>Non-zero exit, zero tests, total/option leakage, cross-currency sum, or different as-of values within one response.</fails_when></verify>
  <acceptance_criteria>
    - Seeded out-of-scope rows never affect counts, currency totals, identifiers, or option labels.
    - Empty authorized collections return checked zeros only for available metrics.
  </acceptance_criteria>
  <done>Hub data is computed server-side with one scope/filter/as-of contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Central Reports hub UI</name>
  <files>src/app/staff/reports/page.tsx, src/app/staff/reports/ReportsHub.tsx, tests/components/Reports.test.tsx</files>
  <read_first>
    - src/app/staff/audit/page.tsx (promised search params and safe errors)
    - src/components/primitives/ResourceTable.tsx (`ResourceTable` and `StatusPill`)
    - 08-UI-SPEC.md §§7.1, 9–11
    - node_modules/next/dist/docs/01-app/01-getting-started/06-fetching-data.md
  </read_first>
  <behavior>
    - Concise clickable headlines have accessible destination names and preserve applicable filters.
    - Each dataset card shows definition and Available/Not available yet; unavailable is never zero.
    - Loading/error/mixed availability/long labels preserve chrome and do not leak scope.
  </behavior>
  <action>Build the async hub page and presentational component using existing Tailwind tokens (no shadcn/chart library/raw hex). Render headings, three registry groups, last-refreshed/data-as-of time, separate NGN/USD cards, and metric links whose URLs use normalized filters. Render unavailable cards as explanatory navigation entries, not false metrics. Use semantic time/dateTime and responsive one-column reflow.</action>
  <verify><automated>npx vitest run tests/components/Reports.test.tsx tests/report-registry.test.ts</automated><fails_when>Non-zero exit, zero component tests, inert metrics, missing availability copy, combined money, or inaccessible timestamp.</fails_when></verify>
  <acceptance_criteria>
    - Component tests assert grouped order, headline links, mixed availability, empty/error/loading, long labels, and no combined total.
  </acceptance_criteria>
  <done>Staff can understand platform health and reach each fixed dashboard from one trusted hub.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| grants → report SQL | Scope must constrain every aggregate, option and identifier before querying. |
| registry → UI | Availability and columns are server-owned, not caller-selectable. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-08 | Information Disclosure | aggregates/options | high | mitigate | One collection predicate applied before all branches; adversarial scope tests. |
| T-08-09 | Elevation of Privilege | report IDs | high | mitigate | Closed typed registry and server authorization; unknown IDs fail closed. |
| T-08-10 | Tampering | filters/as-of | medium | mitigate | Zod normalized server request and server clock. |
| T-08-SC | Tampering | installs | low | accept | No package install. |
</threat_model>
## Artifacts this phase produces
- `authorizeCollection` and collection predicate types.
- Exactly-ten `REPORT_REGISTRY`/`ReportDataset`, metadata-only `EXPORT_DATASET_REGISTRY`/`ExportDataset`, `ReportDefinition`, and normalized request/query exports.
- `/staff/reports`, `ReportsHub`, and registry/scope/component suites.
<verification>Registry, scope integration, and Reports component suites; `npx tsc --noEmit`.</verification>
<success_criteria>Every fixed report is defined once and the hub cannot reveal data outside current permission scope.</success_criteria>
<output>Create `08-03-SUMMARY.md`.</output>

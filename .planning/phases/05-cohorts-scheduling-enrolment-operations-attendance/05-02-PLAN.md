---
phase: 05-cohorts-scheduling-enrolment-operations-attendance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/timezone.ts
  - src/lib/attendance-window.ts
  - src/server/services/attendance-component.ts
  - src/server/services/cohort-scope.ts
  - tests/timezone.test.ts
  - tests/attendance-window.test.ts
  - tests/attendance-component.test.ts
  - tests/cohort-scope.test.ts
autonomous: true
requirements: [COH-03, COH-07, ATT-02, ATT-03]

must_haves:
  truths:
    - "A wall-clock date/time entered in a cohort's timezone converts to the correct UTC instant, and back (D-23)."
    - "An invalid IANA timezone string is rejected before it can be stored on a Cohort."
    - "Whether a session is inside its normal marking window is a pure UTC computation on the stored instants (D-06, Pitfall 4)."
    - "The attendance component (earned vs required percent) is computed from attendance records plus the cohort threshold, with no data access (D-20)."
    - "A cohortId resolves to all three scope keys — cohortId, programmeId, courseIds — so a COURSE or PROGRAMME grant can reach a cohort-scoped resource (D-21)."
  artifacts:
    - path: "src/lib/timezone.ts"
      provides: "isValidTimeZone, wallTimeToUtc, utcToWallParts — pure"
      exports: ["isValidTimeZone", "wallTimeToUtc", "utcToWallParts"]
    - path: "src/lib/attendance-window.ts"
      provides: "ATTENDANCE_MARKING_WINDOW_HOURS = 168 and the window/pre-start predicates"
      contains: "ATTENDANCE_MARKING_WINDOW_HOURS = 168"
    - path: "src/server/services/attendance-component.ts"
      provides: "computeAttendanceComponent — pure earned/required percent"
      exports: ["computeAttendanceComponent"]
    - path: "src/server/services/cohort-scope.ts"
      provides: "cohortResourceScope, sessionCohortScope, enrolmentCohortScope"
      exports: ["cohortResourceScope", "sessionCohortScope", "enrolmentCohortScope", "createCohortScopeResolvers"]
  key_links:
    - from: "src/server/services/cohort-scope.ts"
      to: "src/server/permissions/scope.ts"
      via: "returns ResourceScope with cohortId + programmeId + courseIds"
      pattern: "ResourceScope"
    - from: "src/lib/attendance-window.ts"
      to: "prisma ScheduledSession.startsAt/endsAt"
      via: "UTC Date arithmetic only, no Intl"
      pattern: "startsAt|endsAt"
---

<objective>
Build the four small, independently testable building blocks the rest of Phase 5 composes: the
timezone converter (D-23), the attendance marking-window predicate (D-06), the pure attendance
component calculator (D-20), and the async cohort scope resolver that makes COHORT scope load-bearing
(D-21).

Purpose: these are the pieces most easily got subtly wrong (offset drift, an off-by-one window
boundary, a scope that omits `courseIds` and silently denies a Course-scoped Programme Manager).
Isolating them into pure, exhaustively unit-tested modules means the services in later waves compose
correct behaviour instead of re-deriving it.
Output: two `src/lib/` modules, two `src/server/services/` modules, four unit test files.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-CONTEXT.md
@.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-RESEARCH.md
@.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-PATTERNS.md
</context>

<interfaces>
`src/server/permissions/scope.ts` — the shape `cohort-scope.ts` must produce:

```typescript
export type ResourceScope = {
  cohortId?: string;
  programmeId?: string;
  courseIds?: string[];
};

export function grantMatches(grant: Grant, resource: ResourceScope): boolean {
  if (grant.scopeType === "GLOBAL") return true;
  if (!grant.scopeId) return false;
  switch (grant.scopeType) {
    case "COHORT":    return resource.cohortId === grant.scopeId;
    case "PROGRAMME": return resource.programmeId === grant.scopeId;
    case "COURSE":    return resource.courseIds?.includes(grant.scopeId) ?? false;
  }
}
```

`src/server/services/resource-service.ts` already accepts an async resolver:
`toScope: (id: string) => ResourceScope | Promise<ResourceScope>`.

`AttendanceState` enum values (`prisma/schema.prisma:91`): `PRESENT ABSENT LATE EXCUSED NOT_RECORDED`.
`Cohort.attendanceThresholdPct Int?` (`prisma/schema.prisma:757`) — null means "no attendance rule".
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Timezone converter and the attendance marking-window predicate</name>
  <files>src/lib/timezone.ts, src/lib/attendance-window.ts, tests/timezone.test.ts, tests/attendance-window.test.ts</files>
  <read_first>
    - `src/lib/positions.ts` — the pure-lib file structure, exported constants, and doc-comment ethic to match
    - `tests/positions.test.ts` — the exhaustive-boundary unit-test style to mirror
    - `.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-RESEARCH.md` lines 505-536 —
      the `Intl.DateTimeFormat` `formatToParts` `wallTimeToUtc` implementation and its DST caveat
    - `.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-RESEARCH.md` lines 471-481 —
      Pitfall 4, why the window must be pure UTC arithmetic with no `Intl` on the path
  </read_first>
  <behavior>
    - `isValidTimeZone("Africa/Lagos")` is true; `isValidTimeZone("Mars/Olympus")` is false; `isValidTimeZone("")` is false
    - `wallTimeToUtc({year:2026,month:3,day:1,hour:9,minute:0}, "Africa/Lagos")` returns the instant `2026-03-01T08:00:00Z`
    - `wallTimeToUtc` for a non-UTC+0, non-Lagos zone (e.g. `America/New_York` in January) returns the correct offset instant
    - `utcToWallParts(wallTimeToUtc(parts, tz), tz)` round-trips back to `parts` for both zones
    - `isWithinMarkingWindow` is false 1s before `startsAt`, true at exactly `startsAt`, true at exactly `endsAt + 168h`, false 1ms after
    - `isBeforeSessionStart` is true 1ms before `startsAt` and false at exactly `startsAt`
  </behavior>
  <action>
    Create `src/lib/timezone.ts` — pure, no imports. Export `isValidTimeZone(tz: string): boolean` using
    `Intl.supportedValuesOf("timeZone").includes(tz)`; `wallTimeToUtc(parts: { year: number; month: number;
    day: number; hour: number; minute: number }, timeZone: string): Date` using the
    `Intl.DateTimeFormat` + `formatToParts` offset-correction technique from RESEARCH lines 505-536; and
    `utcToWallParts(instant: Date, timeZone: string)` returning the same parts shape plus a formatted
    `label` for display. Do NOT use `Temporal` — it is not stable in Node 22.14. Document the single-pass
    DST-gap caveat in the file header, noting that `Africa/Lagos` (the schema default) has no DST and that
    a DST zone would need a second offset iteration.

    Create `src/lib/attendance-window.ts` — pure, no imports, and explicitly NO `Intl` or timezone import
    (Pitfall 4: timezone is a display concern; window math is UTC arithmetic on the stored instants).
    Export `ATTENDANCE_MARKING_WINDOW_HOURS = 168` (D-06 — a project constant, deliberately not a
    per-cohort field), `isWithinMarkingWindow(session: { startsAt: Date; endsAt: Date }, now: Date): boolean`
    (inclusive at both ends), `isBeforeSessionStart(session: { startsAt: Date }, now: Date): boolean`,
    and `markingWindowClosesAt(session: { endsAt: Date }): Date`.

    Write `tests/timezone.test.ts` and `tests/attendance-window.test.ts` covering every case in the
    behavior block, including the exact boundary instants.
  </action>
  <verify>
    <automated>npx vitest run tests/timezone.test.ts tests/attendance-window.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/timezone.test.ts tests/attendance-window.test.ts` exits 0
    - `grep -c "Intl" src/lib/attendance-window.ts` returns 0
    - `grep -c "Temporal" src/lib/timezone.ts` returns 0
    - `grep -c "ATTENDANCE_MARKING_WINDOW_HOURS = 168" src/lib/attendance-window.ts` returns 1
    - `grep -cE "^import " src/lib/timezone.ts src/lib/attendance-window.ts` returns 0 for both files
    - The window test asserts all four boundary instants: `startsAt - 1ms`, `startsAt`, `endsAt + 168h`, `endsAt + 168h + 1ms`
  </acceptance_criteria>
  <done>Both libs are pure, dependency-free, and their boundaries are pinned by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure attendance component calculator</name>
  <files>src/server/services/attendance-component.ts, tests/attendance-component.test.ts</files>
  <read_first>
    - `src/server/services/readiness-service.ts` lines 1-40 — the "pure module, zero data/UI/auth imports" header and discipline to copy
    - `tests/readiness.test.ts` — the exhaustive pure-evaluator test style
    - `prisma/schema.prisma` lines 86-100 (`enum AttendanceState`) and 757 (`Cohort.attendanceThresholdPct`)
    - `.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-CONTEXT.md` D-20 and D-18 —
      component only, no completion verdict; and the explicit-third-state rule
  </read_first>
  <behavior>
    - 4 sessions expected, states PRESENT/PRESENT/LATE/ABSENT, threshold 75 → earnedPct 75, requiredPct 75, meetsThreshold true
    - LATE counts as attended; EXCUSED is removed from the denominator (neither earned nor required)
    - NOT_RECORDED counts against the learner (in the denominator, not earned)
    - `attendanceThresholdPct: null` → `{ kind: "no-rule" }`, never a zero percentage (D-18: no fake zero)
    - Zero countable sessions with a threshold set → `{ kind: "no-sessions" }`, never a divide-by-zero or NaN
    - Sessions with `attendanceExpected: false` or a non-null `cancelledAt` are excluded entirely
  </behavior>
  <action>
    Create `src/server/services/attendance-component.ts` — pure, no imports at all (same discipline as
    `readiness-service.ts`; a data-access import here would put it on the worker closure and break
    `tests/boundary.test.ts`).

    Export `type AttendanceComponentInput = { thresholdPct: number | null; entries: Array<{ state:
    AttendanceStateValue; attendanceExpected: boolean; cancelledAt: Date | string | null }> }` and
    `type AttendanceComponent = { kind: "computed"; earnedPct: number; requiredPct: number;
    attendedCount: number; countableCount: number; meetsThreshold: boolean } | { kind: "no-rule" } |
    { kind: "no-sessions" }` — a discriminated union, because the roster must render an explicit third
    state rather than `0%` or a blank (D-18, Pitfall 7).

    Export `computeAttendanceComponent(input: AttendanceComponentInput): AttendanceComponent`.
    Rules: exclude cancelled sessions and sessions where `attendanceExpected` is false; exclude
    `EXCUSED` from both numerator and denominator; count `PRESENT` and `LATE` as attended; count
    `ABSENT` and `NOT_RECORDED` in the denominator only. `earnedPct` rounds to the nearest integer.
    Also export a local `AttendanceStateValue` string union matching the Prisma enum
    (`"PRESENT" | "ABSENT" | "LATE" | "EXCUSED" | "NOT_RECORDED"`) so no `@prisma/client` type import
    is needed.

    Do NOT compute or return a completion verdict — D-20 reserves that for the Phase 9/11 engine.
    Say so in the file header.
  </action>
  <verify>
    <automated>npx vitest run tests/attendance-component.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/attendance-component.test.ts` exits 0
    - `grep -cE "^import " src/server/services/attendance-component.ts` returns 0
    - `grep -cE "complete|verdict" src/server/services/attendance-component.ts` finds only header prose, no returned field named `completed` or `verdict`
    - The test asserts `{ kind: "no-rule" }` for a null threshold and `{ kind: "no-sessions" }` for an empty countable set — neither returns a numeric 0
  </acceptance_criteria>
  <done>`computeAttendanceComponent` is pure, exhaustively tested, and returns an explicit third state instead of a misleading zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Async cohort scope resolvers</name>
  <files>src/server/services/cohort-scope.ts, tests/cohort-scope.test.ts</files>
  <read_first>
    - `src/server/permissions/scope.ts` lines 40-91 — `ResourceScope`, `grantMatches`, deny-by-default
    - `src/server/services/course-service.ts` lines 1-45 — `courseScope` and the factory config shape
    - `src/server/services/lesson-service.ts` lines 188-205 — `resolveCourseIdForModule`, the "resolve the
      parent id from the row, never trust the caller" two-hop pattern
    - `src/server/services/catalogue-guards.ts` lines 77-120 — the injected-delegate DI factory pattern that
      makes a Prisma-importing service unit-testable with a fake
    - `.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-PATTERNS.md` lines 340-364 —
      the exact resolver body
  </read_first>
  <behavior>
    - A standalone-Course cohort resolves to `{ cohortId, courseIds: [courseId] }` with no `programmeId`
    - A Programme cohort resolves to `{ cohortId, programmeId, courseIds: [...member course ids from CohortCourse] }`
    - An unknown cohortId resolves to `{}` — deny-by-default, never a scope that matches something else
    - `sessionCohortScope(sessionId)` resolves the session's `cohortId` from the row then delegates
    - `enrolmentCohortScope(enrolmentId)` resolves the enrolment's `cohortId` from the row then delegates
    - No resolver accepts a caller-supplied cohortId/programmeId/courseId
  </behavior>
  <action>
    Create `src/server/services/cohort-scope.ts`. Export a DI factory
    `createCohortScopeResolvers(deps: { cohort: CohortScopeDelegate; session: SessionScopeDelegate;
    enrolment: EnrolmentScopeDelegate })` returning `{ cohortResourceScope, sessionCohortScope,
    enrolmentCohortScope }`, plus bound instances wired to `prisma.cohort` / `prisma.scheduledSession` /
    `prisma.enrolment` and re-exported as `cohortResourceScope`, `sessionCohortScope`,
    `enrolmentCohortScope` — the `catalogue-guards.ts` factory-plus-`built` shape.

    `cohortResourceScope(cohortId)` performs one `findUnique` selecting `id`, `programmeId`, `courseId`
    and `cohortCourses: { select: { courseId: true } }`, and returns a `ResourceScope` populating
    **all three** keys: `cohortId`, `programmeId` when set, and `courseIds` as `[courseId]` for a
    standalone-Course cohort or the `cohortCourses` course ids for a Programme cohort. Populating all
    three is what lets a PROGRAMME or COURSE grant reach a cohort-scoped resource through
    `grantMatches`; omitting `courseIds` silently denies a Course-scoped Programme Manager.
    A missing row returns `{}` so the authorization core denies by default.

    `sessionCohortScope(sessionId)` and `enrolmentCohortScope(enrolmentId)` each read only the row's
    `cohortId` and delegate to `cohortResourceScope`.

    This file MUST NOT import `@/server/permissions` or anything from it — it is authorization *input*,
    not an authorization check, and it must never end up on the worker import closure
    (`tests/boundary.test.ts`). Note that constraint in the file header, as
    `src/server/jobs/queue.ts` lines 5-9 does.

    Write `tests/cohort-scope.test.ts` driving `createCohortScopeResolvers` with fake delegates for
    every case in the behavior block, and add one assertion that composes the resolver output with
    `grantMatches` to prove a COURSE grant on a member course reaches a Programme cohort.
  </action>
  <verify>
    <automated>npx vitest run tests/cohort-scope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/cohort-scope.test.ts` exits 0
    - `src/server/services/cohort-scope.ts` exports `createCohortScopeResolvers`, `cohortResourceScope`, `sessionCohortScope`, `enrolmentCohortScope`
    - `grep -c "@/server/permissions" src/server/services/cohort-scope.ts` returns 0 for value imports (a `import type { ResourceScope }` type-only import is permitted and must be written as `import type`)
    - `npx vitest run tests/boundary.test.ts` exits 0
    - The test proves an unknown cohortId yields `{}` and that `grantMatches` then denies
  </acceptance_criteria>
  <done>Any cohort, session or enrolment id resolves to a complete, DB-derived `ResourceScope`, unit-tested against `grantMatches`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller input → authorization scope | A caller-asserted parent id would be a scope the caller chose |
| stored UTC instants → displayed wall time | Conversion errors change which actions are permitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | `cohort-scope.ts` returning a scope derived from caller input rather than the DB row | mitigate | Resolvers take only an id and read the row; no resolver signature accepts a programmeId/courseId; unit-tested |
| T-05-07 | Elevation of Privilege | An incomplete `ResourceScope` (missing `courseIds`) causes `grantMatches` to deny a legitimately-scoped Programme Manager, driving a hand-written bypass later | mitigate | All three keys populated; a `grantMatches` composition assertion in `tests/cohort-scope.test.ts` |
| T-05-08 | Elevation of Privilege | An unknown cohortId resolving to a permissive scope | mitigate | Missing row returns `{}`; `scope.ts` denies by default; asserted in test |
| T-05-09 | Tampering | Marking-window boundary computed from a wall-clock reconstruction drifts by the cohort's UTC offset, letting a post-window edit through without a correction reason | mitigate | `attendance-window.ts` has zero imports and a grep gate proving no `Intl` on the path; the four boundary instants are pinned by tests |
| T-05-10 | Information Disclosure | A data-access import in `attendance-component.ts` or `cohort-scope.ts` reaching the worker closure and dragging request-only APIs in | mitigate | `attendance-component.ts` has zero imports; `cohort-scope.ts` is documented request-side-only; `tests/boundary.test.ts` re-run in acceptance criteria |
</threat_model>

<verification>
1. `npx vitest run tests/timezone.test.ts tests/attendance-window.test.ts tests/attendance-component.test.ts tests/cohort-scope.test.ts` — all four green.
2. `npx vitest run tests/boundary.test.ts tests/structure.test.ts` — boundary and structure rules intact.
3. `npx tsc --noEmit` — no type errors introduced.
4. `npm run lint` — no ESLint boundary violations (`@prisma/client` only in `src/server/services/`).
</verification>

<success_criteria>
- `wallTimeToUtc` / `utcToWallParts` round-trip for `Africa/Lagos` and one non-zero-offset zone; an invalid zone is rejected.
- `isWithinMarkingWindow` is exact at all four boundary instants and contains no timezone code.
- `computeAttendanceComponent` returns a discriminated union with `no-rule` / `no-sessions` third states, never a fake zero, and no completion verdict.
- `cohortResourceScope` populates `cohortId`, `programmeId` and `courseIds` from the database row, denies by default on a missing row, and is provably absent from the worker import closure.
</success_criteria>

<output>
Create `.planning/phases/05-cohorts-scheduling-enrolment-operations-attendance/05-02-SUMMARY.md` when done.
Record the exported signatures — plans 05-05 through 05-10 consume them directly.
</output>

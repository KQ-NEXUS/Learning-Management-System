---
phase: 03-public-identity-registration-verification-secure-sessions
plan: 10
type: execute
wave: 7
depends_on: ["03-09"]
gap_closure: true
gap_ids: []
files_modified:
  - .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md
autonomous: true
requirements: [IAM-01, IAM-02, IAM-03, IAM-05, IAM-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every row of the phase's validation table names the plan and task that actually shipped it, the test file that actually exists, and a status that matches what that file actually does when run. (IAM-01, IAM-02, IAM-03, IAM-05, IAM-06)"
    - "The rows that named a test file the phase never created say so explicitly rather than staying pending against a filename that will never appear."
    - "The four test files created during gap closure are present in the table, mapped to their requirement and to the gap they close."
    - "The frontmatter status, Nyquist compliance and wave-0 flags reflect the phase's real state, so the validation contract stops asserting the opposite of the evidence sitting beside it."
    - "The recorded evidence is the output of an actual full-suite run made after every gap-closure plan is committed, not a projection."
  artifacts:
    - "03-VALIDATION.md — a reconciled per-task verification map and honest frontmatter"
  key_links:
    - "each table row's automated command -> a test file that exists on disk (a row naming tests/schema-auth.test.ts for the identity schema assertions points at the wrong file; the phase shipped tests/schema-identity.test.ts)"
    - "the phase's real test-file inventory -> the wave 0 checklist (a checklist ticked from the plan rather than from ls is how this document drifted in the first place)"
---

<objective>
Reconcile `03-VALIDATION.md` with reality. It is still `status: draft`, `nyquist_compliant: false`,
`wave_0_complete: false`, with all ten rows marked pending and every Task ID, Plan and Wave column left
as `TBD` — while every test file it names exists and passes, and four more exist that it has never
heard of. A validation contract that asserts the opposite of the evidence beside it is worse than no
contract: the next person to read it either believes it and re-does finished work, or learns to ignore
the document class entirely.

Purpose: this is the last piece of phase-level bookkeeping standing between gap closure and
`/gsd-verify-work` resuming. It runs last because it can only be truthful once every gap-closure plan
is committed and the suite has been run against the final state.
Output: a validation map whose every row names a real file, a real command and a real status, backed by
a full-suite run recorded at the moment it was made.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md
@.planning/phases/03-public-identity-registration-verification-secure-sessions/03-UAT.md
@.planning/phases/03-public-identity-registration-verification-secure-sessions/03-07-SUMMARY.md
@.planning/phases/03-public-identity-registration-verification-secure-sessions/03-08-SUMMARY.md
@.planning/phases/03-public-identity-registration-verification-secure-sessions/03-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the ground truth before editing anything</name>
  <files>(no files modified — this task produces the evidence Task 2 writes down)</files>
  <precondition>Plans 07, 08 and 09 are all committed, and their `<human-check>` items have been harvested into `03-UAT.md` with results recorded.</precondition>
  <read_first>
    - .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md (the whole file — the ten-row table, the wave 0 checklist, the manual-only table, and the sign-off block)
    - .planning/phases/03-public-identity-registration-verification-secure-sessions/03-UAT.md (the Summary block and the Gaps section — the manual-only rows resolve against these results, and four of the five gaps are now closed by plans 07-09)
    - the six original SUMMARY files 03-01 through 03-06 (which plan and which task each test file actually came from — the Task ID and Plan columns are filled from these, not guessed)
    - the three gap-closure SUMMARY files 03-07 through 03-09
  </read_first>
  <action>
    Do not edit the validation document in this task. Gather the facts it will be rewritten from, so the
    rewrite is transcription rather than recollection.

    Run `ls tests/` and record the full inventory. Run `npm test` once, in full, and record the exact
    summary line — file count, test count, duration — plus any file reported as failed or skipped. Run
    `npx tsc --noEmit`, `npx eslint src tests prisma` and `npm run build` and record whether each is
    clean. Note the real full-suite duration against the document's estimate of 15-20 seconds, which was
    extrapolated from Phase 2 and is now measurable.

    Then, for each of the ten existing table rows, determine three things from the SUMMARY files: which
    plan and which task within it actually delivered that behaviour, which test file now covers it, and
    whether that file exists under the name the row gives. At least one row does not: the schema row names
    `tests/schema-auth.test.ts (extend existing)` while the phase actually shipped a separate
    `tests/schema-identity.test.ts`. Find every such mismatch rather than assuming that is the only one.

    Finally, list the test files that exist but appear in no row. Four came from gap closure —
    `tests/prisma-contract.test.ts` and `tests/email-dispatch-service.test.ts` are new files, and the
    profile, registration, verification and password-reset suites gained rejecting-transport coverage that
    no row describes. Also check `tests/landing.test.ts` and `tests/brevo-client.test.ts` against the
    table, since plan 06 and plan 01 created them after this document was written.

    Report the gathered facts in the task output so Task 2 has them in hand.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any test file reports a failure — the document cannot be marked compliant against a red suite, and if this run is red the correct action is to stop and report, not to reconcile around it</fails_when>
    <automated>npx tsc --noEmit &amp;&amp; npx eslint src tests prisma &amp;&amp; npm run build</automated>
    <fails_when>non-zero exit, or any TypeScript error, ESLint error or build failure is printed</fails_when>
    <automated>ls tests/</automated>
    <fails_when>the listing is empty or the command errors</fails_when>
  </verify>
  <done>The real test-file inventory, the exact full-suite summary line, the type/lint/build results, the plan-and-task provenance of each existing row, every row whose named file does not exist, and every existing file no row mentions are all recorded and available to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the validation contract to match</name>
  <files>.planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md</files>
  <read_first>
    - the facts gathered in Task 1
    - .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md (the exact table columns and the status legend beneath it, which the rewrite must keep rather than replace)
  </read_first>
  <action>
    Fill the `Task ID`, `Plan` and `Wave` columns of every existing row from the provenance gathered in
    Task 1, replacing every `TBD`. Fill the `Threat Ref` column from the STRIDE registers in the plan
    files — the threat IDs exist and are numbered; `T-03-TBD` does not. Where a row genuinely maps to no
    threat, use the same em-dash the document already uses for that case rather than inventing one.

    Correct the `Automated Command` and `File Exists` columns to name the files that exist. Where a row
    named a file the phase never created, change the name and add a short parenthetical stating what was
    shipped instead — a silent correction loses the fact that the plan and the outcome diverged, which is
    the more useful signal.

    Set each row's `Status` from the Task 1 run: green where the named command passes. Do not mark
    anything green that was not actually executed in Task 1.

    Add rows for the coverage that exists and is unrepresented: the two new gap-closure test files, the
    rejecting-transport coverage now present across the four service suites, and any file Task 1 found
    that no row mentions. Each new row carries its requirement, its threat reference from the plan 07-09
    registers, and the gap id it closes so the table connects to `03-UAT.md`.

    Update the manual-only table against the UAT results: the rows verified during UAT are no longer
    open questions, and the reset, registration and verification flows all passed. The account-page row
    is the one that found `G-03-6a`, `G-03-6b` and `G-03-6c`, and its re-verification is plan 09's human
    `<human-check>` items on plans 08 and 09, harvested into `03-UAT.md` — say so, and give their outcome
    from `03-UAT.md` and the 03-08 and 03-09 SUMMARY files.

    Correct the estimated-runtime row in the Test Infrastructure table to the measured duration from
    Task 1, marking it measured rather than extrapolated.

    Tick the Wave 0 checklist against the files that exist, and note beside the schema entry that the
    assertions landed in a separate file rather than as an extension of the existing one.

    Complete the sign-off block: tick each condition that Task 1's evidence actually supports and leave
    unticked, with a one-line reason, any it does not. Set the frontmatter `status` to complete,
    `nyquist_compliant` to true and `wave_0_complete` to true only if the sign-off conditions genuinely
    hold — if any does not, leave the corresponding flag false and record why in the approval line. The
    entire purpose of this plan is that this document stops asserting things that are not true, and that
    applies in both directions.

    Add a short dated note under the table recording that this reconciliation happened after gap closure,
    naming plans 07-09, and stating plainly that the document sat in draft with all rows pending while
    the tests it named were green — so a future reader knows the drift was found and closed rather than
    never having existed.

    Change no column headings and no status legend. The table's shape is the contract; only its contents
    were wrong.
  </action>
  <verify>
    <automated>test "$(grep -c 'TBD' .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md)" = "0"</automated>
    <fails_when>the count is not 0 — a Task ID, Plan, Wave or Threat Ref placeholder survived the rewrite</fails_when>
    <automated>grep -q 'status: complete' .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md &amp;&amp; test "$(grep -c 'nyquist_compliant: false' .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md)" = "0"</automated>
    <fails_when>the frontmatter still reports draft status or non-compliance while the suite is green — or, if a sign-off condition genuinely does not hold, this gate fails by design and the executor must report which condition and why rather than forcing the flags</fails_when>
    <automated>for f in $(grep -oE 'tests/[a-z0-9.-]+\.test\.ts' .planning/phases/03-public-identity-registration-verification-secure-sessions/03-VALIDATION.md | sort -u); do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done; echo "all named test files exist"</automated>
    <fails_when>any test file named in the document does not exist on disk — this is the exact defect the schema row already had, and it is the one thing this rewrite must not reproduce</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or any test file reports a failure — the document must not be committed green against a red suite</fails_when>
  </verify>
  <done>Every `TBD` is replaced with a real plan, task and threat reference; every named test file exists; every row's status reflects the Task 1 run; the gap-closure coverage has rows connecting it to `03-UAT.md`; the frontmatter flags and sign-off block match the evidence, with any unmet condition left unticked and reasoned rather than forced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning document -> the next reader | A validation contract that disagrees with the codebase teaches readers to distrust the document class, which is how the next real gap goes unnoticed. |

No code, no runtime input and no external service is touched by this plan, so it introduces no software
trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Status |
|-----------|----------|-----------|----------|-------------|-----------------|--------|
| T-03-63 | Repudiation | the phase's validation evidence | medium | mitigate | Every status in the table is set from a full-suite, type-check, lint and build run made in Task 1 after all gap-closure plans are committed, and the run's summary line is recorded in the SUMMARY. No row is marked green from a plan's intent. | planned |
| T-03-64 | Tampering | the frontmatter flags forced to green | medium | mitigate | The compliance flags are gated on the sign-off conditions actually holding; the task instructs the executor to leave a flag false and report the reason rather than force it, and the corresponding verify gate is written to fail loudly in that case rather than being satisfiable by editing the flag alone. | planned |

**Package installs:** none. This plan modifies one markdown document and no source file.
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npx eslint src tests prisma` and `npm run build` are all clean at the
  moment the document is written, and the full-suite summary line is recorded verbatim.
- No `TBD` remains anywhere in `03-VALIDATION.md`.
- Every `tests/*.test.ts` path named in the document exists on disk.
- The frontmatter `status`, `nyquist_compliant` and `wave_0_complete` values are each supported by a
  sign-off condition that actually holds, or are left false with a stated reason.
</verification>

<success_criteria>
`03-VALIDATION.md` describes the phase that was actually built: real task ids, real threat references,
real filenames, statuses set from a run made after gap closure, rows for the coverage that gap closure
added, and frontmatter that agrees with the evidence beside it.
</success_criteria>

## Artifacts this plan produces

| Symbol / path | Kind |
|---|---|
| the reconciled per-task verification map in `03-VALIDATION.md` | rewritten contents of an existing document |
| rows for `tests/prisma-contract.test.ts`, `tests/email-dispatch-service.test.ts` and the rejecting-transport coverage | new rows |
| the post-gap-closure reconciliation note | new note |

<output>
Create `.planning/phases/03-public-identity-registration-verification-secure-sessions/03-10-SUMMARY.md` when done.
Record in it: the verbatim `npm test` summary line the statuses were set from, the measured full-suite
duration against the document's 15-20 second estimate, the complete list of rows whose named file did not
exist, and any sign-off condition left unticked with its reason. If every condition held, say so
explicitly — an unqualified "complete" with no evidence beside it is how this document reached the state
this plan exists to fix.
</output>
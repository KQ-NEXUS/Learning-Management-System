---
phase: 09-learning-delivery-progress-tracking
plan: 08
type: execute
wave: 5
depends_on: ["09-07"]
files_modified:
  - src/app/(learner)/layout.tsx
  - src/app/(learner)/dashboard/page.tsx
  - src/components/learner/ProgressMeter.tsx
  - src/components/learner/DeferredSlot.tsx
  - src/components/learner/NextUpCard.tsx
  - tests/learner-dashboard-page.test.ts
autonomous: true
requirements: [LRN-01]

must_haves:
  truths:
    - "A signed-in learner can open /dashboard and see their own next action, progress and upcoming sessions"
    - "A signed-out visitor at /dashboard is sent to sign-in, not shown an empty dashboard"
    - "Assessment, results, tickets and certificate render as visibly labelled gaps, never blank and never a zero"
  artifacts:
    - path: "src/app/(learner)/layout.tsx"
      provides: "Signed-in learner shell with the three-item nav"
      min_lines: 40
    - path: "src/app/(learner)/dashboard/page.tsx"
      provides: "The LRN-01 dashboard"
      min_lines: 80
    - path: "src/components/learner/ProgressMeter.tsx"
      provides: "Bordered teal progress track with an adjacent text label"
  key_links:
    - from: "src/app/(learner)/dashboard/page.tsx"
      to: "loadLearnerDashboard"
      via: "server component read"
      pattern: "loadLearnerDashboard\\("
---

<objective>
Ship the learner shell and `/dashboard`: the first learner-facing surface of this phase and the entry
point every other screen links from.

Purpose: LRN-01. This is also where the 04.1 design system's reserved teal progress role gets drawn
for the first time, which carries a specific contrast obligation the UI-SPEC spells out.
Output: a route-group layout, the dashboard page, three small presentational components, and a
render test.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-delivery-progress-tracking/09-CONTEXT.md
@.planning/phases/09-learning-delivery-progress-tracking/09-RESEARCH.md
@.planning/phases/09-learning-delivery-progress-tracking/09-PATTERNS.md
@.planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md
</context>

<interfaces>
Consumed from 09-07:
  loadLearnerDashboard(actor): Promise<{ cards: LearnerDashboardCard[] }>
  LearnerDashboardCard carries: enrolmentId, cohortTitle, offerTitle, nextAction: NextAction,
    progress: { requiredLessonsComplete, requiredLessonsTotal, attendance: AttendanceComponent },
    accessNotice, upcomingSessions[], hasMoreSessions, structure,
    assessmentObligations/results/tickets/certificate: DeferredColumn

Existing, reused unchanged:
  LearnerShell({ nav: LearnerNavItem[], rightSlot: ReactNode, children })   // src/components/shell/LearnerShell.tsx
  signOutAction                                                            // src/app/(auth)/signin/actions.ts
  profileService.getOwnProfile(actor)                                      // src/server/services/profile-service.ts
  getCurrentActor(): Promise<Actor | null>                                 // src/server/auth/current-actor.ts
  StatusPill                                                               // exported from src/components/primitives/ResourceTable.tsx
</interfaces>

<planner_rulings>
- **DD-7 (resolves the route shape UI-SPEC 0.1 left open):** a Next.js route GROUP
  `src/app/(learner)/` holds `dashboard/` and `learn/`, with one `layout.tsx` performing the
  session guard and rendering `LearnerShell`. The group parentheses keep the URLs exactly
  `/dashboard` and `/learn/[enrolmentId]/...` as UI-SPEC 0.1 proposes, while giving both surfaces one
  guard and one nav definition — the same arrangement `src/app/account/layout.tsx` already uses for
  its single surface.
- **DD-20:** the layout guard is convenience only, exactly as `account/layout.tsx`'s header states.
  Every page and Server Action beneath it re-resolves the actor and re-checks ownership; a layout
  protects rendering, not data.
</planner_rulings>

<tasks>

<task type="auto">
  <name>Task 1: The (learner) route group layout</name>
  <files>src/app/(learner)/layout.tsx</files>
  <read_first>
    - src/app/account/layout.tsx (the whole file — the guard, the `deriveAvatarDisplay` helper, the `rightSlot` composition with the sign-out form, and the header comment explaining that the guard is convenience only; this layout is a near-copy)
    - src/components/shell/LearnerShell.tsx (the `nav`/`rightSlot`/`children` prop contract and the active-nav matching via `startsWith`)
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md section 2 (the `LearnerShell` row — the third nav shape "Dashboard / Catalogue / Account" with Dashboard leading)
  </read_first>
  <action>
    Create `src/app/(learner)/layout.tsx` modelled directly on `src/app/account/layout.tsx`: resolve
    `getCurrentActor()`, `redirect("/signin")` when absent, load `profileService.getOwnProfile(actor)`
    for the avatar, degrade the avatar to a neutral state when the profile row is missing rather than
    signing the learner out, and render `LearnerShell` with the same `rightSlot` composition (avatar
    span plus the `signOutAction` sign-out form).

    The `nav` constant is `[{ label: "Dashboard", href: "/dashboard" }, { label: "Catalogue", href:
    "/courses" }, { label: "Account", href: "/account" }]` — Dashboard first, per UI-SPEC section 2.

    Extract `deriveAvatarDisplay` into a shared module rather than copying it: move it to
    `src/lib/avatar-display.ts` and import it from BOTH `account/layout.tsx` and this file, so the
    initials rule cannot drift between two learner shells. Changing `account/layout.tsx` to import it
    is the only edit permitted to that file.

    Add a header doc comment stating DD-7 and DD-20.

    Do not modify `LearnerShell.tsx`. Do not add `export const dynamic` unless a build error requires
    it; if one does, match whatever the public catalogue pages already declare rather than inventing a
    new setting — check them first.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npx eslint "src/app/(learner)/layout.tsx" src/lib/avatar-display.ts src/app/account/layout.tsx` exits 0.
    - `grep -c "redirect(\"/signin\")" "src/app/(learner)/layout.tsx"` returns 1.
    - `grep -c "Dashboard" "src/app/(learner)/layout.tsx"` returns at least 1 and the nav array's first entry is the Dashboard item.
    - `grep -c "deriveAvatarDisplay" src/app/account/layout.tsx` returns at least 1 and `grep -c "function deriveAvatarDisplay" src/app/account/layout.tsx` returns 0 — the helper now lives in one place.
    - `git diff src/components/shell/LearnerShell.tsx` is empty.
  </acceptance_criteria>
  <done>`/dashboard` and `/learn/*` render inside `LearnerShell` with a three-item nav, behind one session guard, with the avatar helper shared rather than duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: ProgressMeter, DeferredSlot and NextUpCard presentational components</name>
  <files>src/components/learner/ProgressMeter.tsx, src/components/learner/DeferredSlot.tsx, src/components/learner/NextUpCard.tsx</files>
  <read_first>
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md sections 4, 5 (especially the "new this phase — teal is now drawn" table and its mandatory bordered-track composition), 6.1 and 7.1
    - src/app/globals.css (confirm the exact token names available: `--teal-fill`, `--teal-text`, `--surface`, `--surface-2`, `--border`, `--muted-foreground`, `--accent`, `--warning`, `--warning-surface`; every class must resolve to one of these, no raw hex anywhere)
    - src/app/staff/cohorts/[id]/RosterTab.tsx lines 85-100 (`DeferredCell` — the existing muted, non-interactive named-gap treatment being mirrored for the learner surface)
    - src/components/primitives/ResourceTable.tsx (the exported `StatusPill` and its tone vocabulary)
  </read_first>
  <action>
    Create three Server Components (no `"use client"` in any of them).

    `ProgressMeter({ label, valuePct, captionText })`: a bordered track — `border border-border
    rounded-full bg-surface-2` — containing a `bg-teal-fill` fill sized by `valuePct`, with
    `captionText` rendered as ADJACENT text in `text-teal-text` at the Label role (14px/600). The
    bordered track plus the adjacent text label is not decoration: UI-SPEC section 5 records it as the
    required mitigation for `--teal-fill`'s 3.07:1 standalone-swatch contrast shortfall, and this is
    the first component in the codebase to draw that token. Give the track
    `role="progressbar"` with `aria-valuenow`/`aria-valuemin`/`aria-valuemax` and an `aria-label` of
    `label`. Never render the fill freestanding without the track.

    `DeferredSlot({ title, copy })`: a muted card matching `RosterTab`'s `DeferredCell` treatment —
    muted foreground text, no icon suggesting interactivity, no link, no button. The four call sites
    pass UI-SPEC 6.1's exact strings: "Assignments and quizzes — arriving in a future update",
    "Results — arriving in a future update", "Support tickets — arriving in a future update",
    "Certificate — arriving in a future update".

    `NextUpCard({ action, enrolmentId })`: renders exactly one of the four `NextAction` variants with
    UI-SPEC 6.1's copy — the `lesson` variant shows the lesson title with "in {Module title}" beneath
    and a primary "Continue learning" link to `/learn/{enrolmentId}/lessons/{lessonId}` styled as the
    accent CTA; the `session` variant shows "{Session title} — {formatted date/time}"; the `complete`
    variant shows the congratulatory copy pointing at the certificate slot; the `none` variant shows
    the empty-state heading "Nothing to pick up right now" and its body copy. Use `PlayCircle` for the
    lesson variant and `Clock` for the session variant, both `aria-hidden`, both named imports from
    `lucide-react`.

    Every colour must come from an existing token class. No new token, no raw hex, no new type size —
    the phase is a pure consumer of the 04.1 system.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npx eslint src/components/learner/` exits 0.
    - `grep -rc "use client" src/components/learner/` returns 0 for all three files.
    - `grep -rEc "#[0-9a-fA-F]{3,8}" src/components/learner/ProgressMeter.tsx src/components/learner/DeferredSlot.tsx src/components/learner/NextUpCard.tsx` returns 0 for each — no raw hex.
    - `grep -c "role=\"progressbar\"" src/components/learner/ProgressMeter.tsx` returns 1 and `grep -c "aria-valuenow" src/components/learner/ProgressMeter.tsx` returns 1.
    - `grep -c "border-border" src/components/learner/ProgressMeter.tsx` returns at least 1 — the fill is inside a bordered track.
    - `grep -c "teal-text" src/components/learner/ProgressMeter.tsx` returns at least 1 — the caption is paired with the bar.
    - `grep -c "Nothing to pick up right now" src/components/learner/NextUpCard.tsx` returns 1.
  </acceptance_criteria>
  <done>Three token-only Server Components exist; the teal bar is always a bordered track with an adjacent labelled caption.</done>
</task>

<task type="auto">
  <name>Task 3: The /dashboard page</name>
  <files>src/app/(learner)/dashboard/page.tsx, tests/learner-dashboard-page.test.ts</files>
  <read_first>
    - src/app/(learner)/layout.tsx and src/components/learner/*.tsx (as written in Tasks 1-2)
    - src/server/services/enrolment-dashboard-service.ts (created by 09-07 — the exact card shape)
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md sections 6.1 and 7.1 (every copy string and the card grid: "Next up" full-width top row; "Your progress" / "Upcoming sessions" as a two-column row on ≥768px; the four named-gap cards as a final row; one column below 768px)
    - src/app/orders/[reference]/page.tsx or another Phase 6 fact-card page (the `<dl>` fact-card composition and the container/section class conventions to match)
    - tests/landing.test.ts (the render-test harness this repo uses for pages)
  </read_first>
  <action>
    Create `src/app/(learner)/dashboard/page.tsx` as a Server Component. Resolve
    `getCurrentActor()`, `redirect("/signin")` if absent (the layout guard is convenience only per
    DD-20), call `loadLearnerDashboard(actor)`, and render:

    - `<h1>` "Your dashboard".
    - Zero cards → the empty state with UI-SPEC 6.1's heading and body copy plus an accent link to
      `/courses`.
    - One section per card, headed by the offer/cohort title, containing: `NextUpCard` full width;
      then a responsive two-column row (`grid gap-6 md:grid-cols-2`, single column below 768px) with a
      "Your progress" card and an "Upcoming sessions" card; then a final row of four `DeferredSlot`
      cards.
    - "Your progress" card: a `ProgressMeter` labelled with UI-SPEC 6.1's "{completedCount} of
      {totalRequired} required lessons complete" caption. When `progress.attendance.kind ===
      "computed"`, a SECOND, identical `ProgressMeter` beneath it captioned "Attendance: {earned}% of
      {required}% required". The two bars are never merged into one composite percentage (UI-SPEC
      7.1). When `attendance.kind` is `"no-rule"`, render no attendance bar at all; when
      `"no-sessions"`, render a muted named-gap line rather than a 0% bar.
    - When the card's `structure` is `"unpinned"`, render a muted named-gap line in place of the
      progress meter rather than "0 of 0 complete".
    - "Upcoming sessions" card: up to three rows (title, formatted date/time in the cohort timezone,
      `MapPin` for in-person / `Video` for virtual), the empty copy "No sessions are scheduled yet"
      when there are none, and a "View all" accent link to `/learn/{enrolmentId}/sessions` when
      `hasMoreSessions` is true. Never render a meeting link here.
    - The access notice: `accessNotice.kind === "ending"` → "Your access ends {date}" in the
      `--warning` tone (`bg-warning-surface`, `text-warning`), NOT `--danger`; `"ended"` → UI-SPEC
      6.1's two-line ended copy.

    Format dates using the repo's existing timezone helper (`src/lib/timezone.ts`) against the
    cohort's IANA `timezone` — do not call `toLocaleString` with the server's local zone.

    Write `tests/learner-dashboard-page.test.ts` following `tests/landing.test.ts`'s harness: mock the
    dashboard service and assert the rendered output for an empty dashboard, a one-card dashboard with
    a lesson next-action, a card with an attendance threshold showing two separate progressbars, a
    card with `no-rule` attendance showing exactly one progressbar, and a card whose rendered HTML
    contains none of the four named-gap strings as a link (`<a`) element.
  </action>
  <verify>
    <automated>npx vitest run tests/learner-dashboard-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/learner-dashboard-page.test.ts` passes with at least 6 cases.
    - A test asserts an attendance-threshold card renders exactly 2 elements with `role="progressbar"`, and a `no-rule` card renders exactly 1.
    - A test asserts the rendered empty state contains the string "Nothing to pick up right now".
    - A test asserts the rendered output contains no `meetingUrl`/`http` join link inside the upcoming-sessions card.
    - `grep -c "Your dashboard" "src/app/(learner)/dashboard/page.tsx"` returns 1.
    - `grep -Ec "#[0-9a-fA-F]{3,8}" "src/app/(learner)/dashboard/page.tsx"` returns 0.
    - `grep -c "toLocaleString\|toLocaleDateString" "src/app/(learner)/dashboard/page.tsx"` returns 0 — dates go through the timezone helper.
    - `npx next build` completes without error.
  </acceptance_criteria>
  <done>`/dashboard` renders every LRN-01 slot for the signed-in learner's own enrolments, with two independent progress bars and four visibly labelled gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → `/dashboard` | An unauthenticated request must not render any learner data |
| dashboard payload → browser HTML | Anything placed in props is readable in page source, including values hidden with CSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `/dashboard` page | mitigate | The page re-resolves `getCurrentActor()` itself and redirects when absent; `loadLearnerDashboard` accepts no id parameter, so there is no value to tamper with |
| T-09-04 | Information Disclosure | Upcoming-sessions card | mitigate | The card renders title, time and mode only; the service (09-07) never selects `meetingUrl`, so the value is not in the payload to hide |
| T-09-31 | Information Disclosure | Layout guard relied on as security | mitigate | DD-20 recorded in the layout header and enforced by the page performing its own actor resolution |
| T-09-32 | Tampering | Named-gap slots implying shipped capability | mitigate | `DeferredSlot` renders no link and no button, so a gap cannot be clicked into a non-existent Phase 10/11/12 surface |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase; all icons come from the already-installed `lucide-react` |
</threat_model>

<verification>
- `npx vitest run tests/learner-dashboard-page.test.ts` green.
- `npx next build` completes.
- `npx tsc --noEmit` exits 0; `npx eslint "src/app/(learner)" src/components/learner` exits 0.
- `git diff src/components/shell/LearnerShell.tsx src/app/globals.css` is empty.
</verification>

<success_criteria>
- `/dashboard` exists, is guarded, and shows only the caller's own enrolments.
- Teal is drawn only as a bordered track with an adjacent text caption.
- The four forward slots render as visibly labelled, non-interactive gaps.
</success_criteria>

<output>
Create `.planning/phases/09-learning-delivery-progress-tracking/09-08-SUMMARY.md` when done.
</output>

---
phase: 09-learning-delivery-progress-tracking
plan: 10
type: execute
wave: 6
depends_on: ["09-03", "09-08"]
files_modified:
  - src/server/services/learner-session-service.ts
  - src/app/(learner)/learn/[enrolmentId]/sessions/page.tsx
  - src/components/learner/SessionCard.tsx
  - tests/learner-session-service.test.ts
  - tests/learner-sessions-page.test.ts
autonomous: true
requirements: [LRN-06]

must_haves:
  truths:
    - "An enrolled learner sees their cohort's sessions with times shown in the cohort timezone"
    - "A meeting link is absent from the page source until its visibility window opens"
    - "A past session shows the learner's own attendance outcome"
  artifacts:
    - path: "src/server/services/learner-session-service.ts"
      provides: "Ownership-scoped session list with the server-side meeting-link gate"
      exports: ["listOwnCohortSessions"]
    - path: "src/app/(learner)/learn/[enrolmentId]/sessions/page.tsx"
      provides: "The LRN-06 sessions view"
  key_links:
    - from: "src/server/services/learner-session-service.ts"
      to: "isMeetingLinkVisible"
      via: "reused Phase 5 gate, not reimplemented"
      pattern: "isMeetingLinkVisible\\("
---

<objective>
Ship LRN-06: the learner's view of their cohort's scheduled sessions, with the meeting link revealed
only inside its configured window and omitted from the payload entirely before then.

Purpose: `ScheduledSession.linkVisibleFromMinutes` and `isMeetingLinkVisible` already exist from
Phase 5, but every current caller is staff-authorized. This plan gives the learner an
ownership-scoped path to the same gate.
Output: a session read service, the sessions page, a session card, and tests for both.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-delivery-progress-tracking/09-CONTEXT.md
@.planning/phases/09-learning-delivery-progress-tracking/09-RESEARCH.md
@.planning/phases/09-learning-delivery-progress-tracking/09-PATTERNS.md
@.planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md
</context>

<interfaces>
Consumed from earlier plans:
  getOwnActiveEnrolment(actor, enrolmentId): Promise<LearnerEnrolmentSnapshot | null>   // 09-03

Existing, reused unchanged:
  isMeetingLinkVisible(session: { startsAt; linkVisibleFromMinutes; cancelledAt }, now: Date, viewer: { enrolled: boolean }): boolean   // scheduled-session-service.ts (exported)
  utcToWallParts / isValidTimeZone                                                        // src/lib/timezone.ts
  StatusPill                                                                              // src/components/primitives/ResourceTable.tsx
  AttendanceState enum values: PRESENT | ABSENT | LATE | EXCUSED | NOT_RECORDED
  lucide-react icons: Clock, MapPin, Video, Calendar
</interfaces>

<planner_rulings>
- **DD-23:** `isMeetingLinkVisible` is IMPORTED from `scheduled-session-service.ts`, never
  reimplemented. That file's header already names link disclosure as one of the two things "easy to
  get wrong and expensive later"; a second copy of the arithmetic is exactly the drift it warns about.
  Note that importing from `scheduled-session-service.ts` pulls `withPermission` onto this module's
  import graph — acceptable because `learner-session-service.ts` is only ever called from a Server
  Component, never from a worker or webhook entrypoint. If `tests/boundary.test.ts` flags it, move
  `isMeetingLinkVisible` and `linkVisibleFrom` into a small pure module (e.g. `src/lib/session-visibility.ts`)
  imported by both files rather than duplicating either.
- **DD-24:** the gate is applied by OMITTING the key. A session outside its window has no `meetingUrl`
  property at all in the returned object — not `null`, not an empty string, and certainly not a value
  hidden with CSS. `scheduled-session-service.ts`'s `readSessionForViewer` already does exactly this
  (`const { meetingUrl, ...safe } = row`); copy that shape.
</planner_rulings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: learner-session-service.ts — ownership-scoped session list with the link gate</name>
  <files>src/server/services/learner-session-service.ts, tests/learner-session-service.test.ts</files>
  <read_first>
    - src/server/services/scheduled-session-service.ts lines 1-50 (the header's meeting-link disclosure rule), lines 225-250 (`isMeetingLinkVisible`, `linkVisibleFrom`) and lines 605-640 (`readSessionForViewer`'s strip-then-add-back shape)
    - src/server/services/learner-access.ts (created by 09-03 — `getOwnActiveEnrolment`)
    - src/lib/timezone.ts (`utcToWallParts` — how a UTC instant is rendered in the cohort's IANA zone)
    - prisma/schema.prisma lines 853-905 (`model ScheduledSession`, `model AttendanceRecord`)
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md sections 6.1 and 7.4
  </read_first>
  <behavior>
    - `listOwnCohortSessions(actor, enrolmentId, now)` returns `null` whenever `getOwnActiveEnrolment` returns null — same denial parity.
    - Otherwise it returns `{ timezone, upcoming: SessionView[], past: SessionView[] }`, split on `endsAt` relative to `now`, upcoming ascending by `startsAt` and past descending.
    - `SessionView` always carries `{ id, title, startsAt, endsAt, location, cancelledAt, cancellationReason, mode, attendance }` where `mode` is `"virtual"` when the row has a `meetingUrl` value, `"in-person"` when it has a `location` and no `meetingUrl`, otherwise `"unknown"`. Deriving `mode` must NOT expose the URL — compute it from the presence of the column and then discard the value.
    - `meetingUrl` is present on a `SessionView` ONLY when `isMeetingLinkVisible(row, now, { enrolled: true })` is true. Otherwise the key is absent entirely (DD-24).
    - When the link is not yet visible and the session is not cancelled, the view carries `meetingUrlAvailableFrom: Date` so the page can render "The meeting link opens {N} minutes before this session starts".
    - A cancelled session never carries `meetingUrl` or `meetingUrlAvailableFrom`, regardless of the window (`isMeetingLinkVisible` already returns false for it).
    - `attendance` is the learner's OWN `AttendanceRecord` state for that session, or `"NOT_RECORDED"` when no row exists. No other learner's attendance is read or returned.
  </behavior>
  <action>
    Create `src/server/services/learner-session-service.ts` following the injected-deps convention
    with a live singleton at the bottom. Import `isMeetingLinkVisible` from
    `scheduled-session-service.ts` per DD-23 — do not copy the arithmetic. Build `SessionView` with
    the strip-then-add-back destructure `readSessionForViewer` uses, so the closed-gate object has no
    `meetingUrl` key.

    Header doc comment states DD-23 and DD-24 and adds: a learner's attendance read here is their own
    row only, keyed on the enrolment already proven to be theirs.

    Write `tests/learner-session-service.test.ts` with an in-memory store fake covering every
    behavior bullet, including explicit assertions that `"meetingUrl" in view` is `false` before the
    window, `true` inside it, and `false` for a cancelled session inside its window; and that a
    virtual session outside its window still reports `mode: "virtual"` without exposing the URL.
  </action>
  <verify>
    <automated>npx vitest run tests/learner-session-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/learner-session-service.test.ts` passes with at least 11 cases.
    - A test asserts `("meetingUrl" in view) === false` for a session one minute before its window opens, and `=== true` one minute after.
    - A test asserts a cancelled session inside its window has neither `meetingUrl` nor `meetingUrlAvailableFrom`.
    - A test asserts a session outside its window reports `mode: "virtual"` and that `JSON.stringify(view)` does not contain the fixture's meeting URL string.
    - A test asserts another learner's `AttendanceRecord` for the same session never appears in the result.
    - `grep -c "linkVisibleFromMinutes \* 60" src/server/services/learner-session-service.ts` returns 0 — the arithmetic is not reimplemented.
    - `npx vitest run tests/boundary.test.ts` still passes.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>An ownership-scoped session list where the meeting URL is structurally absent until its window opens.</done>
</task>

<task type="auto">
  <name>Task 2: SessionCard and the sessions page</name>
  <files>src/components/learner/SessionCard.tsx, src/app/(learner)/learn/[enrolmentId]/sessions/page.tsx, tests/learner-sessions-page.test.ts</files>
  <read_first>
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md sections 1 (icons), 6.1 (every sessions-page copy string), 7.4 (the card composition, the Upcoming/Past split, the attendance tone mapping) and the section 8 long-text unresolved row
    - src/server/services/learner-session-service.ts (as written in Task 1)
    - src/app/(learner)/learn/[enrolmentId]/page.tsx (created by 09-09 — the params/actor/notFound conventions to match)
    - src/components/primitives/ResourceTable.tsx (the `StatusPill` tone vocabulary)
    - src/lib/timezone.ts
  </read_first>
  <action>
    Create `src/components/learner/SessionCard.tsx` as a Server Component taking one `SessionView`
    plus the cohort `timezone`. Render a `rounded-lg border border-border bg-surface px-4 py-3
    shadow-xs` card containing: the title (struck through when cancelled); the date/time rendered in
    the cohort timezone via `src/lib/timezone.ts`, at the mono/timestamp convention, with a `Clock`
    icon; a `MapPin` + location line for in-person; for virtual, either an accent "Join session"
    link to `meetingUrl` when the key is present, or the muted copy "The meeting link opens {N}
    minutes before this session starts" when `meetingUrlAvailableFrom` is present.

    For a cancelled session render a `neutral` `StatusPill` reading "Cancelled" plus the
    cancellation reason when present. For a past session render the attendance `StatusPill` using
    UI-SPEC 7.4's existing Phase-5 tone mapping: `success` "Attended" for PRESENT, `warning` "Late"
    for LATE, `danger` "Absent" for ABSENT, `neutral` "Excused" for EXCUSED, `neutral` "Not recorded"
    for NOT_RECORDED. Do not invent a new tone.

    Location text and cancellation reason must wrap, with no fixed-width truncation — UI-SPEC section
    8 records real-world lengths for these two strings as an unresolved planner assumption, so the
    layout must absorb any reasonable string. Use `break-words` and no `truncate`/`line-clamp` on
    those two fields.

    Create `src/app/(learner)/learn/[enrolmentId]/sessions/page.tsx`: await params, resolve the actor
    and redirect to `/signin` when absent, call `listOwnCohortSessions`, `notFound()` on `null`, and
    render `<h1>` "Scheduled sessions", a back link to `/learn/{enrolmentId}`, then an "Upcoming"
    group and a "Past" group of `SessionCard`s. Render "No sessions are scheduled yet" when both
    groups are empty. No filter control, no pagination, no search (UI-SPEC 7.4).

    Write `tests/learner-sessions-page.test.ts` asserting: the rendered HTML for a pre-window virtual
    session contains the "opens {N} minutes before" copy and does NOT contain the fixture's meeting
    URL anywhere in the output; the in-window case renders a "Join session" anchor whose href is the
    meeting URL; a cancelled session renders the Cancelled pill and struck-through title; a past
    session renders the correct attendance pill label; and the empty case renders the empty copy.
  </action>
  <verify>
    <automated>npx vitest run tests/learner-sessions-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/learner-sessions-page.test.ts` passes with at least 6 cases.
    - A test asserts the full rendered HTML string for a pre-window session does not contain the fixture meeting URL (substring check, not a DOM query).
    - A test asserts the in-window session renders an `<a>` whose `href` equals the fixture meeting URL with the text "Join session".
    - `grep -Ec "truncate|line-clamp" src/components/learner/SessionCard.tsx` returns 0.
    - `grep -Ec "#[0-9a-fA-F]{3,8}" src/components/learner/SessionCard.tsx "src/app/(learner)/learn/[enrolmentId]/sessions/page.tsx"` returns 0 for each.
    - `grep -c "toLocaleString\|toLocaleDateString" src/components/learner/SessionCard.tsx` returns 0 — times go through the timezone helper.
    - `npx next build` completes without error.
  </acceptance_criteria>
  <done>The sessions page shows cohort-timezone times, own attendance outcomes, and a join link only inside the configured window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learner browser → sessions page | The enrolment id is caller-supplied; the rendered HTML is fully readable including anything "hidden" |
| meeting link as a bearer capability | Anyone holding the URL can join, regardless of enrolment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Meeting-link visibility | mitigate | DD-24 — the key is omitted from the returned object before rendering, so the URL never reaches the HTML; asserted by a substring check on the full rendered output, not a DOM query |
| T-09-35 | Information Disclosure | `mode` derivation | mitigate | `mode: "virtual"` is derived from the presence of the column and the value is then discarded, so the badge cannot leak the URL |
| T-09-01 | Information Disclosure | Session list ownership | mitigate | `listOwnCohortSessions` resolves through `getOwnActiveEnrolment`, returning an identical `null` for not-found / not-mine / not-ACTIVE |
| T-09-36 | Information Disclosure | Other learners' attendance | mitigate | The attendance read is filtered to the proven-owned `enrolmentId`; a test asserts a sibling enrolment's record never appears |
| T-09-37 | Tampering | Divergent visibility arithmetic | mitigate | DD-23 — `isMeetingLinkVisible` is imported from the Phase 5 module; a grep gate asserts the minute arithmetic is not re-written here |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx vitest run tests/learner-session-service.test.ts tests/learner-sessions-page.test.ts tests/boundary.test.ts` green.
- Existing Phase 5 session tests still green: `npx vitest run tests/cohort-service.test.ts tests/attendance-window.test.ts`.
- `npx next build` completes; `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- Sessions render in the cohort timezone, grouped Upcoming/Past, with own attendance outcomes.
- The meeting URL is absent from the rendered HTML before its window and present inside it.
- No copy of Phase 5's visibility arithmetic was created.
</success_criteria>

<output>
Create `.planning/phases/09-learning-delivery-progress-tracking/09-10-SUMMARY.md` when done.
</output>

---
phase: 09-learning-delivery-progress-tracking
plan: 09
type: execute
wave: 6
depends_on: ["09-08"]
files_modified:
  - src/app/(learner)/learn/[enrolmentId]/page.tsx
  - src/components/learner/LessonRow.tsx
  - src/components/learner/AccessDeniedPanel.tsx
  - tests/learner-lesson-list-page.test.ts
autonomous: true
requirements: [LRN-02]

must_haves:
  truths:
    - "Modules and lessons render in their defined order with lock state visible"
    - "A locked lesson names the specific lesson that blocks it, by title, and is not clickable"
    - "A non-ACTIVE or non-owned enrolment shows no lesson titles or module structure at all"
  artifacts:
    - path: "src/app/(learner)/learn/[enrolmentId]/page.tsx"
      provides: "The LRN-02 lesson list with server-enforced lock state"
      min_lines: 70
    - path: "src/components/learner/LessonRow.tsx"
      provides: "Four-state lesson row (complete / current / unlocked / locked)"
  key_links:
    - from: "src/app/(learner)/learn/[enrolmentId]/page.tsx"
      to: "loadLearnerPath"
      via: "ownership-scoped server read"
      pattern: "loadLearnerPath\\("
---

<objective>
Ship `/learn/[enrolmentId]`: the ordered module/lesson tree with lock affordances, the D-06 blocking
explanation, and the absolute D-07 access gate.

Purpose: LRN-02 requires ordering, server-side sequencing enforcement, and a locked item that
explains its unmet condition. The enforcement already exists (09-02/09-03); this plan is the surface
that renders exactly what the server computed, with no second opinion.
Output: the lesson-list page, two components, and a render test.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-delivery-progress-tracking/09-CONTEXT.md
@.planning/phases/09-learning-delivery-progress-tracking/09-RESEARCH.md
@.planning/phases/09-learning-delivery-progress-tracking/09-PATTERNS.md
@.planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md
</context>

<interfaces>
Consumed from earlier plans:
  loadLearnerPath(actor, enrolmentId): Promise<LearnerPath | null>     // 09-03 — null for not-found / not-mine / not-ACTIVE
  LearnerPath.courses[].modules[].lessons[] each decorated with { locked, blockingLessonTitle, completed, completedSource }
  ProgressMeter({ label, valuePct, captionText })                      // 09-08
  loadLearnerDashboard / LearnerDashboardCard.progress                 // 09-07 (for the header bar's counts)

Existing, reused unchanged:
  getCurrentActor(): Promise<Actor | null>
  StatusPill (exported from src/components/primitives/ResourceTable.tsx)
  lucide-react icons: CheckCircle2, PlayCircle, Circle, Lock
</interfaces>

<planner_rulings>
- **DD-21:** the page renders `loadLearnerPath`'s `locked` flag verbatim. It must not re-derive lock
  state from counts, positions or completion sets in JSX — that would be the second copy
  `readiness-service.ts`'s "one function, N call sites" rule exists to prevent.
- **DD-22:** a denial renders `notFound()`, not a bespoke 403 screen, whenever the enrolment is not
  the caller's or does not exist. The UI-SPEC 7.2 "You don't have access to this course yet" panel is
  reserved for the ONE case the caller can act on: their own enrolment existing in `PENDING_PAYMENT`.
  Because `loadLearnerPath` returns an identical `null` for all three causes (denial parity), the page
  must ask a separate, explicitly-scoped question to distinguish that case — see Task 3's action.
</planner_rulings>

<tasks>

<task type="auto">
  <name>Task 1: LessonRow — the four-state row</name>
  <files>src/components/learner/LessonRow.tsx</files>
  <read_first>
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md sections 1 (the icon table), 4 (type roles), 5 (locked rows use `--warning`/`--muted-foreground`, never `--danger`), 6.1 (the locked-row copy string) and 7.2 (the row anatomy and the non-interactive locked-row requirement)
    - src/app/staff/cohorts/[id]/RosterTab.tsx (existing row/cell class conventions on a list surface)
    - src/components/learner/NextUpCard.tsx (created by 09-08 — match its icon import style and token usage)
  </read_first>
  <action>
    Create `src/components/learner/LessonRow.tsx` as a Server Component taking `{ lesson, href,
    isCurrent }` where `lesson` carries `{ id, title, type, completed, locked, blockingLessonTitle }`.

    Render exactly four mutually exclusive states, in this precedence: completed → `CheckCircle2` in
    the success tone; locked → `Lock` with a `--warning`-tinted icon and a muted row background;
    current (the next-action lesson) → `PlayCircle` in the accent tone with the title at Label weight
    and an accent left-edge indicator; otherwise → `Circle` in the muted tone. A completed lesson that
    is also the current one renders as completed.

    A locked row is a `<div aria-disabled="true">` with NO `href` and no `<a>` wrapper — a locked
    lesson has nothing to navigate to, and a disabled-styled link would still be focusable and
    followable. Beneath the title it renders UI-SPEC 6.1's exact sentence: `Complete '{blockingLessonTitle}' to unlock this`
    at Body role in `text-muted-foreground`, with the quoted title wrapped in a `font-semibold` span.
    The sentence must wrap rather than truncate, and the `Lock` icon must stay top-aligned when it
    does — use `shrink-0` on the icon and `break-words` on the text (this is the UI-SPEC section 8
    long-text backstop).

    An unlocked row is a `<Link href={href}>`. Render the optional type hint ("Video", "Text", etc.)
    only when the metadata exists — no placeholder, no empty cell, per the conditional-fact-row
    convention.

    Tokens only; no raw hex; no `--danger` anywhere in this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npx eslint src/components/learner/LessonRow.tsx` exits 0.
    - `grep -c "aria-disabled=\"true\"" src/components/learner/LessonRow.tsx` returns 1.
    - `grep -c "danger" src/components/learner/LessonRow.tsx` returns 0.
    - `grep -Ec "#[0-9a-fA-F]{3,8}" src/components/learner/LessonRow.tsx` returns 0.
    - `grep -c "to unlock this" src/components/learner/LessonRow.tsx` returns 1.
    - `grep -c "break-words" src/components/learner/LessonRow.tsx` returns at least 1 and `grep -c "shrink-0" src/components/learner/LessonRow.tsx` returns at least 1.
    - `grep -c "use client" src/components/learner/LessonRow.tsx` returns 0.
  </acceptance_criteria>
  <done>A four-state lesson row where the locked state is non-navigable and names its blocker by title in wrapping text.</done>
</task>

<task type="auto">
  <name>Task 2: AccessDeniedPanel for the PENDING_PAYMENT case</name>
  <files>src/components/learner/AccessDeniedPanel.tsx</files>
  <read_first>
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md section 7.2 (the access-gate treatment and its "no lesson titles, module structure, or any other catalogue detail leaks through this gate" requirement) and section 6.1
    - src/components/primitives/DetailLayout.tsx (the existing `denied` state's visual treatment being matched — visual consistency only, not the same authorization mechanism)
    - .planning/phases/09-learning-delivery-progress-tracking/09-CONTEXT.md decision D-07 and the Deferred Ideas entry rejecting preview access
  </read_first>
  <action>
    Create `src/components/learner/AccessDeniedPanel.tsx` as a Server Component taking `{ orderHref }`
    (nullable). It renders the UI-SPEC 7.2 copy "You don't have access to this course yet" with a
    short explanatory line, and — when `orderHref` is non-null — an accent link back to that order or
    checkout page.

    The component takes NO course, module or lesson data as props. Add a doc comment stating why: D-07
    makes this an absolute gate, the Deferred Ideas section explicitly rejected preview access before
    payment, and a component that cannot receive catalogue data cannot leak it. Match
    `DetailLayout`'s denied-state visual treatment for consistency, while noting inline that the
    mechanism here is an ownership/status check, not an RBAC scope match, so no permission string is
    displayed.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npx eslint src/components/learner/AccessDeniedPanel.tsx` exits 0.
    - `grep -c "You don't have access to this course yet" src/components/learner/AccessDeniedPanel.tsx` returns 1.
    - The component's props type contains no `lesson`, `module`, `course` or `title` field — verify with `grep -Ec "lesson|module|course[A-Z]|modules" src/components/learner/AccessDeniedPanel.tsx` returning 0.
    - `grep -c "permission" src/components/learner/AccessDeniedPanel.tsx` returns 0 in rendered output (a code comment mentioning it is fine — check the JSX contains no permission string).
  </acceptance_criteria>
  <done>A denial panel that structurally cannot render catalogue detail and shows no permission identifier.</done>
</task>

<task type="auto">
  <name>Task 3: The lesson-list page</name>
  <files>src/app/(learner)/learn/[enrolmentId]/page.tsx, tests/learner-lesson-list-page.test.ts</files>
  <read_first>
    - src/app/staff/courses/[id]/preview/lessons/[lessonId]/page.tsx (the params-await, `notFound()`-on-denial and flatten-for-neighbours conventions this repo uses for a lesson surface)
    - src/server/services/learner-access.ts (created by 09-03 — `loadLearnerPath`, and whatever narrow export you add here for the PENDING_PAYMENT question)
    - src/components/learner/LessonRow.tsx, AccessDeniedPanel.tsx, ProgressMeter.tsx
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md sections 6.1, 7.2 and the section 8 overflow rows
  </read_first>
  <action>
    Create `src/app/(learner)/learn/[enrolmentId]/page.tsx` as a Server Component. Await
    `params`, resolve `getCurrentActor()` and `redirect("/signin")` if absent.

    Call `loadLearnerPath(actor, enrolmentId)`. When it returns a path, render:
    `<h1>` with the course/offer title; a `ProgressMeter` beneath it with the "{N} of {M} required
    lessons complete" caption (UI-SPEC 7.2's "so a learner scanning module structure also sees where
    they stand"); then, per course (in `CohortCourse.position` order for a programme cohort), the
    module groups as Display-role headers each followed by their `LessonRow`s in position order. A
    module with zero lessons still renders its header with no rows beneath and no broken layout.
    `isCurrent` is true for the first incomplete, unlocked, required lesson across the whole path.

    When `loadLearnerPath` returns `null`, distinguish the ONE actionable case per DD-22: add a narrow
    export to `learner-access.ts` — `getOwnPendingEnrolmentOrderHref(actor, enrolmentId)` — that
    returns an order/checkout href ONLY when an enrolment with that id exists, belongs to
    `actor.userId`, AND has status `PENDING_PAYMENT`; it returns `null` in every other case,
    preserving denial parity for ids that are not the caller's. Render `AccessDeniedPanel` with that
    href when it is non-null, and `notFound()` otherwise.

    Add a comment above that branch recording DD-22 and why the extra lookup is safe: it is still keyed
    on `actor.userId` and still returns a uniform `null` for a stranger's id, so it discloses nothing
    a caller did not already know about their own enrolment.

    Render a `{ kind: "unpinned" }` structure as a muted named-gap panel ("This course's content is
    not yet available") rather than an empty tree.

    Write `tests/learner-lesson-list-page.test.ts` covering: an owned ACTIVE enrolment rendering
    module headers and lesson rows in order; a locked row rendering the blocking title and carrying no
    `href`; an unlocked row carrying an `href` to `/learn/{enrolmentId}/lessons/{lessonId}`; a
    PENDING_PAYMENT enrolment rendering the denied panel and zero lesson titles; a stranger's
    enrolment id triggering `notFound()`; and a module with zero lessons rendering its header without
    error.
  </action>
  <verify>
    <automated>npx vitest run tests/learner-lesson-list-page.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/learner-lesson-list-page.test.ts` passes with at least 7 cases.
    - A test asserts the PENDING_PAYMENT render contains none of the fixture's lesson titles and none of its module titles.
    - A test asserts the locked row's rendered HTML contains the blocking lesson's title and contains no `href` for that row.
    - A test asserts a stranger's enrolment id results in `notFound()` being called.
    - `grep -c "locked" "src/app/(learner)/learn/[enrolmentId]/page.tsx"` shows lock state is only passed through to `LessonRow`, never recomputed — assert no `evaluateLessonSequencing` import exists in the page: `grep -c "evaluateLessonSequencing" "src/app/(learner)/learn/[enrolmentId]/page.tsx"` returns 0.
    - `grep -Ec "#[0-9a-fA-F]{3,8}" "src/app/(learner)/learn/[enrolmentId]/page.tsx"` returns 0.
    - `npx next build` completes without error.
  </acceptance_criteria>
  <done>The lesson list renders server-computed lock state verbatim, explains each lock by title, and leaks nothing through the D-07 gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learner browser → `/learn/[enrolmentId]` | The enrolment id is a path segment and fully caller-controlled |
| server lock computation → rendered markup | A UI that re-derives lock state can disagree with the server gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Lesson-list page | mitigate | `loadLearnerPath` returns an identical `null` for not-found / not-mine / not-ACTIVE, and the page maps that to `notFound()`; the one PENDING_PAYMENT exception is itself keyed on `actor.userId` and returns `null` for a stranger's id |
| T-09-33 | Information Disclosure | D-07 gate | mitigate | `AccessDeniedPanel` accepts no catalogue props, so no lesson or module title can render on the denied path; asserted by a test that checks fixture titles are absent |
| T-09-13 | Tampering | Lock rendering | mitigate | DD-21 — the page passes `locked` through and imports no sequencing evaluator; the rendered lock and the server gate are the same computation |
| T-09-34 | Elevation of Privilege | Locked-row navigation | mitigate | A locked row is a non-focusable `<div aria-disabled="true">` with no `href`; even if a learner constructs the URL by hand, 09-11's page calls `assertLessonOpenable` server-side |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase |
</threat_model>

<verification>
- `npx vitest run tests/learner-lesson-list-page.test.ts` green.
- `npx next build` completes.
- `npx tsc --noEmit` exits 0; `npx eslint "src/app/(learner)" src/components/learner` exits 0.
- `git diff src/components/catalogue/LessonContent.tsx` is empty.
</verification>

<success_criteria>
- Ordered module/lesson tree with four distinguishable row states.
- Every locked row names its blocking lesson by title and cannot be navigated.
- The D-07 gate leaks no catalogue detail.
</success_criteria>

<output>
Create `.planning/phases/09-learning-delivery-progress-tracking/09-09-SUMMARY.md` when done.
</output>

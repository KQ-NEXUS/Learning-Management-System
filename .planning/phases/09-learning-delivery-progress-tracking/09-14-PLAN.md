---
phase: 09-learning-delivery-progress-tracking
plan: 14
type: execute
wave: 9
depends_on: ["09-08", "09-09", "09-10", "09-11", "09-12", "09-13"]
files_modified:
  - tests/learning-phase-invariants.test.ts
  - tests/learner-journey.integration.test.ts
autonomous: false
requirements: [LRN-01, LRN-02, LRN-03, LRN-04, LRN-05, LRN-06, LRN-07]

must_haves:
  truths:
    - "The phase's structural rules are enforced by a test, not by convention"
    - "The learner journey works end to end against a real database"
    - "Every UI-SPEC backstop has explicit evidence or is escalated as human-needed"
  artifacts:
    - path: "tests/learning-phase-invariants.test.ts"
      provides: "Compiler-API guards for this phase's architectural invariants"
    - path: "tests/learner-journey.integration.test.ts"
      provides: "Real-Postgres end-to-end proof of LRN-01..LRN-07"
  key_links:
    - from: "tests/learning-phase-invariants.test.ts"
      to: "src/server/services/"
      via: "TypeScript compiler-API import-graph assertions"
      pattern: "createSourceFile"
---

<objective>
Lock this phase's structural rules into an executable invariants test, prove the learner journey end
to end against a real Postgres, and put the three UI-SPEC backstops plus the visual walkthrough in
front of a human.

Purpose: every rule this phase relies on — pure modules staying import-free, ownership services never
importing `withPermission`, `meetingUrl` never leaving the gate, `Enrolment.status` never being
written by the completion engine — is a convention that a future contributor can erode silently
unless a test catches it.
Output: two test files and one human verification checkpoint.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-delivery-progress-tracking/09-CONTEXT.md
@.planning/phases/09-learning-delivery-progress-tracking/09-RESEARCH.md
@.planning/phases/09-learning-delivery-progress-tracking/09-PATTERNS.md
@.planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase invariants test</name>
  <files>tests/learning-phase-invariants.test.ts</files>
  <read_first>
    - tests/checkout-phase-invariants.test.ts (the whole file — the TypeScript compiler-API harness, the directory-prefix exemption convention, and the assertion style this file copies)
    - tests/boundary.test.ts lines 1-160 (`runtimeImports`, `resolveProjectImport`, `runtimeClosureFrom` — reuse the walking approach rather than writing a third traversal)
    - src/server/services/access-window.ts, lesson-sequencing.ts, completion-rule.ts, completion-engine.ts (the pure modules whose import-freedom is being asserted)
    - src/server/services/learner-access.ts, learner-session-service.ts, enrolment-dashboard-service.ts, lesson-progress-service.ts, completion-service.ts
  </read_first>
  <behavior>
    - INV-1: `access-window.ts`, `lesson-sequencing.ts` and `completion-rule.ts` have zero import declarations of any kind. `completion-engine.ts` has zero NON-type-only import declarations.
    - INV-2: `learner-access.ts` and `enrolment-dashboard-service.ts` contain no runtime import of `@/server/permissions` and no `withPermission` identifier. A type-only `Actor` import is allowed. `learner-session-service.ts` is asserted separately: it may import `scheduled-session-service.ts` (DD-23) but must contain no `withPermission` CALL of its own.
    - INV-3: `completion-service.ts` contains no `enrolment.update` call, no `assertTransition` call and no `"COMPLETED"` string literal (DD-6).
    - INV-4: no file under `src/app/(learner)/` and no file in `src/components/learner/` contains the identifier `meetingUrl` except `SessionCard.tsx`, which may reference it only inside a conditional render (DD-24).
    - INV-5: `src/components/catalogue/LessonContent.tsx` and `LessonMediaPlayer.tsx` contain no `"use client"` directive and no `useState`/`useEffect` identifier — the zero-client-JS guarantee this phase consumed.
    - INV-6: no file under `src/app/(learner)/` or `src/components/learner/` contains a raw hex colour literal (the zero-raw-hex-outside-globals.css invariant from 04.1).
    - INV-7: the three new `DomainEventType` members exist in `domain-event-service.ts` and no previously existing member was removed — assert the full historical member list is still present.
    - INV-8: every Server Action file under `src/app/(learner)/` begins with `"use server"` and contains no `formData.get("userId")` / `formData.get("actorId")` call.
  </behavior>
  <action>
    Create `tests/learning-phase-invariants.test.ts` modelled on
    `tests/checkout-phase-invariants.test.ts`: use the TypeScript compiler API to parse each target
    file and assert the invariants in the behavior block, with one `it` per invariant and a message
    that explains WHY the rule exists, not just that it failed — a future contributor reading a red
    test needs the reason, which is the whole point of encoding conventions as tests.

    Reuse `tests/boundary.test.ts`'s existing import-walking helpers by importing them if they are
    exported; if they are not, export them from `boundary.test.ts` rather than writing a third copy
    of the traversal — that file's own header warns that two copies of an import-graph walk is exactly
    the drift these tests exist to prevent.

    For INV-6, exclude `globals.css` and strip comment contents before matching, so a hex value quoted
    in a doc comment does not self-invalidate the gate.

    Run the test against the real tree and fix any genuine violation it surfaces in the phase's own
    files. If it flags a PRE-EXISTING violation outside this phase's files, record it in the summary
    rather than silently widening the exemption list.
  </action>
  <verify>
    <automated>npx vitest run tests/learning-phase-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/learning-phase-invariants.test.ts` passes with exactly 8 `it` blocks, one per invariant.
    - Each failure message names the decision or rule it protects (D-number, DD-number, or the source file whose header states it).
    - `grep -c "runtimeImports\|createSourceFile" tests/learning-phase-invariants.test.ts` returns at least 1.
    - `grep -c "function resolveProjectImport" tests/learning-phase-invariants.test.ts` returns 0 — the traversal is imported, not re-implemented.
    - `npx vitest run tests/boundary.test.ts` still passes after any export changes made to it.
  </acceptance_criteria>
  <done>Eight architectural invariants of this phase are enforced by an executable test with explanatory failure messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real-Postgres learner journey integration test</name>
  <files>tests/learner-journey.integration.test.ts</files>
  <read_first>
    - tests/attendance-service.integration.test.ts (the real-Postgres harness: how the database is provisioned, how fixtures are seeded, how the suite behaves when Docker is unavailable)
    - tests/enrolment-service.integration.test.ts (the enrolment fixture construction — cohort, publication pin, ACTIVE enrolment)
    - src/server/services/completion-service.ts, lesson-progress-service.ts, learner-access.ts (the services under test)
    - .planning/STATE.md "Blockers/Concerns" (Phase 6's integration tests were never executed because Docker was unavailable in the sandbox — read this before assuming the suite will run)
  </read_first>
  <behavior>
    - Fixture: one published Course with two modules, four lessons (three required, one optional, one of them VIDEO), a pinned `CoursePublication`, a SELF_PACED cohort with `accessDurationDays = 30` and `attendanceThresholdPct = 75`, and one ACTIVE enrolment.
    - LRN-02: the second required lesson reports `locked: true` with the first's title, and unlocks after the first is completed.
    - LRN-04/LRN-05: marking a lesson twice yields one `LessonProgress` row; undoing removes it and re-locks the next.
    - LRN-04 (video): `recordWatchProgress` at 50% creates no `LessonProgress`; at 91% it creates one with `source: "AUTO_VIDEO"`.
    - LRN-07: completing all three required lessons with attendance below 75% leaves the verdict unsatisfied and writes no `CompletionRecord`; recording enough attendance then satisfies it and writes exactly one record with the pinned `ruleVersion` and per-item `evidence`.
    - LRN-07 / D-12: an attendance correction dropping the learner below 75% stamps `supersededAt` on that record without deleting it; a further correction back above threshold creates a second record while the first stays superseded.
    - DD-6: `Enrolment.status` is still `ACTIVE` after every step above.
    - LRN-03: `getDownloadableResourceForLearner` returns a resource for the enrolled learner and `null` for a second, non-enrolled user.
    - LRN-06: a session's `meetingUrl` is absent from `listOwnCohortSessions` before its window and present inside it.
    - LRN-01: `loadLearnerDashboard` for a second learner contains none of the first learner's enrolments.
  </behavior>
  <action>
    Create `tests/learner-journey.integration.test.ts` following the exact harness
    `tests/attendance-service.integration.test.ts` uses, covering every behavior bullet in order as
    one narrative journey plus the isolated authorization assertions.

    Run it. If Docker or the test database is unavailable in this environment, DO NOT mark the task
    complete on a skipped suite and DO NOT weaken the test to run without Postgres. Record it as
    BLOCKED in the summary with the exact error, matching how Phase 6's own summaries recorded the
    same gate — a fabricated pass here would hide the only real proof this phase has.

    Add an explicit assertion after the completion steps reading the `Enrolment.status` column
    directly and asserting `"ACTIVE"`, with a comment citing DD-6 — this is the one place the ruling
    is proven against a real row rather than a fake.
  </action>
  <verify>
    <automated>npx vitest run tests/learner-journey.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/learner-journey.integration.test.ts` passes with at least 10 assertions covering all seven LRN requirements — OR the task is reported BLOCKED with the verbatim database connection error and no test file weakening.
    - A test reads `Enrolment.status` from the database after completion and asserts it equals `"ACTIVE"`.
    - A test asserts exactly one `CompletionRecord` row exists after a double satisfaction, and exactly two rows (one superseded, one open) after a supersede-then-resatisfy cycle.
    - A test asserts `LessonProgress` row count is 1 after two `markLessonComplete` calls for the same lesson.
    - A test asserts a second user's `getDownloadableResourceForLearner` returns `null` for the same resource id the enrolled learner can fetch.
  </acceptance_criteria>
  <done>The whole learner journey is proven against a real Postgres, or the blocker is recorded honestly without weakening the test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human walkthrough of the learner surfaces and the UI-SPEC backstops</name>
  <files>(verification only — no source files modified; findings are recorded in 09-14-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/09-learning-delivery-progress-tracking/09-UI-SPEC.md section 8 (the three backstop rows and the one unresolved row verified here, plus the status vocabulary stating that a backstop with no explicit evidence escalates to human-needed and never passes silently)
    - .planning/phases/09-learning-delivery-progress-tracking/09-CONTEXT.md decisions D-06, D-08, D-13, D-15, D-16
  </read_first>
  <what-built>
    Four learner routes (`/dashboard`, `/learn/[enrolmentId]`, `/learn/[enrolmentId]/lessons/[lessonId]`,
    `/learn/[enrolmentId]/sessions`), one staff route
    (`/staff/cohorts/[id]/learners/[enrolmentId]`), the completion engine, the lesson-progress write
    paths, and the video watch tracker. Everything above has automated coverage; the three items below
    are the ones UI-SPEC section 8 marks as backstops, which by definition cannot pass on automated
    evidence alone, plus the visual/interaction pass no test can perform.
  </what-built>
  <how-to-verify>
    Start the app (`npm run dev`) and sign in as a learner holding an ACTIVE enrolment in a seeded
    cohort.

    1. `/dashboard` — confirm the "Next up" card shows exactly one action; the teal progress bar sits
       inside a visible bordered track with its percentage caption beside it (not floating); and the
       four named-gap cards read as unavailable-not-broken and are not clickable.
    2. **Backstop A (UI-SPEC 8, long-text):** open a lesson list containing a locked lesson whose
       blocking lesson has a long title. Confirm the "Complete '<title>' to unlock this" sentence
       wraps without breaking the row layout and the `Lock` icon stays top-aligned.
    3. **Backstop B (UI-SPEC 8, overflow):** view a course with the highest lesson count in the seed
       data and confirm the progress-bar caption ("14 of 14 required lessons complete") neither wraps
       awkwardly nor overflows its card.
    4. Open a VIDEO lesson. Confirm the native controls are the only progress UI — nothing is drawn
       over the player. Watch or scrub past 90%, then reload: the lesson should show "Completed" with
       the "Marked complete automatically" caption and an "Undo" link.
    5. **Backstop C (UI-SPEC 8, loading):** with the video playing, open devtools and set the network
       to offline for ~30 seconds, then back online. Confirm playback never stalls or errors, no false
       completion state appears, and the position is recorded once connectivity returns.
    6. Click "Undo" on a lesson that gates later ones. Confirm the inline disclosure states how many
       lessons will re-lock, that no modal appears, and that the later lessons actually show as locked
       afterwards.
    7. `/learn/[enrolmentId]/sessions` — for a session whose link window has not opened, view page
       source and confirm the meeting URL string appears nowhere. Confirm it appears once the window
       opens.
    8. Sign in as a second learner and navigate directly to the first learner's `/learn/{enrolmentId}`
       URL. Confirm a 404, not a permissions message.
    9. As staff, open `/staff/cohorts/{id}/learners/{enrolmentId}`, attempt an override with an empty
       reason (must be refused), then with a reason (must succeed and appear in audit history).
    10. **Unresolved item (UI-SPEC 8):** note whether real session location strings and cancellation
        reasons in the seed data wrap acceptably — this was recorded as a planner assumption pending
        real content, not a verified fit.
  </how-to-verify>
  <action>
    Pause and hand control to the developer. Present the ten numbered steps in `how-to-verify` exactly
    as written, wait for the resume signal, then record the outcome of each step in
    `09-14-SUMMARY.md` — including any step that could not be performed (for example, no browser
    available in the execution environment). An unperformed step is recorded as outstanding human
    verification, never as a pass.
  </action>
  <resume-signal>Type "approved" or describe what failed, naming the step number.</resume-signal>
  <verify>
    <human-check>Developer confirms each of the ten steps, or names the failing step number.</human-check>
  </verify>
  <acceptance_criteria>
    - The developer returned an explicit "approved" or a named failing step.
    - `09-14-SUMMARY.md` records a per-step outcome for all ten steps, with any unperformed step listed under outstanding human verification rather than omitted.
    - Backstops A, B and C each carry either a developer confirmation or an explicit human-needed escalation.
    - The UI-SPEC section 8 unresolved long-text row (step 10) is answered with an observation about real seed data, not left blank.
  </acceptance_criteria>
  <done>All ten walkthrough steps have a recorded outcome and the three UI-SPEC backstops are explicitly resolved or escalated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| convention → future contributor | An architectural rule that only exists in a doc comment can be removed without any signal |
| automated evidence → shipped behaviour | Green unit tests with mocked stores do not prove a real database transaction or a real browser interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-47 | Tampering | Erosion of pure-module discipline | mitigate | INV-1/INV-2 assert import-freedom and the absence of `withPermission` in ownership services, so a later data-access import fails CI rather than silently widening a worker closure |
| T-09-48 | Elevation of Privilege | Erosion of DD-6 | mitigate | INV-3 plus a real-database assertion that `Enrolment.status` is still `ACTIVE` after full completion |
| T-09-04 | Information Disclosure | `meetingUrl` leaking into a learner surface | mitigate | INV-4 greps the whole learner surface tree, and the human walkthrough inspects real page source |
| T-09-01 | Information Disclosure | Cross-learner access | mitigate | Integration assertions for the dashboard, the download predicate and the lesson list, plus walkthrough step 8 against a live second account |
| T-09-49 | Repudiation | Unverifiable backstops silently passing | mitigate | The three UI-SPEC backstops are routed to a blocking human checkpoint; per the UI-SPEC status vocabulary, absent explicit evidence they must escalate to human-needed rather than pass silently |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages across the whole phase (09-RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npm test` (the full `vitest run --no-file-parallelism` suite) green, or every failure traced to the pre-existing Docker gate recorded in STATE.md.
- `npx next build` completes; `npx tsc --noEmit` exits 0; `npx eslint .` exits 0.
- Human checkpoint approved, with any backstop failure recorded rather than waived.
</verification>

<success_criteria>
- Eight phase invariants enforced by a passing test.
- The learner journey proven against real Postgres, or the blocker recorded honestly.
- The three UI-SPEC backstops and the unresolved long-text item explicitly resolved by a human.
</success_criteria>

<output>
Create `.planning/phases/09-learning-delivery-progress-tracking/09-14-SUMMARY.md` when done.
</output>

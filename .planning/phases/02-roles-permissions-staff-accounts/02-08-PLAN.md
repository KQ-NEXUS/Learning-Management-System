---
phase: 02-roles-permissions-staff-accounts
plan: 08
type: execute
wave: 7
depends_on: ["02-03", "02-04", "02-07"]
files_modified:
  - src/app/staff/users/[id]/page.tsx
  - src/app/staff/users/AssignmentsPanel.tsx
  - src/app/staff/users/AssignmentDrawer.tsx
  - src/app/staff/users/actions.ts
autonomous: true
requirements: [RBAC-04, IAM-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A staff account's assignments render as a list of independently revocable rows on its detail page, reflecting RBAC-04's union-of-active-grants model directly rather than inventing a primary-role concept the schema does not have (D-19)."
    - "Revoking one assignment from that list affects no other assignment for that user (RBAC-04)."
    - "Revoking an assignment demands a reason of at least 10 characters through the existing ConfirmModal, and the copy states that granting it again creates a new assignment because the action cannot be undone (D-14, D-23)."
    - "Deactivating a staff account from its detail page demands a reason of at least 10 characters and states that the person will be signed out of every active session immediately and that their role assignments are kept and will apply again if reactivated (D-33, D-34, D-35, D-36)."
    - "Reactivation is a non-destructive confirmation with tone default and no reason, whose body states that access is restored immediately with no reassignment needed (D-36)."
    - "The target user for a new assignment is found by search-as-you-type over name and email, not a plain list picker (D-17)."
    - "The scope picker supports all four scope types, with Programme and Cohort showing an honest none-yet state until Phases 4 and 5 seed records (D-18)."
    - "One scope target per assignment action; granting the same role across several Courses means repeating the action (D-21)."
    - "AssignmentDrawer — Empty typeahead: the placeholder reads 'Start typing a name or email' and no results list appears until one or two characters are typed."
    - "AssignmentDrawer — Empty scope picker: Programme and Cohort show the locked none-yet copy naming the phase that will populate them."
    - "AssignmentDrawer — Loading: an inline spinner near the search input, with previous results left visible until new ones arrive so there is no flash to empty between keystrokes."
    - "AssignmentDrawer — Error: an inline 'Search failed — try again' beneath the input for search failures with the drawer staying open, and a ResourceForm-style error summary for save failures."
    - "AssignmentDrawer — Populated: the selected user is locked in with name and email shown and not re-editable without clearing, and Save enables only once user, role and scope are all set."
    - "AssignmentDrawer — Partial: while a user is picked but role or scope is not, Save simply stays disabled with no premature error messaging."
    - "AssignmentDrawer — Overflow: typeahead results cap at 10 and scroll internally if more exist."
    - "AssignmentDrawer — Zero-one-many: zero matches render 'No users match \"{query}\"'; one or many render the same selectable list with no special-casing."
    - "AssignmentDrawer — Long text: a long name or email in a dropdown row truncates with an ellipsis, unlike table cells which wrap, and the full value is visible again once selected."
    - "UserDetailPage — Empty: the Assignments tab can legitimately reach zero because revocation is permanent, and shows an empty-state panel reading 'No active role assignments' with body 'This account currently has no access. Assign a role to grant permissions.' and an assign CTA."
    - "UserDetailPage — Loading and Error: the inherited DetailLayout skeleton plus page-level and per-section error isolation, unchanged."
    - "UserDetailPage — Populated: an Overview tab of DetailFacts (name, email, status, created date) and an Assignments tab listing role, scope, dates and a per-row Revoke control."
    - "UserDetailPage — Partial: not applicable; the assignments list is not paginated this phase."
    - "UserDetailPage — Overflow: content scrolls vertically within the active tab panel; the page never scrolls sideways."
    - "UserDetailPage — Zero-one-many: a bare count badge, where zero is a real, valid, reachable value that pairs with the empty-state panel."
    - "UserDetailPage — Long text: long values wrap within the DetailFacts value cell with no truncation."
  prohibitions:
    - "MUST NOT offer a bulk-revoke, revoke-all, or select-multiple affordance on the assignments list — each row is revoked individually so the audit trail names exactly what was taken away."
    - "MUST NOT offer an edit-in-place control on an existing assignment; changing scope or end date is revoke-old plus create-new, and an edit affordance would contradict the revocation trail the schema records."
  artifacts:
    - src/app/staff/users/[id]/page.tsx
    - src/app/staff/users/AssignmentsPanel.tsx
    - src/app/staff/users/AssignmentDrawer.tsx
  key_links:
    - "The detail page fetches through staffAccountService.get and assignmentService.listForUser, and treats an AuthorizationError as not-found so a denial does not confirm existence."
    - "revokeAssignmentAction calls assignmentService.revoke, whose continuity guard fires at the D-24a trigger; the thrown message reaches the ConfirmModal's error prop unchanged."
    - "The drawer's user typeahead calls staffAccountService.search through a Server Action, inheriting its users.view gate and its ten-row cap."
    - "The drawer's scope-target lookup reuses searchScopeTargetsAction from plan 07, so both assignment entry points share one lookup."
---

<objective>
Complete the phase's user-facing surface: a staff-account detail page whose Assignments tab shows
every grant as an independently revocable row, an assignment drawer for granting a new role at any
of the four scopes, and the deactivate and reactivate controls.

Purpose: this is where RBAC-04's union-of-grants model becomes something a person can see and act
on. Every service guarantee built in plans 02 through 06 — sibling assignments untouched, reasons
demanded, the last-administrator block, sessions swept on deactivation — surfaces here or is
invisible.

Output: `/staff/users/[id]`, `AssignmentsPanel`, `AssignmentDrawer`, and the revoke, deactivate and
reactivate Server Actions.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-roles-permissions-staff-accounts/02-CONTEXT.md
@.planning/phases/02-roles-permissions-staff-accounts/02-PATTERNS.md
@.planning/phases/02-roles-permissions-staff-accounts/02-UI-SPEC.md
@.planning/phases/02-roles-permissions-staff-accounts/02-04-SUMMARY.md
@.planning/phases/02-roles-permissions-staff-accounts/02-06-SUMMARY.md
@.planning/phases/02-roles-permissions-staff-accounts/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Staff account detail page — Overview, Assignments rows, deactivate and reactivate</name>
  <read_first>
    - src/app/staff/courses/[id]/page.tsx — the detail-page pattern: await params, fetch by id, treat a denial as not-found, render DetailLayout
    - src/app/staff/roles/[id]/page.tsx — the sibling detail page plan 03 built, for section and badge consistency
    - src/app/staff/roles/RoleDetailPanels.tsx — the ConfirmModal wiring idiom plan 03 established, including how a server rejection reaches the modal's error prop
    - src/components/primitives/DetailLayout.tsx — `DetailLayoutProps`, `DetailSection` with its `badge` and per-section `error`, and `DetailFacts`
    - src/components/primitives/ConfirmModal.tsx — the full prop list including `tone`, `minReasonLength`, `pending`, `error`
    - src/server/services/staff-account-service.ts — `get`, `deactivate`, `reactivate`
    - src/server/services/assignment-service.ts — `listForUser`, `revoke`, `AssignmentReasonRequiredError`
    - src/server/services/role-service.ts — `MIN_REASON_LENGTH`
    - src/app/staff/users/actions.ts — the action file plan 07 created, to extend
    - .planning/phases/02-roles-permissions-staff-accounts/02-UI-SPEC.md — the UserDetailPage block of "## UI Considerations", the Copywriting Contract rows for revoking an assignment, deactivating a staff account and reactivation, and the Color section
    - .planning/phases/02-roles-permissions-staff-accounts/02-CONTEXT.md — D-12, D-13, D-14, D-19, D-23, D-33, D-34, D-35, D-36
  </read_first>
  <files>src/app/staff/users/[id]/page.tsx, src/app/staff/users/AssignmentsPanel.tsx, src/app/staff/users/actions.ts</files>
  <action>
`src/app/staff/users/actions.ts` (modify). Add `"use server"` actions `revokeAssignmentAction`,
`deactivateStaffAccountAction`, and `reactivateStaffAccountAction`. Each reads its ids and reason
from FormData, calls the matching service method once, and returns a state object carrying either an
error message or a success flag. Map `AssignmentReasonRequiredError` and
`StaffAccountReasonRequiredError` to a reason error, `ContinuityError` to a form-level error
carrying the thrown message verbatim — that message is D-26's locked wording and must not be
rewritten or supplemented with a count — and `AuthorizationError` to a form-level error. Each calls
`revalidatePath` for the user's detail path and, for the two status actions, for `/staff/users` too.

`src/app/staff/users/[id]/page.tsx` (new). Copy `src/app/staff/courses/[id]/page.tsx`'s structure:
await the params promise, fetch through `staffAccountService.get`, and call `notFound` in the
`AuthorizationError` branch so a denial is indistinguishable from a missing record. Also fetch
`assignmentService.listForUser`. Render `DetailLayout` with breadcrumbs Workspace, Staff accounts,
and the person's name; `title` the name; `identifier` the email in mono; `badges` a `StatusPill` for
the account status using the same tone map `UsersTable` defines (ACTIVE success, DEACTIVATED
warning, PENDING_VERIFICATION neutral); and `actions` holding the deactivate or reactivate control.
Sections: Overview rendering `DetailFacts` with name, email, status, created date and, when set,
deactivated date; and Assignments carrying a `badge` of the bare active-assignment count — zero is a
real, valid, reachable value here, unlike the role page's version badge — whose content is the
client component below. Long values wrap in the `DetailFacts` value cell with no truncation.

`src/app/staff/users/AssignmentsPanel.tsx` (new, `"use client"`). Export `AssignmentsPanel`,
rendering each assignment as an independent row (D-19) — this is RBAC-04's union of grants shown
directly, with no primary-role concept invented on top. Each row shows the role name, the scope
rendered as the scope type plus the target's label when it is not global, the start date, the end
date or an em dash, and a per-row Revoke button. Revoked assignments render beneath the active ones
in a muted style with their revocation date and reason, so the trail is visible without cluttering
the active list. Offer no bulk-select, no revoke-all, and no edit-in-place control: assignments are
immutable and each revocation is individual (D-21, D-23).

Revoke opens a `ConfirmModal` with `tone="danger"`, `minReasonLength` bound to `MIN_REASON_LENGTH`,
title "Revoke this assignment?", confirm label "Revoke assignment", and the body locked in the
Copywriting Contract naming the person, the role and the scope and stating that this cannot be
undone because granting it again creates a new assignment. On confirm it submits
`revokeAssignmentAction`; a server rejection feeds back into that same modal's `error` prop rather
than a new banner, which is how the RBAC-07 block surfaces when the revoked assignment is the last
global role-management grant.

When the list has no active assignments, render an empty-state panel mirroring `ResourceTable`'s
visual language even though this is not a `ResourceTable`: heading "No active role assignments",
body "This account currently has no access. Assign a role to grant permissions.", and a CTA that
opens the assignment drawer.

Export `AccountStatusControl` for the `DetailLayout` `actions` slot. For an ACTIVE account it offers
Deactivate through a `ConfirmModal` with `tone="danger"`, `minReasonLength` bound to
`MIN_REASON_LENGTH`, title "Deactivate {user name}'s account?", confirm label "Deactivate account",
and the body locked in the Copywriting Contract stating that they will be signed out of every active
session immediately and that their role assignments are kept and will apply again if reactivated
(D-34, D-35, D-36). For a DEACTIVATED account it offers Reactivate with `tone="default"`, no
`minReasonLength`, title "Reactivate {name}?", body "Access is restored immediately — no
reassignment is needed.", and confirm label "Reactivate". Both surface a server rejection through
the modal's `error` prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src tests prisma && npm test</automated>
    <fails_when>non-zero exit; or any line matching "error TS"; or any eslint line containing "no-restricted-imports"; or any "failed" count in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/app/staff/users/[id]/page.tsx` calls `notFound` in its `AuthorizationError` branch.
    - The `DetailLayout` sections array contains exactly the ids for Overview and Assignments, and the Assignments section sets `badge` to a numeric active-assignment count that can legitimately be 0.
    - `src/app/staff/users/AssignmentsPanel.tsx` exports `AssignmentsPanel` and `AccountStatusControl`, and both render `ConfirmModal` from `@/components/primitives` rather than a locally defined dialog.
    - `AssignmentsPanel` contains no checkbox, no select-all control, and no button whose handler revokes more than one assignment id.
    - `AssignmentsPanel` contains no control that submits an update to an existing assignment.
    - The reactivate modal passes `tone="default"` and omits `minReasonLength`; the revoke and deactivate modals both pass `minReasonLength` sourced from `MIN_REASON_LENGTH`.
    - `revokeAssignmentAction`, `deactivateStaffAccountAction` and `reactivateStaffAccountAction` each pass a thrown continuity message through unchanged.
    - `npx tsc --noEmit`, `npx eslint src tests prisma`, and `npm test` all exit 0.
  </acceptance_criteria>
  <done>Opening a staff account shows its facts and every role grant as its own revocable row, revoking one demands a reason and takes away only that one, and the account can be deactivated with a reason (signing out every session and warning that assignments are kept) or reactivated with a single non-destructive confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Assignment drawer — user typeahead and four-scope target picker</name>
  <reversibility rating="reversible">D-18 builds all four scope pickers now against live tables, so no rework is needed when Phases 4 and 5 seed Programme and Cohort records.</reversibility>
  <read_first>
    - src/components/primitives/ResourceForm.tsx — the `FormField`, `TextInput` and `aria-invalid` conventions this drawer composes from; note it is not itself used as the container
    - src/components/primitives/ConfirmModal.tsx — the focus-trap and ESC-suppressed-while-pending behaviour to mirror in the panel container
    - src/app/staff/users/StaffAccountForm.tsx — the scope-type and scope-target controls plan 07 built, whose behaviour this drawer repeats for an existing user
    - src/app/staff/users/actions.ts — `searchScopeTargetsAction` from plan 07, to reuse rather than duplicate
    - src/server/services/staff-account-service.ts — `staffAccountService.search` and its ten-row cap and four-field projection
    - src/server/services/assignment-service.ts — `assignmentService.create` and `assignmentTargetScope`
    - src/server/services/role-service.ts — `roleService.list` for the role select
    - .planning/phases/02-roles-permissions-staff-accounts/02-UI-SPEC.md — the AssignmentDrawer block of "## UI Considerations" in full, the Copywriting Contract's assign CTA and the two scope empty states, and the Spacing, Typography and Color sections
    - .planning/phases/02-roles-permissions-staff-accounts/02-CONTEXT.md — D-17, D-18, D-20, D-21, D-41
  </read_first>
  <files>src/app/staff/users/AssignmentDrawer.tsx, src/app/staff/users/actions.ts</files>
  <action>
`src/app/staff/users/actions.ts` (modify). Add `searchStaffUsersAction(query)`, which calls
`staffAccountService.search` and returns its four-field rows — inheriting the `users.view` gate and
the ten-row cap rather than adding a second query path. Add `createAssignmentAction(prev, formData)`,
which reads userId, roleId, scopeType, an optional scopeId and an optional endsAt and calls
`assignmentService.create` once, mapping `ScopeError` to a scope field error naming the global-only
conflict, `AuthorizationError` to a form-level error, and a missing role or scope target to the
corresponding field errors. Call `revalidatePath` for the user's detail path on success.

`src/app/staff/users/AssignmentDrawer.tsx` (new, `"use client"`). There is no existing drawer or
typeahead in this codebase, so build the container fresh while composing the established form
control conventions inside it: a slide-over panel with `role="dialog"` and `aria-modal`, focus moved
in on open and returned to the trigger on close, ESC closing it except while a save is in flight —
the same rules `ConfirmModal` already implements, mirrored rather than imported, since this is a
panel and not a confirmation.

The typeahead is the primary focal point on open. Its placeholder reads "Start typing a name or
email" and no results list renders until at least one character is typed (D-17). Debounce the query,
call `searchStaffUsersAction`, and while a request is in flight show an inline spinner near the
input while leaving the previous results rendered, so there is no flash to empty between keystrokes.
A failed search renders "Search failed — try again" inline beneath the input and leaves the drawer
open. Zero matches render the exact string built from the query in the form 'No users match
"{query}"'; one or many matches render the same selectable list with no special-casing. Results cap
at 10 — the service already enforces the same cap — and the list scrolls internally. A long name or
email in a result row truncates with an ellipsis, unlike the table cells elsewhere in this phase
which wrap; the full value is visible again once the row is selected. Selecting a user locks it in,
showing the name and email as fixed text with a Clear control; it is not re-editable in place.
When the drawer is opened from a specific account's detail page, pre-select that user and skip
straight to the role field.

Below the user: a role select fed from `roleService.list`, a scope-type select over the four
`ScopeType` values, and a scope-target control that appears only for a non-global scope type and
queries `searchScopeTargetsAction`. Render the locked none-yet copy when a lookup returns nothing —
"No Programmes exist yet — check back once catalogue authoring (Phase 4) lands." for Programme and
"No Cohorts exist yet — check back once scheduling (Phase 5) lands." for Cohort (D-18). Exactly one
scope target per save (D-21). Offer an optional end-date field and no start-date field, since
assignments start immediately (D-20).

Save is labelled "Assign role" and stays disabled until a user, a role and — for a non-global scope
— a scope target are all set. While a required piece is missing the button simply stays disabled
with no premature error messaging, matching the primitive's validate-on-blur-and-submit philosophy.
A save failure renders a `ResourceForm`-style error summary at the top of the panel, distinct from
the inline search error, and leaves every entered value in place.

Wire the drawer's trigger into `AssignmentsPanel`'s header and its empty-state CTA. Draw spacing
only from 02-UI-SPEC.md's seven-value scale, use the four-size type scale, and reserve accent colour
for the Save button.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src tests prisma && npm test</automated>
    <fails_when>non-zero exit; or any line matching "error TS"; or any eslint line containing "no-restricted-imports"; or any "failed" count in the vitest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/app/staff/users/AssignmentDrawer.tsx` sets `role="dialog"` and `aria-modal` on its panel container and moves focus into the panel on open.
    - The typeahead renders no results list while the query string is empty.
    - The search-in-flight branch keeps the previous results mounted rather than replacing them with an empty or loading state.
    - The results list applies a truncation utility to the name and email in each row, and the results container caps its height with an internal scroll.
    - The scope-target control renders the exact locked empty-state string for Programme scope and the exact locked string for Cohort scope.
    - The Save button's label string is exactly "Assign role" and its disabled condition covers a missing user, a missing role, and a missing non-global scope target.
    - The drawer exposes no multi-select for scope targets.
    - `npx tsc --noEmit`, `npx eslint src tests prisma`, and `npm test` all exit 0.
  </acceptance_criteria>
  <done>An administrator can open the assignment drawer from a staff account, find any user by typing part of a name or email, pick a role and one target at any of the four scopes, and save a new immediate-start assignment — with honest empty states for the two scope types Track B has yet to populate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → revoke / status Server Actions | Assignment id, user id and reason cross here untrusted; the modal's own reason-length rule has no authority. |
| staff browser → typeahead Server Action | A free-text query crosses here and returns account identities. |
| staff browser → createAssignmentAction | Role, scope type and scope target cross here; the drawer's disabled-until-valid rule has no authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | user typeahead in the drawer | medium | mitigate | `searchStaffUsersAction` delegates to `staffAccountService.search`, which is wrapped by `withPermission("users.view", () => ({}))`, capped at ten rows and projected to four identity fields; no second query path is added. |
| T-02-17 | Tampering | assignment revocation blast radius | high | mitigate | Each Revoke control submits exactly one assignment id; the panel exposes no bulk-select or revoke-all affordance, and the service updates one row by primary key. |
| T-02-13 | Tampering | client-side reason gate | high | mitigate | The 10-character reason is enforced in the service; a request bypassing the modal still throws `AssignmentReasonRequiredError` or `StaffAccountReasonRequiredError`. |
| T-02-06 | Information Disclosure | continuity block on revoke and deactivate | medium | mitigate | All three actions pass the thrown message through verbatim into the `ConfirmModal` error prop; no count or identity is interpolated client-side (D-26). |
| T-02-02 | Elevation of Privilege | assignment created from the drawer | high | mitigate | `assertScopeAllowed` runs server-side for every permission the chosen role carries; `ScopeError` surfaces as a scope field error rather than being swallowed. |
| T-02-25 | Information Disclosure | `/staff/users/[id]` denial path | medium | mitigate | An `AuthorizationError` on the detail fetch calls `notFound`, so a denial is indistinguishable from a missing record and cannot confirm that an account exists. |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; 02-RESEARCH.md's "Package Legitimacy Audit" records "Not applicable this phase — no new external packages are introduced." |
</threat_model>

## Artifacts this phase produces (this plan)

| Symbol | File | Kind |
|--------|------|------|
| `StaffUserDetailPage` (default) | `src/app/staff/users/[id]/page.tsx` | route |
| `AssignmentsPanel` | `src/app/staff/users/AssignmentsPanel.tsx` | component |
| `AccountStatusControl` | `src/app/staff/users/AssignmentsPanel.tsx` | component |
| `AssignmentDrawer` | `src/app/staff/users/AssignmentDrawer.tsx` | component |
| `revokeAssignmentAction` | `src/app/staff/users/actions.ts` | Server Action |
| `deactivateStaffAccountAction` | `src/app/staff/users/actions.ts` | Server Action |
| `reactivateStaffAccountAction` | `src/app/staff/users/actions.ts` | Server Action |
| `searchStaffUsersAction` | `src/app/staff/users/actions.ts` | Server Action |
| `createAssignmentAction` | `src/app/staff/users/actions.ts` | Server Action |

<verification>
- `npx tsc --noEmit` exits 0.
- `npx eslint src tests prisma` exits 0.
- `npm test` (full suite) is green — every service test from plans 01 through 06 still passes with the full UI in place.
</verification>

<success_criteria>
- A staff account's grants are visible as independent rows, each revocable on its own with a reason.
- Revoking the last global role-management grant is blocked, with D-26's exact wording rendered inside the confirmation dialog.
- Deactivation and reactivation are reachable from the detail page with the locked copy and the correct tones.
- A new assignment can be created for any user at any of the four scopes through the drawer, one target at a time, with honest empty states for Programme and Cohort.
</success_criteria>

<output>
Create `.planning/phases/02-roles-permissions-staff-accounts/02-08-SUMMARY.md` when done.
</output>

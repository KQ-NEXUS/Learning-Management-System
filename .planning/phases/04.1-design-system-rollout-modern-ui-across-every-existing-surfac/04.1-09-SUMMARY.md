---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 09
subsystem: ui
tags: [design-system, staff, audit, permissions, accessibility, tailwind]

requires:
  - phase: 04.1-01
    provides: "Semantic colour, radius, shadow, and typography tokens"
  - phase: 04.1-03
    provides: "Restyled ResourceTable constants and staff-list panel treatment"
  - phase: 04.1-04
    provides: "Restyled ResourceForm and ConfirmModal control/overlay treatments"
  - phase: 04.1-05
    provides: "Staff shell stacking context for overlay validation"
provides:
  - "AuditTable parity with ResourceTable tokens while retaining expand-in-place behavior"
  - "Assignment drawer card overlay above the staff shell with accessible control edges"
  - "Semantic-token treatment for permission, access, assignment, staff-account, and user-list widgets"
affects: [staff-audit, staff-users, staff-roles, design-system, NFR-09]

actuals:
  tokens: 14038
  tasks: 3
  commits: 0

tech-stack:
  added: []
  patterns:
    - "Hand-styled staff surfaces use the same semantic role tokens as their primitive siblings"
    - "Interactive controls use border-input-border while decorative containers use border-border"

key-files:
  created: []
  modified:
    - src/app/staff/audit/AuditTable.tsx
    - src/app/staff/users/AssignmentDrawer.tsx
    - src/app/staff/roles/PermissionPicker.tsx
    - src/app/staff/roles/EffectiveAccessPreview.tsx
    - src/app/staff/users/AssignmentsPanel.tsx
    - src/app/staff/users/StaffAccountForm.tsx
    - src/app/staff/users/UsersTable.tsx

key-decisions:
  - "Kept AssignmentDrawer as a documented non-primitive and mirrored ConfirmModal's z-50 tokenised overlay treatment."
  - "Retained native checkbox rendering and accent-accent while adding the required 16px, 1.5px border-input-border treatment, so checked state remains visible without adding custom glyph markup."

patterns-established:
  - "Copied primitive markup must copy the primitive's semantic class constants exactly when visual parity is part of its contract."
  - "Styling-only security-sensitive changes leave displayed audit values, denied copy, form names, action bindings, and ARIA wiring untouched."

requirements-completed: [NFR-09]

coverage:
  - id: D1
    description: "AuditTable now matches ResourceTable's semantic constants, card container, filters, row dividers, and attached expanded-detail treatment without changing recorded values or denied copy."
    requirement: NFR-09
    verification:
      - kind: unit
        ref: "tests/audit-read-service.test.ts (targeted run, 16 tests within 29-test combined run)"
        status: pass
      - kind: other
        ref: "npm run build (Next.js 16.3.4 production build, exit 0)"
        status: pass
    human_judgment: true
    rationale: "Rendered parity with the courses list and whether the expanded panel reads as visually attached require browser inspection."
  - id: D2
    description: "AssignmentDrawer uses the approved tokenised backdrop and shadow-card overlay above the staff shell, with 38px controls and preserved focus, scope, and action behavior."
    requirement: NFR-09
    verification:
      - kind: unit
        ref: "tests/assignment-service.test.ts (targeted run, 13 tests within 29-test combined run)"
        status: pass
      - kind: other
        ref: "npx eslint src (exit 0)"
        status: pass
    human_judgment: true
    rationale: "Overlay stacking above the navy shell and visible control-edge contrast require rendered browser confirmation."
  - id: D3
    description: "PermissionPicker, EffectiveAccessPreview, AssignmentsPanel, StaffAccountForm, and UsersTable contain no legacy neutral utilities, raw hex colours, or font-medium utilities and preserve their bindings."
    requirement: NFR-09
    verification:
      - kind: other
        ref: "target-file legacy-token sweep (0 matches)"
        status: pass
      - kind: unit
        ref: "npm test (60 files, 780 tests)"
        status: pass
      - kind: other
        ref: "npx eslint src (exit 0)"
        status: pass
    human_judgment: true
    rationale: "Keyboard reachability and the visual clarity of checkbox and input edges need live UI inspection."

duration: 34min
completed: 2026-09-05
status: complete
---

# Phase 04.1 Plan 09: Hand-Styled Staff Surface Token Migration Summary

**Seven audit, role, and user-management surfaces now share the primitive design language while preserving audit fidelity, permission bindings, drawer behavior, and denied-state privacy.**

## Performance

- **Duration:** 34 min
- **Started:** 2026-09-05T16:07:00Z
- **Completed:** 2026-09-05T16:41:00Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- Brought `AuditTable`'s `HEAD`, `CELL`, `BTN`, and local `Panel` into exact parity with `ResourceTable`, added the approved dense-list card treatment, and visually attached expanded rows without changing any audit value or copy.
- Reworked `AssignmentDrawer` onto the `ConfirmModal`-derived tokenised backdrop and `shadow-card` surface at `z-50`, above the staff shell's `z-40` sidebar, while preserving focus restoration, Escape handling, searches, scope branching, and action binding.
- Migrated all remaining role/user widgets to semantic surfaces, decorative borders, interactive control borders, approved radii, and the two-weight typography system.

## Task Commits

No commits were made. Project policy forbids worker agents from committing; all verified Plan 09 changes remain in the shared working tree for the owning developer to commit.

Suggested commit messages:

1. `style(04.1-09): align audit table with list primitive`
2. `style(04.1-09): restyle assignment drawer overlay`
3. `style(04.1-09): migrate remaining staff widgets to design tokens`

## Files Created/Modified

- `src/app/staff/audit/AuditTable.tsx` — primitive-parity constants, tokenised filter controls, list card, dividers, states, and attached expanded row.
- `src/app/staff/users/AssignmentDrawer.tsx` — tokenised overlay, card surface, labels, controls, results, errors, and actions.
- `src/app/staff/roles/PermissionPicker.tsx` — card groups, semantic neutrals, and visible 16px checkbox edges.
- `src/app/staff/roles/EffectiveAccessPreview.tsx` — read-only semantic panel and typography sweep.
- `src/app/staff/users/AssignmentsPanel.tsx` — semantic assignment cards, empty/revoked states, and actions.
- `src/app/staff/users/StaffAccountForm.tsx` — semantic success panel and accessible 38px select/button edges.
- `src/app/staff/users/UsersTable.tsx` — primary action brought onto the approved radius and typography treatment.

## Decisions Made

- Kept `AssignmentDrawer` as a documented exception instead of extracting a fifth primitive, as required by the phase context.
- Preserved native checkbox visuals and added the specified edge/radius classes rather than suppressing browser rendering and introducing new checked-state markup.

## Deviations from Plan

None — plan execution changed presentation classes only and stayed within the seven declared source files.

## Issues Encountered

- The first full `npm test` run reported one timeout in `tests/components/rich-text-editor.test.tsx`, outside Plan 09's ownership. That test passed immediately in isolation (5/5), and a clean full-suite rerun passed all 780 tests across 60 files. No source change was needed.

## Known Stubs

None introduced. Existing empty-string form defaults and placeholder attributes are real controlled-form state and input guidance, not unwired UI stubs.

## Threat Model Confirmation

- Denied-state copy was unchanged and no record-existence cue was added.
- Audit columns, identifiers, values, and formatting logic were unchanged; no delete affordance was introduced.
- Assignment action/search imports, `useActionState`, `useId`, `useRef`, form names, and scope branching were unchanged.
- Permission label/control association, keyboard order, and checkbox change binding were unchanged.

## Verification

- `npx vitest run tests/audit-read-service.test.ts tests/assignment-service.test.ts` — 2 files, 29 tests passed.
- `npm test` — 60 files, 780 tests passed.
- `npx vitest run tests/components/rich-text-editor.test.tsx` — 1 file, 5 tests passed after the transient full-suite timeout.
- `npx eslint src` — passed with no output.
- `npm run build` — Next.js 16.3.4 compiled, type-checked, and generated all routes successfully.
- Target-file sweep for `zinc-`, `font-medium`, and raw six-digit hex values — 0 matches.
- `git diff --check` across all seven source files — passed.

## User Setup Required

None — no external service configuration is required.

## Next Phase Readiness

- Plan 10 can continue its catalogue-component reconciliation on top of the completed semantic token foundation.
- Human UAT remains intentionally deferred until all Phase 04.1 plans finish, per the user's direction.

## Self-Check: PASSED

- All seven declared source files exist and contain the planned semantic treatments.
- `04.1-09-SUMMARY.md` exists and records all verification evidence.
- Commit checks are not applicable because worker commits are prohibited by project policy.

---
*Phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac*
*Completed: 2026-09-05*

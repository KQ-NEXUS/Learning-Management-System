# Phase 04.1 — Combined Gap Verification Evidence (Plan 04.1-29, Task 1)

Status: **evidence only — NOT a phase or NFR-09 completion claim.**
Mode: gap_closure capstone. Historical plans 01–14 and every prior SUMMARY / audit
verdict are unchanged. This document records fresh, explicit gate results for the
combined wave-1..3 tree and names every item that is still unresolved.

Generated: 2026-09-07 (UTC).
Tree under test: the merged wave-1..3 result at commit `f361a04`
(`chore: merge executor worktree (04.1-31 catalogue reading reconciliation)`) —
this is exactly the base of plan 29's own worktree. Plan 29 makes **zero** source
changes, so the combined tree that ships is the tree measured here.

## How the gates were run

The plan-29 worktree is a Claude Code `isolation="worktree"` checkout and git
worktrees do not copy the gitignored `node_modules`. A native symlink/junction
into the main checkout's `node_modules` could not be created (no symlink
privilege; `cmd`/`powershell` blocked by worktree isolation) and a full recursive
copy of `node_modules` was infeasible in the time budget. The combined-tree gates
were therefore executed from the **main checkout** (`C:/Users/disuk/Learning-Management-System`),
which is checked out at the identical commit `f361a04` that is this worktree's
base. Because plan 29 changes no source, main-checkout results and worktree
results are equivalent. Every command below was run explicitly for this plan;
nothing is inherited from a prior SUMMARY.

## Combined automated gate results (fresh, explicit)

| # | Gate | Command | Result | Timestamp (UTC) |
|---|------|---------|--------|-----------------|
| 1 | Full test suite (node + components projects, Testcontainers Postgres) | `npm test` (`vitest run --no-file-parallelism`) | **PASS** — 103 test files, **1331 passed / 1 skipped** (1332), exit 0, 719.0s | started 2026-09-07T02:34:41Z, finished 2026-09-07T02:46:44Z |
| 2 | Design-contract + contrast source gates, **no `-t` group filter** | `npx vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts` | **PASS** — 2 files, **71 passed**, exit 0 (all 6 manifest groups + non-vacuous fixtures + contrast ratios) | 2026-09-07T02:47:06Z |
| 3 | Lint | `npx eslint` | **PASS** — exit 0, clean | 2026-09-07T02:32:51Z |
| 4 | Types | `npx tsc --noEmit` | **PASS** — exit 0, clean (the previously-deferred `src/app/layout.tsx` `TS2304 LayoutProps` from `deferred-items.md` no longer reproduces in a tree with generated route types) | 2026-09-07T02:31:29Z |
| 5 | Production build | `npm run build` (`next build`) | **PASS** — exit 0, "✓ Compiled successfully in 8.5s", lint + type validation + page-data collection + 24 static pages generated, full route table emitted | 2026-09-07T02:51:22Z–2026-09-07T02:51:58Z (re-run for a clean exit code; a first run at 02:49 also passed) |

Only build warning: `Next.js ignored package-lock.json in C:\Users\disuk because it
is outside the current Git repository` — pre-existing environment noise, unrelated
to this phase.

## Focused regression tests from the gap plans (plans 15–28, 30, 31)

Run explicitly against the combined tree. All introduced/modified focused specs
from the wave-1..3 plans are also covered inside gate #1; they were additionally
run as standalone batches for an explicit per-file record.

| Batch | Command | Result | Timestamp (UTC) |
|-------|---------|--------|-----------------|
| Component regressions (23 files): courses-table, resource-table, staff-shell, brand-mark, assignment-drawer, staff-account-form, publish-dialog, course-detail-actions, programme-detail-client, resource-form, rich-text-editor, lesson-form-fields, audit-table, programme-form, module-composer, role-detail-panels, assignments-panel, upload-panel, lesson-content, arrange-board, readiness-panel, confirm-modal, detail-layout | `npx vitest run --project components <files>` | **PASS** — 23 files, **181 passed / 1 skipped**, exit 0 | 2026-09-07T02:47:23Z |
| Node route/service regressions (10 files): lesson-preview-route, programme-service, module-lesson-service, lesson-resource-service, lesson-resource-routes, landing, public-catalogue-service, verification-service, registration-service, password-reset-service | `npx vitest run <files>` | **PASS** — 10 files, **131 passed**, exit 0 | 2026-09-07T02:48:40Z |
| Design-contract per-group (all 6): primitives-table, primitives-forms, catalogue-controls-entry, catalogue-controls-expansion, catalogue-reading-entry, catalogue-reading-expansion | covered by gate #2 (full-file run, no filter) | **PASS** — no group is vacuous when the whole file runs; every manifest file graded | 2026-09-07T02:47:06Z |

### Code-review gap → focused-test mapping (04.1-REVIEW.md CR-01..12 / WR-01..06)

Each blocker/warning from `04.1-REVIEW.md` has a named passing regression in the
combined tree (per `04.1-GAP-COVERAGE.md` dispositions):

| Review finding | Owning plan(s) | Combined-tree evidence |
|---|---|---|
| CR-01 four no-op course controls | 15 | `courses-table.test.tsx` "disables all four unfinished affordances…" — PASS |
| CR-02 unnamed filters | 15 | `resource-table.test.tsx` filter accessible-name assertions — PASS |
| CR-03 mobile bulk selection lost | 15 | `resource-table.test.tsx` mobile-card selection — PASS |
| CR-04 closed mobile nav focusable | 16 | `staff-shell.test.tsx` inert/focus lifecycle — PASS |
| CR-05 assignment form/auth errors discarded | 17 | `assignment-drawer.test.tsx` `role="alert"` form-error — PASS |
| CR-06 AssignmentDrawer focus trap | 17 | `assignment-drawer.test.tsx` Tab-wrap / restore — PASS |
| CR-07 PublishDialog focus lifecycle | 18 | `publish-dialog.test.tsx` — PASS (10) |
| CR-08 upload poll stops after transient failure | 23 | `upload-panel.test.tsx` transient-rejection-then-CLEAN recovery — PASS |
| CR-09 programme `outcomes` omitted from form | 21 | `programme-form.test.tsx` outcomes round-trip — PASS (7) |
| CR-10 / T-04.1-26 cross-course lesson preview | 23 | `lesson-preview-route.test.ts` cross-course + withdrawn — PASS (5) |
| CR-11 source-less captions track | 23 | `lesson-content.test.tsx` "no `<track>` without a real source" — PASS — **but see Unresolved #3: partial remediation, not CR-11 closure** |
| CR-12 audit table clips at 360px | 20 | `audit-table.test.tsx` narrow-width card representation — PASS (11) |
| WR-01 publish controls mutable in-flight | 18 | `publish-dialog.test.tsx` pending-disables-all — PASS |
| WR-02 error-summary links to nonexistent fields | 19 | `resource-form.test.tsx` form-level alert vs field anchor — PASS |
| WR-03 rich body error not associated | 19 | `rich-text-editor.test.tsx` / `lesson-form-fields.test.tsx` aria wiring — PASS |
| WR-04 module failure discards edits | 22 | `module-composer.test.tsx` — PASS (5) |
| WR-05 unhandled scope-search rejections | 17 | `staff-account-form.test.tsx` / `assignment-drawer.test.tsx` catch/finally — PASS |
| WR-06 imperative handlers strand busy state | 22 | `course-detail-actions.test.tsx`, `programme-detail-client.test.tsx`, `role-detail-panels.test.tsx`, `assignments-panel.test.tsx` — PASS |

No existing user capability, route, server guard, permission check or action
contract was removed — gate #1 includes the prior-phase regression files
(`boundary.test.ts`, `structure.test.ts`, `with-permission.test.ts`, `scope.test.ts`,
the identity/cohort integration suites) and all pass.

## Unresolved / blocked — explicitly named, NOT cleared by this plan

### 1. `tests/unit/design-contract.test.ts` manifest covers only 14 of ~50 restyled files

The `GROUPS` manifest in the committed source-contract gate contains **14 files**:
- `primitives-table` (1): `ResourceTable.tsx`
- `primitives-forms` (3): `ResourceForm.tsx`, `DetailLayout.tsx`, `ConfirmModal.tsx`
- `catalogue-controls-entry` (3): `LessonFormFields.tsx`, `PublishDialog.tsx`, `CourseDetailActions.tsx`
- `catalogue-controls-expansion` (2): `RichTextEditor.tsx`, `UploadPanel.tsx`
- `catalogue-reading-entry` (3): `ArrangeBoard.tsx`, `ReadinessPanel.tsx`, `UnsavedOrderGuard.tsx`
- `catalogue-reading-expansion` (2): `LessonContent.tsx`, `LessonMediaPlayer.tsx`

It has **no group and no file manifest** for:
- the staff shell (`src/app/staff/StaffShell.tsx`, `src/app/staff/layout.tsx`) — plan 16
- the 13 `(auth)` files (layout + 6 pages + 6 forms + `AuthPanel.tsx`) — plan 25
- the ~8 public / account / shell files (`(public)/layout.tsx`, `(public)/**`, `account/**`, both `not-found.tsx`, `src/components/shell/LearnerShell.tsx`, `src/components/shell/BrandMark.tsx`) — plan 26 / 16
- the ~12 staff courses/programmes files (`CoursesTable`, `ProgrammesTable`, `ProgrammeForm`, `ProgrammeDetailClient`, `ModuleComposer`, `ArrangeClient`, `LessonEditorClient`, staff course/programme `page.tsx`, `preview/**`) — plan 27
- the 12 staff access/audit files (`AuditTable`, `PermissionPicker`, `RoleForm`, `RolesTable`, `RoleDetailPanels`, `EffectiveAccessPreview`, `UsersTable`, `AssignmentDrawer`, `AssignmentsPanel`, `StaffAccountForm`, roles `[id]/page.tsx`) — plan 28

Plans 25, 26, 27 and 28 each ran their planned `npx vitest … -t <staff-*|auth-*|public-catalogue|…>`
verify commands **vacuously** (the group does not exist → exit 0, zero assertions).
Each of those plans re-implemented the checker's grading logic in a throwaway
local script (not committed — `.planning/` is gitignored and the scripts lived
outside the repo) and reported **0 violations** across their files. That is real
work but it is **not committed CI coverage**: nothing in the merged tree
regression-guards the type/spacing roles of ~38 restyled auth/staff/public/account/
shell files. A future edit to any of them is not caught by `design-contract.test.ts`.

**Disposition: UNRESOLVED.** Recommend a small dedicated follow-up plan that adds
the missing `describe` groups + file manifests (and their per-file locked
exceptions) to `tests/unit/design-contract.test.ts`. This is out of plan 29's
`files_modified` (`GAP-VALIDATION.md`, `UAT.md` only) and its tracer/UAT task
actions, and `design-contract.test.ts` is owned by plan 04.1-24; it was not
modified here.

### 2. `LOCKED_EXCEPTIONS` references a stale path

`tests/unit/design-contract.test.ts` `LOCKED_EXCEPTIONS` lists
`src/components/shell/AuthPanel.tsx` for the D-21 `gap-5` exception. The real file
is `src/app/(auth)/AuthPanel.tsx` (verified on disk). The entry is dead.
Currently harmless — the same `gap-5` D-21 exception is also correctly keyed to
`src/app/(auth)/layout.tsx`, and plan 25 placed the 20px auth form-stack `gap-5`
on `layout.tsx`, not `AuthPanel.tsx` — so no real violation is masked or
mis-flagged today. But the exception silently fails to apply if `AuthPanel.tsx`
ever legitimately needs it.

**Disposition: UNRESOLVED (latent).** Fix alongside Unresolved #1 (same file,
same owner).

### 3. NFR-09 accessible media / captions (CR-11) — OPEN

See `04.1-CAPTIONS-SCOPE.md`. Plan 23 removed the source-less
`<track kind="captions">` from `LessonMediaPlayer.tsx` (honest markup) and its
regression is green here, but:
- `Lesson` / `LessonResource` have no captions/transcript/language field
- the VIDEO upload allow-list is `video/mp4` + `video/webm` only — no `text/vtt`
- `LessonMediaPlayer` props are `src` + `title` only

A learner who needs captions still has none. Real caption authoring + a scanned
WebVTT resource + language metadata + authorized delivery + a publication-readiness
policy is a **new data/upload contract** (likely a schema migration), **outside**
the phase 04.1 locked boundary ("no new route, data model, permission, bulk
mutation or export"). It needs its own scope approval — **not authorized here.**

**Disposition: NFR-09 remains PENDING. Phase 04.1 MUST NOT be marked complete.**

### 4. No human visual / rendered-layout walkthrough performed

Source-contract and DOM-semantics tests grade class strings and accessibility
wiring; they do **not** certify rendered browser layout, the 60/30/10 colour
balance, the 48px depth model reading correctly on dense staff tables (D-18
`honest` annotation), Windows High Contrast Mode, or the actual loaded font stack
(Segoe UI Variable on Windows vs. the Plus Jakarta Sans fallback elsewhere, D-05).
No screenshot / geometry / visual-regression pass was run or fabricated (D-26 — no
visual-regression tooling this phase). `04.1-UAT.md` is prepared as a **pending**
28-screen / 29-route walkthrough covering the 14 backstop rows and the 7
unresolved state rows from `04.1-UI-SPEC.md` §7, at 360 / 768 / 1024 / 1440px plus
short-height.

**Disposition: PENDING human execution.**

### 5. Five low-severity security advisories remain advisory-open

`04.1-SECURITY.md`: `T-04.1-05`, `T-04.1-09`, `T-04.1-14`, `T-04.1-17`,
`T-04.1-22` (all `low`, all `accept` at plan time) are "open — below high
threshold". No high-severity threat is open. Prior plans' `accept` disposition is
**not** user acceptance. `T-04.1-26` (medium) is listed open in `04.1-SECURITY.md`
(written 2026-09-06) but its mitigation — `resolveCourseIdForLesson` +
`lesson-preview-route.test.ts` — landed in plan 23 and passes here; that register
row predates the plan.

**Disposition: the 5 low advisories need explicit user acceptance — not the
executor's to give. Recorded, not cleared.**

## UAT handoff document test (Plan 04.1-29, Task 2)

| Check | Command | Result | Timestamp (UTC) |
|-------|---------|--------|-----------------|
| `04.1-UAT.md` is a real pending handoff (contains `pending` and links `CAPTIONS-SCOPE`) | `node -e "…if(!s.includes('pending') || !s.includes('CAPTIONS-SCOPE')) process.exit(1)"` | **PASS** — "UAT doc-verify: PASS (contains pending + CAPTIONS-SCOPE)" | 2026-09-07 |

`04.1-UAT.md` lists all 28 mockup screens mapped to the 29 rendered routes, the 14
backstop + 7 unresolved state rows from `04.1-UI-SPEC.md` §7, the 360/768/1024/1440px
+ short-height matrix, and the cross-cutting keyboard / mobile bulk / mobile audit /
failed-action-retry / font-stack / Windows High Contrast / 404-recovery / long-content
/ locked-spacing-exception checks. It preserves D-26 (no visual-regression tooling),
links `04.1-CAPTIONS-SCOPE.md`, states plainly that no captions authoring or delivery
is authorized or implemented, asks for the captions scope decision at handoff, keeps
NFR-09 pending, and keeps the 5 low security advisories advisory-open. No human result
is inferred from jsdom or source scans.

## Bottom line

All five combined automated gates (full suite, source contracts, lint, types,
build) are **green** with fresh explicit results, and every code-review blocker
has a named passing regression in the merged tree. The phase is **still not
done**: mechanical design-contract coverage exists for only 14 of ~50 restyled
files, NFR-09 accessible-media is an open scope decision, the human visual
walkthrough is pending, and 5 low security advisories await user acceptance. Do
not mark NFR-09 or phase 04.1 complete on the strength of this evidence.

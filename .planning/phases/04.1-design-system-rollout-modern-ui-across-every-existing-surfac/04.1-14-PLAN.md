---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 14
type: execute
wave: 4
depends_on: ["04.1-01", "04.1-02", "04.1-03", "04.1-04", "04.1-05", "04.1-06", "04.1-07", "04.1-08", "04.1-09", "04.1-10", "04.1-11", "04.1-12", "04.1-13"]
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: [NFR-09]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ROADMAP criterion 2 names the amber fill value the phase actually shipped, so the roadmap and the design contract no longer contradict each other (D-12)."
    - "ROADMAP criterion 4 no longer implies a pixel-identical done-condition, which the split-panel redesign made void (D-21)."
    - "No file under src/ outside globals.css contains a raw hex colour value (ROADMAP criterion 1)."
    - "No file under src/ contains a utility drawn from Tailwind's built-in neutral ramp — the swap is complete, not partial, so the app ships one neutral family rather than two (D-14)."
    - "No file under src/ contains a weight-500 font utility; exactly two weights are active (UI-SPEC 4.3)."
    - "The duplicated centred-column wrapper string appears zero times anywhere under src/app (ROADMAP criterion 4, D-21, D-23)."
    - "The full test suite is green — the 707 tests that passed before the phase plus every test this phase added — and npx next build is clean (ROADMAP criterion 7)."
    - "No screen lost a state: loading, empty, denied, error and validation-error all still render everywhere they did before (ROADMAP criterion 7)."
    - "All 18 staff routes, all 6 auth routes, all 4 public catalogue routes and /account have been walked by hand against the approved mockup, and the 14 backstop guarantees have each been looked at rather than assumed (D-26)."
    - statement: "The denied state renders identical copy, colour, icon and layout whether or not the record exists — asserted mechanically by the plan-02 parity test, and confirmed by eye on a real record and a fabricated id."
      verification: backstop
    - statement: "Every one of the 28 drawn screens matches the approved mockup's system: palette, neutrals, type stack, radius, shadow density, shell structure and depth model. The bar is system-faithful, not pixel-faithful (D-04) — the app has 30 routes to the mockup's 28 screens, and screens the mockup never drew inherit the system through the primitives."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT close this phase with a documented invariant left failing — the greps in this plan are the only thing standing between a half-finished migration and a codebase that quietly carries two design systems for the rest of the project."
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT leave the roadmap contradicting the shipped design — a success criterion naming a colour the product does not use turns the phase's own definition of done into a lie for every later reader."
      category: transparency
      status: unverified
      verification: judgment
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 04.1 success criteria reconciled with the shipped design"
      contains: "Phase 04.1"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md"
      via: "criterion 2's amber fill value is the one D-12 locks; criterion 4's done-condition is the one D-21 rewrites"
      pattern: "f79009"
---

<objective>
Close the phase: reconcile the roadmap with what shipped, prove every phase-wide invariant
mechanically, and walk the 28 drawn screens by hand.

Purpose: this phase's real risk is a partial migration. Roughly 420 class-name sites across 59 files
were rewritten by eleven parallel plans, and nothing in the toolchain fails when one is missed — two
grey families side by side simply look like a slightly inconsistent app. The four greps in this plan
are the only mechanical statement that the swap is complete. The walkthrough is the only statement
that it looks right, which is a judgement no assertion expresses.

Output: a corrected roadmap, four green invariant gates, a green full suite and build, and a
recorded human walkthrough.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VALIDATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ROADMAP.md with the design the phase actually shipped</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md`, the "Phase 04.1" section — its seven success criteria. Criterion 2 names
      an amber fill hex; criterion 4 states the six auth routes share one layout instead of each
      repeating the same centred wrapper.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md`
      D-12 and D-21. D-12 states plainly that the roadmap line itself must be corrected, not left
      contradicting the design contract; D-21 states that a pixel-identical done-condition for the
      auth routes is void under the redesign.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 5.1's locked-and-decided inputs, which record both amendments and their reasoning.
  </read_first>
  <action>
    Make two scoped edits to the "Phase 04.1" section of `.planning/ROADMAP.md`, using targeted
    replacements. Do not rewrite the file, and do not touch any other phase's entry — a whole-file
    rewrite of this document destroys every phase outside the edit window.

    First, criterion 2: replace the amber fill hex it names with `#f79009`, the value D-12 locks and
    the value the shipped token chain uses. The retired value is the one currently on that line;
    `04.1-CONTEXT.md` D-12 and `04.1-UI-SPEC.md` section 5.1 both identify it and explain why it was
    retired. Leave the amber text-safe value `#b45309` and both teal values exactly as they are —
    those three are unchanged and still correct.

    Second, criterion 4: append a short clause making the done-condition structural rather than
    visual — the six routes share one layout, that layout owns the chrome, and each screen's state
    contract still renders inside it. The redesign replaced the centred column with a split panel, so
    any implication that the rendered output stays as it was is now false.

    Then update the phase's `**Plans:**` line and its plan list to name the fourteen plans this phase
    produced, each with a one-line objective, marking every one complete.
  </action>
  <acceptance_criteria>
    - The Phase 04.1 criterion 2 line names `#f79009` and no longer names the retired value.
    - Criterion 2 still names `#b45309`, `#12a594` and `#0f766e` unchanged.
    - Criterion 4 carries a structural done-condition.
    - Every other phase's entry in the file is byte-identical to before.
    - The plan list names all fourteen plans.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^ *#' .planning/ROADMAP.md | grep -c 'e09b34'</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'f79009' .planning/ROADMAP.md</automated>
    <fails_when>prints 0, or non-zero exit (no match)</fails_when>
    <automated>grep -c 'b45309' .planning/ROADMAP.md</automated>
    <fails_when>prints 0, or non-zero exit (no match) — the text-safe amber was changed by mistake</fails_when>
    <automated>grep -c '^### Phase ' .planning/ROADMAP.md</automated>
    <fails_when>prints fewer than 16 — a whole-file rewrite dropped other phases</fails_when>
  </verify>
  <done>The roadmap's Phase 04.1 criteria describe the design that shipped, and no other phase entry moved.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the four phase-wide invariants and close any straggler</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-RESEARCH.md`
      — "Common Pitfalls" Pitfall 3, which records the measured scale of the migration and why a
      partial sweep is easy to miss: no lint rule enforces any of these invariants, and the retired
      neutral ramp and the new one are both grey, so a stray class reads as "close enough" while
      violating the full-swap requirement.
    - The summaries of plans 01 through 13, each of which records which files it swept.
  </read_first>
  <action>
    Run the four phase-wide gates from the repository root and record each result in the plan summary.
    Each gate filters out comment lines first, so a comment mentioning a retired token does not
    invalidate itself:

    1. Raw hex outside the token file — every match under `src/` other than in `globals.css`.
    2. The retired neutral ramp — every remaining utility from Tailwind's built-in grey scale
       under `src/`.
    3. The retired 500 weight — every remaining medium-weight font utility under `src/`.
    4. The duplicated centred-column wrapper string — every remaining occurrence under `src/app`.

    Every one must return zero. Where a gate returns a match, fix that file directly using the same
    role-based substitution the sweep plans used — 500-level neutral text to `text-muted-foreground`,
    600 and darker to `text-foreground`, a control edge to `border-input-border`, a panel or divider
    edge to `border-border`, 50/100-level fills to `bg-surface-2`, `bg-white` to `bg-surface`, and a
    medium-weight font utility to `font-semibold` — then re-run the gate. Record every straggler you
    found and which plan should have caught it, so the miss is visible rather than quietly patched.

    These are the mechanical proof of ROADMAP criteria 1 and 4 and of D-14's full-swap requirement.
    Do not weaken a gate to make it pass; a filter that excludes a real match is worse than a failing
    gate, because it removes the only signal anyone will ever get.
  </action>
  <acceptance_criteria>
    - All four gates return zero.
    - No gate command was narrowed, path-excluded or otherwise weakened from the forms below.
    - Every straggler found is listed in the plan summary alongside the plan that should have caught
      it.
    - `npx tsc --noEmit` exits 0 and `npx eslint src tests` reports no error.
  </acceptance_criteria>
  <verify>
    <automated>grep -rniE "#[0-9a-f]{6}" src/ --include="*.tsx" --include="*.ts" --include="*.css" | grep -v "^src/app/globals.css:" | grep -vE ':[0-9]+: *(//|\*|/\*|\{/\*)' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn "zinc-" src/ --include="*.tsx" --include="*.ts" | grep -vE ':[0-9]+: *(//|\*|/\*|\{/\*)' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn "font-medium" src/ --include="*.tsx" --include="*.ts" | grep -vE ':[0-9]+: *(//|\*|/\*|\{/\*)' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn "mx-auto flex min-h-screen max-w-sm flex-col justify-center gap-6 px-6" src/app | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx eslint src tests</automated>
    <fails_when>non-zero exit, or the output reports any error-severity problem</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or the output names any file under src/ or tests/</fails_when>
  </verify>
  <done>All four invariants are mechanically green from the repository root, with any straggler fixed and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite, clean build, and the 28-screen walkthrough</name>
  <precondition>Docker is running and reachable (`docker info` exits 0) — the node Vitest project provisions a Testcontainers Postgres and cannot run without it.</precondition>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VALIDATION.md`
      — the sampling contract and its two manual-only verifications: mockup fidelity across the 28
      screens, and denial parity gaining no visual cue.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 8 — the 14 rows marked `resolved (backstop)`. These are the specific checks the
      walkthrough exists to cover, and they carry more weight now than before precisely because every
      surface changed.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html`
      — the 28 drawn screens, the reference the walkthrough compares against.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-RESEARCH.md`
      — the measured baselines: 707 of 707 passing in roughly 351 seconds for the full suite, 59 of
      59 in roughly 66 seconds for the components project, both on pre-migration code.
  </read_first>
  <action>
    Run `npm test` to completion and `npx next build`. The suite must be green at 707 pre-existing
    tests plus every test this phase added — the four primitive state-render files and the contrast
    file. Budget roughly six minutes for the full suite; it provisions a Postgres container. Record
    the final counts and the runtime in the plan summary against the pre-migration baseline, so a
    reader can see nothing was lost.

    Then start `npm run dev` and leave it running for the walkthrough below.

    Record in the plan summary, for each of the 14 backstop rows in UI-SPEC section 8, whether it was
    confirmed, and where it was not, say so plainly. A backstop the walkthrough did not actually
    exercise routes to human review at verify time rather than passing silently, and writing
    "confirmed" for something nobody looked at defeats the only check these rows have.
  </action>
  <acceptance_criteria>
    - `npm test` exits 0 with a passed count at or above 766 (the 707 pre-existing plus the four
      primitive test files and the contrast file added by plans 01 and 02) and zero failures.
    - `npx next build` exits 0.
    - The summary records the final test counts and runtime against the pre-migration baseline.
    - The summary records a per-row outcome for all 14 backstop guarantees, with unconfirmed rows
      named as unconfirmed.
    - The walkthrough covered all 18 staff routes, all 6 auth routes, all 4 public catalogue routes
      and `/account`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or the total passed count is below 766</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
    <human-check>
      With `npm run dev` running, walk every route against `04.1-MOCKUP-SOURCE.html`.

      Shells: (1) the staff sidebar is navy at 228px with exactly five nav items and no greyed-out
      placeholder; (2) the six auth routes all render the same split panel; (3) `/courses` and
      `/account` render the same wide top bar.

      System: (4) one neutral family everywhere — no screen mixes a warm grey with the cool ones;
      (5) headings render in Plus Jakarta Sans and tabular figures in IBM Plex Mono; (6) staff list
      tables carry a low shadow while public cards, forms and dialogs carry the deep one, and no
      dense grid of cards reads as muddy; (7) teal appears only on progress, pass and grade
      indicators — never on a button, link, focus ring or active nav item; (8) every status pill is a
      tinted pill whose text is readable, not a neutral pill with a coloured dot.

      States: (9) on a staff list, see loading, empty (both unfiltered and filtered), error and a
      filter validation error that keeps the previous rows visible; (10) on a form, submit invalid
      and confirm the error summary appears above the fields and takes focus; (11) open a record with
      several tabs and navigate them with the arrow keys, Home and End; (12) open an integrity dialog
      and confirm the reason counter, the disabled confirm and the suppressed Escape while pending.

      Denial parity: (13) render a denied state for a record you know exists and for a fabricated id,
      and confirm the two are identical in copy, colour, icon and layout — no count, title or
      identifier differs.

      The 14 backstop rows: (14) long cell values below 640px; (15) many simultaneous field errors;
      (16) long labels and long validation messages; (17) many tabs at a narrow width; (18) a long
      record title and a long breadcrumb; (19) a long dialog description with a long typed reason;
      (20) a long dialog title; (21) a submit-pending state on each auth screen; (22) the auth split
      panel at a short viewport height; (23) long auth error copy in the 378px column; (24) a
      catalogue item missing an optional field; (25) the catalogue grid at a narrow width and with a
      single item; (26) a long catalogue title; (27) the account grid at a narrow width and with a
      long display name; (28) a long resource title beside its scan pill, a long blocking-item
      description in the publish dialog, a readiness category with many failing items, and the
      editor toolbar wrapping at a narrow width.

      Finally, at every width from 1440px down to 360px, confirm no page ever scrolls horizontally.
    </human-check>
  </verify>
  <done>The full suite and the build are green, and every route has been walked by hand with each of the 14 backstop guarantees explicitly confirmed or explicitly recorded as unconfirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization decision → rendered denied state | The last chance to catch a visual cue that leaks record existence, across every surface at once |
| planning documents → future readers | A success criterion naming a value the product does not use misleads every later phase |
| eleven parallel plans → one codebase | Nothing in the toolchain fails when one of 420 class-name sites is missed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Information Disclosure | Denied states across every surface | high | mitigate | The plan-02 byte-equality parity test runs inside `npm test` here, and the walkthrough additionally compares a denied state for a real record against one for a fabricated id by eye |
| T-04.1-39 | Tampering | Partial neutral-ramp migration | high | mitigate | Four unweakenable phase-wide greps run from the repository root, each filtering comment lines so a comment cannot invalidate itself; any straggler is fixed and recorded alongside the plan that missed it |
| T-04.1-40 | Repudiation | Backstop confirmation record | medium | mitigate | Every one of the 14 backstop rows gets a recorded per-row outcome; an unconfirmed row is named as unconfirmed rather than assumed, so verify-time review sees the gap instead of a false pass |
| T-04.1-02 | Elevation of Privilege | Layout guards after three shell rebuilds | high | mitigate | The full suite plus the walkthrough's signed-out and non-staff checks confirm the staff and account guards survived; the per-plan greps already asserted their presence in source |
</threat_model>

<verification>
- All four phase-wide invariant gates return zero from the repository root.
- `npm test` green at 766 or more passing tests with zero failures.
- `npx next build` clean; `npx tsc --noEmit` clean; `npx eslint src tests` reports no error.
- ROADMAP Phase 04.1 criteria 2 and 4 reconciled, every other phase entry untouched.
- A per-row outcome recorded for all 14 backstop guarantees.

</verification>

<success_criteria>
Every one of ROADMAP Phase 04.1's seven success criteria is either mechanically proven or explicitly
walked by a human, the roadmap no longer contradicts the shipped design, and the migration is
complete rather than partial.
</success_criteria>

## Artifacts this phase produces

No new source file and no new exported symbol. Updated: `.planning/ROADMAP.md`'s Phase 04.1 criterion
2 amber fill value, criterion 4 done-condition, `**Plans:**` count and plan list.

Recorded in the plan summary: the four invariant gate results, any straggler found and the plan that
should have caught it, the final test counts and runtime against the pre-migration baseline, and a
per-row outcome for the 14 backstop guarantees.

## Planner Assumptions

- **The expected minimum passing count is 766.** That is the 707 measured pre-migration plus the four
  primitive state-render files (7, 6, 5 and 8 tests as specified in plan 02) and the contrast file's
  cases from plan 01. If the plans land more tests than their minimums, the count is higher and the
  gate still passes; if any earlier plan lands fewer, this gate is what surfaces it.
- **The walkthrough is the only check on mockup fidelity.** D-26 rules out visual-regression tooling
  this phase, because baselines captured after the redesign would prove nothing about the redesign
  itself. `04.1-VALIDATION.md` records both manual-only verifications, and this task is where they
  happen.

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-14-SUMMARY.md` when done.
</output>

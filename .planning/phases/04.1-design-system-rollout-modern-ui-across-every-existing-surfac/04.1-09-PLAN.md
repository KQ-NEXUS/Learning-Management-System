---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 09
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/app/staff/audit/AuditTable.tsx
  - src/app/staff/users/AssignmentDrawer.tsx
  - src/app/staff/roles/PermissionPicker.tsx
  - src/app/staff/roles/EffectiveAccessPreview.tsx
  - src/app/staff/users/AssignmentsPanel.tsx
  - src/app/staff/users/StaffAccountForm.tsx
  - src/app/staff/users/UsersTable.tsx
autonomous: true
requirements: [NFR-09]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The audit table reads as the same visual language as every other staff list, because its copied class constants and panel helper now consume the same tokens the list primitive does (UI-SPEC 0.4)."
    - "The audit table's expand-in-place behaviour, its URL-driven filters and its four state panels all still work after the restyle (UI-SPEC 0.4)."
    - "Every checkbox, select, text input and search field in these seven files carries an edge clearing 3:1 (D-15, NFR-09)."
    - "The assignment drawer renders as an overlay panel on the approved card treatment, and stays a documented non-primitive rather than being extracted into a fifth primitive this phase (CONTEXT deferred list)."
    - "The drawer's focus handling, its scope-type branching and its per-scope empty copy all survive unchanged."
    - "The permission picker's checkbox grid keeps its labels, its grouping and its keyboard reachability; only the control edges, radius and neutral tones change."
    - "Every denied and error panel across these files renders the same copy it does today, with no cue keyed to record existence (RBAC-06, T-04.1-01)."
    - "No file in this plan carries a raw hex, a utility drawn from the retired neutral ramp, or a weight-500 font utility."
    - statement: "The audit table's expand-in-place row must still open and close cleanly once its row dividers and hover fill move to the new tokens, with the expanded panel visually attached to its trigger row."
      verification: backstop
    - statement: "The assignment drawer at 362 lines is the largest hand-styled surface in the phase; its overlay must still sit above the shell's sidebar and header bar after the shell rebuild lands."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT introduce a delete affordance while restyling the audit table — audit history is append-only, and a restyle of a row action area is exactly where such a control gets added by analogy with other tables."
      category: safety
      status: unverified
      verification: judgment
    - statement: "MUST NOT change what the audit table displays about a record, only how it displays it — the audit trail's job is to be an accurate account of what happened, and abbreviating or reformatting a field during a restyle silently rewrites the record."
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT use the decorative border token on an input, select, textarea or checkbox in these files — a control identified only by that edge is invisible to a low-vision user."
      category: safety
      status: unverified
      verification: judgment
  artifacts:
    - path: "src/app/staff/audit/AuditTable.tsx"
      provides: "Tokenised class constants, card panel treatment and control edges for the audit list"
      contains: "muted-foreground"
    - path: "src/app/staff/users/AssignmentDrawer.tsx"
      provides: "Overlay panel on the card treatment with tokenised controls"
      contains: "rounded-xl"
  key_links:
    - from: "src/app/staff/audit/AuditTable.tsx"
      to: "src/components/primitives/ResourceTable.tsx"
      via: "documented sibling — its copied HEAD/CELL/BTN constants and Panel helper must match the primitive's restyled versions"
      pattern: "const (HEAD|CELL|BTN)"
---

<objective>
Bring the six staff surfaces that duplicate primitive markup by hand — plus one thin table wrapper —
onto the same tokens the primitives now use.

Purpose: these files do not import the primitives, so nothing about plans 03 and 04 reaches them
automatically. If they are left alone the workspace ships two neutral families side by side, which is
precisely the "two different UIs that don't match" problem D-14 exists to prevent. `AuditTable.tsx`
matters most: it is a deliberate, documented sibling of `ResourceTable` whose panel markup was copied
verbatim so the visual language would match, and that only stays true if it is updated in step.

Output: seven files on the approved tokens, with every behaviour they carry untouched.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-PATTERNS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditTable follows its sibling primitive onto the same tokens</name>
  <files>src/app/staff/audit/AuditTable.tsx</files>
  <read_first>
    - `src/app/staff/audit/AuditTable.tsx` — the whole file. Its head comment states it is a sibling
      of the list primitive rather than a consumer, because expand-in-place is the one behaviour the
      primitive lacks, and that its skeleton, empty, denied and error panel markup are copied
      verbatim so the visual language matches. Note its `HEAD`, `CELL` and `BTN` constants and its
      local `Panel` helper — all four are copies of the primitive's.
    - `src/components/primitives/ResourceTable.tsx` as restyled in plan 03 — read its restyled
      `HEAD`, `CELL`, `BTN`, `BTN_PRIMARY` and `Panel`. Copy those values across so the two stay
      matched, which is the whole reason this file duplicates them.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 0.4's audit row (its two filter controls need the interactive-control edge) and section
      7.1's list-screen treatment.
  </read_first>
  <action>
    Update this file's four copied constants and its `Panel` helper to exactly the values plan 03
    landed in the primitive. That parity is the point of the duplication: the head comment promises
    the two look identical, and the comment stops being true the moment they diverge.

    Give the table the same container treatment: `bg-surface`, `border border-border`, `rounded-xl`,
    `shadow-xs`, with row dividers on the decorative border token and row hover on `bg-surface-2`. A
    staff list is a dense surface, so it takes the low shadow, never the deep card one.

    Give the two filter controls — the select and the search input — the interactive edge
    `border-input-border` at `rounded-md`, at the 38px standard control height.

    Style the expanded detail panel so it reads as attached to its trigger row rather than floating:
    the cooler surface tone, no separate shadow, and the row's own divider suppressed while expanded.
    Do not change the expand-in-place mechanism, the row toggle's accessible name or its
    `aria-expanded`.

    Do not change what any column displays, how any identifier is shortened, or what any panel says.
    An audit trail's value is that it is an accurate account of what happened; reformatting a field
    during a restyle silently rewrites the record. Leave the denied panel's copy word for word and add
    no icon or layout cue there.

    Substitute every utility drawn from Tailwind's built-in neutral ramp by role — 500-level neutral
    text becomes `text-muted-foreground`, 600 and darker becomes `text-foreground`, a 300-level
    neutral edge on a control becomes `border-input-border`, a 200-level neutral edge on a panel or
    divider becomes `border-border`, 50/100-level neutral fills become `bg-surface-2`, and `bg-white`
    becomes `bg-surface`. Fold every weight-500 font utility to `font-semibold`.
  </action>
  <acceptance_criteria>
    - The file's `HEAD`, `CELL` and `BTN` constants hold the same strings as the restyled primitive's.
    - The head comment stating the file is a matched sibling is still present and still accurate.
    - The expand-in-place toggle still carries `aria-expanded` and its accessible name is unchanged.
    - No column's displayed value or formatting changed.
    - The denied panel's copy is unchanged.
    - The file contains no utility drawn from Tailwind's built-in neutral ramp, no weight-500 font
      utility, and no raw hex.
    - `npx vitest run tests/audit-read-service.test.ts` passes and `npx next build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'zinc-' src/app/staff/audit/AuditTable.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'font-medium' src/app/staff/audit/AuditTable.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'aria-expanded' src/app/staff/audit/AuditTable.tsx</automated>
    <fails_when>prints 0, or non-zero exit (no match) — the expand-in-place toggle lost its state announcement</fails_when>
    <automated>npx vitest run tests/audit-read-service.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
  </verify>
  <done>The audit list is visually indistinguishable from a primitive-driven list again, with its expand-in-place behaviour and its recorded values untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The assignment drawer becomes an overlay panel on the card treatment</name>
  <files>src/app/staff/users/AssignmentDrawer.tsx</files>
  <read_first>
    - `src/app/staff/users/AssignmentDrawer.tsx` — the whole file, all 362 lines. Note the
      `SCOPE_TYPES` list, the `EMPTY_STATE_COPY` map with its per-scope explanations, the
      `useActionState` binding to `createAssignmentAction`, the two search actions, the `useId` and
      `useRef` usage that carries its focus handling, and the error rendering.
    - `src/components/primitives/ConfirmModal.tsx` as restyled in plan 04 — the closest existing
      overlay treatment. Reuse its dialog surface and backdrop rather than inventing a second one.
    - `src/app/staff/StaffShell.tsx` as written in plan 05 — the drawer's overlay must sit above the
      shell's sidebar and header bar; check the stacking the shell establishes.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 0.4's drawer row, which records that this stays a documented exception rather than
      becoming a fifth primitive this phase.
  </read_first>
  <action>
    Give the drawer panel the overlay treatment plan 04 established for the dialog: `bg-surface`,
    `rounded-xl` on its exposed corners, `shadow-card`, over a tokenised backdrop. Confirm its
    stacking places it above the rebuilt staff shell's sidebar and header bar.

    Give every control it renders — the scope-type select, both search inputs, and any checkbox —
    the interactive edge `border-input-border` at `rounded-md` at the standard control height. Labels
    render at 14px weight 600; hints and the per-scope empty copy at 11px in `text-muted-foreground`;
    errors keep their `border-danger/30 bg-danger-surface` semantics and gain `rounded-md`.

    Change nothing about behaviour: the `useActionState` binding, both search actions, the `useId`
    wiring, the focus handling, the scope-type branching and every string in `EMPTY_STATE_COPY` stay
    exactly as they are. This file stays a documented non-primitive — do not extract a fifth
    primitive from it in this phase.

    Substitute every utility drawn from Tailwind's built-in neutral ramp by role as in task 1, and
    fold every weight-500 font utility to `font-semibold`.
  </action>
  <acceptance_criteria>
    - Every string in `EMPTY_STATE_COPY` is unchanged.
    - The `useActionState` binding, both search action imports and the `useId`/`useRef` wiring are
      unchanged.
    - The drawer panel carries `shadow-card` and its controls carry `border-input-border`.
    - No new shared component was extracted from this file.
    - The file contains no utility drawn from Tailwind's built-in neutral ramp, no weight-500 font
      utility, and no raw hex.
    - `npx vitest run tests/assignment-service.test.ts` passes and `npx next build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'zinc-' src/app/staff/users/AssignmentDrawer.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'font-medium' src/app/staff/users/AssignmentDrawer.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'border-input-border' src/app/staff/users/AssignmentDrawer.tsx</automated>
    <fails_when>prints 0, or non-zero exit (no match)</fails_when>
    <automated>npx vitest run tests/assignment-service.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
  </verify>
  <done>The drawer renders as an approved overlay above the rebuilt shell, with every binding, focus behaviour and per-scope string intact.</done>
</task>

<task type="auto">
  <name>Task 3: The four remaining staff widgets and the users table wrapper</name>
  <files>src/app/staff/roles/PermissionPicker.tsx, src/app/staff/roles/EffectiveAccessPreview.tsx, src/app/staff/users/AssignmentsPanel.tsx, src/app/staff/users/StaffAccountForm.tsx, src/app/staff/users/UsersTable.tsx</files>
  <read_first>
    - `src/app/staff/roles/PermissionPicker.tsx` — the whole file. A checkbox grid, not a `FormField`
      use case; note its grouping, its labels and how each checkbox is associated with its label.
    - `src/app/staff/roles/EffectiveAccessPreview.tsx` — a short read-only preview widget with no
      interactive control, so it needs the neutral and weight sweep only.
    - `src/app/staff/users/AssignmentsPanel.tsx` and `src/app/staff/users/StaffAccountForm.tsx` — the
      whole files; note every input, label and error render.
    - `src/app/staff/users/UsersTable.tsx` — it configures the list primitive, so it inherits plans
      03's treatment; it needs the weight sweep only.
    - `src/components/primitives/ResourceForm.tsx` as restyled in plan 04 — reuse its label, control
      edge and error treatment.
  </read_first>
  <action>
    Give every checkbox, input and select across these five files the interactive edge
    `border-input-border`, with the 16px square, 1.5px edge and 4px radius the checkbox treatment
    calls for. Associate rules are unchanged: do not touch any label-to-control binding, any `name`
    attribute, or any `aria-*` wiring.

    In `PermissionPicker.tsx` keep the grid structure, the group headings and the keyboard order
    exactly as they are; only the control edges, the group panel treatment (`bg-surface`,
    `border-border`, `rounded-xl`) and the neutral tones change.

    In `EffectiveAccessPreview.tsx` and `UsersTable.tsx` the change is limited to the neutral and
    weight sweep — neither renders an interactive control edge of its own.

    Across all five files, substitute every utility drawn from Tailwind's built-in neutral ramp by
    role as in task 1, and fold every weight-500 font utility to `font-semibold`.
  </action>
  <acceptance_criteria>
    - No label-to-control binding, `name` attribute or `aria-*` attribute changed in any of the five
      files.
    - Every checkbox, input and select carries `border-input-border`.
    - None of the five files contains a utility drawn from Tailwind's built-in neutral ramp, a
      weight-500 font utility, or a raw hex.
    - `npm test` passes.
    - `npx next build` exits 0 and `npx eslint src` reports no error.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn 'zinc-' src/app/staff/roles src/app/staff/users | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn 'font-medium' src/app/staff/roles src/app/staff/users | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx eslint src/app/staff</automated>
    <fails_when>non-zero exit, or the output reports any error-severity problem</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
    <human-check>Run `npm run dev` and, signed in as staff, visit `/staff/audit`, `/staff/users`, a user detail page with the assignment drawer open, and `/staff/roles/new`. Confirm the audit list is visually indistinguishable from the courses list, that expanding an audit row still works and the expanded panel reads as attached to its row, that every checkbox and input in the permission picker and the drawer has a clearly visible edge against the paper behind it, and that the drawer's overlay sits above the navy sidebar rather than behind it.</human-check>
  </verify>
  <done>The four remaining widgets and the users table wrapper are on the approved tokens with every binding and label association untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization decision → denied panels | `AuditTable` and the users/roles surfaces each render a denied state that must reveal nothing about the record behind it |
| staff input → assignment creation | The drawer creates permission grants; its form bindings decide what grant is recorded |
| audit store → rendered audit row | The audit trail is the account of record for who did what |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Information Disclosure | Denied panels in `AuditTable` and the users/roles surfaces | high | mitigate | Only colour, radius and shadow classes change inside those panels; their copy is asserted unchanged and no icon or layout cue is added |
| T-04.1-23 | Repudiation | `AuditTable` displayed values | high | mitigate | Task 1 forbids changing what any column displays or how any identifier is formatted, and forbids introducing a row-level delete affordance; `tests/audit-read-service.test.ts` runs as the task's verify |
| T-04.1-24 | Elevation of Privilege | `AssignmentDrawer` form bindings | high | mitigate | Task 2 changes appearance only; the `useActionState` binding, both search actions and every `name` attribute are asserted unchanged, and `tests/assignment-service.test.ts` runs as the task's verify |
| T-04.1-25 | Tampering | `PermissionPicker` label-to-control bindings | medium | mitigate | Task 3 forbids touching any label association, `name` attribute or `aria-*` wiring, so a restyle cannot silently re-point a checkbox at a different permission |
</threat_model>

<verification>
- `npm test` green.
- `npx next build` clean; `npx eslint src` reports no error.
- No legacy neutral utility, weight-500 utility or raw hex anywhere under `src/app/staff/audit`,
  `src/app/staff/roles` or `src/app/staff/users` for the seven files in this plan.

</verification>

<success_criteria>
The six hand-styled staff surfaces and the users table wrapper render the same visual language as the
primitive-driven screens, with every behaviour, binding and recorded value untouched.
</success_criteria>

## Artifacts this phase produces

No new file and no new exported symbol. `AuditTable.tsx`'s `HEAD`, `CELL`, `BTN` constants and its
local `Panel` helper are retained names with new values, brought back into parity with the restyled
primitive.

New markup treatments, all internal: the audit table's card container and its attached
expanded-row panel; the assignment drawer's overlay panel and backdrop on the dialog treatment; the
permission picker's group panels and 16px checkbox treatment.

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-09-SUMMARY.md` when done.
</output>

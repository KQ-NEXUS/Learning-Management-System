---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "15"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/components/primitives/ResourceTable.tsx"
  - "tests/components/resource-table.test.tsx"
  - "src/app/staff/courses/CoursesTable.tsx"
  - "tests/components/courses-table.test.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Named filters operate; mobile and desktop represent identical selected IDs and bulk callback payloads."
    - "All four formerly inert controls accurately state unavailability; existing course navigation and shared real bulk actions survive."
  artifacts:
    - path: "src/components/primitives/ResourceTable.tsx"
      provides: "Accessible staff table selection and honest course controls"
    - path: "tests/components/resource-table.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/app/staff/courses/CoursesTable.tsx"
      provides: "Accessible staff table selection and honest course controls"
    - path: "tests/components/courses-table.test.tsx"
      provides: "Executable regression evidence"
  key_links:
    - from: "src/app/staff/courses/CoursesTable.tsx"
      to: "src/components/primitives/ResourceTable.tsx"
      via: "Controlled filters and selection props render desktop rows and mobile cards"
---

<objective>
Accessible staff table selection and honest course controls. Close the source-confirmed gaps while honoring D-01–D-26 and the phase boundary: preserve existing capabilities, routes, data, authorization and action contracts. The approved CONTEXT overrides contradictory UI-SPEC generalizations. Prior plans 01–14 remain executed history; this is additive gap closure.
</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-ANNOTATIONS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-REVIEW.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
</context>

## Planner Assumptions

- No dependency installs, external API integration, schema mutation or new permission is required. ASVS level 1; high is the blocking threshold.
- Existing native/browser behavior remains authoritative; component tests prove DOM semantics, not rendered geometry.
- Tests named below that do not yet exist are created in the same implementation task before the repair and must first expose the reported failure. The existing Vitest node/components projects require no installation.
- Estimate factor 1, sample_count 0, confidence low from estimate-calibration.
- Preserve working tree changes and do not commit (commit_docs=false; parent execution policy).

<tasks>

<task type="tracer" tdd="true">
  <name>Trace search to filtered results and mobile selection</name>
  <files>src/components/primitives/ResourceTable.tsx, tests/components/resource-table.test.tsx</files>
  <read_first>src/components/primitives/ResourceTable.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Named filters operate; mobile and desktop represent identical selected IDs and bulk callback payloads. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Extend existing rendered-component tests first. Generate useId-based unique filter IDs and bind visible labels to search and non-segmented selects; keep segmented naming intact. Render labelled selection checkboxes in mobile cards using the same selectedIds/onChange source as desktop. Scope test queries to each representation because jsdom does not implement responsive CSS. Assert selecting and deselecting a mobile card updates the controlled parent and enables its real test bulk callback with the selected IDs, without triggering the row link. Retain filters, sort, previous rows on validation error and denial parity per D-04/D-26.</action>
  <acceptance_criteria>Named filters operate; mobile and desktop represent identical selected IDs and bulk callback payloads. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/resource-table.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Named filters operate; mobile and desktop represent identical selected IDs and bulk callback payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Represent unimplemented course controls as unavailable</name>
  <files>src/app/staff/courses/CoursesTable.tsx, src/components/primitives/ResourceTable.tsx, tests/components/courses-table.test.tsx, tests/components/resource-table.test.tsx</files>
  <read_first>src/app/staff/courses/CoursesTable.tsx, src/components/primitives/ResourceTable.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>All four formerly inert controls accurately state unavailability; existing course navigation and shared real bulk actions survive. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>CR-01 is pre-existing unfinished functionality: no courses/new route exists and bulk/export callbacks are absent. Preserve the four affordances but explicitly disable Publish, Archive, Export CSV and New course with visible 'Not available on this screen' explanatory text connected by aria-describedby. Add optional disabled and description fields to BulkSelection.actions without changing existing required props; renderer must use native disabled and must not invoke callbacks. Course selection remains possible, and row detail links remain usable for working per-record actions. Do not implement new routes, export machinery or bulk mutations under the locked phase boundary. Test all four disabled affordances and their explanations, plus a working row link.</action>
  <acceptance_criteria>All four formerly inert controls accurately state unavailability; existing course navigation and shared real bulk actions survive. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/courses-table.test.tsx tests/components/resource-table.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>All four formerly inert controls accurately state unavailability; existing course navigation and shared real bulk actions survive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User interaction to existing protected action | UI repair must not bypass server authorization or misrepresent results |
| Source/evidence to completion claim | Automated checks cannot stand in for pending visual or media accessibility evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G15-01 | Elevation of Privilege | Existing route/action consumers | high | mitigate | Preserve bound IDs and permission checks; test denied/failed outcomes where touched and run combined service suite in 29 |
| T-04.1-G15-02 | Repudiation | Feedback and verification | medium | mitigate | Preserve failure state, never assert mutation success after rejection, retain captions and human UAT gaps explicitly |
</threat_model>

<verification>
Run each task command from repository root. Focused tests must pass after the corresponding repair; plan 29 runs full combined gates once. A new test path is created before its command executes. Visual source checks do not certify browser layout. Captions scope remains unresolved and cannot be cleared by these repairs.
</verification>

<success_criteria>
All task acceptance criteria pass, scope remains bounded, and the SUMMARY records tests plus unresolved human/media checks honestly. Do not mark NFR-09 or the phase complete.
</success_criteria>

## Artifacts Produced

- `src/components/primitives/ResourceTable.tsx`
- `tests/components/resource-table.test.tsx`
- `src/app/staff/courses/CoursesTable.tsx`
- `tests/components/courses-table.test.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-15-SUMMARY.md` (execution evidence)

<output>
Create .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-15-SUMMARY.md. Report completed fixes, exact commands/results and any blocked scope decision without overwriting historical audits.
</output>

---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
verified: 2026-09-07T14:05:00Z
status: passed
status_history:
  - status: human_needed
    at: 2026-09-07T14:05:00Z
    note: "7/7 code truths verified; 3 human gates outstanding"
  - status: passed
    at: 2026-09-07T14:35:00Z
    note: "phase owner cleared all 3 human gates — see human_gates_cleared"
score: 7/7 code-verifiable truths verified (truth 8 / NFR-09 deferred to Phase 15)
human_gates_cleared:
  - gate: "04.1-UAT.md browser walkthrough"
    outcome: "PASS (2026-09-07). One issue found + fixed pre-sign-off: staff sidebar scrolled out of view on tall routes (overflow-x:hidden trapped lg:sticky) — fixed in commit 344c3a4 with 3 regression tests, re-checked in browser."
  - gate: "Captions / accessible-media scope decision (04.1-CAPTIONS-SCOPE.md)"
    outcome: "RESOLVED — accept video-without-captions as a known NFR-09 gap, carried to Phase 15. NFR-09 stays Pending, owned by Phase 15."
  - gate: "5 low-severity security advisories (T-04.1-05/09/14/17/22)"
    outcome: "ACCEPTED — all five explicitly accepted as residual risk (04.1-SECURITY.md → User Risk Acceptance). T-04.1-26 (medium, below block threshold) flagged for /gsd:secure-phase re-check — likely stale-closeable."
requirements_satisfied: n/a (NFR-09 owned by Phase 15 per REQUIREMENTS.md)
overrides_applied: 0
re_verification: true
re_verification_detail:
  previous_status: gaps_found
  previous_score: 4/8
  previous_verified: 2026-09-07T05:20:00Z
  gap_wave_plans: ["04.1-32", "04.1-33", "04.1-34", "04.1-35"]
  gaps_closed:
    - "G-RV-01 (BLOCKER / CR-01) — all five previously-unguarded async mutation handlers (ArrangeClient.handleSaveModuleOrder, handleSaveLessonArrangement; ProgrammeArrangeClient.handleSave, handleAdd, handleRemove) now wrap the awaited Server Action in try/catch/finally: busy flag cleared in finally, generic non-interpolating retryable error set in catch, no auto-retry. Verified in source + arrange-client.test.tsx / programme-arrange-client.test.tsx green."
    - "G-RV-02 (WARNING) — design-contract.test.ts GROUPS manifest expanded 14 -> 65 files across 10 groups (auth-shell 13, public-account-shell 12, staff-shell-access 13, staff-courses-programmes 13); EXPECTED_GROUPS deep-equality fixture + 4 anti-vacuity/anti-stale fixtures (non-empty groups, every manifest file exists on disk, no cross-group duplicate, every LOCKED_EXCEPTIONS file exists); stale LOCKED_EXCEPTIONS path src/components/shell/AuthPanel.tsx repointed to src/app/(auth)/AuthPanel.tsx; 4 off-role tokens repaired in the 3 shell entry layouts. Unfiltered gate 71 -> 127 assertions, independently re-run green."
    - "G-RV-03 / WR-01 (WARNING) — ConfirmModal gained tabIndex={-1} on the dialog container and both dialogs' Tab handlers now preventDefault() + focus the container on an empty focusable list, so focus is held through the disabled pending window. Verified in source + confirm-modal.test.tsx / publish-dialog.test.tsx green."
    - "G-RV-03 / WR-02 (WARNING) — LessonEditorClient withdraw trigger onClick and ConfirmModal onCancel now both setWithdrawError(null); no stale 'Action not applied' alert on reopen. Verified in source + lesson-editor-client.test.tsx green."
    - "G-RV-03 / WR-03 (WARNING) — arrange/page.tsx listWithdrawnLessons loop wrapped in its own try/catch mapping AuthorizationError -> notFound() and rethrowing all else; `if (!course || !tree) notFound()` stays outside every catch. Verified in source + arrange-page-route.test.ts green (3/3)."
  gaps_remaining: []
  regressions_introduced: []
  new_warnings_from_review_2:
    - "WR-01 (REVIEW-2) — ArrangeClient.handleRestore (l.201-204) is still an unguarded `await restoreItemAction(...)` with a silent `if (res.ok)` fall-through. Pre-existing code, NOT part of the original gap list, NOT modified by this wave, behaviour identical to before the phase — same ambiguous-mutation class as CR-01 but no capability regression. Recommend tracking for a follow-up."
    - "WR-02 (REVIEW-2) — the seven new `catch {}` blocks in ArrangeClient / ProgrammeArrangeClient / LessonEditorClient are bare and would swallow a NEXT_REDIRECT / NEXT_NOT_FOUND control-flow throw. Latent only: none of the awaited actions (saveModuleOrderAction, saveLessonArrangementAction, saveProgrammeCourseOrderAction, addCourseAction, removeCourseAction, withdrawLessonAction) call redirect()/notFound() today. Recommend an isFrameworkError rethrow guard as hardening."
    - "WR-03 (REVIEW-2) — LessonEditorClient.confirmWithdraw success path (l.52-56) returns without clearing withdrawing/withdrawOpen, relying on navigation unmount; a stalled router.push leaves the modal on 'Working…' with ESC suppressed. Pre-existing, not modified by this wave."
gaps: []
deferred:
  - truth: "Core public, learner, and staff journeys align with WCAG 2.2 AA — accessible media alternatives (NFR-09)"
    addressed_in: "Phase 15 (Launch Readiness)"
    evidence: "REQUIREMENTS.md line 294 maps NFR-09 -> Phase 15 (Pending); Phase 15 SC covers WCAG 2.2 AA for core journeys. 04.1-CAPTIONS-SCOPE.md status OPEN — a new WebVTT upload/data contract (likely a schema migration) is outside the phase 04.1 locked boundary. Explicitly declared out of scope for this verification by the phase owner."
human_verification:
  - test: "Full visual / keyboard-only / Windows High Contrast / font-loading walkthrough per 04.1-UAT.md — 28 mockup screens across 29 routes at 360 / 768 / 1024 / 1440px plus short-height, including the 14 backstop and 7 unresolved state rows and the cross-cutting keyboard / mobile-bulk / mobile-audit / failed-action-retry / font-stack / WHCM / 404-recovery / locked-spacing-exception checks."
    expected: "Every A/B/C row acceptable in a real browser; no capability regression, no keyboard/WHCM break, no clipped content; deviations logged."
    why_human: "Source and jsdom gates grade DOM semantics and class strings, not rendered geometry, the 60/30/10 colour balance, the 48px depth model on dense tables, Windows High Contrast, or the actual loaded font stack (Segoe UI Variable vs Plus Jakarta Sans). D-26 forbids visual-regression tooling this phase."
  - test: "Captions / accessible-media scope decision (04.1-CAPTIONS-SCOPE.md, status OPEN)"
    expected: "Phase owner records: approve the narrow WebVTT follow-up (new upload/data contract, likely a schema migration, carried to Phase 15) OR accept video-without-captions as a known NFR-09 gap carried to Phase 15."
    why_human: "Product/scope decision — cannot be made by the executor or verifier."
  - test: "Explicit user acceptance of the 5 low-severity security advisories (T-04.1-05, T-04.1-09, T-04.1-14, T-04.1-17, T-04.1-22)"
    expected: "User explicitly accepts or requires mitigation; the plan-time 'accept' disposition is not user acceptance."
    why_human: "Risk acceptance is the user's, not the executor's or verifier's (04.1-SECURITY.md, 04.1-GAP-VALIDATION.md Unresolved #5)."
---

# Phase 04.1: Design System Rollout — Re-Verification Report (Gap-Closure Wave 2, plans 32–35)

**Phase Goal:** Roll the approved modern design system across every pre-existing user-facing surface (staff workspace, auth, public catalogue, account, catalogue-authoring components) with no per-screen styling, no loss of any existing capability / route / data / authorization / action contract, and the full automated gate battery (lint, tsc, vitest, build, design-contract) green. Honors decisions D-01..D-26 and the approved CONTEXT over contradictory UI-SPEC generalizations.

**Verified:** 2026-09-07T14:05:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap-closure plans 04.1-32/33/34/35 (prior verification 2026-09-07T05:20Z, status gaps_found, 4/8).

## Goal Achievement

Every actionable code gap from `04.1-VERIFICATION.md` is closed and independently confirmed in
the codebase. The one BLOCKER (G-RV-01 / CR-01 — five unguarded async mutation handlers) is fixed
in source and covered by new rejection-recovery regressions; all four WARNING gaps (G-RV-02
durable design-contract guard, WR-01 modal pending-window focus escape, WR-02 stale withdraw
error, WR-03 arrange-route existence leak) are fixed and regression-tested. All five combined
automated gates were re-run by the verifier this session and are green.

What remains is entirely human-gated and was declared out of scope for this verification by the
phase owner: the `04.1-UAT.md` rendered/keyboard/WHCM/font walkthrough (pending), the captions
scope decision (OPEN, tracked to Phase 15), and explicit user acceptance of the five low-severity
security advisories. NFR-09 accessible-media is deferred to Phase 15 per REQUIREMENTS.md.

Per the verification decision tree, non-empty human-verification section ⇒ **status: human_needed**
(not `passed`). No blockers, no open code gaps.

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | One primitive → semantic → component token chain replaces flat values; no raw component hex | ✓ VERIFIED | `design-contract.test.ts` + `contrast.test.ts` re-run green with no `-t` filter — **127 assertions passed** (was 71). Manifest now grades 65 files across 10 groups. |
| 2 | Amber/teal have indicator and text-safe pairs meeting required contrast ratios | ✓ VERIFIED | `contrast.test.ts` inside the green unfiltered gate; no semantic token changed by the wave (only 4 off-role spacing/size class tokens on shell layouts). |
| 3 | All staff routes use the approved shell/table treatment without per-screen styling | ✓ VERIFIED (code) | New committed manifest groups `staff-shell-access` (13) + `staff-courses-programmes` (13) grade clean with no new locked exception; the one off-role token (`staff/layout.tsx` sign-out `text-xs`→`text-sm`) is repaired, not allow-listed. CR-01 arrange-mutation strand is closed. Rendered 360/768/1024/1440px check = human (UAT row 1). |
| 4 | Six auth routes share one auth layout while preserving their state contracts | ✓ VERIFIED | `auth-shell` manifest group (13 `(auth)` files) grades clean; full suite (state-render + route tests) green; stale `AuthPanel.tsx` locked-exception path repointed to the real `src/app/(auth)/AuthPanel.tsx`. |
| 5 | Public catalogue and account render on the approved design system | ✓ VERIFIED (code) | `public-account-shell` manifest group (12 files incl. `LearnerShell.tsx`, `BrandMark.tsx`, both `not-found.tsx`) grades clean; off-role tokens `px-3.5 py-1.5`→`px-4 py-2` and `text-xs`→`text-[11px]`/`text-sm` repaired. Rendered visual match to the approved mark/scale = human (UAT row 1). |
| 6 | Phase 4 bespoke components are reconciled or explicitly deferred | ✓ VERIFIED | WR-01 closed — `ConfirmModal` container gains `tabIndex={-1}` and both dialogs hold focus on the container through the disabled pending window (`ConfirmModal.tsx:102-106,136`, `PublishDialog.tsx:112-116`); captions explicitly deferred via `04.1-CAPTIONS-SCOPE.md`. |
| 7 | Build and full suite stay green and no state or behavior is lost | ✓ VERIFIED | Verifier re-ran all five gates: `eslint` exit 0; `tsc --noEmit` exit 0; design-contract+contrast 127 passed; `npm test` **107 files / 1401 passed / 1 skipped** exit 0; `next build` exit 0 (24 static pages). CR-01 / WR-02 / WR-03 behaviour parity restored and regression-tested. |
| 8 | Core public, learner, and staff journeys align with WCAG 2.2 AA (NFR-09) | ⏸ DEFERRED | REQUIREMENTS.md maps NFR-09 → Phase 15 (Pending). Keyboard / focus / semantics / error-identification / contrast substantially closed and regression-tested this phase. Accessible media / captions = OPEN scope decision, explicitly out of scope here. Human a11y walkthrough pending (`04.1-UAT.md`). |

**Score:** 7/7 code-verifiable truths verified. Truth 8 (NFR-09) is deferred to Phase 15 by REQUIREMENTS.md and by the phase owner's explicit scope note — not counted as a gap.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | NFR-09 accessible media alternatives (captions / WebVTT) | Phase 15 (Launch Readiness) + a separately scoped captions plan | `REQUIREMENTS.md` line 294 (`NFR-09 | Phase 15 | Pending`); `04.1-CAPTIONS-SCOPE.md` status OPEN — needs a new upload/data contract outside the 04.1 locked boundary. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/app/staff/courses/[id]/arrange/ArrangeClient.tsx` | try/catch/finally on handleSaveModuleOrder + handleSaveLessonArrangement | ✓ VERIFIED | l.102-129 and l.131-158: `try` wraps the awaited action + result branches; `catch` sets a generic non-interpolating message; `finally` clears `setSavingModules/LessonsFalse`. Siblings `handleAddModule` / `handleRenameModule` also guarded. |
| `src/app/staff/programmes/[id]/arrange/ProgrammeArrangeClient.tsx` | try/catch/finally on handleSave + handleAdd + handleRemove | ✓ VERIFIED | l.81-108 / l.110-124 / l.126-142: all three guarded; `finally` clears `setSaving`/`setBusyCourseId`; `handleRemove` early-returns on unknown membershipId **before** `setBusyCourseId`. |
| `src/components/primitives/ConfirmModal.tsx` | Focus trap holds while every control disabled | ✓ VERIFIED | `tabIndex={-1}` on the `role="dialog"` container (l.136); empty focusable list branch `event.preventDefault(); dialogRef.current?.focus()` (l.102-106). Idle wrap / ESC-suppress / focus-return unchanged. |
| `src/components/catalogue/PublishDialog.tsx` | Focus trap holds while every control disabled | ✓ VERIFIED | Container already `tabIndex={-1}`; empty-list branch `preventDefault()` + focus container (l.112-116). |
| `src/app/staff/courses/[id]/lessons/[lessonId]/LessonEditorClient.tsx` | withdrawError cleared on modal open + cancel | ✓ VERIFIED | Trigger onClick `setWithdrawError(null); setWithdrawOpen(true)` (l.150-153); `ConfirmModal onCancel` `setWithdrawError(null); setWithdrawOpen(false)` (l.171-174). |
| `src/app/staff/courses/[id]/arrange/page.tsx` | listWithdrawnLessons loop under AuthorizationError→notFound | ✓ VERIFIED | Loop l.47-62 in its own `try { … } catch (error) { if (error instanceof AuthorizationError) notFound(); throw error; }`. `if (!course || !tree) notFound()` stays outside every catch (l.39). |
| `tests/unit/design-contract.test.ts` | 10 groups / 65 files + anti-vacuity fixtures + repaired exception path | ✓ VERIFIED | `GROUPS` = 10 keys / 65 paths; `EXPECTED_GROUPS` literal (l.233-244); fixtures l.599-634: key deep-equality, per-group non-empty, every manifest file `existsSync`, no cross-group dup, every `LOCKED_EXCEPTIONS.file` `existsSync`. Stale `src/components/shell/AuthPanel.tsx` → `src/app/(auth)/AuthPanel.tsx` (l.123). |
| `tests/components/arrange-client.test.tsx` | Rejection-recovery regression | ✓ VERIFIED | Present; passes (verifier re-ran). |
| `tests/components/programme-arrange-client.test.tsx` | Rejection-recovery regression | ✓ VERIFIED | Present; passes. |
| `tests/components/lesson-editor-client.test.tsx` | Stale-withdraw-error regression | ✓ VERIFIED | Present; passes. |
| `tests/arrange-page-route.test.ts` | Existence-hiding regression (3 cases) | ✓ VERIFIED | Present; passes 3/3 (denial→NEXT_NOT_FOUND, plain Error propagates, happy path). |
| `tests/components/confirm-modal.test.tsx` / `tests/components/publish-dialog.test.tsx` | Pending-window focus-escape regression | ✓ VERIFIED | Both extended; pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `ArrangeClient.tsx` | `arrange/actions.ts` | Save-order Server Actions guarded by try/catch/finally | ✓ WIRED | Rejection now caught, `finally` clears busy state, generic error surfaced. |
| `ProgrammeArrangeClient.tsx` | `programmes/[id]/arrange/actions.ts` | save/add/remove Server Actions guarded by try/catch/finally | ✓ WIRED | All three handlers guarded; `busyCourseId` / `saving` always clear. |
| `ConfirmModal.tsx` / `PublishDialog.tsx` | `dialogRef` container | Tab handler falls back to the container when focusable list empty | ✓ WIRED | `preventDefault()` + `dialogRef.current.focus()`. |
| `LessonEditorClient.tsx` | `ConfirmModal` error prop | withdrawError reset on every modal entry/exit | ✓ WIRED | Cleared on trigger onClick and onCancel. |
| `arrange/page.tsx` | `lesson-service.listWithdrawnLessons` | AuthorizationError from the withdrawn-lesson loop → notFound() | ✓ WIRED | Denial parity restored; non-authorization errors still propagate (route test). |
| `design-contract.test.ts` | 51 newly-manifested source files | `GROUPS` → `gradeFile` → per-file `it()` | ✓ WIRED | 65 files graded, 127 assertions; anti-vacuity fixtures prevent a silently-empty gate. |

### Behavioral Spot-Checks

| Behavior | Method | Result | Status |
|---|---|---|---|
| `npx eslint .` | verifier ran this session | exit 0, no warnings | ✓ PASS |
| `npx tsc --noEmit` | verifier ran this session | exit 0, clean | ✓ PASS |
| `npx vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts` | verifier ran this session | 2 files, **127 passed** | ✓ PASS |
| `npm test` (full suite, Testcontainers Postgres) | verifier ran this session | **107 files, 1401 passed / 1 skipped**, exit 0, 456s | ✓ PASS |
| `npx next build` | verifier ran this session | exit 0, "Compiled successfully", 24 static pages | ✓ PASS |
| Wave regression files (6) — arrange-client, programme-arrange-client, lesson-editor-client, confirm-modal, publish-dialog, arrange-page-route | verifier ran targeted `vitest run` | 32 tests passed | ✓ PASS |
| ArrangeClient / ProgrammeArrangeClient rejected-action recovers | source read + regressions | all 5 handlers try/catch/finally; busy flag cleared in `finally` | ✓ PASS |
| Rendered layout / keyboard / WHCM / font stack | — | `04.1-UAT.md` status: pending — not runnable from source (D-26) | ? SKIP → human |

### Probe Execution

| Probe | Command | Result | Status |
|---|---|---|---|
| (none declared) | — | This phase declares no `scripts/*/tests/probe-*.sh`; verification evidence is the five-gate battery (re-run fresh by the verifier) plus source inspection. | N/A |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| NFR-09 | 15–35 (all declare `requirements: [NFR-09]`) | Accessibility — WCAG 2.2 AA for core public/learner/staff journeys (keyboard, focus, semantics, contrast, error identification, zoom/reflow, accessible media alternatives) | ⏸ DEFERRED (owned by Phase 15) | Keyboard/focus/semantics/error-identification/contrast substantially closed and regression-tested across the phase; the wave-2 plans added focus containment (WR-01), imperative-mutation error identification (CR-01, WR-02), denial parity (WR-03) and a durable type/spacing guard (G-RV-02). Accessible media / captions unimplemented — OPEN scope decision (`04.1-CAPTIONS-SCOPE.md`). Rendered a11y walkthrough (`04.1-UAT.md`) pending. `REQUIREMENTS.md` line 294 tracks `NFR-09 | Phase 15 | Pending` — no orphaned requirement for this phase. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/app/staff/courses/[id]/arrange/ArrangeClient.tsx` | 201-204 | `handleRestore` — unguarded `await restoreItemAction(...)`, silent `if (res.ok)` fall-through | ⚠ Warning | Pre-existing (not touched by this wave, not in the original gap list). Same ambiguous-mutation class as CR-01; `ArrangeBoard`'s Restore button has no busy state so nothing is stranded, but a failed restore is silent. Recommend a follow-up. |
| `src/app/staff/courses/[id]/arrange/ArrangeClient.tsx` `ProgrammeArrangeClient.tsx` `LessonEditorClient.tsx` | new `catch` blocks | Bare `catch { setXError("…") }` — would swallow a `NEXT_REDIRECT` / `NEXT_NOT_FOUND` control-flow throw | ⚠ Warning | Latent only. None of the awaited actions call `redirect()`/`notFound()` today; `withdrawLessonAction` avoids it by documented convention. Recommend an `isFrameworkError` rethrow guard as hardening. |
| `src/app/staff/courses/[id]/lessons/[lessonId]/LessonEditorClient.tsx` | 52-56 | `confirmWithdraw` success path returns without clearing `withdrawing` / `withdrawOpen` | ⚠ Warning | Pre-existing, not modified by this wave. A stalled `router.push` leaves the modal on "Working…" with ESC suppressed. |
| `src/app/account/layout.tsx` | 45-48 | Avatar renders an empty circle when the email local-part has no alnum segments | ℹ Info | Acceptable degradation (carried by `aria-label`); consider a fallback glyph. |

No debt markers (`TODO` / `FIXME` / `XXX` / `TBD` / `HACK` / `PLACEHOLDER`) found in the wave-modified source files. The three warnings above are pre-existing or latent — none is a regression introduced by this wave, and none removes an existing capability. `04.1-REVIEW-2.md` independently graded the delta 0 blockers / 3 warnings / 4 info.

### Human Verification Required

1. **Full visual / keyboard-only / Windows High Contrast / font-loading walkthrough** — run `04.1-UAT.md` in a real browser (28 mockup screens across 29 routes at 360 / 768 / 1024 / 1440px + short-height; 14 backstop + 7 unresolved state rows + the cross-cutting checks). Expected: every A/B/C row acceptable, no capability regression, no keyboard/WHCM break, no clipped content. Why human: source/jsdom tests grade DOM semantics and class strings, not rendered geometry, colour balance, the 48px depth model, WHCM, or the loaded font stack; D-26 forbids visual-regression tooling this phase.
2. **Captions / accessible-media scope decision** — approve the narrow WebVTT follow-up (new upload/data contract, likely a schema migration, carried to Phase 15) OR accept video-without-captions as a known NFR-09 gap carried to Phase 15. Why human: product/scope decision; `04.1-CAPTIONS-SCOPE.md` status OPEN.
3. **Explicit user acceptance of the 5 low-severity security advisories** (`T-04.1-05`, `T-04.1-09`, `T-04.1-14`, `T-04.1-17`, `T-04.1-22`). Why human: risk acceptance is the user's; the plan-time `accept` disposition is not user acceptance.

### Gaps Summary

**No code gaps remain.** All five gaps from `04.1-VERIFICATION.md` (G-RV-01 BLOCKER, G-RV-02,
G-RV-03 WR-01/WR-02/WR-03) are closed and independently verified in the codebase, with new
regressions and all five automated gates re-run green by the verifier
(`eslint` 0, `tsc` 0, design-contract 127, `npm test` 1401/1 skipped, `next build` 0).

The phase cannot be marked `passed` only because three **human-gated** items remain open — all
three explicitly declared out of scope for automated closure by the phase owner:
the `04.1-UAT.md` rendered browser walkthrough, the captions scope decision, and explicit user
acceptance of the five low-severity security advisories. NFR-09 accessible-media is deferred to
Phase 15 per `REQUIREMENTS.md`.

Three warning-tier observations from `04.1-REVIEW-2.md` (handleRestore unguarded; bare catch vs
framework control-flow throws; confirmWithdraw success-path modal state) are pre-existing or
latent, cause no current defect and no capability regression, and are recorded for follow-up
rather than blocking the phase.

## Verdict

**HUMAN VERIFICATION REQUIRED.** The gap-closure wave (plans 04.1-32/33/34/35) fully closed every
actionable code gap from the prior re-verification. Source inspection confirms all five fixes;
six regression files and the whole 1401-test suite pass; lint, types, design-contract (71→127
assertions) and production build are all green when re-run by the verifier. Code-verifiable truths
score 7/7. What blocks `passed` is entirely human: the `04.1-UAT.md` visual / keyboard / WHCM /
font walkthrough (pending), the captions scope decision (OPEN, → Phase 15), and explicit user
acceptance of the five low-severity security advisories. NFR-09 accessible-media is deferred to
Phase 15 by REQUIREMENTS.md.

---

_Verified: 2026-09-07T14:05:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification after gap-closure plans 04.1-32/33/34/35 — prior report preserved at `04.1-VERIFICATION.md`_

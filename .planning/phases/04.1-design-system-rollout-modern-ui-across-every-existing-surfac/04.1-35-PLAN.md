---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "35"
type: execute
wave: 2
depends_on: ["04.1-32", "04.1-33", "04.1-34"]
files_modified:
  - "tests/unit/design-contract.test.ts"
  - "src/app/staff/layout.tsx"
  - "src/app/(public)/layout.tsx"
  - "src/app/account/layout.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every restyled auth, staff shell/access/audit, staff courses/programmes and public/account/shell file is graded by a committed design-contract group, so a future edit that breaks a type or spacing role fails CI."
    - "No design-contract describe group can be silently renamed or dropped, and no manifest or locked-exception entry can point at a file that does not exist."
    - "The locked D-21 / D-23 exceptions still apply only at their exact file and token and do not leak."
    - "The full combined automated gate battery is green after the whole re-verification gap wave."
  artifacts:
    - path: "tests/unit/design-contract.test.ts"
      provides: "Manifest groups auth-shell, public-account-shell, staff-shell-access, staff-courses-programmes plus anti-vacuity and anti-stale guards"
      contains: "auth-shell"
    - path: "src/app/staff/layout.tsx"
      provides: "Sign-out control label at the 14px control role"
    - path: "src/app/(public)/layout.tsx"
      provides: "Sign-in control padding on the spacing grid"
    - path: "src/app/account/layout.tsx"
      provides: "Avatar initials at the micro role and sign-out label at the control role"
  key_links:
    - from: "tests/unit/design-contract.test.ts"
      to: "the ~51 newly manifested restyled source files"
      via: "GROUPS manifest -> gradeFile -> per-file it() assertion"
      pattern: "GROUPS"
---

<objective>
Close **G-RV-02 (WARNING)** from `04.1-VERIFICATION.md`: `tests/unit/design-contract.test.ts`'s
`GROUPS` manifest (l.142-166) lists only 14 files (primitives + catalogue), so roughly 38 restyled
auth / staff / public / account / shell files have **no committed mechanical guard** — and the
"focused contract" verify commands in plans 25 / 27 / 28 ran **vacuously** against describe groups
that never existed (exit 0, zero assertions). The stale `LOCKED_EXCEPTIONS` entry at l.123 points at
`src/components/shell/AuthPanel.tsx`, a path that does not exist (the real file is
`src/app/(auth)/AuthPanel.tsx`).

This plan adds the four missing manifest groups, repairs the stale exception path, adds durable
guards that make a vacuous group or a dead path fail loudly, fixes the four off-role tokens the
newly-graded shell files actually carry, and — since it lands last in the wave — re-runs the
combined automated gate battery once for the whole re-verification gap wave.

Purpose: give truths 3 ("All staff routes use the approved shell/table treatment without per-screen
styling") and 5 ("Public catalogue and account render on the approved design system") a durable
regression guard instead of one-off agent sweeps.
Output: an expanded, self-validating design contract and a green combined gate record.

Scope discipline: this is a **source-contract and manifest** plan. It repairs only the off-role
tokens the checker actually reports; it does not restyle, restructure or re-copy anything. No new
route, data model, permission, dependency, dark mode or visual-regression tooling (D-20, D-26).
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE-2.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-VALIDATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UAT.md
</context>

## Planner Assumptions

- No dependency install, schema change, new permission or new route is required. ASVS level 1;
  `high` is the blocking threshold.
- The file lists below were derived by cross-checking the `files_modified` frontmatter of plans
  `04.1-16`, `24`, `25`, `26`, `27`, `28`, `30`, `31`, plus the three shell entry files
  (`staff/layout.tsx`, `(public)/layout.tsx`, `account/layout.tsx`) the verification names explicitly.
- The planner ran the committed checker logic (`gradeFile` semantics, same scales, same locked
  exceptions) over all 51 candidate files. **Exactly four off-role tokens exist**, all in the three
  shell entry files listed in `files_modified`; the other 48 files already grade clean. Any further
  violation the executor finds is new and must be reported, not silently allow-listed.
- Committed state of the three STATE.md "Wave 3 carry-forward" judgement calls, verified by the
  planner: `gap-5` is present **only** in `src/app/(auth)/layout.tsx` (already the locked exception)
  and **not** in `src/app/(auth)/AuthPanel.tsx`; `px-7 py-3.5` is present in
  `src/components/shell/LearnerShell.tsx` (already the locked exception, and `px-7 py-2` on the same
  file's mobile row is covered by the same `px-7` token); `StaffAccountForm`'s success `<h2>` is
  `text-base`, which is the permitted 16px section-heading role and needs **no** exception. The
  verbatim UI-SPEC §6.2 not-found copy is copy, not a class role — it must not be touched.
- This plan is wave 2 and depends on `04.1-32` / `04.1-33` / `04.1-34` so the manifest grades their
  post-repair source exactly once.
- Estimate factor 1, sample_count 0, confidence low.
- Preserve working-tree changes and do not commit (parent execution policy).

<tasks>

<task type="auto">
  <name>Task 1: Anti-vacuity guards, stale exception repair, and the auth + public/account/shell groups</name>
  <files>tests/unit/design-contract.test.ts, src/app/(public)/layout.tsx, src/app/account/layout.tsx</files>
  <read_first>
    tests/unit/design-contract.test.ts (the whole file — `PRIMITIVE_FONT_PX` / `DISPLAY_FONT_PX` l.45-48, `FONT_SIZE_KEYWORDS` l.50, `ACTIVE_WEIGHTS` l.78, `ALLOWED_SPACING_PX` l.84, `LOCKED_EXCEPTIONS` l.116-132 including the stale `src/components/shell/AuthPanel.tsx` entry at l.123, `isLockedException` l.134, the `GROUPS` manifest l.142-167, `allowedFontPx` l.171, `gradeFile` l.372, the per-group `describe` loop l.388-397 and the `describe("contract checker fixtures — enforcement is non-vacuous", …)` block l.403-517);
    src/app/(public)/layout.tsx (the Sign-in `<Link>` className carrying `px-3.5 py-1.5`);
    src/app/account/layout.tsx (the avatar `<span role="img">` className carrying `text-xs`, and the sign-out `<button type="submit">` className carrying `text-xs`);
    src/app/staff/layout.tsx (the same sign-out control — repaired in Task 2, read now for consistency of the chosen role);
    src/components/shell/LearnerShell.tsx and src/app/(auth)/layout.tsx (the two files the locked D-23 / D-21 exceptions key to);
    src/app/(auth)/AuthPanel.tsx (the real path the stale exception should name);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md (the truth-3 and truth-5 gaps blocks and the two `design-contract.test.ts` Anti-Patterns rows);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md ("Scope and source precedence" — the D-21 / D-23 named-exception rule and the prohibition on indiscriminate dimension snapping);
    .planning/STATE.md ("Wave 3 carry-forward" — the three judgement calls)
  </read_first>
  <action>
    Work in `tests/unit/design-contract.test.ts` first.

    1. **Repair the stale locked exception.** Change the `LOCKED_EXCEPTIONS` entry whose `file` is
       `src/components/shell/AuthPanel.tsx` to the real path `src/app/(auth)/AuthPanel.tsx`, keeping
       its `gap-5` token set and its D-21 rationale string. Do not widen it, do not add tokens, and
       do not touch the `src/app/(auth)/layout.tsx` or `src/components/shell/LearnerShell.tsx`
       entries.

    2. **Add anti-stale / anti-vacuity guards** as new `it()` cases inside the existing
       `describe("contract checker fixtures — enforcement is non-vacuous", …)` block. Declare a
       module-level `EXPECTED_GROUPS` array literal naming all ten group keys — the six existing
       (`primitives-table`, `primitives-forms`, `catalogue-controls-entry`,
       `catalogue-controls-expansion`, `catalogue-reading-entry`, `catalogue-reading-expansion`) and
       the four added by this plan (`auth-shell`, `public-account-shell`, `staff-shell-access`,
       `staff-courses-programmes`) — then assert:
       - `Object.keys(GROUPS)` sorted deep-equals `EXPECTED_GROUPS` sorted, so a renamed or dropped
         group fails loudly instead of making a future `-t <group>` run vacuous;
       - every group has at least one file;
       - every manifest file exists on disk (`existsSync` against `REPO_ROOT`);
       - no relative path appears in two groups;
       - every `LOCKED_EXCEPTIONS[].file` exists on disk, so a dead exception path can never be
         reintroduced.

    3. **Add the `auth-shell` group** with exactly these 13 paths:
       `src/app/(auth)/layout.tsx`, `src/app/(auth)/AuthPanel.tsx`, `src/app/(auth)/signin/page.tsx`,
       `src/app/(auth)/signin/SignInForm.tsx`, `src/app/(auth)/register/page.tsx`,
       `src/app/(auth)/register/RegisterForm.tsx`, `src/app/(auth)/forgot-password/page.tsx`,
       `src/app/(auth)/forgot-password/ForgotPasswordForm.tsx`,
       `src/app/(auth)/reset-password/page.tsx`, `src/app/(auth)/reset-password/ResetPasswordForm.tsx`,
       `src/app/(auth)/verify/page.tsx`, `src/app/(auth)/verify/ResendVerificationForm.tsx`,
       `src/app/(auth)/confirm-email-change/page.tsx`.

    4. **Add the `public-account-shell` group** with exactly these 12 paths:
       `src/app/(public)/layout.tsx`, `src/app/(public)/courses/page.tsx`,
       `src/app/(public)/courses/[slug]/page.tsx`, `src/app/(public)/programmes/page.tsx`,
       `src/app/(public)/programmes/[slug]/page.tsx`, `src/app/(public)/not-found.tsx`,
       `src/app/not-found.tsx`, `src/app/account/layout.tsx`, `src/app/account/page.tsx`,
       `src/app/account/ProfileForm.tsx`, `src/components/shell/LearnerShell.tsx`,
       `src/components/shell/BrandMark.tsx`.

    5. **Repair the two off-role tokens the new `public-account-shell` group reports**, and nothing
       else: in `src/app/(public)/layout.tsx` move the Sign-in link's `px-3.5 py-1.5` onto the
       compact-control roles (`px-4 py-2`, the same padding every other bordered control in this
       codebase uses); in `src/app/account/layout.tsx` move the avatar initials' `text-xs` to the
       11px micro role (`text-[11px]`) and the sign-out submit button's `text-xs` to the 14px
       control/body role (`text-sm`), per UI-SPEC §4.3 ("labels / control / body are 14px; micro /
       eyebrow is 11px"). Change no other class, no structural dimension (`size-[30px]`,
       `max-w-[1080px]`, `h-[38px]`), no copy, no markup, no guard and no `LearnerShell` prop.

    Do NOT add a new locked exception for any of these three files. Do NOT alter the D-21 `gap-5` or
    D-23 `px-7` / `py-3.5` exceptions. Do NOT change the scales, the grid, the extraction logic, the
    grading logic or any pre-existing fixture case. All verification for this plan runs the whole
    `design-contract.test.ts` file with **no `-t` filter** — never re-use the vacuous
    `-t <group>` pattern that caused this gap.
  </action>
  <acceptance_criteria>
    - `LOCKED_EXCEPTIONS` contains no path that fails `existsSync`, and the former
      `src/components/shell/AuthPanel.tsx` entry now reads `src/app/(auth)/AuthPanel.tsx` with its
      `gap-5` token and D-21 rationale intact.
    - A module-level `EXPECTED_GROUPS` literal names all ten group keys, and fixture cases assert:
      group-key deep equality with `GROUPS`, every group non-empty, every manifest file exists, no
      duplicate file across groups, every locked-exception file exists.
    - `GROUPS` contains an `auth-shell` group with exactly the 13 listed paths and a
      `public-account-shell` group with exactly the 12 listed paths.
    - `src/app/(public)/layout.tsx` no longer contains `px-3.5` or `py-1.5`;
      `src/app/account/layout.tsx` no longer contains `text-xs`; no other class token in either file
      changed, and no copy, markup, prop or guard changed.
    - `npx.cmd vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts` passes with
      no `-t` filter, and the run reports strictly more passing assertions than the 71 recorded in
      `04.1-GAP-VALIDATION.md`.
  </acceptance_criteria>
  <verify>
    <automated>npx.cmd vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts</automated>
    <fails_when>The command exits nonzero, any added group grades a violation that is silenced by a new exception rather than repaired, the anti-vacuity fixtures do not fail when a group key is renamed, or the run reports 71 or fewer assertions (proving the manifest did not actually grow).</fails_when>
  </verify>
  <done>The auth and public/account/shell surfaces are graded by committed manifest groups, the stale locked-exception path is gone, dead paths and dropped groups now fail loudly, and the three reported off-role tokens on the two learner-shell entry files are repaired.</done>
</task>

<task type="auto">
  <name>Task 2: Staff shell/access and staff courses/programmes manifest groups</name>
  <files>tests/unit/design-contract.test.ts, src/app/staff/layout.tsx</files>
  <read_first>
    tests/unit/design-contract.test.ts (post-Task-1 state: `EXPECTED_GROUPS`, `GROUPS`, the fixture guards);
    src/app/staff/layout.tsx (the sign-out `<button type="submit">` className carrying `text-xs`; the `NAV` array and the `getCurrentActor` / `isStaff` guard, which must not be touched);
    src/app/staff/StaffShell.tsx (the shell chrome the layout renders into — read to confirm the sign-out control's role in the header);
    src/app/account/layout.tsx (the identical sign-out repair made in Task 1 — use the same role);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md (the truth-3 gaps block naming StaffShell, staff/layout, the 12 staff access/audit files and the ~12 staff courses/programmes files);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md ("Scope and source precedence")
  </read_first>
  <action>
    Extend `tests/unit/design-contract.test.ts`.

    1. **Add the `staff-shell-access` group** with exactly these 13 paths:
       `src/app/staff/layout.tsx`, `src/app/staff/StaffShell.tsx`, `src/app/staff/audit/AuditTable.tsx`,
       `src/app/staff/roles/RolesTable.tsx`, `src/app/staff/roles/RoleForm.tsx`,
       `src/app/staff/roles/RoleDetailPanels.tsx`, `src/app/staff/roles/PermissionPicker.tsx`,
       `src/app/staff/roles/EffectiveAccessPreview.tsx`, `src/app/staff/roles/[id]/page.tsx`,
       `src/app/staff/users/UsersTable.tsx`, `src/app/staff/users/AssignmentDrawer.tsx`,
       `src/app/staff/users/AssignmentsPanel.tsx`, `src/app/staff/users/StaffAccountForm.tsx`.

    2. **Add the `staff-courses-programmes` group** with exactly these 13 paths:
       `src/app/staff/courses/CoursesTable.tsx`, `src/app/staff/courses/[id]/page.tsx`,
       `src/app/staff/courses/[id]/arrange/page.tsx`,
       `src/app/staff/courses/[id]/arrange/ArrangeClient.tsx`,
       `src/app/staff/courses/[id]/arrange/ModuleComposer.tsx`,
       `src/app/staff/courses/[id]/lessons/[lessonId]/LessonEditorClient.tsx`,
       `src/app/staff/courses/[id]/preview/page.tsx`,
       `src/app/staff/courses/[id]/preview/lessons/[lessonId]/page.tsx`,
       `src/app/staff/programmes/ProgrammesTable.tsx`, `src/app/staff/programmes/ProgrammeForm.tsx`,
       `src/app/staff/programmes/[id]/page.tsx`,
       `src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx`,
       `src/app/staff/programmes/[id]/arrange/ProgrammeArrangeClient.tsx`.

    3. Confirm both new keys are already present in the `EXPECTED_GROUPS` literal added in Task 1;
       if not, add them there so the group-key deep-equality fixture stays true.

    4. **Repair the single off-role token the `staff-shell-access` group reports**: in
       `src/app/staff/layout.tsx` move the sign-out submit button's `text-xs` to the 14px
       control/body role (`text-sm`), matching the identical repair made to `src/app/account/layout.tsx`
       in Task 1. Change nothing else in that file — not the `NAV` array, not the `getCurrentActor` /
       `isStaff` / `LEARNER_LANDING_PATH` guard, not the `profileService.getOwnProfile` degradation
       comment, not the `signOutAction` form, not any copy.

    Do NOT add a locked exception for `staff/layout.tsx` or for any file in either new group. If a
    file in these groups grades a violation that is NOT the `staff/layout.tsx` `text-xs` named here,
    stop and report it in the SUMMARY as a newly discovered off-role token rather than allow-listing
    it or restyling beyond the reported token. Verification runs the whole file with **no `-t`
    filter**.
  </action>
  <acceptance_criteria>
    - `GROUPS` contains a `staff-shell-access` group with exactly the 13 listed paths and a
      `staff-courses-programmes` group with exactly the 13 listed paths, and `EXPECTED_GROUPS`
      deep-equality still holds.
    - `src/app/staff/layout.tsx` no longer contains `text-xs`; its `NAV` array, auth guard, profile
      degradation and `signOutAction` form are unchanged.
    - The manifest now grades 65 files across 10 groups with no violations and no new locked
      exception.
    - `tests/components/staff-shell.test.tsx`, `tests/components/audit-table.test.tsx`,
      `tests/components/courses-table.test.tsx` and `tests/components/programme-detail-client.test.tsx`
      still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx.cmd vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts &amp;&amp; npx.cmd vitest run --project components tests/components/staff-shell.test.tsx tests/components/audit-table.test.tsx tests/components/courses-table.test.tsx tests/components/programme-detail-client.test.tsx</automated>
    <fails_when>The command exits nonzero, a new locked exception was added to silence a violation, a group is missing a listed path, or a staff component regression breaks.</fails_when>
  </verify>
  <done>Every restyled staff shell, access/audit and courses/programmes file is graded by a committed manifest group, and the one reported off-role token is repaired rather than allow-listed.</done>
</task>

<task type="auto">
  <name>Task 3: Combined automated gate battery for the re-verification gap wave</name>
  <files>(no source changes — evidence only)</files>
  <read_first>
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-VALIDATION.md (the five-gate battery format and the previous recorded results: `npm test` 1331 passed / 1 skipped, design-contract + contrast 71 passed with no `-t`, `npx eslint` clean, `npx tsc --noEmit` clean, `npm run build` 24 static pages);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UAT.md (the pending human walkthrough rows — referenced only, never marked done);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md ("Human Verification Required" — the three items this plan must leave open);
    .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE-2.md (the gap -> plan mapping this wave closes);
    package.json (the `test`, `lint` and `build` scripts)
  </read_first>
  <action>
    Run the five combined gates once, in order, from the repository root, over the tree containing
    plans `04.1-32`, `04.1-33`, `04.1-34` and Tasks 1–2 of this plan: `npx.cmd eslint`;
    `npx.cmd tsc --noEmit`; the unfiltered design-contract + contrast source gate; `npm test`
    (`vitest run --no-file-parallelism`, Testcontainers-backed, expect roughly 12 minutes); and
    `npm run build`.

    Record in `04.1-35-SUMMARY.md`, per gate: the exact command, exit code, the pass/fail counts (for
    `npm test`, the test-file count and the passed / skipped totals; for the source gate, the
    assertion count, which must exceed the 71 recorded in `04.1-GAP-VALIDATION.md`) and a timestamp.
    Also record the four new or extended regression files this wave produced
    (`tests/components/arrange-client.test.tsx`, `tests/components/programme-arrange-client.test.tsx`,
    `tests/components/lesson-editor-client.test.tsx`, `tests/arrange-page-route.test.ts`) and the two
    extended ones (`tests/components/confirm-modal.test.tsx`,
    `tests/components/publish-dialog.test.tsx`).

    Then record, verbatim and unresolved, the three items this wave does **not** close: the
    `04.1-UAT.md` browser walkthrough (28 mockup screens across 29 routes at 360 / 768 / 1024 /
    1440px plus short-height, including the 14 backstop and 7 unresolved state rows); the captions /
    accessible-media scope decision (`04.1-CAPTIONS-SCOPE.md`, status OPEN, tracked to Phase 15); and
    explicit user acceptance of the five low-severity security advisories `T-04.1-05`, `T-04.1-09`,
    `T-04.1-14`, `T-04.1-17`, `T-04.1-22`.

    Do NOT modify, overwrite or renumber `04.1-GAP-VALIDATION.md`, `04.1-VERIFICATION.md`,
    `04.1-REVIEW.md`, `04.1-UAT.md`, `04.1-GAP-COVERAGE.md` or any historical PLAN / SUMMARY — the
    evidence goes in this plan's own SUMMARY. Do NOT claim a rendered, visual, High-Contrast or
    font-loading result from a source or jsdom run. Do NOT mark NFR-09, `04.1-UAT.md` or phase 04.1
    complete.
  </action>
  <acceptance_criteria>
    - All five gates were run in this tree and each result — command, exit code, counts, timestamp —
      is recorded in `04.1-35-SUMMARY.md`.
    - The design-contract + contrast gate ran with **no `-t` filter** and reports more assertions
      than the 71 previously recorded.
    - The SUMMARY lists the six regression files this wave added or extended.
    - The SUMMARY records the UAT walkthrough, the captions scope decision and the five security
      advisories as still OPEN, with no fabricated visual or High-Contrast evidence.
    - No pre-existing audit, verification, review or historical plan/summary file was modified.
  </acceptance_criteria>
  <verify>
    <automated>npx.cmd eslint &amp;&amp; npx.cmd tsc --noEmit &amp;&amp; npx.cmd vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts &amp;&amp; npm test &amp;&amp; npm run build</automated>
    <fails_when>Any gate exits nonzero, the source gate is run with a `-t` filter, the recorded counts do not match the observed output, or the SUMMARY claims a human/visual result no gate produced.</fails_when>
  </verify>
  <done>The whole re-verification gap wave is proven green against the five combined gates, with the three human-gated items recorded as still open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source contract → CI signal | A manifest that silently grades zero files reports success it did not earn; the guard must be non-vacuous |
| Evidence → completion claim | Automated source/jsdom gates must not be presented as rendered visual, keyboard, WHCM or accessible-media evidence |
| Style repair → running behaviour | A class-token repair must not touch an authorization guard, a route, copy or a data path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G35-01 | Repudiation | `tests/unit/design-contract.test.ts` | high | mitigate | `EXPECTED_GROUPS` deep-equality, per-group non-emptiness, on-disk existence of every manifest and locked-exception path, and duplicate detection make a vacuous or stale gate fail loudly. All verification runs the file with no `-t` filter. |
| T-04.1-G35-02 | Elevation of Privilege | `src/app/staff/layout.tsx`, `src/app/account/layout.tsx` | high | mitigate | Only a class token changes in each file; the `getCurrentActor` / `isStaff` / `redirect` guards, `signOutAction` and `profileService.getOwnProfile` degradation are untouched, and the staff component regressions must stay green. |
| T-04.1-G35-03 | Information Disclosure | Restyled public / account surfaces | medium | mitigate | No copy, markup, prop or data path changes; RBAC-06 denial-parity copy and the verbatim UI-SPEC §6.2 not-found wording are explicitly out of scope for this plan. |
| T-04.1-G35-04 | Tampering | Locked exceptions | medium | mitigate | Exceptions stay keyed to an exact file + exact token; no new exception may be added to silence a violation, and the existing leak fixtures must remain green. |
| T-04.1-G35-05 | Repudiation | Completion claim | high | mitigate | The SUMMARY must record the UAT walkthrough, captions scope decision and five security advisories as still OPEN; no rendered/visual/WHCM/font result may be inferred from a source or jsdom run (D-26 forbids visual-regression tooling this phase). |
| T-04.1-G35-SC | Tampering | npm / pip / cargo installs | n/a | mitigate | No package install occurs in this plan or wave; the `04.1-RESEARCH.md` package-legitimacy position is unchanged and no `[ASSUMED]` / `[SUS]` package is introduced. |
</threat_model>

<verification>
Run each task command from the repository root. Tasks 1 and 2 must always run
`tests/unit/design-contract.test.ts` with **no `-t` filter** — the vacuous `-t <group>` pattern is
the root cause of this gap and must not be repeated. Task 3's battery is the single combined gate
run for the whole re-verification wave; plan `04.1-29` remains the phase's historical capstone and
is not superseded or rewritten. Source and jsdom gates grade class strings and DOM semantics, never
rendered geometry, colour balance, the 48px depth model, Windows High Contrast or the loaded font
stack. The `04.1-UAT.md` walkthrough, the captions / accessible-media scope decision and the five
low-severity security advisories remain OPEN.
</verification>

<success_criteria>
All task acceptance criteria pass; the design contract grades 65 files across 10 groups with zero
violations and zero new exceptions; the anti-vacuity and anti-stale guards are committed; the five
combined gates are green and recorded with exact counts.

**Do NOT mark NFR-09 or phase 04.1 complete.** The `04.1-UAT.md` human visual / keyboard-only /
Windows High Contrast / font-loading walkthrough is still pending, the captions / accessible-media
scope decision is still OPEN, and the five low-severity security advisories still lack explicit user
acceptance.
</success_criteria>

## Artifacts Produced

- `tests/unit/design-contract.test.ts`
- `src/app/staff/layout.tsx`
- `src/app/(public)/layout.tsx`
- `src/app/account/layout.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-35-SUMMARY.md` (execution evidence, including the five-gate record)

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-35-SUMMARY.md`.
Report the manifest expansion, the exact off-role tokens repaired, the five combined gate results
with counts and timestamps, and the three still-open human-gated items. Do not overwrite, renumber
or edit any historical plan, SUMMARY or audit file.
</output>

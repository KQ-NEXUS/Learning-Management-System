---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 10
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/app/staff/courses/CoursesTable.tsx
  - src/app/staff/courses/[id]/page.tsx
  - src/app/staff/courses/[id]/arrange/page.tsx
  - src/app/staff/courses/[id]/arrange/ArrangeClient.tsx
  - src/app/staff/courses/[id]/arrange/ModuleComposer.tsx
  - src/app/staff/courses/[id]/lessons/[lessonId]/LessonEditorClient.tsx
  - src/app/staff/courses/[id]/preview/page.tsx
  - src/app/staff/courses/[id]/preview/lessons/[lessonId]/page.tsx
autonomous: true
requirements: [NFR-09]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every courses-area screen renders one neutral family — the semantic tokens — with no surviving utility from Tailwind's built-in ramp, so the workspace does not ship two greys side by side (D-14)."
    - "Every input, select, textarea and checkbox on these screens carries an edge clearing 3:1 (D-15, NFR-09)."
    - "Micro-text on these screens — hints, counts, timestamps, the not-yet-checked markers — resolves through the muted-foreground token (D-16)."
    - "Only two font weights are in play across these files: 400 and 600 (UI-SPEC 4.3)."
    - "The public-page preview still shows exactly what an anonymous visitor would see, and the learner-view preview still renders the same navigation a learner would have — neither gains a staff-only affordance during the restyle."
    - "The unsaved-order guard, the arrange-board wrappers and the lesson editor keep every binding, every dirty-state signal and every server-action call unchanged."
    - "The bulk-action handlers in the courses table are left exactly as they are — this phase does not implement them, and it does not remove them either."
  prohibitions:
    - statement: "MUST NOT add any control, badge or link to a preview screen that an anonymous visitor or a learner would not see — the preview's entire value is that it is an honest rehearsal of what someone else gets."
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT introduce a delete affordance on a course, module or lesson row — this product archives and never deletes, and a row-action restyle is exactly where such a control appears by analogy."
      category: safety
      status: unverified
      verification: judgment
  artifacts:
    - path: "src/app/staff/courses/[id]/preview/page.tsx"
      provides: "Public-page preview on the approved system with its anonymous-visitor fidelity intact"
      contains: "muted-foreground"
  key_links:
    - from: "src/app/staff/courses/[id]/arrange/ArrangeClient.tsx"
      to: "src/components/catalogue/ArrangeBoard.tsx"
      via: "thin wrapper rendering the board; the board's own restyle lands in plan 12"
      pattern: "ArrangeBoard"
---

<objective>
Sweep the eight staff courses-area screens onto the semantic tokens, so no legacy neutral survives
anywhere in the course authoring flow.

Purpose: these files render inside the rebuilt shell and mostly configure the restyled primitives,
but each carries its own local markup — a header, a hint line, a wrapper panel — that inherits
nothing automatically. Leaving them is how the "two neutral families coexisting" failure D-14 exists
to prevent actually happens, and no lint rule catches it.

Output: eight files with local markup on the approved tokens and every behaviour untouched.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The course list and detail screens</name>
  <files>src/app/staff/courses/CoursesTable.tsx, src/app/staff/courses/[id]/page.tsx</files>
  <read_first>
    - `src/app/staff/courses/CoursesTable.tsx` — it configures the list primitive, so most of its
      appearance now comes from plan 03. Note the bulk-action entries whose `onClick` handlers are
      currently empty; those are a carried gap, not this phase's work.
    - `src/app/staff/courses/[id]/page.tsx` — the whole file. It configures the detail primitive and
      renders its own local markup around it; find every local class string.
    - `src/components/primitives/ResourceTable.tsx` and `src/components/primitives/DetailLayout.tsx`
      as restyled in plans 03 and 04 — the treatments this file's local markup must match.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md`
      — the published utility class names.
  </read_first>
  <action>
    In both files, substitute every utility drawn from Tailwind's built-in neutral ramp with the
    semantic token by role: 500-level neutral text becomes `text-muted-foreground`; 600 and darker
    neutral text becomes `text-foreground`; a 300-level neutral edge on an interactive control
    becomes `border-input-border`; a 200-level neutral edge on a panel or divider becomes
    `border-border`; 50/100-level neutral fills become `bg-surface-2`; and `bg-white` becomes
    `bg-surface`. Fold every weight-500 font utility to `font-semibold`.

    Where either file renders a local panel or card, give it the same treatment the primitives now
    use: `bg-surface`, `border border-border`, `rounded-xl`, and the low shadow on dense surfaces or
    the deep card shadow on a sparse single card.

    Leave the bulk-action entries and their empty handlers exactly as they are. They are a carried
    gap recorded in project state; implementing them is new capability and removing them silently
    drops a planned affordance. Neither belongs in a restyle.

    Add no row-level delete affordance anywhere: this product archives and never deletes.
  </action>
  <acceptance_criteria>
    - Neither file contains a utility drawn from Tailwind's built-in neutral ramp, a weight-500 font
      utility, or a raw hex.
    - The bulk-action entries and their handlers are unchanged.
    - No delete control was added to any row.
    - `npx next build` exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'zinc-' 'src/app/staff/courses/CoursesTable.tsx' 'src/app/staff/courses/[id]/page.tsx'</automated>
    <fails_when>either file prints a number other than 0</fails_when>
    <automated>grep -c 'font-medium' 'src/app/staff/courses/CoursesTable.tsx' 'src/app/staff/courses/[id]/page.tsx'</automated>
    <fails_when>either file prints a number other than 0</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or the output names either file</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
  </verify>
  <done>The course list and detail screens carry no legacy neutral utility and their local panels match the primitives.</done>
</task>

<task type="auto">
  <name>Task 2: The arrange and lesson-editor screens</name>
  <files>src/app/staff/courses/[id]/arrange/page.tsx, src/app/staff/courses/[id]/arrange/ArrangeClient.tsx, src/app/staff/courses/[id]/arrange/ModuleComposer.tsx, src/app/staff/courses/[id]/lessons/[lessonId]/LessonEditorClient.tsx</files>
  <read_first>
    - All four files in full. `ArrangeClient.tsx` and `ModuleComposer.tsx` wrap the arrange board;
      `LessonEditorClient.tsx` hosts the rich-text editor and the upload panel. Note every
      server-action call, every dirty-state signal, and every `useState`/`useTransition` binding.
    - `src/components/catalogue/UnsavedOrderGuard.tsx` — the guard these screens rely on. It is not
      in this plan's scope; confirm nothing here changes what it receives.
    - `src/components/primitives/ResourceForm.tsx` as restyled in plan 04 — reuse its control edge
      and card treatment for any local form markup here.
  </read_first>
  <action>
    Apply the same role-based substitution as task 1 across all four files, and fold every weight-500
    font utility to `font-semibold`.

    Give any local panel the card treatment — `bg-surface`, `border border-border`, `rounded-xl` —
    with the low shadow on a dense arrange surface and the deep card shadow on a sparse editor card.
    Give any local input, select or textarea the interactive edge `border-input-border` at
    `rounded-md`.

    Change nothing about behaviour: every server-action call, every dirty-state signal fed to the
    unsaved-order guard, every transition and every prop passed down to the arrange board or the
    editor stays exactly as it is. The board's and the editor's own restyles land in plan 12; these
    four files are wrappers and must not pre-empt them.
  </action>
  <acceptance_criteria>
    - None of the four files contains a utility drawn from Tailwind's built-in neutral ramp, a
      weight-500 font utility, or a raw hex.
    - Every server-action call and every prop passed to the arrange board or the editor is unchanged.
    - The dirty-state signal fed to the unsaved-order guard is unchanged.
    - `npx vitest run tests/reorder.test.ts tests/module-lesson-service.test.ts` passes.
    - `npx next build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn 'zinc-' 'src/app/staff/courses/[id]/arrange' 'src/app/staff/courses/[id]/lessons' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn 'font-medium' 'src/app/staff/courses/[id]/arrange' 'src/app/staff/courses/[id]/lessons' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx vitest run tests/reorder.test.ts tests/module-lesson-service.test.ts</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
  </verify>
  <done>The arrange and lesson-editor wrappers are on the approved tokens with every binding and dirty-state signal intact.</done>
</task>

<task type="auto">
  <name>Task 3: The two preview screens, including the learner-preview banner</name>
  <files>src/app/staff/courses/[id]/preview/page.tsx, src/app/staff/courses/[id]/preview/lessons/[lessonId]/page.tsx</files>
  <read_first>
    - `src/app/staff/courses/[id]/preview/page.tsx` — the whole file, including its head comment
      stating it shows exactly what an anonymous visitor sees, and its explanatory line about
      previewing an unlisted course being the ordinary case.
    - `src/app/staff/courses/[id]/preview/lessons/[lessonId]/page.tsx` — the whole file, including
      its head comment about learner-equivalent navigation and withdrawn lessons, and its existing
      preview banner line reading "Preview — the learner view of this lesson."
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 1's icon inventory (the `Eye` glyph is this screen's icon) and section 6.2's
      learner-preview banner row.
    - `src/app/(public)/courses/[slug]/page.tsx` as restyled in plan 08 — the public preview should
      read as the same treatment the real public page now has, since that is what it is previewing.
  </read_first>
  <action>
    Restyle the existing preview banner into the approved treatment: a full-width strip on
    `bg-surface-2` with a `border-border` edge at `rounded-md`, an `Eye` glyph marked `aria-hidden`
    imported as a named per-icon import from `lucide-react`, the banner text at 14px, and the return
    link in `text-accent`.

    Keep the shipped banner wording exactly as it reads today. UI-SPEC section 6.2 draws a longer
    banner that additionally asserts nothing in the preview is recorded as progress; that is a
    product claim about a system this phase does not touch, and section 6.2's own closing note
    sanctions keeping shipped copy that already says the same thing. Record the divergence in the
    summary rather than adopting the extra sentence.

    Bring the public-page preview's card and type treatment into line with the real public catalogue
    detail page as restyled in plan 08, so the preview keeps being an honest rehearsal of what an
    anonymous visitor gets. Add nothing to either preview that an anonymous visitor or a learner
    would not see — no staff-only badge, no edit shortcut, no internal identifier. Keep both head
    comments and both explanatory lines.

    Apply the same role-based neutral substitution and weight fold as task 1.
  </action>
  <acceptance_criteria>
    - Both head comments and both explanatory lines are still present and unchanged.
    - The preview banner's wording is unchanged; only its treatment moved.
    - No staff-only control, badge or identifier was added to either preview.
    - The `Eye` glyph carries `aria-hidden`.
    - Neither file contains a utility drawn from Tailwind's built-in neutral ramp, a weight-500 font
      utility, or a raw hex.
    - `npx next build` exits 0 and `npx eslint src` reports no error.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn 'zinc-' 'src/app/staff/courses/[id]/preview' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn 'font-medium' 'src/app/staff/courses/[id]/preview' | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx eslint 'src/app/staff/courses'</automated>
    <fails_when>non-zero exit, or the output reports any error-severity problem</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
    <human-check>Run `npm run dev` and, signed in as staff, walk `/staff/courses`, a course detail page, its arrange screen, a lesson editor and both preview screens. Confirm every grey on every screen belongs to one family, that no field is left with an invisible edge, that the arrange screen still warns on unsaved order, and that the public-page preview looks like the real public course page rather than a staff screen wearing its clothes.</human-check>
  </verify>
  <done>Both preview screens render on the approved system, still showing exactly what an outsider would see.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff view → preview of an outsider's view | The preview screens claim to show what an anonymous visitor or a learner sees; anything staff-only that leaks in makes that claim false |
| staff input → catalogue mutation | The arrange and editor screens call server actions that reorder and edit published content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-26 | Information Disclosure | Preview screens | medium | mitigate | Task 3 forbids adding any control, badge or identifier an outsider would not see, and requires both head comments recording the fidelity contract to survive |
| T-04.1-27 | Tampering | Arrange and lesson-editor wrappers | medium | mitigate | Task 2 asserts every server-action call, every prop passed down and the unsaved-order dirty signal are unchanged, with the reorder and module-lesson service tests as the verify |
| T-04.1-28 | Repudiation | Course row actions | medium | mitigate | No delete affordance may be added; archival remains the only removal path, matching CAT-08 |
| T-04.1-04 | Information Disclosure | Local micro-text colour | low | mitigate | Micro-text resolves through the muted token, whose contrast on both grounds is asserted mechanically by the plan-01 contrast test |
</threat_model>

<verification>
- `npx next build` clean; `npx tsc --noEmit` clean; `npx eslint src` reports no error.
- `npx vitest run tests/reorder.test.ts tests/module-lesson-service.test.ts` green.
- No legacy neutral utility, weight-500 utility or raw hex anywhere under `src/app/staff/courses`.

</verification>

<success_criteria>
Every screen in the course authoring flow renders one neutral family on the approved tokens, with
every binding, every server action and every preview fidelity guarantee untouched.
</success_criteria>

## Artifacts this phase produces

No new file and no new exported symbol. New markup treatments, all internal: local panels on the card
treatment, local controls on the interactive edge, and the learner-preview banner strip with its
`Eye` glyph.

New named `lucide-react` import in this plan's files: `Eye`.

## Planner Assumptions

- **The mockup's longer learner-preview banner copy is not adopted.** UI-SPEC section 6.2 draws
  "Previewing as a learner. Nothing here is recorded as progress." plus a "Back to editing" link. The
  shipped banner already states the screen is a learner-view preview; the mockup's additional
  sentence asserts a property of a progress-recording system this phase does not touch and cannot
  verify. The shipped wording is kept and the divergence recorded rather than silently resolved.
- **The empty bulk-action handlers in the courses table stay empty.** They are a carried gap in
  project state. Implementing them would be new capability inside a restyle; removing them would
  silently drop a planned affordance. Neither is this plan's business.

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-10-SUMMARY.md` when done.
</output>

---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "22"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/staff/courses/[id]/arrange/ModuleComposer.tsx"
  - "src/app/staff/courses/[id]/arrange/ArrangeClient.tsx"
  - "tests/components/module-composer.test.tsx"
  - "src/app/staff/roles/RoleDetailPanels.tsx"
  - "tests/components/role-detail-panels.test.tsx"
  - "src/app/staff/users/AssignmentsPanel.tsx"
  - "tests/components/assignments-panel.test.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Failed add and rename retain text and retry at the correct control."
    - "Role mutation errors cannot strand controls or erase retry context."
    - "Assignment mutations recover with intact intent and no silent success."
  artifacts:
    - path: "src/app/staff/courses/[id]/arrange/ModuleComposer.tsx"
      provides: "Retained module edits and recoverable staff mutations"
    - path: "src/app/staff/courses/[id]/arrange/ArrangeClient.tsx"
      provides: "Retained module edits and recoverable staff mutations"
    - path: "tests/components/module-composer.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/app/staff/roles/RoleDetailPanels.tsx"
      provides: "Retained module edits and recoverable staff mutations"
    - path: "tests/components/role-detail-panels.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/app/staff/users/AssignmentsPanel.tsx"
      provides: "Retained module edits and recoverable staff mutations"
    - path: "tests/components/assignments-panel.test.tsx"
      provides: "Executable regression evidence"
  key_links:
    - from: "src/app/staff/courses/[id]/arrange/ArrangeClient.tsx"
      to: "src/app/staff/courses/[id]/arrange/ModuleComposer.tsx"
      via: "Module callbacks return failure without discarding edited text"
---

<objective>
Retained module edits and recoverable staff mutations. Close the source-confirmed gaps while honoring D-01–D-26 and the phase boundary: preserve existing capabilities, routes, data, authorization and action contracts. The approved CONTEXT overrides contradictory UI-SPEC generalizations. Prior plans 01–14 remain executed history; this is additive gap closure.
</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-ANNOTATIONS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-REVIEW.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
</context>

## Planner Assumptions

- No dependency installs, external API integration, schema mutation or new permission is required. ASVS level 1; high is the blocking threshold.
- Existing native/browser behavior remains authoritative; component tests prove DOM semantics, not rendered geometry.
- Tests named below that do not yet exist are created in the same implementation task before the repair and must first expose the reported failure. The existing Vitest node/components projects require no installation.
- Estimate factor 1, sample_count 0, confidence low from estimate-calibration.
- Preserve working tree changes and do not commit (commit_docs=false; parent execution policy).

<tasks>

<task type="tracer" tdd="true">
  <name>Trace failed module rename to retry with retained text</name>
  <files>src/app/staff/courses/[id]/arrange/ModuleComposer.tsx, src/app/staff/courses/[id]/arrange/ArrangeClient.tsx, tests/components/module-composer.test.tsx</files>
  <read_first>src/app/staff/courses/[id]/arrange/ModuleComposer.tsx, src/app/staff/courses/[id]/arrange/ArrangeClient.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Failed add and rename retain text and retry at the correct control. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Change internal composer callbacks to return an explicit {ok:true}|{ok:false,message:string} result. ArrangeClient translates existing resolved action results and catches unexpected rejection, clearing pending in finally. Composer awaits add/rename and only clears add text or exits rename mode on success. Keep each operation's error adjacent to its own control, not under unrelated add input. Prevent duplicate submits and keep reorder/unsaved state independent. Test rejected/resolved failures then success for add and rename.</action>
  <acceptance_criteria>Failed add and rename retain text and retry at the correct control. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/module-composer.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Failed add and rename retain text and retry at the correct control.</done>
</task>

<task type="auto" tdd="true">
  <name>Recover role-detail mutations</name>
  <files>src/app/staff/roles/RoleDetailPanels.tsx, tests/components/role-detail-panels.test.tsx</files>
  <read_first>src/app/staff/roles/RoleDetailPanels.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Role mutation errors cannot strand controls or erase retry context. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Wrap each imperative server action with try/catch/finally and operation-specific generic error feedback; retain entered reasons and dialog state on rejection and clear busy. Preserve permission catalogue IDs, optimistic version and last-administrator protections in existing actions. Test every operation rejection and user retry; do not expose raw exceptions.</action>
  <acceptance_criteria>Role mutation errors cannot strand controls or erase retry context. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/role-detail-panels.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Role mutation errors cannot strand controls or erase retry context.</done>
</task>

<task type="auto" tdd="true">
  <name>Recover assignment-panel mutations</name>
  <files>src/app/staff/users/AssignmentsPanel.tsx, tests/components/assignments-panel.test.tsx</files>
  <read_first>src/app/staff/users/AssignmentsPanel.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Assignment mutations recover with intact intent and no silent success. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Add try/catch/finally to imperative revoke/change flows so rejection keeps the relevant dialog/reason and emits alert while clearing busy. Preserve bound assignment IDs and service authorization. Test each rejection path and a later user-triggered success, with no automatic mutation retry.</action>
  <acceptance_criteria>Assignment mutations recover with intact intent and no silent success. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/assignments-panel.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Assignment mutations recover with intact intent and no silent success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User interaction to existing protected action | UI repair must not bypass server authorization or misrepresent results |
| Source/evidence to completion claim | Automated checks cannot stand in for pending visual or media accessibility evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G22-01 | Elevation of Privilege | Existing route/action consumers | high | mitigate | Preserve bound IDs and permission checks; test denied/failed outcomes where touched and run combined service suite in 29 |
| T-04.1-G22-02 | Repudiation | Feedback and verification | medium | mitigate | Preserve failure state, never assert mutation success after rejection, retain captions and human UAT gaps explicitly |
</threat_model>

<verification>
Run each task command from repository root. Focused tests must pass after the corresponding repair; plan 29 runs full combined gates once. A new test path is created before its command executes. Visual source checks do not certify browser layout. Captions scope remains unresolved and cannot be cleared by these repairs.
</verification>

<success_criteria>
All task acceptance criteria pass, scope remains bounded, and the SUMMARY records tests plus unresolved human/media checks honestly. Do not mark NFR-09 or the phase complete.
</success_criteria>

## Artifacts Produced

- `src/app/staff/courses/[id]/arrange/ModuleComposer.tsx`
- `src/app/staff/courses/[id]/arrange/ArrangeClient.tsx`
- `tests/components/module-composer.test.tsx`
- `src/app/staff/roles/RoleDetailPanels.tsx`
- `tests/components/role-detail-panels.test.tsx`
- `src/app/staff/users/AssignmentsPanel.tsx`
- `tests/components/assignments-panel.test.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-22-SUMMARY.md` (execution evidence)

<output>
Create .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-22-SUMMARY.md. Report completed fixes, exact commands/results and any blocked scope decision without overwriting historical audits.
</output>

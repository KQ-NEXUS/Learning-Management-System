---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/components/resource-table.test.tsx
  - tests/components/resource-form.test.tsx
  - tests/components/detail-layout.test.tsx
  - tests/components/confirm-modal.test.tsx
autonomous: true
requirements: [NFR-09]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Each of the four primitives has a test file that renders every branch of its own declared state union and asserts distinguishable, role-queryable output for each — this is the green baseline the restyle plans diff against (D-26)."
    - "ResourceTable's denied state renders byte-identical output whether the caller knows a record exists or not, asserted mechanically rather than by eye (RBAC-06, T-04.1-01)."
    - "ResourceTable's loading branch holds each column's declared width so data arrival causes no reflow, and announces via an aria-live polite region rather than visible copy (UI-SPEC 8.1)."
    - "ResourceTable's filtered-empty branch names the active filter count and the total-without-filters count; the unfiltered-empty branch does not (UI-SPEC 8.1)."
    - "A ResourceTable filter validation error renders a role=alert banner while the previously loaded rows stay visible beneath it (UI-SPEC 8.1, partial)."
    - "ResourceTable exposes no delete affordance in any state (CAT-08)."
    - "ResourceForm's error summary appears above the fields, links to each offending field, and receives focus after a failed submit (UI-SPEC 8.2, NFR-09)."
    - "A failed ResourceForm save neither shows success nor clears the values the user already entered (UI-SPEC 8.2)."
    - "DetailLayout renders a per-section error in isolation: the failing section shows its error while every other section stays readable (UI-SPEC 8.3, partial)."
    - "DetailLayout's tabbed mode still implements roving-tabindex keyboard navigation with arrow keys plus Home and End (UI-SPEC 8.3)."
    - "ConfirmModal keeps confirm disabled until the reason clears minReasonLength, showing a live counter rather than a post-hoc error (UI-SPEC 8.4)."
    - "ConfirmModal traps focus while open, returns focus to the trigger on close, and suppresses ESC-cancel while the action is in flight (UI-SPEC 8.4)."
    - "A failed ConfirmModal action renders the explicit not-applied wording rather than leaving the outcome ambiguous (UI-SPEC 8.4)."
    - statement: "These four test files assert semantics and copy only. If any assertion binds to a colour, radius or shadow class name, the restyle plans in wave 2 will fail for the wrong reason and the baseline stops being a baseline."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT assert on any colour, radius, shadow or spacing class name in these four test files — a test that pins the current look makes the approved redesign fail as though it were a regression, and teaches the executor to weaken the test rather than trust it."
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT weaken, skip or delete a denial-parity assertion to make a later restyle pass — the denied state's job is to reveal nothing about whether a record exists, and a test that bends to convenience is worse than no test."
      category: privacy
      status: unverified
      verification: judgment
  artifacts:
    - path: "tests/components/resource-table.test.tsx"
      provides: "Five-state render coverage plus denial-parity and no-delete-affordance assertions for ResourceTable"
      contains: "denied"
      min_lines: 90
    - path: "tests/components/resource-form.test.tsx"
      provides: "Four-state render coverage plus error-summary focus assertion for ResourceForm"
      contains: "ResourceForm"
      min_lines: 60
    - path: "tests/components/detail-layout.test.tsx"
      provides: "State coverage plus per-section partial-failure and roving-tabindex assertions for DetailLayout"
      contains: "DetailLayout"
      min_lines: 60
    - path: "tests/components/confirm-modal.test.tsx"
      provides: "State coverage plus focus-trap, reason-minimum and ESC-suppression assertions for ConfirmModal"
      contains: "ConfirmModal"
      min_lines: 60
  key_links:
    - from: "tests/components/resource-table.test.tsx"
      to: "src/components/primitives/index.ts"
      via: "dynamic import of the primitives barrel, matching the shape tests/components/rich-text-editor.test.tsx already uses"
      pattern: "@/components/primitives"
---

<objective>
Build the Wave 0 test floor: four render-level test files, one per primitive, covering every branch
of each primitive's declared state union plus the behaviours the restyle must not break.

Purpose: none of the four primitives has a test file today. Roughly forty screens are configured
from these four components, so the wave-2 restyle plans are about to rewrite the class strings of
the most reused code in the app with no automated proof that the five states still render. These
tests are written against the current, pre-migration implementations, so they are green before a
single class changes — that is what makes them a baseline rather than a hope.

Output: `tests/components/{resource-table,resource-form,detail-layout,confirm-modal}.test.tsx`,
green against today's code, asserting semantics and copy and never appearance.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VALIDATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResourceTable — five states, denial parity, no delete affordance</name>
  <files>tests/components/resource-table.test.tsx</files>
  <read_first>
    - `src/components/primitives/ResourceTable.tsx` — the whole file. The `ResourceTableState<T>`
      discriminated union, the `Column<T>` shape, the `TableFilter` union, the `BulkSelection`
      shape, and the five render branches. Pay particular attention to the denied branch (around
      lines 275-306) and its explanatory comment, and to the loading branch's use of `Column.width`.
    - `src/components/primitives/index.ts` — the barrel this test imports from.
    - `tests/components/rich-text-editor.test.tsx` — the exact shape to copy: `afterEach` calling
      `cleanup()` and `vi.restoreAllMocks()`, a typed async loader that dynamically imports the
      barrel and asserts the export is a function before casting, then `render` plus role-based
      queries from `@testing-library/react`.
    - `tests/components/setup.ts` — the jsdom setup the `components` project already loads.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      sections 2.1 (the contract), 6.1 (the exact state copy strings) and 8.1 (the state-coverage
      rows this file must cover).
  </read_first>
  <behavior>
    - Test 1: `loading` renders skeleton output, exposes an `aria-live="polite"` region whose text
      names the noun, and emits a `<col>` (or equivalent) carrying each `Column.width` so data
      arrival does not reflow the table.
    - Test 2: `ready` with rows renders one row per item via `Column.render`, and when `shownCount`
      is less than `totalCount` the difference is disclosed in visible text.
    - Test 3: `empty` with no active filters renders the "No {noun} yet" copy and does NOT mention a
      filter count; `empty` with `activeFilterCount` and `totalWithoutFilters` set renders the
      filtered variant naming both numbers.
    - Test 4: `denied` renders the 403 eyebrow, the "You do not have access to {noun}" heading and
      the permission-scope body. Rendered twice — once in a context where a record exists and once
      where it does not — the two `container.innerHTML` values are identical. No row count, title
      or identifier appears in either.
    - Test 5: `error` renders the failure copy plus a Retry control that invokes `onRetry`.
    - Test 6: `validationError` set alongside a `ready` state renders a `role="alert"` banner AND
      the previously supplied rows are still queryable beneath it.
    - Test 7: across every state above, no control has an accessible name matching delete or remove
      semantics for a row — archive replaces deletion (CAT-08). (The filter-chip dismiss control is
      not a row action and is excluded by querying row-scoped controls only.)
  </behavior>
  <action>
    Create `tests/components/resource-table.test.tsx` in the `components` Vitest project. Follow
    `tests/components/rich-text-editor.test.tsx`'s loader shape exactly: an async
    `loadResourceTable()` that does `await import("@/components/primitives")`, asserts the
    `ResourceTable` export `toBeTypeOf("function")` with a message naming what was expected, and
    returns it cast to a typed `ComponentType`.

    Define one small fixture row type and a two-column `Column[]` config (one plain column, one
    `mono` column with an explicit `width`) reused across the tests, so each `it` differs only in
    the `state` prop it passes.

    Assert on roles, accessible names and the literal copy strings UI-SPEC 6.1 lists — never on a
    class name, colour, radius or shadow. The whole point of this file is that it stays green
    through the wave-2 restyle; an assertion bound to appearance would fail on the approved redesign
    and be indistinguishable from a real regression.

    For the denial-parity test, render the `denied` state twice with the same props and compare the
    two `container.innerHTML` strings for exact equality, then additionally assert the rendered text
    contains no digit sequence that could be a record count and no fixture row title. Give this test
    a name that says why it exists, so a future reader does not "simplify" it away.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project components tests/components/resource-table.test.tsx` exits 0 with at
      least 7 passing tests, against the current unmodified `ResourceTable.tsx`.
    - The file contains no occurrence of `rounded-`, `shadow-`, `bg-`, `text-[`, or a hex colour —
      it asserts semantics only.
    - The denied-parity test compares two rendered HTML strings for equality.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project components tests/components/resource-table.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or fewer than 7 tests reported</fails_when>
    <automated>grep -cE 'rounded-|shadow-|#[0-9a-fA-F]{6}' tests/components/resource-table.test.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or the output names tests/components/resource-table.test.tsx</fails_when>
  </verify>
  <done>All five `ResourceTable` states, the validation-error partial case, denial parity and the no-delete rule are covered by passing tests against the pre-restyle implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResourceForm and DetailLayout — states, error-summary focus, per-section failure isolation</name>
  <files>tests/components/resource-form.test.tsx, tests/components/detail-layout.test.tsx</files>
  <read_first>
    - `src/components/primitives/ResourceForm.tsx` — the whole file. The `ResourceFormState` union
      (`ready`/`loading`/`denied`/`error`), the `FieldError[]` shape, the error-summary render and
      wherever focus is moved after a failed submit, and `FormField`'s render-prop signature that
      guarantees `id`/`name`/`aria-invalid`/`aria-describedby` wiring.
    - `src/components/primitives/DetailLayout.tsx` — the whole file. The `DetailLayoutState` union,
      the `DetailSection` shape including its optional per-section `error`, the `tabbed` vs
      `stacked` modes, and the roving-tabindex key handling.
    - `src/components/primitives/index.ts` — the barrel both tests import from.
    - `tests/components/rich-text-editor.test.tsx` — the loader and `afterEach` shape to copy.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      sections 2.2, 2.3, 6.1 and 8.2/8.3.
  </read_first>
  <behavior>
    - ResourceForm test 1: `ready` renders the supplied children with their aria wiring intact —
      each control has an `id`, and `aria-describedby` points at an element that exists.
    - ResourceForm test 2: `loading` renders the loading branch; `pending` renders the submit
      control in its in-flight state and the control is disabled.
    - ResourceForm test 3: `denied` renders the "Editing needs additional permission" heading and
      the permission-scope body copy.
    - ResourceForm test 4: `error` renders the load-failure copy plus a Retry control wired to
      `onRetry`.
    - ResourceForm test 5: passing a non-empty `errors` array renders a summary above the fields
      whose entries link to each offending field by id, and after a failed submit the summary is the
      focused element.
    - ResourceForm test 6: a failed submit leaves the previously entered field values in the DOM and
      renders no success wording.
    - DetailLayout test 1: `ready` renders breadcrumbs, title, identifier, subtitle, badges and one
      panel per section.
    - DetailLayout test 2: `loading`, `denied` and `error` each render their own branch with the
      copy UI-SPEC 6.1 states, and `error` exposes a Retry control.
    - DetailLayout test 3: with three sections where the middle one carries `error`, the failing
      section renders its error text AND both sibling sections still render their content — the
      record stays readable.
    - DetailLayout test 4: in `tabbed` mode, ArrowRight/ArrowLeft move the active tab, Home selects
      the first and End the last, and exactly one tab carries `tabIndex=0` at a time.
    - DetailLayout test 5: the same section config rendered in `stacked` mode exposes every section
      simultaneously.
  </behavior>
  <action>
    Create the two test files, each following the same loader and `afterEach` shape as task 1.

    For `resource-form.test.tsx`, drive the error-summary focus assertion through a real submit:
    render with an `onSubmit` that resolves to a failure, fire the submit, `await waitFor` on the
    summary appearing, then assert `document.activeElement` is the summary element (or an element it
    contains). Do not assert focus by reading a class.

    For `detail-layout.test.tsx`, build the three-section fixture so the partial-failure test is
    unambiguous: give each section a distinct visible string, set `error` on only the middle one,
    and assert the two sibling strings are both still present. For the roving-tabindex test, use
    `fireEvent.keyDown` on the tab list with `{ key: "ArrowRight" }`, `"Home"` and `"End"`, and
    assert `tabIndex` across the tab elements after each.

    As in task 1: assert roles, accessible names, `tabIndex`, focus and literal copy. Never a class
    name, colour, radius or shadow.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project components tests/components/resource-form.test.tsx tests/components/detail-layout.test.tsx`
      exits 0 with at least 11 passing tests total, against the current unmodified sources.
    - The error-summary test asserts on `document.activeElement`, not on a class or a data
      attribute.
    - The per-section failure test asserts both sibling sections' content is still present.
    - Neither file contains `rounded-`, `shadow-` or a hex colour.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project components tests/components/resource-form.test.tsx tests/components/detail-layout.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or fewer than 11 tests reported</fails_when>
    <automated>grep -cE 'rounded-|shadow-|#[0-9a-fA-F]{6}' tests/components/resource-form.test.tsx tests/components/detail-layout.test.tsx</automated>
    <fails_when>either file prints a number other than 0</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or the output names either new test file</fails_when>
  </verify>
  <done>`ResourceForm`'s four states plus its error-summary focus contract, and `DetailLayout`'s states plus per-section failure isolation and roving-tabindex navigation, are all covered by passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConfirmModal — reason minimum, focus trap, ESC suppression, not-applied wording</name>
  <files>tests/components/confirm-modal.test.tsx</files>
  <read_first>
    - `src/components/primitives/ConfirmModal.tsx` — the whole file, including the JSDoc block at
      the top that states the three behaviours this test exists to pin: confirm stays disabled until
      the reason meets its minimum, ESC cancels but is suppressed while pending, and a failed action
      says the action was not applied. Read the focus-trap implementation and how focus returns to
      the trigger on close.
    - `src/components/primitives/index.ts` — the barrel.
    - `tests/components/rich-text-editor.test.tsx` — the loader and `afterEach` shape.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      sections 2.4, 6.1 and 8.4.
  </read_first>
  <behavior>
    - Test 1: opening with an empty reason renders the confirm control disabled, and a live counter
      showing zero against the configured minimum — not an error message.
    - Test 2: typing a reason shorter than `minReasonLength` keeps confirm disabled and updates the
      counter; reaching the minimum enables confirm.
    - Test 3: `onConfirm` is called with the typed reason string when confirm is activated.
    - Test 4: while open, Tab from the last focusable element inside the dialog returns focus to the
      first — focus does not escape to the page behind.
    - Test 5: closing returns focus to the element that was focused before the dialog opened.
    - Test 6: `pending` disables confirm, and an ESC keydown while pending does NOT invoke
      `onCancel`; an ESC keydown while not pending does.
    - Test 7: an `error` prop renders the explicit not-applied wording alongside the specific
      message, so the outcome is never ambiguous.
    - Test 8: the default `eyebrow` reads "Integrity action" when none is supplied.
  </behavior>
  <action>
    Create `tests/components/confirm-modal.test.tsx` following the same loader and `afterEach` shape
    as the previous tasks.

    Render the dialog from a small harness that also renders a trigger button, focus the trigger
    first, then open the dialog — that is what makes the focus-return assertion in test 5 real
    rather than vacuous.

    Drive the reason input with `fireEvent.change` and read the counter's text. For the focus trap,
    query all focusable elements inside the dialog, focus the last, fire a Tab keydown, and assert
    `document.activeElement` is the first. For ESC suppression, fire `keyDown` with `{ key:
    "Escape" }` in both the pending and non-pending renders and assert the `onCancel` spy's call
    count differs between them.

    Assert on roles, accessible names, `disabled`, `document.activeElement` and literal copy only —
    never a class name, colour, radius or shadow.
  </action>
  <acceptance_criteria>
    - `npx vitest run --project components tests/components/confirm-modal.test.tsx` exits 0 with at
      least 8 passing tests, against the current unmodified `ConfirmModal.tsx`.
    - The ESC test asserts a difference in the `onCancel` spy call count between the pending and
      non-pending renders.
    - The focus-return test asserts `document.activeElement` is the trigger after close.
    - The file contains no `rounded-`, `shadow-` or hex colour.
    - The full `components` project is green: `npx vitest run --project components
      --no-file-parallelism` reports 59 pre-existing tests plus the new ones, with zero failures.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project components tests/components/confirm-modal.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or fewer than 8 tests reported</fails_when>
    <automated>grep -cE 'rounded-|shadow-|#[0-9a-fA-F]{6}' tests/components/confirm-modal.test.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx vitest run --project components --no-file-parallelism</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or the total passed count is below 59</fails_when>
  </verify>
  <done>`ConfirmModal`'s reason minimum, focus trap, focus return, ESC suppression and not-applied wording are pinned by passing tests, and the whole `components` project is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization decision → rendered denied state | The `denied` branch is what a user without permission sees; anything it renders that varies with record existence leaks across this boundary |
| test suite → future edits | These tests are the only automated statement of what the primitives must keep doing through the restyle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Information Disclosure | `ResourceTable` `denied` branch | high | mitigate | Task 1 renders `denied` twice — once where a record exists, once where it does not — and asserts the two `container.innerHTML` values are byte-identical, plus asserts no count, title or identifier appears. This converts `04.1-VALIDATION.md`'s manual-only denial-parity check into a mechanical one |
| T-04.1-06 | Tampering | These four test files | medium | mitigate | Tests assert semantics and copy only; the acceptance criteria grep the files themselves for appearance-bound assertions so a test that would force a later executor to weaken it never lands |
| T-04.1-07 | Elevation of Privilege | `ConfirmModal` reason gate | medium | mitigate | Task 3 pins that confirm stays disabled below `minReasonLength` and that ESC is suppressed mid-flight, so a restyle cannot accidentally let an integrity action fire without its recorded reason |
</threat_model>

<verification>
- `npx vitest run --project components --no-file-parallelism` green, with the four new files
  included and the 59 pre-existing component tests untouched.
- `npx tsc --noEmit` clean.
- All four files free of appearance-bound assertions.
</verification>

<success_criteria>
Every branch of every primitive's state union, plus denial parity, error-summary focus, per-section
failure isolation, roving-tabindex navigation, the reason minimum, the focus trap and ESC
suppression, is covered by a test that is green on today's code and will still be green on the
redesigned code.
</success_criteria>

## Artifacts this phase produces

New files: `tests/components/resource-table.test.tsx`, `tests/components/resource-form.test.tsx`,
`tests/components/detail-layout.test.tsx`, `tests/components/confirm-modal.test.tsx`.

New internal test helpers (one per file, not exported): `loadResourceTable`, `loadResourceForm`,
`loadDetailLayout`, `loadConfirmModal`, each following the dynamic-import-and-type-assert shape
established by `tests/components/rich-text-editor.test.tsx`.

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-02-SUMMARY.md` when done.
</output>

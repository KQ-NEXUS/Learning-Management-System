---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "35"
subsystem: ui
tags: [design-contract, vitest, ci-guard, tailwind, accessibility, manifest]

requires:
  - phase: 04.1 (plans 01-31)
    provides: "design-contract.test.ts GROUPS manifest + gradeFile checker (plan 04.1-24); restyled auth/staff/public/account/shell source (plans 16, 24-28, 30, 31)"
  - phase: 04.1 wave 1 (plans 04.1-32/33/34)
    provides: "post-repair arrange-client / modal / lesson-editor source the expanded manifest now grades once"
provides:
  - "Four new design-contract manifest groups: auth-shell (13), public-account-shell (12), staff-shell-access (13), staff-courses-programmes (13) — graded set 14 -> 65 files across 10 groups"
  - "EXPECTED_GROUPS deep-equality guard + per-group non-emptiness + on-disk existence of every manifest and locked-exception path + duplicate-path detection — a vacuous or stale gate now fails loudly"
  - "Repointed stale LOCKED_EXCEPTIONS path src/components/shell/AuthPanel.tsx -> src/app/(auth)/AuthPanel.tsx (D-21 gap-5 intact)"
  - "Four off-role tokens repaired on the three shell entry files (no restyle, no new exception)"
  - "Combined five-gate battery record for the whole re-verification gap wave (plans 32-35)"
affects: [verify-phase, NFR-09, 04.1-UAT]

tech-stack:
  added: []
  patterns:
    - "A manifest-driven CI gate must carry an EXPECTED_GROUPS deep-equality fixture so a renamed/dropped describe group fails instead of running vacuously"
    - "Every manifest path and every locked-exception path is existsSync-checked in a fixture so a dead path cannot be reintroduced"

key-files:
  created: []
  modified:
    - tests/unit/design-contract.test.ts
    - src/app/(public)/layout.tsx
    - src/app/account/layout.tsx
    - src/app/staff/layout.tsx

key-decisions:
  - "EXPECTED_GROUPS grew in two steps to keep every commit's unfiltered gate green: Task 1 landed 8 keys (6 pre-existing + auth-shell + public-account-shell), Task 2 appended staff-shell-access + staff-courses-programmes together with their GROUPS entries. Final state names all 10 keys as the plan requires."
  - "The stale src/components/shell/AuthPanel.tsx exception was repointed, not deleted — gap-5 token set and D-21 rationale kept verbatim; the real src/app/(auth)/AuthPanel.tsx carries no gap-5 today so the entry is harmless but now valid."
  - "Off-role token repairs are one class token each: (public)/layout Sign-in px-3.5 py-1.5 -> px-4 py-2; account/layout avatar text-xs -> text-[11px] and sign-out text-xs -> text-sm; staff/layout sign-out text-xs -> text-sm. No copy, markup, prop, guard, NAV array or structural dimension changed. No new locked exception added."
  - "All verification ran design-contract.test.ts with NO -t filter — the vacuous -t <group> pattern that caused G-RV-02 was not reused."

patterns-established:
  - "Manifest CI gate = GROUPS record + EXPECTED_GROUPS literal + fixture asserting key deep-equality, non-emptiness, on-disk existence and no cross-group duplication."

requirements-completed: []

duration: ~12min (source) + ~14min (combined gate battery)
completed: 2026-09-07
---

# Phase 04.1 Plan 35: Design-contract manifest expansion + re-verification gap-wave gate battery Summary

**`design-contract.test.ts` now mechanically grades 65 restyled source files across 10 manifest groups (was 14 across 6), with anti-vacuity / anti-stale fixtures that make a dropped group or a dead path fail loudly; the stale `AuthPanel.tsx` locked-exception path is repaired, the four off-role tokens on the three shell entry files are fixed with no new exception, and all five combined automated gates are green for the whole plans 32-35 wave.**

## Performance

- **Duration:** ~26 min total (source repairs ~12 min, five-gate battery ~14 min)
- **Started:** 2026-09-07T12:52:00Z
- **Completed:** 2026-09-07T12:08:00Z (gate battery finished)
- **Tasks:** 3 (2 source, 1 evidence-only)
- **Files modified:** 4 (1 test, 3 shell entry layouts)

## Accomplishments

### Task 1 — auth + public/account/shell groups, anti-vacuity guards, stale-path repair (`045b1fb`)

- Repointed the dead `LOCKED_EXCEPTIONS` entry `src/components/shell/AuthPanel.tsx` -> `src/app/(auth)/AuthPanel.tsx`; `gap-5` token set and the D-21 rationale string are unchanged. The `src/app/(auth)/layout.tsx` and `src/components/shell/LearnerShell.tsx` entries were not touched.
- Added a module-level `EXPECTED_GROUPS` literal and five fixture cases inside the existing `contract checker fixtures — enforcement is non-vacuous` describe block:
  - `Object.keys(GROUPS)` sorted deep-equals `EXPECTED_GROUPS` sorted (a renamed/dropped group now fails loudly);
  - every group has >= 1 file;
  - every manifest file exists on disk (`existsSync` against `REPO_ROOT`);
  - no relative path appears in two groups;
  - every `LOCKED_EXCEPTIONS[].file` exists on disk.
- Added the `auth-shell` group with exactly the 13 listed `(auth)` paths.
- Added the `public-account-shell` group with exactly the 12 listed public / account / shell paths.
- Repaired the two off-role tokens the new `public-account-shell` group reported:
  - `src/app/(public)/layout.tsx` — Sign-in `<Link>` `px-3.5 py-1.5` -> `px-4 py-2` (compact-control spacing role).
  - `src/app/account/layout.tsx` — avatar `<span role="img">` `text-xs` -> `text-[11px]` (11px micro role); sign-out `<button type="submit">` `text-xs` -> `text-sm` (14px control/body role, UI-SPEC §4.3).
- No other class, structural dimension (`size-[30px]`, `max-w-[1080px]`, `h-[38px]`), copy, markup, guard or `LearnerShell` prop changed. No new locked exception.

### Task 2 — staff shell/access + staff courses/programmes groups (`ad6b122`)

- Added the `staff-shell-access` group with exactly the 13 listed staff shell / roles / users / audit paths.
- Added the `staff-courses-programmes` group with exactly the 13 listed staff courses / programmes paths.
- Appended `staff-shell-access` and `staff-courses-programmes` to `EXPECTED_GROUPS`; the deep-equality fixture now covers all 10 groups.
- Repaired the single off-role token the `staff-shell-access` group reported: `src/app/staff/layout.tsx` sign-out `<button type="submit">` `text-xs` -> `text-sm`, matching the identical `account/layout.tsx` repair. The `NAV` array, the `getCurrentActor` / `isStaff` / `LEARNER_LANDING_PATH` guard, the `profileService.getOwnProfile` degradation comment and the `signOutAction` form are unchanged.
- No file in either new group graded any violation other than the one `staff/layout.tsx` `text-xs` named in the plan — the other 48 candidate files grade clean, confirming the plan-25/26/27/28/30/31 sweeps were correct in substance.

### Task 3 — combined five-gate battery (evidence only, no source change)

All five gates run in order from the repository root over the tree containing plans 04.1-32, 04.1-33, 04.1-34 and Tasks 1-2 of this plan (HEAD `ad6b122`).

## Combined automated gate results

| # | Gate | Command | Exit | Result | Timestamp (UTC) |
|---|------|---------|------|--------|-----------------|
| 1 | Lint | `npx.cmd eslint` | 0 | clean, no warnings | 2026-09-07T11:56:42Z -> 11:57:25Z |
| 2 | Types | `npx.cmd tsc --noEmit` | 0 | clean (the previously-deferred generated `.next/dev/types/routes.d.ts` parse errors do not reproduce in this tree) | 2026-09-07T11:57:30Z -> 11:58:09Z |
| 3 | Design-contract + contrast source gate, **no `-t` filter** | `npx.cmd vitest run tests/unit/design-contract.test.ts tests/unit/contrast.test.ts` | 0 | 2 files, **127 assertions passed** (was 71 in `04.1-GAP-VALIDATION.md` — +56 from the 4 new groups + 5 anti-vacuity fixtures) | 2026-09-07T11:58:15Z -> 11:58:22Z |
| 4 | Full test suite (node + components projects, Testcontainers Postgres) | `npm test` (`vitest run --no-file-parallelism`) | 0 | **107 test files, 1401 passed / 1 skipped** (1402), 458.5s | started 2026-09-07T11:58:28Z, finished 2026-09-07T12:06:09Z |
| 5 | Production build | `npm run build` (`next build`) | 0 | "✓ Compiled successfully in 9.2s", lint + type validation + **24 static pages generated (24/24)**, full route table emitted | 2026-09-07T12:06:26Z -> 12:07:20Z |

Assertion-growth check (Task 1 / Task 3 acceptance): the unfiltered source gate reports **127** passing assertions, strictly more than the **71** recorded in `04.1-GAP-VALIDATION.md` — proving the manifest actually grew (14 -> 65 graded files, 6 -> 10 groups) rather than being silently vacuous.

Only build warning: `Next.js ignored package-lock.json in C:\Users\disuk because it is outside the current Git repository` — pre-existing environment noise, unrelated to this phase.

## Regression files this wave added or extended

New (created inside their owning wave-1 repair task, each reproducing the reported failure before the fix):

- `tests/components/arrange-client.test.tsx` — course arrange rejected-save board recovery (04.1-32)
- `tests/components/programme-arrange-client.test.tsx` — programme arrange rejected save/add/remove recovery (04.1-32)
- `tests/components/lesson-editor-client.test.tsx` — stale withdraw-error cleared on modal open/cancel (04.1-34)
- `tests/arrange-page-route.test.ts` — `listWithdrawnLessons` `AuthorizationError -> notFound()` denial parity (04.1-34)

Extended:

- `tests/components/confirm-modal.test.tsx` — pending-window focus-escape regression + idle-wrap companion (04.1-33)
- `tests/components/publish-dialog.test.tsx` — pending-window focus-escape regression (04.1-33)

## Off-role tokens repaired (exact)

| File | Before | After | Role (UI-SPEC) |
|---|---|---|---|
| `src/app/(public)/layout.tsx` | `px-3.5 py-1.5` (Sign-in link) | `px-4 py-2` | §3.1 compact-control spacing |
| `src/app/account/layout.tsx` | `text-xs` (avatar initials) | `text-[11px]` | §4.3 micro role |
| `src/app/account/layout.tsx` | `text-xs` (sign-out submit) | `text-sm` | §4.3 control/body 14px |
| `src/app/staff/layout.tsx` | `text-xs` (sign-out submit) | `text-sm` | §4.3 control/body 14px |

Exactly the four tokens the planner predicted. No further violation was found; no new locked exception was added.

## Deviations from Plan

**1. [Rule 3 - blocking issue] `EXPECTED_GROUPS` populated across two commits instead of all-ten in Task 1.**
- **Found during:** Task 1.
- **Issue:** Task 1's action text says to declare `EXPECTED_GROUPS` "naming all ten group keys", but Task 1 only adds two of the four new groups to `GROUPS`. With all ten keys in `EXPECTED_GROUPS` and only eight in `GROUPS`, the new deep-equality fixture would fail — making Task 1's own acceptance command (`vitest run … design-contract.test.ts …` with no `-t`) exit nonzero.
- **Fix:** Task 1 declared `EXPECTED_GROUPS` with the eight keys valid at that commit; Task 2 appended `staff-shell-access` + `staff-courses-programmes` together with their `GROUPS` entries (exactly what Task 2 step 3 anticipates: "if not, add them there"). Final committed state names all ten keys, satisfying the plan's success criteria and both tasks' acceptance.
- **Files modified:** `tests/unit/design-contract.test.ts`.
- **Commits:** `045b1fb` (8 keys), `ad6b122` (10 keys).

No other deviations. No architectural change. No package install. No historical plan/SUMMARY/audit file modified.

## Issues Encountered

- Running on branch `Khaliddev` in the main checkout (sequential executor), not an isolated worktree — consistent with plans 32-34. `Khaliddev` is the user's own dev branch and not a protected ref; per-task commits proceeded with hooks (no `--no-verify`). Only this plan's four files were staged per commit.
- `.planning/` is gitignored (`.gitignore:64`), so this SUMMARY is written to disk only and cannot be committed — consistent with every prior plan in this phase.

## Known Stubs

None. The manifest additions are pure test coverage; the token repairs are single-class edits to already-wired chrome.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change. The three threat-model boundaries are all tightened, not widened:
- **Source contract -> CI signal** (T-04.1-G35-01): the new `EXPECTED_GROUPS` deep-equality, non-emptiness, on-disk existence and duplicate fixtures make a vacuous or stale gate fail loudly; all verification ran with no `-t` filter.
- **Style repair -> running behaviour** (T-04.1-G35-02): only one class token changed per shell file; the `staff/layout.tsx` and `account/layout.tsx` auth guards, `signOutAction` and profile degradation are untouched and the staff component regressions (`staff-shell`, `audit-table`, `courses-table`, `programme-detail-client`) stay green.
- **Evidence -> completion claim** (T-04.1-G35-05): see below — the three human-gated items are recorded OPEN and no rendered/visual/WHCM/font result is inferred from a source or jsdom run.

## Still OPEN — NOT closed by this plan or wave

Recorded verbatim, unresolved:

1. **`04.1-UAT.md` browser walkthrough** — the 28 mockup screens across 29 routes at 360 / 768 / 1024 / 1440px plus short-height, including the 14 backstop rows and the 7 unresolved state rows from `04.1-UI-SPEC.md` §7, plus the cross-cutting keyboard / mobile-bulk / mobile-audit / failed-action-retry / font-stack / Windows High Contrast / 404-recovery / long-content / locked-spacing-exception checks. **Status: PENDING human execution.** Source and jsdom gates grade class strings and DOM semantics only — never rendered geometry, the 60/30/10 colour balance, the 48px depth model, Windows High Contrast Mode or the loaded font stack (Segoe UI Variable vs. the Plus Jakarta Sans fallback, D-05). D-26 forbids visual-regression tooling this phase; no screenshot / geometry pass was run or fabricated.
2. **Captions / accessible-media scope decision** — `04.1-CAPTIONS-SCOPE.md`, status **OPEN**, tracked to Phase 15. Needs a new WebVTT upload/data contract (likely a schema migration), outside the phase 04.1 locked boundary. Not authorized here.
3. **Explicit user acceptance of the five low-severity security advisories** — `T-04.1-05`, `T-04.1-09`, `T-04.1-14`, `T-04.1-17`, `T-04.1-22` (all `low`, all plan-time `accept`). A plan-time `accept` disposition is not user acceptance. Recorded, not cleared.

**NFR-09 and phase 04.1 MUST NOT be marked complete.**

## Self-Check: PASSED

- `tests/unit/design-contract.test.ts` — FOUND (modified; 10 groups, 65 graded files, EXPECTED_GROUPS + 5 anti-vacuity fixtures, repointed exception)
- `src/app/(public)/layout.tsx` — FOUND (modified; no `px-3.5` / `py-1.5`)
- `src/app/account/layout.tsx` — FOUND (modified; no `text-xs`)
- `src/app/staff/layout.tsx` — FOUND (modified; no `text-xs`)
- Commit `045b1fb` — FOUND in `git log`
- Commit `ad6b122` — FOUND in `git log`
- No historical plan / SUMMARY / audit / verification file modified — confirmed (`git status` clean apart from the two task commits)

---
*Phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac*
*Completed: 2026-09-07*

---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "27"
subsystem: ui
tags: [tailwind, design-system, staff-ui, typography, spacing, courses, programmes]

# Dependency graph
requires:
  - phase: 04.1-24
    provides: "design-contract source gate, primitive role classes (14/16/25/11px type scale, 4/8/16/24/32/48 spacing grid), locked D-21/D-23 exceptions"
  - phase: 04.1-15..23
    provides: "focused interaction/error/resilience repairs on the same staff course & programme surfaces"
provides:
  - "Course list/detail/arrange staff surfaces reconciled to named type & spacing roles"
  - "Lesson editor + public-page / learner-view preview chrome reconciled to roles (learner-preview lesson heading uses display 22px)"
  - "Programme list/detail/arrangement staff surfaces reconciled to named type & spacing roles"
affects: [04.1-29, 04.1-30, 04.1-31]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Staff route className sweeps follow the plan 24 mapping: px-2.5/py-1.5 buttons -> px-4/py-2, text-xs body/label/control -> text-sm, mono eyebrows/numeric markers -> text-[11px], gap-0.5/1.5/3 -> gap-1/1/4, off-grid card padding -> py-4"
    - "Page-title <h1> -> text-[25px] leading-[1.2]; card/section <h2> -> text-base (matches DetailLayout / ResourceForm primitives)"

key-files:
  created: []
  modified:
    - src/app/staff/courses/CoursesTable.tsx
    - src/app/staff/courses/[id]/page.tsx
    - src/app/staff/courses/[id]/arrange/ArrangeClient.tsx
    - src/app/staff/courses/[id]/arrange/ModuleComposer.tsx
    - src/app/staff/courses/[id]/arrange/page.tsx
    - src/app/staff/courses/[id]/lessons/[lessonId]/LessonEditorClient.tsx
    - src/app/staff/courses/[id]/preview/page.tsx
    - src/app/staff/courses/[id]/preview/lessons/[lessonId]/page.tsx
    - src/app/staff/programmes/ProgrammeForm.tsx
    - src/app/staff/programmes/ProgrammesTable.tsx
    - src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx
    - src/app/staff/programmes/[id]/arrange/ProgrammeArrangeClient.tsx
    - src/app/staff/programmes/[id]/page.tsx

key-decisions:
  - "The plan's verify commands (-t staff-courses / staff-preview / staff-programmes) match zero tests in the shared tests/unit/design-contract.test.ts because its GROUPS manifest (owned by plan 04.1-24, not editable here) has no staff groups. Real verification was done with a throwaway local copy of the gate, then deleted (not committed)."
  - "Page-title <h1> snapped to text-[25px]; card/section <h2> at text-sm bumped to text-base to match the primitive heading role."
  - "Type-select and course-search inputs given h-[38px] to match the standard input-control dimension (UI-SPEC 3.2) when their padding was regridded."

patterns-established:
  - "Role reconciliation sweep is className-only: no prop, action-binding, permission, data-shape or route changes."

requirements-completed: []  # NFR-09 intentionally NOT marked complete

# Metrics
duration: 40min
completed: 2026-09-07
---

# Phase 04.1 Plan 27: Course & Programme Staff Visual Reconciliation Summary

**13 staff course/programme surfaces reconciled to the phase's named type (14/16/25/11px, 22px learner-preview heading) and spacing (4/8/16/24/32/48) roles with D-21/D-23 exceptions untouched — className-only, all existing behavior and server guards preserved.**

## Performance

- **Duration:** ~40 min
- **Started:** 2026-09-07T01:58:00Z
- **Completed:** 2026-09-07T02:13:00Z
- **Tasks:** 3
- **Files modified:** 13

## Accomplishments

- **Task 1 — course list/detail/arrange:** `CoursesTable`, `courses/[id]/page.tsx`, `ArrangeClient`, `ModuleComposer`, `arrange/page.tsx`. Buttons `px-2.5 py-1.5 text-xs` -> `px-4 py-2 text-sm`; body/label/link `text-xs` -> `text-sm`; `gap-0.5/1.5/3` -> `gap-1/1/4`; off-grid `py-2.5/py-3/pt-3` -> `py-2/pt-4`; `list-disc pl-5` -> `pl-6`; arrange page `<h1> text-lg` -> `text-[25px] leading-[1.2]`; `ModuleComposer` section `<h2>` -> `text-base`. Disabled-affordance explanations, retained module errors and `UnsavedOrderProvider` wrappers untouched.
- **Task 2 — editor + preview reading chrome:** `LessonEditorClient`, `preview/page.tsx`, `preview/lessons/[lessonId]/page.tsx`. Editor `<h1> text-lg` -> `text-[25px]`; status/alert banners `px-3` -> `px-4`; type-select regridded to `h-[38px] px-4 py-2`; learner-preview lesson `<h1> text-xl` -> `text-[22px]` (display role, per plan); banner `gap-5` (not a locked exception here) -> `gap-6`, `py-2.5` -> `py-2`, `mt-0.5` -> `mt-1`. Course/lesson real-parent integrity check (`resolveCourseIdForLesson`) and withdrawn-content marker preserved.
- **Task 3 — programme list/detail/arrangement:** `ProgrammeForm`, `ProgrammesTable`, `ProgrammeDetailClient`, `ProgrammeArrangeClient`, `programmes/[id]/page.tsx`. Same button/text/grid sweep; 7 identical action-bar buttons regridded together; textarea + search input to `px-4 py-2` (search gets `h-[38px]`); `p-3` card -> `p-4`; `ProgrammeArrangeClient` "Add a course" `<h2>` -> `text-base`; member position mono marker `text-xs` -> `text-[11px]`. Outcomes field round-trip, mutation-retry context (`setModalError`/`setPublishError` keep typed input) and membership-order dirty comparison preserved.

## Task Commits

1. **Task 1: Trace course list/detail and authoring page rhythm** - `737bbe4` (fix)
2. **Task 2: Reconcile editor and preview reading chrome** - `6c577b2` (fix)
3. **Task 3: Reconcile programme list/detail/arrangement** - `b22b9b0` (fix)

**Plan metadata:** committed with this SUMMARY.

## Files Created/Modified

All 13 files listed in frontmatter `key-files.modified`. Each change is a Tailwind
utility swap inside an existing `className` string or `UPPER_CASE` class constant.
`git diff --stat 669cc99 HEAD`: 13 files, 75 insertions / 75 deletions.

## Decisions Made

- **Verification gap (see Issues):** created a temporary `tests/unit/_local27.test.ts`
  (a copy of the shared gate with `staff-courses` / `staff-preview` / `staff-programmes`
  groups added) to actually grade the 13 files, iterated to green, then removed it. It
  was never committed. The shared `tests/unit/design-contract.test.ts` was not modified.
- `<h1>` -> `text-[25px] leading-[1.2]` and card `<h2>` at `text-sm` -> `text-base` to
  match `DetailLayout` / `ResourceForm` primitive heading roles rather than leaving
  authoring pages a size below their own primitives.
- Added `h-[38px]` to the lesson-type `<select>` and the course-search `<input>` when
  regridding their padding, so they keep the standard 38px control height (UI-SPEC section 3.2).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Focused verification command matches no tests**
- **Found during:** Task 1
- **Issue:** The plan's `npx vitest run tests/unit/design-contract.test.ts -t staff-courses`
  (and `staff-preview`, `staff-programmes`) exit 0 but run **zero assertions** - the shared
  gate's `GROUPS` manifest, owned by plan 04.1-24, only defines `primitives-*` and
  `catalogue-*` groups. `<parallel_execution>` forbids editing that file.
- **Fix:** Copied the gate to `tests/unit/_local27.test.ts`, swapped in the three staff
  groups (the 13 files from this plan), ran each group, fixed every reported font-size /
  spacing violation across all 13 files until the local gate was green, then deleted the
  copy. Shared test file untouched.
- **Files modified:** none committed (temp file removed)
- **Verification:** local gate `5 + 3 + 5` tests green; then re-ran the plan's real
  (vacuous) commands - exit 0.
- **Committed in:** n/a

**2. [Rule 2 - Role correctness] Heading + input-control role upgrades**
- **Found during:** Tasks 1-3
- **Issue:** Authoring-page `<h1>` used `text-lg`, some card `<h2>` used `text-sm`, and a
  couple of `<select>`/search `<input>` controls had no explicit height once padding moved
  onto the grid - all below their own primitive's role.
- **Fix:** `<h1>` -> `text-[25px] leading-[1.2]`; card `<h2>` -> `text-base`; regridded
  controls -> `h-[38px]`.
- **Files modified:** `arrange/page.tsx`, `LessonEditorClient.tsx`, `ModuleComposer.tsx`,
  `ProgrammeArrangeClient.tsx`
- **Verification:** local gate + component tests green.
- **Committed in:** `737bbe4`, `6c577b2`, `b22b9b0`

---

**Total deviations:** 2 (1 blocking-verification workaround, 1 role-correctness).
**Impact on plan:** No scope creep - still className-only, no behavior/prop/route/permission
changes. The verification workaround means the *committed* focused commands remain vacuous;
honest results are recorded below.

## Issues Encountered

- **`tests/unit/design-contract.test.ts` full run:** `primitives-table`, `primitives-forms`
  and the enforcement fixtures pass (20). The 8 `catalogue-*` group tests **fail** - these
  files are owned by plans 04.1-30 / 04.1-31 which have not run yet. Pre-existing, out of
  scope for this plan.
- **`tests/programme-service.test.ts`** fails to load: `Cannot find module '.prisma/client/default'`
  - the generated Prisma client is absent in this worktree environment. Pre-existing infra
  issue, unrelated to className changes. Component tests that don't touch Prisma
  (`programme-detail-client`, `programme-form`) pass.
- `tsc --noEmit` reports only pre-existing errors (Prisma types, `LayoutProps`, implicit
  `any` in services/tests) - none in the 13 modified files.

## Behavior tests run (all green)

`tests/components/{courses-table,module-composer,arrange-board,course-detail-actions,
lesson-content,lesson-form-fields,programme-detail-client,programme-form}.test.tsx`,
`tests/lesson-preview-route.test.ts` - 100+ assertions, no regressions.

## Unresolved / NOT done

- **NFR-09 is NOT complete.** Do not mark it or the phase complete.
- Rendered layout, font loading, 60/30/10 balance, colour contrast and Windows High
  Contrast Mode are **not** verified here - source className grading cannot certify
  rendered geometry. Deferred to plan 04.1-29 / manual walkthrough.
- Accessible media alternatives / captions (CR-11) remain an open scope decision
  (see 04.1-GAP-COVERAGE.md); nothing in this plan touches it.

## Threat surface

No new endpoints, auth paths, file access or schema. `ProgrammeDetailClient` /
`LessonEditorClient` action bindings, IDs and `can(...)` / server-action re-checks are
byte-for-byte unchanged. T-04.1-G27-01 (privilege) and T-04.1-G27-02 (repudiation)
mitigations hold: failure states, "not applied" messaging and retry-preserving inputs
are all preserved.

## Next Phase Readiness

- Wave 3 staff sweep for courses + programmes done. Plans 30/31 (catalogue components)
  and 29 (combined gate + human walkthrough) still pending.
- **Recommend plan 04.1-24 owner add `staff-courses` / `staff-preview` / `staff-programmes`
  groups to `tests/unit/design-contract.test.ts` GROUPS** so plan 29's capstone actually
  covers these 13 files.

## Self-Check: PASSED

- All 13 modified source files present on disk.
- Task commits verified in `git log`: `737bbe4`, `6c577b2`, `b22b9b0`.
- SUMMARY.md present.

---
*Phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac*
*Completed: 2026-09-07*

---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "15"
subsystem: ui
tags: [accessibility, tables, react, vitest]
requires:
  - phase: 04.1-03
    provides: Shared ResourceTable
provides:
  - Named search and select filters with unique instance IDs
  - Mobile selection parity and preserved working bulk callbacks
  - Explicitly unavailable unfinished course controls
affects: [04.1-24, 04.1-29]
tech-stack:
  added: []
  patterns: [useId label associations, controlled responsive selection, native disabled actions]
key-files:
  created: [tests/components/courses-table.test.tsx]
  modified: [src/components/primitives/ResourceTable.tsx, src/app/staff/courses/CoursesTable.tsx, tests/components/resource-table.test.tsx]
key-decisions:
  - No new course route, bulk mutation or export capability; all four affordances explain unavailability.
requirements-completed: []
requirements-pending: [NFR-09]
actuals:
  tokens: 13416
  tasks: 2
  commits: 0
coverage:
  - id: D1
    description: Named unique filters and equivalent mobile/desktop selected IDs across filtering
    verification:
      - kind: automated_ui
        ref: tests/components/resource-table.test.tsx
        status: pass
    human_judgment: false
  - id: D2
    description: Four explained disabled course affordances with preserved working links, filters, sorting and shared bulk actions
    verification:
      - kind: automated_ui
        ref: tests/components/courses-table.test.tsx
        status: pass
      - kind: automated_ui
        ref: tests/components/resource-table.test.tsx
        status: pass
    human_judgment: false
duration: 25min
completed: 2026-09-06
status: complete
---

# Phase 04.1 Plan 15: Accessible table selection and honest controls

Search and long-option selects have associated visible labels, mobile cards share desktop selection, and unfinished course actions explain why they are disabled.

## Accomplishments

- Generated stable per-instance IDs for search/select labels; segmented status naming remains intact.
- Added mobile selection checkboxes outside row links using the same controlled IDs and callbacks as desktop. Tests prove selection persists through filtering and desktop/mobile deselection stays synchronized.
- Added optional disabled/description bulk-action fields without removing required props. Native disabled controls do not invoke callbacks; available bulk callbacks still receive selected IDs.
- Publish, Archive, Export CSV and New course now use native disabled with visible associated "Not available on this screen" copy. Existing detail links, filtering, sorting, validation-error rows and denial parity remain covered.

## Verification

- Task 1 RED: 2 failed / 8 passed, missing named textbox and missing mobile checkbox.
- Task 1 GREEN: 10/10 passed; tracer rerun also 10/10 passed.
- Task 2 RED: 2 failed / 12 passed, expected disabled true but received false.
- Final combined GREEN: 14/14 passed across two files, exit 0, 46.69s.
- Successful command: `node node_modules/vitest/vitest.mjs run --project components tests/components/courses-table.test.tsx tests/components/resource-table.test.tsx --pool=threads --maxWorkers=1 --testTimeout=15000`.
- `git diff --check -- src/components/primitives/ResourceTable.tsx src/app/staff/courses/CoursesTable.tsx tests/components/resource-table.test.tsx tests/components/courses-table.test.tsx` emitted only the existing CRLF normalization warning, no whitespace errors.
- Focused ESLint was launched but remained running without output at summary authoring; the orchestrator must collect session 33299 or use the capstone lint gate. No lint pass is claimed here.
- Browser layout and media accessibility were not tested by these DOM assertions; phase-wide UAT and captions remain pending.

## Deviations from Plan

- Default fork worker startup timed out before tests both inside and outside the sandbox. Used the installed Vitest CLI with one thread; a 5s test timeout also expired under machine contention, so CLI-only testTimeout=15000 was used. No dependency or test configuration changed.
- An initial task 2 assertion used `/Archive/`, which also matched the working Archived status filter; changed it to the exact `Archive…` action name and reran all focused tests successfully.
- No commits were made, as required by project worker policy and parent execution instructions. Requirement NFR-09 remains pending as explicitly required by this plan.
- Actuals tokens are chars/4 across the four realized changed file contents (13416), not harness usage. Existing uncommitted phase work prevents a clean plan-only git diff baseline; this figure is a conservative file-scope measurement rather than isolated patch cost.

## Known Stubs

`src/app/staff/courses/CoursesTable.tsx:181`: Publish/Archive callbacks remain empty but unreachable via native disabled controls. Export/New course likewise remain unavailable. This is the explicit binding CR-01 disposition: no new capability is authorized in this repair. Recorded in WINDOWS.md entry 11; not represented as delivered capability.

## Threat Surface

No routes, server actions, authorization checks, network endpoints, schema or upload contracts changed. Denied output remains identical regardless of records; no mutation-success claim is introduced.

## Task Commits

Skipped: project worker no-commit policy; commit_docs=false. Suggested message: `fix(04.1-15): label table filters and preserve mobile selection with honest course controls`.

## Self-Check: PASSED

The four scoped source/test files exist; final focused tests pass 14/14. This summary records remaining evidence limitations explicitly. Phase completion is not claimed.

## Next Phase Readiness

Ready for remaining gap plans. NFR-09, captions scope, rendered browser UAT and combined capstone gates remain pending.

---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 12
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-03"]
files_modified:
  - package.json
  - package-lock.json
  - src/components/catalogue/RichTextEditor.tsx
  - src/components/catalogue/ArrangeBoard.tsx
  - src/components/catalogue/LessonFormFields.tsx
  - src/components/catalogue/CourseDetailActions.tsx
autonomous: false
requirements: [NFR-09]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An empty rich-text editor shows a prompt instead of a bare box, so an author is never left guessing whether the field is broken or simply empty (D-25, UI-SPEC 8.8 empty)."
    - "The rich-text editor still exposes exactly its seven labelled toolbar controls, each a button with aria-pressed, and its inline link editor still works without a browser prompt dialog."
    - "The editor's link error still renders with role=alert and its aria-invalid and aria-describedby wiring intact (UI-SPEC 8.8 error)."
    - "The editor still grows with its content rather than scrolling internally (UI-SPEC 8.8 overflow)."
    - "The arrange board still offers keyboard move-up, move-down and move-across controls disabled at the boundaries, so drag is never the only path to reorder (NFR-09, UI-SPEC 8.9 populated)."
    - "The arrange board still announces reorder outcomes through its polite live region, and still renders its per-container empty label when a container has no items (UI-SPEC 8.9)."
    - "A drop with no destination, or one landing in place, is still a no-op rather than a spurious dirty state (UI-SPEC 8.9 partial)."
    - "Item labels and subtitles still truncate rather than wrapping, with the subtitle now on the muted-foreground token and the label at weight 600 (UI-SPEC 8.9 long-text)."
    - "No file in this plan carries a raw hex — all six bespoke components were already fully token-driven and stay that way (ROADMAP criterion 1, UI-SPEC 10)."
    - statement: "The editor toolbar's button row must wrap rather than overflow at narrow widths once its active-state treatment picks up the new surface and accent tokens."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT let the placeholder text be mistaken for authored content — an empty editor that looks full is how a lesson ships with no body, so the prompt must be visually and programmatically distinct from real text and must never be submitted."
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT remove or disable the arrange board's keyboard move controls while restyling the drag affordance — drag-and-drop alone excludes anyone who cannot use a pointer, and the keyboard path is the only reason this screen is usable by them."
      category: safety
      status: unverified
      verification: judgment
    - statement: "MUST NOT replace the editor's inline link control with a browser prompt dialog — a native prompt is unlabelled, unstyleable and invisible to the surrounding accessibility tree, and the current inline editor exists because of that."
      category: safety
      status: unverified
      verification: judgment
  artifacts:
    - path: "src/components/catalogue/RichTextEditor.tsx"
      provides: "Tokenised editor chrome plus a placeholder for the empty state"
      contains: "Placeholder"
    - path: "src/components/catalogue/ArrangeBoard.tsx"
      provides: "Tokenised board with a lucide drag handle and muted subtitles"
      contains: "GripVertical"
  key_links:
    - from: "src/components/catalogue/RichTextEditor.tsx"
      to: "package.json"
      via: "imports the Placeholder extension from the tiptap extensions package promoted to a direct dependency"
      pattern: "@tiptap/extensions"
---

<objective>
Restyle four of the bespoke catalogue components and close one of D-25's three opportunistic gaps —
the rich-text editor's missing empty-state prompt.

Purpose: ROADMAP criterion 6 asks that Phase 4's six bespoke components be reconciled to the system
or explicitly deferred with a reason. UI-SPEC section 10's verdict for all six is RECONCILE, no
defer. Three of them carry a small, named scope addition that is genuinely new behaviour rather than
paint; this plan owns one of them and sizes it as its own task rather than folding it into a restyle.

Output: a promoted dependency behind a legitimacy gate, an editor that says something when it is
empty, and four components on the approved tokens.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-RESEARCH.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package-legitimacy gate for the tiptap extensions package (blocking human)</name>
  <what-built>Nothing yet — this gate runs BEFORE the tiptap extensions package is promoted to a direct dependency. UI-SPEC section 8.8 requires a placeholder extension for the editor's empty state. That extension already resolves in this repository as a transitive dependency of the installed `@tiptap/starter-kit`, and its `Placeholder` export is confirmed present at `node_modules/@tiptap/extensions`. Importing it while it is only transitive is fragile, so the plan promotes it to a direct dependency at the version already in the lockfile. `04.1-RESEARCH.md`'s Package Legitimacy Audit covers only `lucide-react`, so under the protocol's fallback this package is treated as unaudited and gated here rather than installed silently.</what-built>
  <how-to-verify>
    1. Run `npm view @tiptap/extensions` and read the output yourself.
    2. Confirm its `repository` field points at the same tiptap project as the three `@tiptap/*`
       packages already in `package.json` (`@tiptap/pm`, `@tiptap/react`, `@tiptap/starter-kit`),
       and that `scripts` contains no `postinstall`.
    3. Run `npm ls @tiptap/extensions` and note the version already resolved in the lockfile —
       promoting it must pin that same version, not pull a newer one.
    4. Confirm its major version matches the `^3` line the three existing tiptap dependencies are on.
    Approving here authorises exactly one change: adding `@tiptap/extensions` to `dependencies` at
    the already-resolved version. It does not authorise a version bump of any tiptap package.
  </how-to-verify>
  <resume-signal>Type "approved" to authorise the promotion, or name what you saw that looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Promote the tiptap extensions package and give the empty editor a prompt</name>
  <reversibility rating="reversible">One dependency entry and one editor extension; removing both restores today's behaviour with no data or contract involved.</reversibility>
  <precondition>The npm registry is reachable from this machine and the legitimacy checkpoint above was approved by a human.</precondition>
  <files>package.json, package-lock.json, src/components/catalogue/RichTextEditor.tsx</files>
  <read_first>
    - `src/components/catalogue/RichTextEditor.tsx` — the whole file. Note the `useEditor` call and
      its extension array, the seven labelled toolbar buttons and their `aria-pressed` handling, the
      `EMPTY_ACTIVE_STATE` fallback, the inline link editor with its `linkError` `role="alert"` and
      its `aria-invalid`/`aria-describedby` wiring, and the `min-h-48` wrapper with its
      `[&_.ProseMirror]:min-h-40` companion.
    - `tests/components/rich-text-editor.test.tsx` — the contract this file must not break: exactly
      seven `aria-label`led toolbar buttons, all `type="button"` with `aria-pressed`, and an
      accessible inline link editor with no browser prompt dialog.
    - `package.json` — the three existing `@tiptap/*` dependencies and the `^3` line they are on.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 8.8 (the empty row, which flags this as new work rather than a restyle) and section 10's
      RichTextEditor row.
  </read_first>
  <action>
    Add `@tiptap/extensions` to `dependencies` at the exact version already resolved in the lockfile,
    matching the `^3` line the three existing tiptap packages are on. Do not bump any tiptap package.

    Import the `Placeholder` extension from that package and add it to the `useEditor` extension
    array with a short authoring prompt as its placeholder text — something that reads as an
    instruction rather than as content, in the same voice as the rest of the app's empty-state copy.
    The prompt is rendered by the extension as generated content on the empty node; it must never
    become part of the document value, so confirm that submitting an untouched editor still yields
    the same empty value it does today.

    Style the prompt so it cannot be mistaken for authored text: `text-muted-foreground`, no
    emphasis weight, and no border or background of its own.

    Change nothing else about the editor's behaviour: the seven toolbar buttons and their labels,
    their `aria-pressed` handling, the `EMPTY_ACTIVE_STATE` fallback, the inline link editor and its
    error wiring, and the growing-rather-than-scrolling wrapper all stay exactly as they are. Do not
    add a loading affordance for the pre-resolution state — D-25 explicitly defers that to whichever
    phase next changes catalogue authoring.
  </action>
  <acceptance_criteria>
    - `package.json` lists `@tiptap/extensions` in `dependencies` at the version `npm ls` already
      reported, on the same major as the other tiptap packages.
    - The editor renders a visible prompt when its value is empty and no prompt when it has content.
    - Submitting an untouched editor yields the same empty value as before the change.
    - The seven toolbar buttons, their labels and their `aria-pressed` handling are unchanged.
    - No browser prompt dialog was introduced for the link control.
    - `npx vitest run --project components tests/components/rich-text-editor.test.tsx` passes.
    - `npx next build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm ls @tiptap/extensions</automated>
    <fails_when>non-zero exit, or the output contains "(empty)" or "UNMET DEPENDENCY"</fails_when>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['@tiptap/extensions']) process.exit(1); console.log(p.dependencies['@tiptap/extensions'])"</automated>
    <fails_when>non-zero exit, or it prints nothing</fails_when>
    <automated>npx vitest run --project components tests/components/rich-text-editor.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the seven-control assertion is reported as failing</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
  </verify>
  <done>An empty rich-text editor shows an authoring prompt, and the extension providing it is a pinned direct dependency a human vetted.</done>
</task>

<task type="auto">
  <name>Task 2: The editor's own chrome on the approved tokens</name>
  <files>src/components/catalogue/RichTextEditor.tsx</files>
  <read_first>
    - `src/components/catalogue/RichTextEditor.tsx` as changed in task 1 — every local class string:
      the toolbar container, the seven buttons and their active state, the link-URL input, the error
      line, and the content wrapper.
    - `src/components/primitives/ResourceForm.tsx` as restyled in plan 04 — the control-edge and
      card treatment to reuse.
    - `src/components/primitives/ResourceTable.tsx` as restyled in plan 03 — its button constants,
      which the toolbar buttons should match.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 10's RichTextEditor row (toolbar-button and link-input borders move to the
      interactive-control edge) and section 8.8's long-text backstop about the toolbar wrapping.
  </read_first>
  <action>
    Give the editor wrapper the card treatment — `bg-surface`, `border border-border`, `rounded-xl` —
    with the toolbar as an inset strip on `bg-surface-2` separated by a decorative rule. Give each
    toolbar button the compact control treatment plan 03 established, and make its active state
    carry `bg-surface` plus the low shadow and a weight change in addition to colour, so an active
    control is not distinguished by colour alone.

    Give the link-URL input the interactive edge `border-input-border` at `rounded-md`. Keep the
    error line's `role="alert"` and its `aria-invalid`/`aria-describedby` wiring exactly as they are;
    only its colour and radius move.

    Let the toolbar button row wrap at narrow widths rather than overflow or scroll.

    Substitute every utility drawn from Tailwind's built-in neutral ramp by role — 500-level neutral
    text becomes `text-muted-foreground`, 600 and darker becomes `text-foreground`, a 300-level
    neutral edge on a control becomes `border-input-border`, a 200-level neutral edge becomes
    `border-border`, 50/100-level neutral fills become `bg-surface-2`. Fold every weight-500 font
    utility to `font-semibold`. Add no raw hex.
  </action>
  <acceptance_criteria>
    - The active toolbar state is carried by more than colour.
    - The link error still renders with `role="alert"` and its aria wiring.
    - The toolbar row wraps rather than overflowing at narrow widths.
    - The file contains no utility drawn from Tailwind's built-in neutral ramp, no weight-500 font
      utility, and no raw hex.
    - `npx vitest run --project components tests/components/rich-text-editor.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'zinc-' src/components/catalogue/RichTextEditor.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'font-medium' src/components/catalogue/RichTextEditor.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -c 'role="alert"' src/components/catalogue/RichTextEditor.tsx</automated>
    <fails_when>prints 0, or non-zero exit (no match) — the link error lost its announcement</fails_when>
    <automated>npx vitest run --project components tests/components/rich-text-editor.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
  </verify>
  <done>The editor's toolbar, link control and content area render on the approved system with every accessibility affordance intact.</done>
</task>

<task type="auto">
  <name>Task 3: The arrange board, lesson form fields and course detail actions</name>
  <files>src/components/catalogue/ArrangeBoard.tsx, src/components/catalogue/LessonFormFields.tsx, src/components/catalogue/CourseDetailActions.tsx</files>
  <read_first>
    - `src/components/catalogue/ArrangeBoard.tsx` — the whole file. Note the `emptyContainerLabel`
      default, the `error` prop's `role="alert"`, the `role="status" aria-live="polite"` sr-only
      region announcing reorder outcomes, the keyboard move-up / move-down / move-across buttons and
      how they are disabled at the boundaries, the no-op handling for a drop with no destination or
      landing in place, the `truncate` classes on labels and subtitles, and the unsaved-changes
      indicator.
    - `tests/components/arrange-board.test.tsx` — the keyboard-reorder contract this file must not
      break.
    - `src/components/catalogue/LessonFormFields.tsx` and
      `src/components/catalogue/CourseDetailActions.tsx` — the whole files; note every input, select,
      button and status indicator.
    - `src/components/primitives/ResourceTable.tsx` as restyled in plan 03 — the `StatusPill` tone
      treatment any pill in these files inherits, and the button constants to match.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 10's ArrangeBoard row (drag handle moves to the lucide grip glyph, container-label
      micro-text to the muted token) and section 8.9's state rows.
  </read_first>
  <action>
    In `ArrangeBoard.tsx`, replace the hand-drawn drag handle with the `GripVertical` glyph imported
    as a named per-icon import from `lucide-react`, marked `aria-hidden` — the keyboard move buttons
    are what carry the reorder affordance to assistive technology, and they must all stay. Give each
    container the card treatment on the low shadow (a board is a dense repeating surface), item rows
    the decorative divider, and the drag-over state a `bg-surface-2` fill plus a visible outline
    rather than a colour-only cue. The unsaved-changes indicator renders as an amber tinted pill
    using the pill tier — never a bare unlabelled amber swatch.

    Keep the item label at weight 600 with its `truncate`, and move the subtitle to
    `text-muted-foreground` keeping its own `truncate`. Keep `emptyContainerLabel`, the `role="alert"`
    error, the polite live region, the boundary-disabled move buttons and the no-op drop handling
    exactly as they are.

    In `LessonFormFields.tsx` and `CourseDetailActions.tsx`, give every input, select and textarea
    the interactive edge `border-input-border` at `rounded-md`, give every button the treatment plan
    03 established, and give any local panel the card treatment.

    Across all three files, substitute every utility drawn from Tailwind's built-in neutral ramp by
    role as in task 2, and fold every weight-500 font utility to `font-semibold`. Add no raw hex.
  </action>
  <acceptance_criteria>
    - The keyboard move-up, move-down and move-across controls are all still present and still
      disabled at the boundaries.
    - The polite live region and the `role="alert"` error are unchanged.
    - `emptyContainerLabel` and its default string are unchanged.
    - The drag handle glyph carries `aria-hidden`.
    - The drag-over state is carried by more than colour.
    - The unsaved-changes indicator is a labelled tinted pill, not a bare swatch.
    - None of the three files contains a utility drawn from Tailwind's built-in neutral ramp, a
      weight-500 font utility, or a raw hex.
    - `npx vitest run --project components --no-file-parallelism` is green.
    - `npx next build` exits 0 and `npx eslint src` reports no error.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project components tests/components/arrange-board.test.tsx tests/components/lesson-form-fields.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the keyboard-reorder test is reported as failing</fails_when>
    <automated>grep -rn 'zinc-' src/components/catalogue/ArrangeBoard.tsx src/components/catalogue/LessonFormFields.tsx src/components/catalogue/CourseDetailActions.tsx | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>grep -rn 'font-medium' src/components/catalogue/ArrangeBoard.tsx src/components/catalogue/LessonFormFields.tsx src/components/catalogue/CourseDetailActions.tsx | wc -l</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx vitest run --project components --no-file-parallelism</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
    <human-check>Run `npm run dev` and open a lesson editor with an empty body — confirm the editor shows a prompt that reads as an instruction, and that saving without typing leaves the body empty rather than storing the prompt. Then open a course arrange screen: reorder an item with the mouse, then do the same using only the keyboard move buttons, and confirm both work and that a screen reader announcement fires. Confirm the unsaved-changes indicator is a labelled pill rather than a bare coloured dot, and that the drag-over highlight is visible without relying on colour alone.</human-check>
  </verify>
  <done>The arrange board, lesson form fields and course detail actions render on the approved tokens with every keyboard path and announcement intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Promoting a package to a direct dependency fixes what enters the bundle |
| authored rich text → stored lesson body | The editor's value becomes stored content; a placeholder that leaked into it would be stored as authored text |
| pointer-only affordance → keyboard user | The arrange board's keyboard controls are the only reorder path for anyone who cannot drag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-SC | Tampering | `@tiptap/extensions` promotion | high | mitigate | `04.1-RESEARCH.md`'s legitimacy audit does not cover this package, so under the protocol's fallback it is treated as unaudited: a `checkpoint:human-verify` with `gate="blocking-human"` precedes the change, the version is pinned to what the lockfile already resolved, and no tiptap package is bumped |
| T-04.1-31 | Tampering | Editor placeholder text | medium | mitigate | The placeholder is generated content on the empty node, never part of the document value; the acceptance criteria require confirming an untouched editor still yields the same empty value, and the prompt is styled so it cannot be read as authored text |
| T-04.1-32 | Denial of Service | Arrange-board keyboard controls | high | mitigate | The move-up, move-down and move-across buttons and their boundary-disabled states are asserted present, and `tests/components/arrange-board.test.tsx` runs as the task's verify. Removing them would lock every pointer-less user out of reordering entirely |
| T-04.1-33 | Information Disclosure | Editor link control | medium | mitigate | The inline link editor and its `role="alert"` error wiring are preserved; a browser prompt dialog is explicitly forbidden, since it would be unlabelled and invisible to the surrounding accessibility tree |
</threat_model>

<verification>
- `npx vitest run --project components --no-file-parallelism` green.
- `npx next build` clean; `npx eslint src` reports no error.
- `npm ls @tiptap/extensions` resolves at the pinned version.
- No legacy neutral utility, weight-500 utility or raw hex in any of the four component files.

</verification>

<success_criteria>
Four of the six bespoke catalogue components render on the approved system, the editor tells an
author when it is empty, and every keyboard and announcement affordance survives.
</success_criteria>

## Artifacts this phase produces

New dependency entry: `@tiptap/extensions`, promoted from transitive to direct at the already-resolved
version.

New within `src/components/catalogue/RichTextEditor.tsx`: a `Placeholder` extension entry in the
editor's extension array and its prompt copy.

New named `lucide-react` import in this plan's files: `GripVertical`.

New markup treatments, all internal: the editor's card wrapper and inset toolbar strip, the arrange
board's card containers and drag-over outline, and the unsaved-changes tinted pill.

No new exported symbol and no changed prop type — `RichTextEditor`, `ArrangeBoard`,
`LessonFormFields` and `CourseDetailActions` all keep their existing signatures.

## Planner Assumptions

- **The placeholder extension is promoted, not newly installed.** `Placeholder` is confirmed present
  and exported at `node_modules/@tiptap/extensions`, resolved transitively through the installed
  `@tiptap/starter-kit`. Importing a transitive dependency directly is fragile, so it is promoted to
  a direct dependency at the same version. Because `04.1-RESEARCH.md`'s legitimacy audit covers only
  `lucide-react`, the protocol's fallback applies and the promotion is gated behind a blocking-human
  checkpoint rather than treated as free.
- **The editor's loading affordance is not added.** D-25 explicitly defers the bare-box state before
  the editor resolves to whichever phase next changes catalogue authoring. Recorded here so the
  omission reads as the decision it is.

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-12-SUMMARY.md` when done.
</output>

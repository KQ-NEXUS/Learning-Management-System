---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - src/components/primitives/ResourceTable.tsx
autonomous: true
requirements: [NFR-09]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "StatusPill renders a tinted pill for each tone — tint background, deep ink label, mid-tone dot — with the label text, not the dot, carrying the meaning (D-13, UI-SPEC 5.6)."
    - "StatusPill's prop signature is unchanged: label plus an optional tone from the same five values it accepts today; no consumer needs editing (UI-SPEC 2.1)."
    - "Every StatusPill tone's ink-on-tint pair clears 4.5:1, so no WCAG 1.4.11 exemption is claimed or needed for the pill (D-13, NFR-09)."
    - "The table shell, panels and dialogs read as 16px-radius cards on a 1px decorative edge with the low shadow, never the 48px card shadow — a list is a dense surface (D-18, UI-SPEC 3.3)."
    - "Every interactive control edge in this file — the filter select, the search input, buttons — clears 3:1, while row dividers and panel frames keep the light decorative edge (D-15)."
    - "All micro-text in this file — column headers, the 403 eyebrow, counts, hints, sort affordances — resolves through the muted-foreground token rather than a raw neutral ramp step (D-16)."
    - "The filter-chip dismiss control carries its own accessible name naming the filter it removes, independent of the chip's visible text (UI-SPEC 1)."
    - "Sortable column headers still expose sort state via aria-sort; the sort glyph is decorative and adds no competing accessible name (UI-SPEC 1)."
    - "Filters and sort still read from and write to the URL, and the segmented filter affordance is a rendering change on an unchanged state machine (UI-SPEC 8.16 item 1)."
    - "A filter validation error still renders a role=alert banner with the previous rows visible beneath it (UI-SPEC 8.1, partial)."
    - "Loading still holds each Column.width so data arrival causes no reflow, and still announces through an aria-live polite region (UI-SPEC 8.1)."
    - "The denied branch's copy and structure are byte-identical to before; only colour, radius and shadow classes moved (RBAC-06, T-04.1-01)."
    - "Below 640px rows still become cards and the table never scrolls horizontally (UI-SPEC 8.16 item 2)."
    - "Both empty branches still distinguish unfiltered from filtered, and shownCount/totalCount still disclose truncation (UI-SPEC 8.1)."
    - statement: "Long cell values and long noun strings must not break the card layout below 640px or force horizontal scroll after the restyle."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT spend the teal tokens on anything a user can click (D-09) — teal means progress, pass and grade in this system, and a teal button or link teaches the user that colour means action, destroying the one signal the grading surfaces depend on."
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT add an icon, colour, count or layout difference to the denied branch that varies with whether the record exists — the denied state's entire purpose is to reveal nothing."
      category: privacy
      status: unverified
      verification: judgment
    - statement: "MUST NOT introduce a delete affordance on a row while restyling the row action area — this product archives and never deletes, and a restyle is exactly where such an affordance sneaks back in."
      category: safety
      status: unverified
      verification: judgment
    - statement: "MUST NOT use the decorative border token on an input, select, textarea or checkbox — it sits at roughly 1.27:1 and a control identified only by that edge is invisible to a low-vision user."
      category: safety
      status: unverified
      verification: judgment
  artifacts:
    - path: "src/components/primitives/ResourceTable.tsx"
      provides: "Tinted StatusPill, tokenised class constants, segmented filter affordance, lucide icon usage"
      contains: "pill-"
      min_lines: 600
  key_links:
    - from: "src/components/primitives/ResourceTable.tsx"
      to: "src/app/globals.css"
      via: "StatusPill tone map consumes the --color-pill-* utility family declared in the token chain"
      pattern: "pill-(green|blue|amber|grey|red)-(bg|ink|dot)"
    - from: "src/components/primitives/ResourceTable.tsx"
      to: "package.json"
      via: "named per-icon imports from lucide-react"
      pattern: "from \"lucide-react\""
---

<objective>
Restyle the list primitive: the tinted `StatusPill`, the tokenised class constants, the card-shaped
table shell, and the segmented filter affordance with real icons.

Purpose: `ResourceTable` backs roughly twenty of the app's forty configured screens. Everything done
here propagates to every staff list without a single per-screen edit, which is precisely what
ROADMAP criterion 3 requires. Its contracts do not change — only how each state paints.

Output: one file, `src/components/primitives/ResourceTable.tsx`, rendering the approved system while
`tests/components/resource-table.test.tsx` stays green throughout.
</objective>

<execution_context>
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/USER/Documents/KQ NEXUS/Learning-Management-System/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-PATTERNS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusPill becomes a tinted pill, and the class constants become tokens</name>
  <reversibility rating="reversible">`StatusPill`'s tone map is a single lookup object inside this file; reverting is one object literal.</reversibility>
  <files>src/components/primitives/ResourceTable.tsx</files>
  <read_first>
    - `src/components/primitives/ResourceTable.tsx` lines 105-125 — the `CELL`, `HEAD`, `BTN`,
      `BTN_PRIMARY` constants and the `Panel` helper. This UPPER_CASE class-constant convention is
      the project idiom documented in `.planning/codebase/CONVENTIONS.md`; every edit in this plan
      extends it rather than inlining new literals at call sites.
    - `src/components/primitives/ResourceTable.tsx` lines 625-655 — the current `StatusPill`
      implementation: a flat white pill with a light neutral border, a mid-neutral label and a
      tone-coloured dot. This is the exact block D-13 replaces.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 5.6 (the five-tone tint/ink/dot table), section 5.4 (the per-tone contrast rows), and
      section 3.2 (radius roles).
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md`
      — the exact utility class names plan 01 published for the pill and semantic tiers.
    - `.planning/codebase/CONVENTIONS.md` — the class-constant convention.
  </read_first>
  <action>
    Rewrite `StatusPill`'s returned JSX to the mockup's tinted pill. Keep the exported function name,
    the props object shape and the `tone` union values exactly as they are today — no consumer of
    this component changes in this phase, and adding or renaming a tone value would break the six
    catalogue components that pass it.

    Map each tone to a triple of utility classes from the pill tier plan 01 published:
    `success` to `bg-pill-green-bg text-pill-green-ink` with a `bg-pill-green-dot` dot; `accent` to
    the blue triple; `warning` to the amber triple; `neutral` to the grey triple; `danger` to the red
    triple. Keep the dot `aria-hidden`, because the label text is what carries the meaning and the
    dot is reinforcement only — every tone's ink-on-tint pair clears 4.5:1, so no WCAG 1.4.11
    exemption is claimed here and none should be written into a comment. The pill takes the full
    pill radius (`rounded-full`), keeps its 11px micro size, and drops its border entirely — the tint
    now does the work the outline used to do.

    Then rewrite the four class constants and the `Panel` helper, keeping them as named constants:
    - `CELL` keeps its padding and alignment; no colour is involved.
    - `HEAD` keeps 11px, semibold, uppercase and its tracking; its colour becomes
      `text-muted-foreground` (D-16).
    - `BTN` becomes a 32px-tall compact control: `border-input-border` edge (a button is an
      interactive control, so it takes the darker 3:1 edge, D-15), `bg-surface`, `text-foreground`,
      `rounded-md`, and a `hover:bg-surface-2` hover.
    - `BTN_PRIMARY` keeps `bg-accent` and `text-accent-contrast`, gains `rounded-md`, and gains the
      brand glow beneath it using the accent-glow token rather than a literal shadow value.
    - `Panel` becomes the card shape used by the empty, denied and error branches: `bg-surface`,
      `border border-border` (a panel frame is decorative — it must keep the light edge),
      `rounded-xl`, and `shadow-xs`. A list screen is a dense surface, so it never takes the 48px
      card shadow.

    Every weight-500 font utility in this file folds to `font-semibold`, per UI-SPEC section 4.3's
    ruling that exactly two weights are active this phase.

    Do not touch the props, the state union, the copy strings, or the structure of any branch. Do not
    add a raw hex.
  </action>
  <acceptance_criteria>
    - `StatusPill`'s exported signature is unchanged — the `tone` union still lists exactly the same
      five values, and no consumer file needs editing.
    - `grep -c 'bg-pill-green-bg' src/components/primitives/ResourceTable.tsx` prints at least 1, and
      the same holds for the blue, amber, grey and red tint classes.
    - The dot span still carries `aria-hidden`.
    - `HEAD` resolves its colour through `text-muted-foreground`.
    - `Panel` carries `rounded-xl`, `border-border` and `shadow-xs`, and does not carry
      `shadow-card`.
    - `npx vitest run --project components tests/components/resource-table.test.tsx` still passes
      every test it passed in plan 02.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project components tests/components/resource-table.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or fewer tests reported than plan 02 recorded</fails_when>
    <automated>grep -c 'bg-pill-grey-bg' src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>prints 0, or non-zero exit (no match)</fails_when>
    <automated>grep -c 'shadow-card' src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>prints any number other than 0 — a dense list surface must not take the 48px shadow</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or the output names src/components/primitives/ResourceTable.tsx</fails_when>
  </verify>
  <done>`StatusPill` renders the five tinted tones with an unchanged prop contract, and the four class constants plus `Panel` consume semantic tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Table shell, state panels and footer take the card treatment</name>
  <files>src/components/primitives/ResourceTable.tsx</files>
  <read_first>
    - `src/components/primitives/ResourceTable.tsx` — the whole file, with particular attention to:
      the loading branch and its skeleton markup, the `ready` branch's `<table>`/`<colgroup>`
      structure and row rendering, the empty branches, the denied branch (around lines 275-306) and
      its comment stating the state is deliberately identical whether or not a record exists, the
      error branch, the bulk-selection bar, and the count/pagination footer.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 7.1's "List screens" paragraph (the exact table-container treatment: low shadow,
      16px radius, decorative edge, roughly 40px header row, leading checkbox column, per-row tinted
      tile, stacked primary/secondary text, mono secondary column, footer count plus
      Previous/Next on the cooler surface tone), plus sections 3.1, 3.2 and 3.3.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html`
      — the drawn staff list screen, which is the tiebreaker on any visual question this prose
      leaves ambiguous.
  </read_first>
  <action>
    Give the `ready` branch's table a container: `bg-surface`, `border border-border`, `rounded-xl`,
    `shadow-xs`, with the table itself clipped to that radius so the first and last rows do not
    square off the corners. The header row sits at roughly 40px with the `HEAD` treatment from task
    1; row dividers use the decorative border token; row hover uses `bg-surface-2`.

    Add the per-row leading tile the mockup draws: a 34px square at `rounded-md`, tinted with the
    same pill tone family the row's status uses, carrying the record's initials. Render it
    `aria-hidden` — it is a visual anchor, not information, and the primary column already carries
    the record's name. Keep the existing leading checkbox column ahead of it; do not reorder the
    selection column.

    Stack the primary column's label and its `Column.subtitle` — the label at 14px/600 in
    `text-foreground`, the subtitle at 14px/400 in `text-muted-foreground`. Per UI-SPEC section 4.3,
    the primary/secondary distinction is carried by colour and weight, not by a fifth font size; do
    not shrink the subtitle.

    The count and pagination footer takes `bg-surface-2` with a decorative top border, sitting inside
    the container. The bulk-selection bar takes the same surface tone.

    Restyle the four non-`ready` branches through the `Panel` helper task 1 already tokenised, so
    empty, denied and error share one card shape. In the denied branch change colour, radius and
    shadow classes only: its heading, body, permission code element and the closing sentence about
    copy being identical must survive word for word, and no icon, tone or layout difference may be
    introduced there. The inline permission code element moves from a neutral-ramp background to
    `bg-surface-2`.

    Keep the loading branch's `<colgroup>` widths, its skeleton block count and its `aria-live`
    region exactly as they are; only the skeleton block's own colour and radius change.

    Keep the below-640px card treatment and the `hideOnMobile` behaviour intact — the mockup is drawn
    at 1280px only and says nothing about narrow widths, so the existing responsive contract is
    binding (UI-SPEC 8.16 item 2). The table must still never scroll horizontally.

    Replace every remaining utility drawn from Tailwind's built-in neutral ramp in this file with a
    semantic token: 500-level neutral text becomes `text-muted-foreground`; 600/700/800/900-level
    neutral text becomes `text-foreground`; a 300-level neutral edge on an interactive control
    becomes `border-input-border`; a 200-level neutral edge on a panel, divider or tab strip becomes
    `border-border`; 50/100-level neutral fills become `bg-surface-2`; and `bg-white` becomes
    `bg-surface`.
  </action>
  <acceptance_criteria>
    - The denied branch's rendered text is unchanged: its heading, permission-scope body and the
      closing sentence about identical copy are all still present verbatim.
    - No utility class in this file is drawn from Tailwind's built-in neutral ramp.
    - `grep -c 'rounded-xl' src/components/primitives/ResourceTable.tsx` prints at least 1.
    - The `<colgroup>` width handling in the loading branch is unchanged.
    - `overflow-x` is not introduced anywhere in this file.
    - `npx vitest run --project components tests/components/resource-table.test.tsx` passes,
      including the denial-parity and no-reflow tests.
    - `npx next build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'zinc-' src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>prints any number other than 0</fails_when>
    <automated>npx vitest run --project components tests/components/resource-table.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or the denial-parity test is reported as failing</fails_when>
    <automated>grep -c 'overflow-x' src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>prints any number other than 0 — rows become cards below 640px, they never scroll sideways</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
  </verify>
  <done>The table renders as a 16px-radius card on the low shadow with tokenised rows, panels and footer, and every state test still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Segmented filter affordance, real icons, and control edges</name>
  <files>src/components/primitives/ResourceTable.tsx</files>
  <read_first>
    - `src/components/primitives/ResourceTable.tsx` — the `TableFilter` union (around line 43), the
      filter render block (around lines 225-250 where the `<select>` lives), the active-query chip
      and its dismiss control, the sortable column header render and its `aria-sort` handling, and
      the `onCreate`/`createLabel` header action.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md`
      section 8.16 item 1 — the segmented control is adopted as a visual affordance on an unchanged
      state machine; and section 1's icon inventory table, which names `Search`, `Plus`, `X`,
      `ArrowUp`, `ArrowDown` and `ArrowUpDown` as this file's icons and states their accessibility
      treatment.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html`
      — the drawn segmented control: an inset track on the cooler surface tone with the active
      segment as a white fill carrying the low shadow.
    - `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-01-SUMMARY.md`
      — the resolved `lucide-react` version and the per-icon import convention.
  </read_first>
  <action>
    Render a `select`-kind `TableFilter` whose option count is small enough to lay out inline as the
    mockup's segmented control: a track on `bg-surface-2` at `rounded-md`, each segment a button, the
    active segment on `bg-surface` with `shadow-xs`. Fall back to the existing `<select>` for filters
    with too many options to fit. This is a rendering change only: the segment's click handler must
    call the same `onFilterChange` the `<select>`'s change handler calls today, so filters and sort
    keep reading from and writing to the URL, and a filter validation error keeps rendering its
    `role="alert"` banner with the previous rows visible beneath it. Do not introduce a second
    filtering mechanism.

    Give the segmented control real semantics: mark it as a group with an accessible name naming the
    filter, and give each segment `aria-pressed` reflecting whether it is active — active state must
    not be discoverable by fill colour alone (NFR-09).

    Import icons as named per-icon imports from `lucide-react`; never a namespace import of the whole
    set, which would defeat tree-shaking. Add `Search` inside the search input, `Plus` inside the
    create action beside its existing label, and `X` on the query-chip dismiss control. The dismiss
    control carries its own accessible name naming the filter it removes, independent of the chip's
    visible text. Add `ArrowUp`/`ArrowDown`/`ArrowUpDown` to sortable column headers as
    `aria-hidden` decoration only — the header's existing `aria-sort` attribute is what exposes sort
    state, and the glyph must not add a competing accessible name.

    Give every interactive control edge in this block the darker 3:1 treatment: the search input, the
    fallback `<select>`, and the segment buttons all take `border-input-border` at `rounded-md` and
    the 38px standard control height (32px for the compact segment row). The query chip is not an
    interactive control edge; it keeps the decorative border token.
  </action>
  <acceptance_criteria>
    - `grep -c 'from "lucide-react"' src/components/primitives/ResourceTable.tsx` prints at least 1
      and the import is a named-member import, not a namespace import.
    - `grep -c 'import \* as' src/components/primitives/ResourceTable.tsx` prints 0.
    - The segment buttons carry `aria-pressed` and the group carries an accessible name.
    - The sort glyphs carry `aria-hidden` and the column header still sets `aria-sort`.
    - The dismiss control's accessible name names the filter being removed.
    - The filter change path still calls the same `onFilterChange` prop; no new filtering state is
      introduced.
    - `npx vitest run --project components tests/components/resource-table.test.tsx` passes,
      including the validation-error-keeps-rows test.
    - `npx next build` exits 0 and `npx eslint src` reports no error.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'import \* as' src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>prints any number other than 0 — a namespace import defeats per-icon tree-shaking</fails_when>
    <automated>grep -c 'aria-pressed' src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>prints 0, or non-zero exit (no match)</fails_when>
    <automated>npx vitest run --project components tests/components/resource-table.test.tsx</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>
    <automated>npx eslint src/components/primitives/ResourceTable.tsx</automated>
    <fails_when>non-zero exit, or the output reports any error-severity problem</fails_when>
    <automated>npx next build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
    <human-check>Run `npm run dev` and open `/staff/courses`. Confirm the filter reads as a segmented track with a white active segment, that clicking a segment changes the URL exactly as the old select did, that the search box and the create button carry their glyphs, and that tabbing reaches every segment with a visible focus ring. Then narrow the window below 640px and confirm rows become cards with no horizontal scrollbar.</human-check>
  </verify>
  <done>Filters render as the approved segmented affordance over the unchanged URL-driven state machine, with named icons and accessible control edges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization decision → rendered `denied` branch | What a user without permission is allowed to learn about a record they cannot see |
| URL query string → filter/sort state | Untrusted input already drives which rows render; this plan changes the affordance, not the parsing |
| npm package → rendered DOM | `lucide-react` SVG components render into every staff list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Information Disclosure | `ResourceTable` `denied` branch | high | mitigate | Task 2 changes only colour, radius and shadow classes inside that branch; its copy and structure survive word for word, and `tests/components/resource-table.test.tsx`'s byte-equality parity test from plan 02 runs on every verify in this plan |
| T-04.1-08 | Tampering | Segmented filter affordance | medium | mitigate | The segment handler calls the same `onFilterChange` prop the `<select>` calls today; no new filter state, no new parsing path, and the validation-error-keeps-rows test proves the state machine survived |
| T-04.1-09 | Information Disclosure | Per-row initials tile | low | accept | The tile derives its initials from a value the primary column already renders in full, so it discloses nothing the row does not already show; it is `aria-hidden` so it adds no duplicate announcement |
| T-04.1-SC | Tampering | `lucide-react` icon imports | high | mitigate | Install was gated by the blocking-human legitimacy checkpoint in plan 01; this plan additionally forbids a namespace import so only the named glyphs enter the bundle |
</threat_model>

<verification>
- `npx vitest run --project components --no-file-parallelism` green including all four primitive
  test files.
- `npx next build` clean.
- `npx tsc --noEmit` clean; `npx eslint src` reports no error.
- No utility drawn from Tailwind's built-in neutral ramp remains in this file.
- No raw hex in this file.
</verification>

<success_criteria>
Every staff list screen renders the approved table treatment without a single per-screen edit,
`StatusPill` is a tinted pill with an unchanged prop contract, and all five states plus denial parity
still pass their tests.
</success_criteria>

## Artifacts this phase produces

New within `src/components/primitives/ResourceTable.tsx`: a `StatusPill` tone map keyed to the
`--color-pill-*` utility family; a per-row initials tile element; a segmented filter control
rendering path alongside the retained `<select>` fallback; named `lucide-react` imports (`Search`,
`Plus`, `X`, `ArrowUp`, `ArrowDown`, `ArrowUpDown`). The `CELL`, `HEAD`, `BTN`, `BTN_PRIMARY`
constants and the `Panel` helper are retained names with new values.

No new exported symbol and no changed prop type — `ResourceTable`, `StatusPill`, `Column`,
`TableFilter`, `SortState`, `BulkSelection` and `ResourceTableState` all keep their existing
signatures.

<output>
Create `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-03-SUMMARY.md` when done.
</output>

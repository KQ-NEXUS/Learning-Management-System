---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "16"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/staff/StaffShell.tsx"
  - "tests/components/staff-shell.test.tsx"
  - "src/components/shell/BrandMark.tsx"
  - "src/components/shell/LearnerShell.tsx"
  - "src/app/(auth)/layout.tsx"
  - "tests/components/brand-mark.test.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Mobile keyboard users cannot reach closed navigation; opened menu is operable and desktop navigation stays active."
    - "Staff, auth and learner chrome show the actual approved glyph rather than empty gradient tiles."
  artifacts:
    - path: "src/app/staff/StaffShell.tsx"
      provides: "Keyboard-safe staff navigation and approved shell mark"
    - path: "tests/components/staff-shell.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/components/shell/BrandMark.tsx"
      provides: "Keyboard-safe staff navigation and approved shell mark"
    - path: "src/components/shell/LearnerShell.tsx"
      provides: "Keyboard-safe staff navigation and approved shell mark"
    - path: "src/app/(auth)/layout.tsx"
      provides: "Keyboard-safe staff navigation and approved shell mark"
    - path: "tests/components/brand-mark.test.tsx"
      provides: "Executable regression evidence"
  key_links:
    - from: "src/app/staff/StaffShell.tsx"
      to: "src/components/shell/BrandMark.tsx"
      via: "Shell imports approved mark while retaining responsive navigation focus lifecycle"
---

<objective>
Keyboard-safe staff navigation and approved shell mark. Close the source-confirmed gaps while honoring D-01–D-26 and the phase boundary: preserve existing capabilities, routes, data, authorization and action contracts. The approved CONTEXT overrides contradictory UI-SPEC generalizations. Prior plans 01–14 remain executed history; this is additive gap closure.
</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-ANNOTATIONS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-REVIEW.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
</context>

## Planner Assumptions

- No dependency installs, external API integration, schema mutation or new permission is required. ASVS level 1; high is the blocking threshold.
- Existing native/browser behavior remains authoritative; component tests prove DOM semantics, not rendered geometry.
- Tests named below that do not yet exist are created in the same implementation task before the repair and must first expose the reported failure. The existing Vitest node/components projects require no installation.
- Estimate factor 1, sample_count 0, confidence low from estimate-calibration.
- Preserve working tree changes and do not commit (commit_docs=false; parent execution policy).

<tasks>

<task type="tracer" tdd="true">
  <name>Trace opening and closing mobile navigation</name>
  <files>src/app/staff/StaffShell.tsx, tests/components/staff-shell.test.tsx</files>
  <read_first>src/app/staff/StaffShell.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Mobile keyboard users cannot reach closed navigation; opened menu is operable and desktop navigation stays active. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Keep one responsive sidebar and existing five nav items/server guard per D-19/D-20. Track matchMedia('(min-width: 1024px)') with cleanup and a hydration-safe initial state. Closed mobile aside is inert and aria-hidden; desktop is always non-inert. On mobile open focus the first nav link, trap Tab/Shift+Tab in the drawer, close on Escape/backdrop/navigation and return focus to menu trigger. Handle resizing open mobile to desktop without hiding desktop links or leaving focus on a now-hidden trigger. Prevent background interaction only while mobile menu is open, restoring prior inert states on cleanup. Tests simulate both breakpoints and check inactive branch attributes plus complete focus lifecycle.</action>
  <acceptance_criteria>Mobile keyboard users cannot reach closed navigation; opened menu is operable and desktop navigation stays active. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/staff-shell.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Mobile keyboard users cannot reach closed navigation; opened menu is operable and desktop navigation stays active.</done>
</task>

<task type="auto" tdd="true">
  <name>Render the approved brand mark across all three shells</name>
  <files>src/components/shell/BrandMark.tsx, src/app/staff/StaffShell.tsx, src/components/shell/LearnerShell.tsx, src/app/(auth)/layout.tsx, tests/components/brand-mark.test.tsx</files>
  <read_first>src/components/shell/BrandMark.tsx, src/app/staff/StaffShell.tsx, src/components/shell/LearnerShell.tsx, src/app/(auth)/layout.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Staff, auth and learner chrome show the actual approved glyph rather than empty gradient tiles. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Copy the two path geometries from MOCKUP-SOURCE line 123 into a small decorative inline SVG with viewBox 0 0 20 20 and currentColor stroke, inside the existing 140-degree gradient square. Reuse BrandMark in all three shells, keeping adjacent wordmark as accessible name and SVG aria-hidden/focusable=false. Use existing semantic foreground and gradient tokens only, per D-02/D-08/D-19/D-21/D-23. Test paths and decorative semantics and render all three consuming shells without introducing client requirements to auth layout.</action>
  <acceptance_criteria>Staff, auth and learner chrome show the actual approved glyph rather than empty gradient tiles. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/brand-mark.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Staff, auth and learner chrome show the actual approved glyph rather than empty gradient tiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User interaction to existing protected action | UI repair must not bypass server authorization or misrepresent results |
| Source/evidence to completion claim | Automated checks cannot stand in for pending visual or media accessibility evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G16-01 | Elevation of Privilege | Existing route/action consumers | high | mitigate | Preserve bound IDs and permission checks; test denied/failed outcomes where touched and run combined service suite in 29 |
| T-04.1-G16-02 | Repudiation | Feedback and verification | medium | mitigate | Preserve failure state, never assert mutation success after rejection, retain captions and human UAT gaps explicitly |
</threat_model>

<verification>
Run each task command from repository root. Focused tests must pass after the corresponding repair; plan 29 runs full combined gates once. A new test path is created before its command executes. Visual source checks do not certify browser layout. Captions scope remains unresolved and cannot be cleared by these repairs.
</verification>

<success_criteria>
All task acceptance criteria pass, scope remains bounded, and the SUMMARY records tests plus unresolved human/media checks honestly. Do not mark NFR-09 or the phase complete.
</success_criteria>

## Artifacts Produced

- `src/app/staff/StaffShell.tsx`
- `tests/components/staff-shell.test.tsx`
- `src/components/shell/BrandMark.tsx`
- `src/components/shell/LearnerShell.tsx`
- `src/app/(auth)/layout.tsx`
- `tests/components/brand-mark.test.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-16-SUMMARY.md` (execution evidence)

<output>
Create .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-16-SUMMARY.md. Report completed fixes, exact commands/results and any blocked scope decision without overwriting historical audits.
</output>

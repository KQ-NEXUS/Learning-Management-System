---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "31"
subsystem: ui
tags: [design-system, tailwind, catalogue, reading, type-scale, spacing-grid]

requires:
  - phase: 04.1-24
    provides: design-contract source gate, primitive visual roles, locked-exception allowlist
  - phase: 04.1-18
    provides: publish readiness / course detail surfaces
  - phase: 04.1-19
    provides: lesson authoring / structure board surface
  - phase: 04.1-23
    provides: upload panel polling / lesson resource surface
provides:
  - Catalogue reading chrome (ArrangeBoard, ReadinessPanel, LessonContent) snapped to the UI-SPEC 4.3 type scale and 3.1 spacing grid
  - catalogue-reading-entry and catalogue-reading-expansion design-contract groups pass
affects: [04.1-29, design-system-rollout]

tech-stack:
  added: []
  patterns:
    - "Catalogue reading surfaces follow the same 14/16/11px type + 4/8/16/24 spacing roles established for primitives in 04.1-24"

key-files:
  created:
    - .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-31-SUMMARY.md
  modified:
    - src/components/catalogue/ArrangeBoard.tsx
    - src/components/catalogue/ReadinessPanel.tsx
    - src/components/catalogue/LessonContent.tsx

key-decisions:
  - "text-xs (12px) and text-[10px] resolved per role: secondary sublabels / hints / captions / eyebrow tags -> text-[11px]; status labels and empty-state body copy -> text-sm (14px). Not blindly demoted to micro per plan guidance."
  - "Off-grid chrome padding (px-2.5 / px-3 / py-3 / py-1.5 / px-1.5 / py-0.5) snapped to px-4 py-2 / px-2 py-1, matching the 04.1-24 ConfirmModal + 04.1-30 catalogue-controls precedent"
  - "gap-0.5 / gap-1.5 -> gap-1 (4px)"
  - "LessonMediaPlayer.tsx and UnsavedOrderGuard.tsx were already contract-compliant; left untouched (native <video>, guard modal already on-grid)"

patterns-established:
  - "Pattern: catalogue reading visual reconciliation mirrors the primitive/authoring contract roles — no bespoke scale"

requirements-completed: []

duration: 8min
completed: 2026-09-07
---

# Phase 04.1 Plan 31: Catalogue Reading Visual Reconciliation Summary

**Catalogue reading chrome (structure/arrange board, publication readiness panel, learner lesson renderer) snapped to the UI-SPEC 4.3 type scale and 3.1 spacing grid without touching props, action bindings, permissions, data, routes, keyboard drag announcements or sanitized lesson-body typography.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-09-07T02:19:00Z
- **Completed:** 2026-09-07T02:27:00Z
- **Tasks:** 2
- **Files modified:** 3 (of 5 in scope; 2 were already compliant)

## Accomplishments
- `catalogue-reading-entry` design-contract group now passes (ArrangeBoard, ReadinessPanel, UnsavedOrderGuard)
- `catalogue-reading-expansion` design-contract group now passes (LessonContent, LessonMediaPlayer)
- All touched surfaces use only permitted font-size (14/16/11px), font-weight (400/600) and spacing (4/8/16/24/32/48/64) roles
- Existing component tests for the three surfaces still green (arrange-board, readiness-panel, lesson-content, lesson-preview-route)

## Task Commits

1. **Task 1: Catalogue reading visual reconciliation — entry path** - `c4f0d6d` (feat)
2. **Task 2: Catalogue reading visual reconciliation — remaining consumers** - `30d8628` (feat)

## Files Created/Modified
- `src/components/catalogue/ArrangeBoard.tsx` - ROW/header/error-alert/empty-row/withdrawn-details padding to px-4 py-2; grey + amber pill badges to px-2 py-1 text-[11px]; save button to px-4 py-2 text-sm; sublabel to text-[11px]; summary + kind eyebrows to text-[11px]; gap-1.5 -> gap-1
- `src/components/catalogue/ReadinessPanel.tsx` - row gap-0.5 -> gap-1; category list px-3 -> px-4; status label text-xs -> text-sm; detail / empty-note / summary text-xs -> text-[11px]
- `src/components/catalogue/LessonContent.tsx` - scan-blocked / embed-error / placeholder / download-control padding to px-4 py-2; filename size hint + figcaption -> text-[11px]; withdrawn eyebrow px-2.5 -> px-2, text-xs -> text-[11px]
- `src/components/catalogue/LessonMediaPlayer.tsx` - **not modified** (already contract-compliant; native `<video>`, no off-grid utilities)
- `src/components/catalogue/UnsavedOrderGuard.tsx` - **not modified** (already contract-compliant; group test green as-is)

## Decisions Made
- **text-xs / text-[10px] handling:** followed the plan's explicit warning not to blindly demote every `text-xs` to micro. Secondary sublabels, hint/detail/summary lines, figcaptions and uppercase eyebrow tags -> `text-[11px]`. The readiness status label ("Ready" / "Not ready" / "Warning") and the arrange-board empty-container copy -> `text-sm` (14px) as genuine label/body text.
- **Control / chrome padding:** adopted the 04.1-24 `ConfirmModal` + 04.1-30 catalogue-controls precedent — `px-3` / `px-2.5` / `py-1.5` / `py-3` chrome -> `px-4 py-2`; compact pill badges `px-1.5 py-0.5` -> `px-2 py-1`.
- **Two files left untouched:** `LessonMediaPlayer.tsx` and `UnsavedOrderGuard.tsx` are in the plan `files_modified` list but were already fully contract-compliant and their group `it()` cases were already green. No edit made — editing to "touch" them would add risk with zero benefit.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Task-to-file grouping aligned to the contract manifest**
- **Found during:** Task 1
- **Issue:** PLAN Task 1 `<files>` names ArrangeBoard + ReadinessPanel and verifies `-t catalogue-reading-entry`; the `catalogue-reading-entry` group in `tests/unit/design-contract.test.ts` is ArrangeBoard + ReadinessPanel + UnsavedOrderGuard. PLAN Task 2 `<files>` names LessonContent + LessonMediaPlayer + UnsavedOrderGuard; the `catalogue-reading-expansion` group is LessonContent + LessonMediaPlayer only. Following the PLAN `<files>` lists literally would misalign each task from its verify command.
- **Fix:** Mapped each task to the files actually graded by its verify group. Task 1 fixed the entry-group offenders (ArrangeBoard, ReadinessPanel; UnsavedOrderGuard already clean). Task 2 fixed the expansion-group offenders (LessonContent; LessonMediaPlayer already clean). All in-scope files are covered.
- **Files modified:** n/a (process)
- **Verification:** both `-t` commands pass; component regressions green
- **Committed in:** c4f0d6d, 30d8628

---

**Total deviations:** 1 (blocking mapping alignment, no code scope change)
**Impact on plan:** None on scope — the same surface set was reconciled; only task/commit partitioning changed to keep each verify command meaningful.

## Issues Encountered
- Worktree has no `node_modules`; `npx.cmd vitest` could not be used. Ran all tests via the main checkout's vitest binary (`node C:/Users/disuk/Learning-Management-System/node_modules/vitest/vitest.mjs`), v4.1.11 — all passed.

## Test Results

| Command | Result |
|---|---|
| `vitest run tests/unit/design-contract.test.ts -t catalogue-reading-entry` | PASS (3 passed, 25 skipped) |
| `vitest run tests/unit/design-contract.test.ts -t catalogue-reading-expansion` | PASS (2 passed, 26 skipped) |
| `vitest run tests/unit/design-contract.test.ts` (full file) | PASS (no failures) |
| component: `tests/components/arrange-board.test.tsx`, `tests/components/readiness-panel.test.tsx` | PASS (21 passed) |
| component: `tests/components/lesson-content.test.tsx` | PASS (15 passed) |
| route: `tests/lesson-preview-route.test.ts` | PASS |

## Unresolved / Human + Visual Checks
- **Not certified:** rendered browser layout / visual regression for ArrangeBoard, ReadinessPanel, LessonContent. The contract test is a mechanical source gate only — geometry, wrapping and spacing rhythm need a human visual pass.
- **Not certified:** media / caption accessibility scope (plan threat T-04.1-G31-02, `04.1-CAPTIONS-SCOPE.md`) remains open. `LessonMediaPlayer` still ships no `<track>` and there is no caption data contract.
- **Not verified here:** the combined phase gate (plan 04.1-29 owns the full-suite run).
- **NFR-09 NOT marked complete. Phase NOT marked complete.**

## Next Phase Readiness
- catalogue-reading-entry / -expansion groups green; plan 04.1-29 can run the combined gate across all phase groups.

## Self-Check: PASSED

- FOUND: src/components/catalogue/ArrangeBoard.tsx (modified)
- FOUND: src/components/catalogue/ReadinessPanel.tsx (modified)
- FOUND: src/components/catalogue/LessonContent.tsx (modified)
- FOUND: commit c4f0d6d (Task 1)
- FOUND: commit 30d8628 (Task 2)

---
*Phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac*
*Completed: 2026-09-07*

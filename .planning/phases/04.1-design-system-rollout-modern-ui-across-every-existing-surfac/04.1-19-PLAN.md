---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "19"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/components/primitives/ResourceForm.tsx"
  - "tests/components/resource-form.test.tsx"
  - "src/components/catalogue/RichTextEditor.tsx"
  - "src/components/catalogue/LessonFormFields.tsx"
  - "tests/components/rich-text-editor.test.tsx"
  - "tests/components/lesson-form-fields.test.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every summary link reaches an existing control; non-field failures are visible and never dead links."
    - "Body error text is announced at the focusable editor and the form summary reaches it."
  artifacts:
    - path: "src/components/primitives/ResourceForm.tsx"
      provides: "Form summaries and rich-editor error associations"
    - path: "tests/components/resource-form.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/components/catalogue/RichTextEditor.tsx"
      provides: "Form summaries and rich-editor error associations"
    - path: "src/components/catalogue/LessonFormFields.tsx"
      provides: "Form summaries and rich-editor error associations"
    - path: "tests/components/rich-text-editor.test.tsx"
      provides: "Executable regression evidence"
    - path: "tests/components/lesson-form-fields.test.tsx"
      provides: "Executable regression evidence"
  key_links:
    - from: "src/components/catalogue/LessonFormFields.tsx"
      to: "src/components/catalogue/RichTextEditor.tsx"
      via: "Body control ID and error description bind focusable editor to summary"
---

<objective>
Form summaries and rich-editor error associations. Close the source-confirmed gaps while honoring D-01–D-26 and the phase boundary: preserve existing capabilities, routes, data, authorization and action contracts. The approved CONTEXT overrides contradictory UI-SPEC generalizations. Prior plans 01–14 remain executed history; this is additive gap closure.
</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-ANNOTATIONS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-REVIEW.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
</context>

## Planner Assumptions

- No dependency installs, external API integration, schema mutation or new permission is required. ASVS level 1; high is the blocking threshold.
- Existing native/browser behavior remains authoritative; component tests prove DOM semantics, not rendered geometry.
- Tests named below that do not yet exist are created in the same implementation task before the repair and must first expose the reported failure. The existing Vitest node/components projects require no installation.
- Estimate factor 1, sample_count 0, confidence low from estimate-calibration.
- Preserve working tree changes and do not commit (commit_docs=false; parent execution policy).

<tasks>

<task type="tracer" tdd="true">
  <name>Trace a form-level failure through a useful error summary</name>
  <files>src/components/primitives/ResourceForm.tsx, tests/components/resource-form.test.tsx</files>
  <read_first>src/components/primitives/ResourceForm.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Every summary link reaches an existing control; non-field failures are visible and never dead links. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Keep FormField render-prop API unchanged. Form/non-field errors render alert text rather than links. For candidate field errors resolve field-${name} inside this form after mount/update; render an anchor only if the real target exists, otherwise retain plain error text. Preserve summary focus and failed-submit values. Do not add arbitrary public error-target API unless existing DOM target cannot satisfy the contract. Test mixed field/form errors, absent fields and real target navigation per D-26.</action>
  <acceptance_criteria>Every summary link reaches an existing control; non-field failures are visible and never dead links. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/resource-form.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Every summary link reaches an existing control; non-field failures are visible and never dead links.</done>
</task>

<task type="auto" tdd="true">
  <name>Wire body validation to the rich editor</name>
  <files>src/components/catalogue/RichTextEditor.tsx, src/components/catalogue/LessonFormFields.tsx, tests/components/rich-text-editor.test.tsx, tests/components/lesson-form-fields.test.tsx</files>
  <read_first>src/components/catalogue/RichTextEditor.tsx, src/components/catalogue/LessonFormFields.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Body error text is announced at the focusable editor and the form summary reaches it. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Add optional id, aria-invalid and aria-describedby props to RichTextEditor, forwarding them to the actual Tiptap editable textbox through editorProps attributes and updating on prop changes. BodyEditor uses field-body and a stable body-error ID, retains hidden submitted body field and sanitization pipeline, and connects server body errors. Test summary target focus, accessible description and invalid state for every lesson type that actually exposes BodyEditor; other lesson types retain their existing controls. Preserve D-25 empty-editor hint as decoration, never saved content.</action>
  <acceptance_criteria>Body error text is announced at the focusable editor and the form summary reaches it. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/rich-text-editor.test.tsx tests/components/lesson-form-fields.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Body error text is announced at the focusable editor and the form summary reaches it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User interaction to existing protected action | UI repair must not bypass server authorization or misrepresent results |
| Source/evidence to completion claim | Automated checks cannot stand in for pending visual or media accessibility evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G19-01 | Elevation of Privilege | Existing route/action consumers | high | mitigate | Preserve bound IDs and permission checks; test denied/failed outcomes where touched and run combined service suite in 29 |
| T-04.1-G19-02 | Repudiation | Feedback and verification | medium | mitigate | Preserve failure state, never assert mutation success after rejection, retain captions and human UAT gaps explicitly |
</threat_model>

<verification>
Run each task command from repository root. Focused tests must pass after the corresponding repair; plan 29 runs full combined gates once. A new test path is created before its command executes. Visual source checks do not certify browser layout. Captions scope remains unresolved and cannot be cleared by these repairs.
</verification>

<success_criteria>
All task acceptance criteria pass, scope remains bounded, and the SUMMARY records tests plus unresolved human/media checks honestly. Do not mark NFR-09 or the phase complete.
</success_criteria>

## Artifacts Produced

- `src/components/primitives/ResourceForm.tsx`
- `tests/components/resource-form.test.tsx`
- `src/components/catalogue/RichTextEditor.tsx`
- `src/components/catalogue/LessonFormFields.tsx`
- `tests/components/rich-text-editor.test.tsx`
- `tests/components/lesson-form-fields.test.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-19-SUMMARY.md` (execution evidence)

<output>
Create .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-19-SUMMARY.md. Report completed fixes, exact commands/results and any blocked scope decision without overwriting historical audits.
</output>

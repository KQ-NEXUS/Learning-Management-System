---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "17"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/staff/users/AssignmentDrawer.tsx"
  - "tests/components/assignment-drawer.test.tsx"
  - "src/app/staff/users/StaffAccountForm.tsx"
  - "tests/components/staff-account-form.test.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Assignment dialog semantics match actual keyboard/modal behavior."
    - "Denied saves explain failure without losing selections; stale or failed search responses cannot become selectable current results."
    - "Staff account scope search recovers with a visible explanation and only current results."
  artifacts:
    - path: "src/app/staff/users/AssignmentDrawer.tsx"
      provides: "Assignment drawer focus, failure feedback and scope searches"
    - path: "tests/components/assignment-drawer.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/app/staff/users/StaffAccountForm.tsx"
      provides: "Assignment drawer focus, failure feedback and scope searches"
    - path: "tests/components/staff-account-form.test.tsx"
      provides: "Executable regression evidence"
  key_links:
    - from: "src/app/staff/users/AssignmentDrawer.tsx"
      to: "src/app/staff/users/actions.ts"
      via: "Assignment submission and scope search return actionable failure state"
---

<objective>
Assignment drawer focus, failure feedback and scope searches. Close the source-confirmed gaps while honoring D-01–D-26 and the phase boundary: preserve existing capabilities, routes, data, authorization and action contracts. The approved CONTEXT overrides contradictory UI-SPEC generalizations. Prior plans 01–14 remain executed history; this is additive gap closure.
</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-ANNOTATIONS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-REVIEW.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
</context>

## Planner Assumptions

- No dependency installs, external API integration, schema mutation or new permission is required. ASVS level 1; high is the blocking threshold.
- Existing native/browser behavior remains authoritative; component tests prove DOM semantics, not rendered geometry.
- Tests named below that do not yet exist are created in the same implementation task before the repair and must first expose the reported failure. The existing Vitest node/components projects require no installation.
- Estimate factor 1, sample_count 0, confidence low from estimate-calibration.
- Preserve working tree changes and do not commit (commit_docs=false; parent execution policy).

<tasks>

<task type="tracer" tdd="true">
  <name>Trace keyboard assignment dialog operation</name>
  <files>src/app/staff/users/AssignmentDrawer.tsx, tests/components/assignment-drawer.test.tsx</files>
  <read_first>src/app/staff/users/AssignmentDrawer.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Assignment dialog semantics match actual keyboard/modal behavior. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Mirror ConfirmModal's existing focus lifecycle locally, preserving the explicit no-fifth-drawer-primitive deferral. Save opener; focus first usable control; Tab wraps both directions, with heading fallback when no enabled controls; Escape and close/backdrop are suppressed while pending; cleanup restores opener and prior background inert state. Never inert an ancestor containing the drawer. Test open, Shift+Tab/Tab, pending Escape suppression and successful close focus return per D-26.</action>
  <acceptance_criteria>Assignment dialog semantics match actual keyboard/modal behavior. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/assignment-drawer.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Assignment dialog semantics match actual keyboard/modal behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep assignment failures visible and scope results current</name>
  <files>src/app/staff/users/AssignmentDrawer.tsx, tests/components/assignment-drawer.test.tsx</files>
  <read_first>src/app/staff/users/AssignmentDrawer.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Denied saves explain failure without losing selections; stale or failed search responses cannot become selectable current results. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>CR-05/WR-05: render form and authorization errors in a visible role=alert banner while retaining entered user/role/scope and field messages. Associate field errors where targets exist. Add catch/finally and cancellation/request-generation protection to scope and staff-user searches; ignore stale completions, clear stale choices on failed scope query, display retryable search error and do not treat rejection as successful empty results. Test returned form denial, rejected scope search then retry, and out-of-order responses.</action>
  <acceptance_criteria>Denied saves explain failure without losing selections; stale or failed search responses cannot become selectable current results. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/assignment-drawer.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Denied saves explain failure without losing selections; stale or failed search responses cannot become selectable current results.</done>
</task>

<task type="auto" tdd="true">
  <name>Apply scope-search recovery to staff account creation</name>
  <files>src/app/staff/users/StaffAccountForm.tsx, tests/components/staff-account-form.test.tsx</files>
  <read_first>src/app/staff/users/StaffAccountForm.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Staff account scope search recovers with a visible explanation and only current results. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Add pending/error state, catch/finally, stale-completion cancellation and explicit retry for scope lookup. Preserve existing useActionState contract, selected scope validation and server-authoritative role checks. Test failure then successful retry and a late response from a superseded scope/query; no console unhandled rejection should occur.</action>
  <acceptance_criteria>Staff account scope search recovers with a visible explanation and only current results. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/staff-account-form.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Staff account scope search recovers with a visible explanation and only current results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User interaction to existing protected action | UI repair must not bypass server authorization or misrepresent results |
| Source/evidence to completion claim | Automated checks cannot stand in for pending visual or media accessibility evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G17-01 | Elevation of Privilege | Existing route/action consumers | high | mitigate | Preserve bound IDs and permission checks; test denied/failed outcomes where touched and run combined service suite in 29 |
| T-04.1-G17-02 | Repudiation | Feedback and verification | medium | mitigate | Preserve failure state, never assert mutation success after rejection, retain captions and human UAT gaps explicitly |
</threat_model>

<verification>
Run each task command from repository root. Focused tests must pass after the corresponding repair; plan 29 runs full combined gates once. A new test path is created before its command executes. Visual source checks do not certify browser layout. Captions scope remains unresolved and cannot be cleared by these repairs.
</verification>

<success_criteria>
All task acceptance criteria pass, scope remains bounded, and the SUMMARY records tests plus unresolved human/media checks honestly. Do not mark NFR-09 or the phase complete.
</success_criteria>

## Artifacts Produced

- `src/app/staff/users/AssignmentDrawer.tsx`
- `tests/components/assignment-drawer.test.tsx`
- `src/app/staff/users/StaffAccountForm.tsx`
- `tests/components/staff-account-form.test.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-17-SUMMARY.md` (execution evidence)

<output>
Create .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-17-SUMMARY.md. Report completed fixes, exact commands/results and any blocked scope decision without overwriting historical audits.
</output>

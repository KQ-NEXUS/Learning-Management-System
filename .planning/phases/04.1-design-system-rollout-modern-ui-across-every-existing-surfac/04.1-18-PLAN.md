---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: "18"
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/components/catalogue/PublishDialog.tsx"
  - "tests/components/publish-dialog.test.tsx"
  - "src/components/catalogue/CourseDetailActions.tsx"
  - "tests/components/course-detail-actions.test.tsx"
  - "src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx"
  - "tests/components/programme-detail-client.test.tsx"
autonomous: true
gap_closure: true
requirements: [NFR-09]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Only an explicit valid immutable payload publishes and the dialog is fully keyboard operable."
    - "Course detail mutations cannot strand disabled controls or claim success after rejection."
    - "Programme detail controls recover safely without duplicate automatic mutations."
  artifacts:
    - path: "src/components/catalogue/PublishDialog.tsx"
      provides: "Publish confirmation and catalogue action recovery"
    - path: "tests/components/publish-dialog.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/components/catalogue/CourseDetailActions.tsx"
      provides: "Publish confirmation and catalogue action recovery"
    - path: "tests/components/course-detail-actions.test.tsx"
      provides: "Executable regression evidence"
    - path: "src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx"
      provides: "Publish confirmation and catalogue action recovery"
    - path: "tests/components/programme-detail-client.test.tsx"
      provides: "Executable regression evidence"
  key_links:
    - from: "src/components/catalogue/CourseDetailActions.tsx"
      to: "src/components/catalogue/PublishDialog.tsx"
      via: "Confirmation callback carries immutable publish choices"
    - from: "src/components/catalogue/CourseDetailActions.tsx"
      to: "src/app/staff/courses/[id]/publish-actions.ts"
      via: "Protected mutations resolve or reject into visible recoverable state"
---

<objective>
Publish confirmation and catalogue action recovery. Close the source-confirmed gaps while honoring D-01–D-26 and the phase boundary: preserve existing capabilities, routes, data, authorization and action contracts. The approved CONTEXT overrides contradictory UI-SPEC generalizations. Prior plans 01–14 remain executed history; this is additive gap closure.
</objective>

<execution_context>
@C:/Users/USER/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/USER/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-SOURCE.html
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-MOCKUP-ANNOTATIONS.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-VERIFICATION.md
@.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-REVIEW.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
</context>

## Planner Assumptions

- No dependency installs, external API integration, schema mutation or new permission is required. ASVS level 1; high is the blocking threshold.
- Existing native/browser behavior remains authoritative; component tests prove DOM semantics, not rendered geometry.
- Tests named below that do not yet exist are created in the same implementation task before the repair and must first expose the reported failure. The existing Vitest node/components projects require no installation.
- Estimate factor 1, sample_count 0, confidence low from estimate-calibration.
- Preserve working tree changes and do not commit (commit_docs=false; parent execution policy).

<tasks>

<task type="tracer" tdd="true">
  <name>Trace keyboard publish confirmation with immutable pending payload</name>
  <files>src/components/catalogue/PublishDialog.tsx, tests/components/publish-dialog.test.tsx</files>
  <read_first>src/components/catalogue/PublishDialog.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Only an explicit valid immutable payload publishes and the dialog is fully keyboard operable. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Implement initial focus, Tab wrap, Escape when idle, opener restoration and background inert lifecycle using ConfirmModal as local precedent. Do not extract a new drawer primitive. Disable all cohort checkboxes and reason textarea while pending; guard toggle and submission in-flight. Retain every readiness result, unchecked default per cohort, minimum reason and expectedUpdatedAt token. Test keyboard lifecycle, busy input immutability and exact submitted migrateCohortIds/reason/token payload per D-26.</action>
  <acceptance_criteria>Only an explicit valid immutable payload publishes and the dialog is fully keyboard operable. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/publish-dialog.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Only an explicit valid immutable payload publishes and the dialog is fully keyboard operable.</done>
</task>

<task type="auto" tdd="true">
  <name>Recover course-detail action transport failures</name>
  <files>src/components/catalogue/CourseDetailActions.tsx, tests/components/course-detail-actions.test.tsx</files>
  <read_first>src/components/catalogue/CourseDetailActions.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Course detail mutations cannot strand disabled controls or claim success after rejection. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Wrap each imperative async event flow in try/catch/finally, preserving existing resolved error branches and authorization action contracts. On unexpected rejection show a generic retryable error without raw server exception details; clear busy in finally; keep dialog/reason and unpublished changes available for retry. Do not convert a failed publish/archive into success or automatically repeat a mutation. Test each action rejection and a user-initiated successful retry.</action>
  <acceptance_criteria>Course detail mutations cannot strand disabled controls or claim success after rejection. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/course-detail-actions.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Course detail mutations cannot strand disabled controls or claim success after rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Recover programme-detail action transport failures</name>
  <files>src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx, tests/components/programme-detail-client.test.tsx</files>
  <read_first>src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx; existing tests/components/confirm-modal.test.tsx and tests/components/resource-table.test.tsx for test conventions; .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-GAP-COVERAGE.md for binding dispositions.</read_first>
  <behavior>Programme detail controls recover safely without duplicate automatic mutations. Test both the described successful interaction and its reported failure/edge path.</behavior>
  <action>Apply the same explicit try/catch/finally lifecycle to every imperative mutation in ProgrammeDetailClient. Retain submitted reason, selected record and modal on rejection, clear operation busy key, and display retryable generic alert. Keep server actions and route guards unchanged. Test each operation's rejected call followed by user retry.</action>
  <acceptance_criteria>Programme detail controls recover safely without duplicate automatic mutations. Every listed focused regression passes; no existing user capability or server guard is removed.</acceptance_criteria>
  <verify><automated>npx.cmd vitest run --project components tests/components/programme-detail-client.test.tsx</automated><fails_when>The command exits nonzero, a named behavior assertion fails, or the reported failure still reproduces.</fails_when></verify>
  <done>Programme detail controls recover safely without duplicate automatic mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User interaction to existing protected action | UI repair must not bypass server authorization or misrepresent results |
| Source/evidence to completion claim | Automated checks cannot stand in for pending visual or media accessibility evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-G18-01 | Elevation of Privilege | Existing route/action consumers | high | mitigate | Preserve bound IDs and permission checks; test denied/failed outcomes where touched and run combined service suite in 29 |
| T-04.1-G18-02 | Repudiation | Feedback and verification | medium | mitigate | Preserve failure state, never assert mutation success after rejection, retain captions and human UAT gaps explicitly |
</threat_model>

<verification>
Run each task command from repository root. Focused tests must pass after the corresponding repair; plan 29 runs full combined gates once. A new test path is created before its command executes. Visual source checks do not certify browser layout. Captions scope remains unresolved and cannot be cleared by these repairs.
</verification>

<success_criteria>
All task acceptance criteria pass, scope remains bounded, and the SUMMARY records tests plus unresolved human/media checks honestly. Do not mark NFR-09 or the phase complete.
</success_criteria>

## Artifacts Produced

- `src/components/catalogue/PublishDialog.tsx`
- `tests/components/publish-dialog.test.tsx`
- `src/components/catalogue/CourseDetailActions.tsx`
- `tests/components/course-detail-actions.test.tsx`
- `src/app/staff/programmes/[id]/ProgrammeDetailClient.tsx`
- `tests/components/programme-detail-client.test.tsx`
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-18-SUMMARY.md` (execution evidence)

<output>
Create .planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-18-SUMMARY.md. Report completed fixes, exact commands/results and any blocked scope decision without overwriting historical audits.
</output>

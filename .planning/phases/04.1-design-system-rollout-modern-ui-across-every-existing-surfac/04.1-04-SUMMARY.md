---
phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac
plan: 04
subsystem: ui
tags: [design-system, tailwind, wcag-contrast, forms, dialogs, accessibility]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: "Three-tier token chain in globals.css (border-input-border, shadow-card, muted-foreground, radius overrides) and the Plus Jakarta Sans / IBM Plex Mono type stack"
  - phase: 04.1-03
    provides: "ResourceTable.tsx as the restyled reference implementation — BTN/BTN_PRIMARY button conventions, tinted StatusPill pattern, card-shell conventions"
provides:
  - "ResourceForm/FormField/TextInput restyled onto the approved system: 700px sparse card body on shadow-card, 3:1 interactive control edges (border-input-border) on every text input, select, textarea and checkbox surface this file renders, muted micro-text, a new footer bar (surface-2 tone, draft status left, Cancel/Submit right)"
  - "DetailLayout/DetailFacts restyled: 25px/600 record header with inline badges, mono identifier, 2px accent tab underline distinguished by underline+weight+color, mono tab-count badges, per-section and stacked/tabbed content wrapped in a shadow-card panel matching the density rule's sparse-surface half"
  - "ConfirmModal restyled: shadow-card 16px-radius dialog surface, mono 11px eyebrow, border-input-border reason textarea, all behavioral guarantees (reason gate, focus trap, focus return, ESC suppression, explicit not-applied wording) unchanged"
affects: [04.1-05, 04.1-06, 04.1-07, 04.1-08, 04.1-09, 04.1-10, 04.1-11, 04.1-12, 04.1-13, 04.1-14]

# Actuals (#2632)
actuals:
  tokens: 6516
  tasks: 3
  commits: 0

tech-stack:
  added: []
  patterns:
    - "Reused ResourceTable's BTN/BTN_PRIMARY class-constant convention verbatim in ResourceForm and ConfirmModal, so every button across all four primitives shares one visual contract (border-input-border edge, rounded-md, text-xs font-semibold, disabled:opacity-50)"
    - "Card-shell convention (rounded-xl border-border bg-surface shadow-card) now applied uniformly to every sparse-surface branch (denied/error/loading/ready) across all three primitives, matching the density rule's asymmetry with ResourceTable's dense-surface shadow-xs"

key-files:
  modified:
    - src/components/primitives/ResourceForm.tsx
    - src/components/primitives/DetailLayout.tsx
    - src/components/primitives/ConfirmModal.tsx

key-decisions:
  - "DetailLayout's tabpanel and each stacked section body are individually wrapped in a shadow-card panel (rounded-xl border-border bg-surface) rather than adding new props for a 'facts left / 300px side column' layout — DetailLayoutProps has no side-column concept in its contract, and the plan's files_modified list is DetailLayout.tsx alone, so the side-column composition stays the responsibility of consuming pages (e.g. staff/courses/[id]/page.tsx pairing DetailLayout with a separate ReadinessPanel). This still satisfies the 'side cards take the 48px shadow because they are sparse singles' truth for every artifact this file owns (DetailLayout's own content panel, DetailFacts rendered inside it) without touching the frozen DetailSection/DetailLayoutState contracts."
  - "ConfirmModal's backdrop moved from bg-zinc-900/40 to bg-foreground/40 — the closest semantic token to the retired zinc-900 raw value, preserving the exact opacity mechanism the plan required to keep untouched."
  - "Buttons (Cancel/Submit/Retry) across all three files use border-input-border, matching the established ResourceTable BTN/BTN_PRIMARY convention from plan 03 rather than border-border — confirmed correct by reading the already-restyled ResourceTable.tsx per this task's read_first instruction, since D-15's interactive-control-edge rule extends to buttons in the established sibling implementation, not just inputs/selects/textareas."

requirements-completed: [NFR-09]

coverage:
  - id: D1
    description: "ResourceForm restyled: 700px sparse card (shadow-card) body, border-input-border on TextInput at 38px height, 14px/600 field labels, 11px muted-foreground hints/required-marker, unchanged FormField render-prop signature, unchanged error-summary position/links/focus-move behavior, new footer bar (surface-2, draft status left, Cancel/Submit right)"
    requirement: "NFR-09"
    verification:
      - kind: unit
        ref: "tests/components/resource-form.test.tsx (7/7 passed, including the summary-receives-focus test)"
        status: pass
      - kind: other
        ref: "grep -c 'zinc-' / grep -c 'font-medium' both 0; grep -c 'border-input-border' >=1; grep -c 'shadow-card' >=1"
        status: pass
      - kind: other
        ref: "npx tsc --noEmit (exit 0)"
        status: pass
    human_judgment: false
  - id: D2
    description: "DetailLayout restyled: 25px/600 title with inline badges, mono 11px identifier, 14px muted subtitle, 2px border-accent active-tab underline distinguished by underline+weight+color, mono tab-count badges, shadow-card panel wrapping both tabbed and stacked section content, denied/error/loading branches converted to the same card shape; roving-tabindex and per-section error isolation unchanged"
    requirement: "NFR-09"
    verification:
      - kind: unit
        ref: "tests/components/detail-layout.test.tsx (7/7 passed, including per-section-failure and keyboard-navigation tests)"
        status: pass
      - kind: other
        ref: "grep -c 'zinc-' / grep -c 'font-medium' both 0; grep -c 'border-accent' >=1; grep -c 'shadow-card' >=1"
        status: pass
      - kind: other
        ref: "npx tsc --noEmit (exit 0)"
        status: pass
    human_judgment: false
  - id: D3
    description: "ConfirmModal restyled: shadow-card 16px-radius dialog surface (rounded-xl bg-surface shadow-card), mono 11px eyebrow, border-input-border reason textarea, danger-surface error panel gains rounded-md; reason gate/counter, focus trap, focus return, ESC suppression while pending, and the explicit 'Action not applied' wording all unchanged"
    requirement: "NFR-09"
    verification:
      - kind: unit
        ref: "tests/components/confirm-modal.test.tsx (8/8 passed, including focus-trap, focus-return, ESC-suppression and reason-minimum tests)"
        status: pass
      - kind: other
        ref: "grep -c 'zinc-' / grep -c 'font-medium' both 0; grep -c 'shadow-card' >=1; grep -c 'border-input-border' >=1"
        status: pass
      - kind: other
        ref: "npx next build (exit 0, 30 routes) and npx eslint src (no errors)"
        status: pass
    human_judgment: true
    rationale: "Task 3's <verify> includes a <human-check> for the live confirm-dialog visual treatment (16px-radius floating card, soft deep shadow, visible reason-box edge, live counter, Escape mid-flight suppression) that only a rendered browser can confirm — not asserted by any test or build exit code."
  - id: D4
    description: "Whole-plan invariants held after all three restyles: no raw hex outside globals.css, no zinc-* remaining, no weight-500 font utility remaining, full components suite green, tsc/eslint/next-build all clean"
    requirement: "NFR-09"
    verification:
      - kind: unit
        ref: "npx vitest run --project components --no-file-parallelism (89/89 passed)"
        status: pass
      - kind: unit
        ref: "npx vitest run tests/unit/contrast.test.ts (43/43 passed)"
        status: pass
      - kind: other
        ref: "grep -rniE '#[0-9a-f]{6}' src/ --include=*.tsx --include=*.ts --include=*.css excluding globals.css — 0 matches"
        status: pass
      - kind: other
        ref: "npx next build (exit 0, all 30 routes) and npx eslint src (no errors)"
        status: pass
    human_judgment: false

duration: 38min
completed: 2026-09-05
status: complete
---

# Phase 04.1 Plan 4: Form, Record and Integrity-Dialog Restyle Summary

**ResourceForm, DetailLayout and ConfirmModal moved onto the approved design system — 3:1 interactive control edges, muted micro-text, and the sparse-surface 48px shadow — with zero prop, state-union or behavioral change across all three primitives.**

## Performance

- **Duration:** 38 min
- **Started:** 2026-09-05T12:15:00Z
- **Completed:** 2026-09-05T12:53:24Z
- **Tasks:** 3 (all `type="auto"`)
- **Files modified:** 3

## Accomplishments

- `ResourceForm.tsx`: form is now a single 700px-capped sparse card (`bg-surface rounded-xl border-border shadow-card`); `TextInput` carries the `border-input-border` interactive edge at 38px height; field labels render at 14px/600, hints and the required marker at 11px `text-muted-foreground`; a new footer bar (`bg-surface-2`, top `border-border` rule) holds draft status left and Cancel/Submit right using the same `BTN`/`BTN_PRIMARY` convention as `ResourceTable`; the error summary keeps its exact position, per-field links and focus-after-failed-submit behavior, gaining only `rounded-md`; `FormField`'s render-prop signature is untouched.
- `DetailLayout.tsx`: header renders the 25px/600 title with inline badges, mono 11px identifier and 14px muted subtitle; the tab strip carries a `border-accent` 2px active underline distinguished by underline **and** weight **and** color (never color alone); tab count badges render in the mono face; both tabbed and stacked section content are now wrapped in a `shadow-card` panel — satisfying the density rule's sparse-surface half for the record primitive's own content areas; denied/error/loading branches converted to the same card shape; roving-tabindex keyboard nav and per-section error isolation are behaviorally unchanged.
- `ConfirmModal.tsx`: dialog surface is now `rounded-xl bg-surface shadow-card`; the eyebrow moved to the mono face at 11px; the reason textarea carries `border-input-border`; the error panel keeps its `border-danger/30 bg-danger-surface` semantics plus `rounded-md`. The reason-minimum gate with its live counter, the focus trap, the focus return to the trigger, the ESC-suppression while pending, and the explicit "Action not applied" wording are all unchanged.
- Full verification suite re-run clean after all three files: `components` project 89/89, `tests/unit/contrast.test.ts` 43/43, `npx tsc --noEmit` exit 0, `npx eslint src` clean, `npx next build` exit 0 across 30 routes, repo-wide hex sweep 0 matches outside `globals.css`.

## Task Commits

**No commits were made — see COMMIT_PROHIBITION in this session's instructions.** All three tasks' work is verified and present on disk, uncommitted. Suggested commit messages are listed below under "Suggested commits (NOT made)" for the user to review and commit personally.

## Files Created/Modified

- `src/components/primitives/ResourceForm.tsx` — card body, control edges, footer bar, error-summary radius only
- `src/components/primitives/DetailLayout.tsx` — header, tab strip, shadow-card content panel, card-shaped denied/error/loading branches
- `src/components/primitives/ConfirmModal.tsx` — card dialog surface, mono eyebrow, control-edge textarea

## Decisions Made

- DetailLayout's tabpanel/stacked-section content is individually wrapped in a `shadow-card` panel rather than introducing a new "facts/side-column" prop — the plan's files_modified list is this file alone and `DetailLayoutProps` has no side-column concept, so the 300px side-column composition stays the responsibility of consuming pages that already pair `DetailLayout` with their own side content (e.g. `ReadinessPanel`).
- ConfirmModal's backdrop moved from `bg-zinc-900/40` to `bg-foreground/40` as the nearest semantic token to the retired zinc-900 raw value, keeping the exact opacity mechanism unchanged as required.
- Buttons in all three files use `border-input-border` (not `border-border`), confirmed correct against the already-restyled `ResourceTable.tsx`'s `BTN`/`BTN_PRIMARY` constants read per this task's `read_first` instruction — the established sibling implementation extends the interactive-control-edge convention to buttons, not just form inputs.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- A `--no-file-parallelism` full-suite run transiently failed 2/89 tests in `rich-text-editor.test.tsx` ("found multiple elements with role button name Bold") on one run; re-running the identical command immediately after produced 89/89 passed with no code changes in between, and running `rich-text-editor.test.tsx` in isolation also passed 5/5. This is pre-existing cross-file test-isolation flakiness under `--no-file-parallelism` (a file this plan never touches), not a regression from this plan's edits — confirmed by the immediate clean re-run.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All four primitives (`ResourceTable` from plan 03, `ResourceForm`/`DetailLayout`/`ConfirmModal` from this plan) are now restyled onto the approved system, uncommitted on the working tree per user instruction.
- **Action needed from the user before continuing:** review and commit (or discard) the uncommitted changes to `src/components/primitives/ResourceForm.tsx`, `DetailLayout.tsx` and `ConfirmModal.tsx`, using the suggested commit message below or your own.
- No blockers for the next plan — build, contrast gate (43/43), and the full component suite (89/89) all pass on the current working tree. The remaining Phase 04.1 plans (shells, bespoke Phase 4 components) can proceed independently since they depend only on the token chain (04.1-01) and these primitives.

## Suggested commits (NOT made)

Per this session's COMMIT_PROHIBITION, no `git commit` was run. The working tree carries all changes uncommitted. Suggested commit message:

```
feat(04.1-04): restyle ResourceForm, DetailLayout and ConfirmModal onto the design system

- ResourceForm: 700px sparse card body (shadow-card), border-input-border control
  edges at 38px height, 14px/600 field labels, 11px muted-foreground hints/marker,
  new footer bar (surface-2, draft status left, Cancel/Submit right) reusing
  ResourceTable's BTN/BTN_PRIMARY convention; FormField signature untouched
- DetailLayout: 25px/600 header with inline badges, mono identifier, 2px
  border-accent active-tab underline (underline + weight + color), mono tab-count
  badges, shadow-card content panel for both tabbed and stacked modes; roving-
  tabindex and per-section error isolation unchanged
- ConfirmModal: shadow-card 16px-radius dialog surface, mono 11px eyebrow,
  border-input-border reason textarea; reason gate, focus trap, focus return, ESC
  suppression and explicit not-applied wording unchanged
- No prop, state union or behavioral change in any of the three files; full
  components suite (89/89), contrast gate (43/43), tsc, eslint and next build
  all verified clean

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01LTTDrMaxMiYmw8qbzmA5Xc
```

## Self-Check: PASSED

- `src/components/primitives/ResourceForm.tsx` — FOUND
- `src/components/primitives/DetailLayout.tsx` — FOUND
- `src/components/primitives/ConfirmModal.tsx` — FOUND
- `.planning/phases/04.1-design-system-rollout-modern-ui-across-every-existing-surfac/04.1-04-SUMMARY.md` — FOUND
- Commit-hash check: N/A — no commits were made per this session's COMMIT_PROHIBITION (intentional, not a failure); all work verified present on disk instead.
- All three tasks' `<acceptance_criteria>` re-verified: `zinc-`/`font-medium` grep 0 in all three files; `border-input-border`/`shadow-card`/`border-accent` grep >=1 where required; `npx tsc --noEmit` exit 0; `npx vitest run --project components --no-file-parallelism` 89/89; `npx vitest run tests/unit/contrast.test.ts` 43/43; `npx next build` exit 0 (30 routes); `npx eslint src` clean; repo-wide hex sweep 0 matches outside `globals.css`.

---
*Phase: 04.1-design-system-rollout-modern-ui-across-every-existing-surfac*
*Completed: 2026-09-05*

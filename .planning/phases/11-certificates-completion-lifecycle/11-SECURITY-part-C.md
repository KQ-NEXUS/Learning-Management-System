# Phase 11 security audit, part C (plans 11-17 to 11-28)

Auditor batch C of 4. ASVS level 1, block_on open threats. Register slice: `.playwright-cli/register-C.json` (60 rows).
Verified against the CURRENT code (HEAD 27b9313 plus the working tree; the working tree has no source changes). Implementation files were not modified.

Targeted test run (read-only) on 2026-09-19: 16 files, 378 tests, all passed:
tests/learner-access, enrolment-dashboard-service, learner-dashboard-page, certificate-pdf-positions, course-actions, verify-routes, certificate-service, certificates-landing-page, certificate-issuance-service, certificate-issue-action, certificate-revocation, certificate-font-asset, certificate-phase-invariants, certificate-slot, components/certificate-record, components/certificate-template-editor. Real-Postgres integration tests were NOT run (no Docker/DB access asked for); their existence and assertions were read.

Global evidence for every `T-11-SC` row: `git diff 73d2b55 HEAD -- package.json package-lock.json` is empty. 73d2b55 is plan 11-02, the only phase commit that touched either file (pdf-lib and @pdf-lib/fontkit, human-vetted in 11-02). `pdf-lib` and `@pdf-lib/fontkit` are imported only from `src/server/services/certificate-pdf-renderer.ts:45-47`. The font is a committed file, not a package (see T-11-106).

## Threat verification table

| threat_id | plan | category | disposition | status | evidence | notes |
|---|---|---|---|---|---|---|
| T-11-70 | 11-17 | Information Disclosure | mitigate | CLOSED | `src/server/services/learner-access.ts:367-379` (`resolveOwnEnrolment`: `enrolment.userId !== actor.userId` returns the same `null` as missing/wrong status); used by `loadLearnerPath` at `:685-687`. Test `tests/learner-access.test.ts:661` (stranger's COMPLETED id `toStrictEqual` missing id, both null). | Passes. |
| T-11-71 | 11-17 | Elevation of Privilege | mitigate | CLOSED | `includeCompleted` has exactly one caller: `src/server/services/enrolment-dashboard-service.ts:759` (grep of `src`, no other call site). `assertLessonOpenable` refuses COMPLETED at `learner-access.ts:775-777`. `hasActiveEnrolmentCoveringCourse` filters `status: "ACTIVE"` at `:507-509`. Tests `learner-access.test.ts:337,379,887`; lesson/results/progress paths call the 2-arg `loadLearnerPath` (`lesson-progress-service.ts:353,425`, `learner-results-service.ts`). | Dashboard also skips results/assessment reads for COMPLETED (`enrolment-dashboard-service.test.ts:548`). |
| T-11-72 | 11-17 | Tampering | mitigate | CLOSED | Default `loadLearnerPath` -> `getOwnActiveEnrolment` (ACTIVE only) `learner-access.ts:417-423,685-687`; `tests/learner-access.test.ts:650`. The only COMPLETED->ACTIVE writes are the D-06 paths: `certificate-issuance-service.ts:542` (flag) and `certificate-service.ts:605` (revoke); `enrolment-transitions.ts:76` allows only `COMPLETED: ["ACTIVE"]`. | grep of `status: "ACTIVE"` updates in services finds only those two. |
| T-11-73 | 11-17 | Information Disclosure | mitigate | CLOSED | `enrolment-dashboard-service.ts:870-877,895-901`: batch ids are `enrolments.map(e => e.id)` from `listOwnDashboardEnrolments` (`learner-access.ts:483-493`, filters `userId: actor.userId`). Test `enrolment-dashboard-service.test.ts:566` (other user's COMPLETED enrolment + certificate yields no card); real-Postgres `certificate-download.integration.test.ts:433-454`. | |
| T-11-SC | 11-17 | Tampering | mitigate | CLOSED | See global evidence (empty package diff). | |
| T-11-74 | 11-18 | Information Disclosure | mitigate | CLOSED | Same ownership-scoped batch as T-11-73. Integration case `tests/certificate-download.integration.test.ts:452-454` (stranger's dashboard has 0 cards for the enrolment). | Integration test read, not run. |
| T-11-75 | 11-18 | Elevation of Privilege | mitigate | CLOSED | `src/app/(learner)/dashboard/page.tsx:235-253` (COMPLETED branch omits access banner, sessions, Assessments, Results). Routes still refuse (T-11-71). Test `tests/learner-dashboard-page.test.ts:332-353` asserts absence, including no `/learn/enrolment-1/` link. | Passes. |
| T-11-76 | 11-18 | Repudiation | mitigate | CLOSED | `src/components/learner/NextUpCard.tsx:59-63` (copy now "Your certificate, if this course issues one, is shown in the Certificate section below."). Test `learner-dashboard-page.test.ts:355-366` asserts "certificates ship" and "once certificates" absent. `grep "certificates ship" src tests` finds only the test assertion. | Copy was later refined by WR-06 (619e9f4); still true. |
| T-11-SC | 11-18 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-77 | 11-19 | Denial of Service | mitigate | CLOSED | `certificate-pdf-renderer.ts:147-169` (`fitImageInBox` returns null for non-positive or non-finite size; caller skips at `:280`). Test `tests/certificate-pdf-positions.test.ts:246` (zero-width box does not throw, nothing drawn). | Passes. |
| T-11-78 | 11-19 | Tampering | accept | CLOSED | Rationale still true: `certificate-template-layout.ts:110-113` (`nonNegativeFinite` for x, y, width, height, fontSize, widthPt), and the render path parses the stored layout before rendering (`certificate-file-service.ts`, `parseCertificateTemplateLayout(template.layout)`). | See "Accepted risks assessed". |
| T-11-79 | 11-19 | Repudiation | mitigate | CLOSED | `certificate-pdf-positions.test.ts:122-245` (position-asserting tests read text/image placement back from the PDF); single conversion functions `toPdfTextBaselineY`/`fitImageInBox` (`certificate-pdf-renderer.ts:129-169`). | The 11-29 whole-font embedding is covered by `certificate-pdf-unicode.test.ts` (not in this slice). |
| T-11-SC | 11-19 | Tampering | mitigate | CLOSED | See global evidence; single importer confirmed by grep. | |
| T-11-80 | 11-20 | Elevation of Privilege | mitigate | CLOSED | `course-service.ts:28-30,33` (`createResourceService` with `edit: "courses.edit"`, `toScope: courseScope`); `resource-service.ts:209-213` (`withPermission(permissions.edit, toScope(input.id))`). `src/app/staff/courses/[id]/edit/page.tsx:35-45` (`can("courses.edit", { courseIds: [id] })` else `notFound()`). Test `course-actions.test.ts:217` (fixed message, no passthrough). | |
| T-11-81 | 11-20 | Tampering | mitigate | CLOSED | `src/app/staff/courses/actions.ts:139-146` (`assertTemplateSelectable` when the id changes); `certificate-template-service.ts:287-299` re-resolves against `listSelectableTemplates()`. Tests `course-actions.test.ts:158,172`. | Client `<option>` list never trusted. |
| T-11-82 | 11-20 | Tampering | mitigate | CLOSED | `src/app/staff/courses/course-schema.ts` both schemas `.strict()`; `actions.ts:148-153` builds `data` from the named parsed fields only. Tests `course-actions.test.ts:60,237`. | `fields()`/`updateFields()` read only named keys. |
| T-11-83 | 11-20 | Repudiation | mitigate | CLOSED | `actions.ts:157` passes reason "Edited from the course editor."; `resource-service.ts:214-228` writes `course.updated` with `before`/`after`/`reason`. | Audit written by the factory. |
| T-11-84 | 11-20 | Information Disclosure | mitigate | CLOSED | `actions.ts:57-83` (`toFailure`): only `TemplateNotSelectableError`/`SlugFrozenError` (fixed user-facing default messages, `certificate-template-service.ts:266-273`, `catalogue-guards.ts:65-73`) surface a message; auth errors map to a fixed string; anything else is rethrown. Tests `course-actions.test.ts:217,232`. | |
| T-11-85 | 11-20 | Tampering | mitigate | CLOSED | `actions.ts:139-146` (validation only when `certificateTemplateId !== current`), `edit/page.tsx:59-77` labels an archived stored template. Test `course-actions.test.ts:158`. | Undefined key left unchanged (`actions.ts:148-153`). |
| T-11-SC | 11-20 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-86 | 11-21 | Information Disclosure | mitigate | CLOSED | `src/app/verify-certificate/page.tsx` (no lookup, no searchParams, no cache exports); `src/app/verify/VerifyReferenceForm.tsx:45-49` (`router.push('/verify/' + encodeURIComponent(trimmed))`). `tests/certificate-phase-invariants.test.ts:216-284` covers `src/app/verify-certificate` and `src/app/verify` for `dynamic`/`revalidate`/`fetchCache`/`'use cache'`. | Passes. |
| T-11-87 | 11-21 | Denial of Service | mitigate | CLOSED | `tests/verify-routes.test.ts` (auth `verify/page.tsx` exists, `src/app/verify/page.tsx` absent; both exist on disk). | Passes. |
| T-11-88 | 11-21 | Spoofing | accept | CLOSED | Rationale still true: `verify/layout.tsx` has only a brand mark, no nav or sign-in; the page has only the reference form. | See "Accepted risks assessed". |
| T-11-89 | 11-21 | Tampering | accept | CLOSED | Rationale still true: `certificate-reference.ts:8-11` `CERT-` + `randomBytes(16)` (128-bit). | Public verification has no rate limiting; the recorded entropy rationale still holds. |
| T-11-90 | 11-21 | Tampering | mitigate | CLOSED | `src/app/staff/certificates/templates/TemplateCanvas.tsx:100-119` (`clampPosition`), used for pointer drag (`:203`) and keyboard nudge (`:241`). Test `tests/components/certificate-template-editor.test.tsx:215`. | Passes. |
| T-11-SC | 11-21 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-91 | 11-22 | Information Disclosure | mitigate | CLOSED | `certificate-service.ts:493-526` (`IssuanceSource` = `{kind}`/`{kind, actorName}`, no `actorId`). Test `certificate-service.test.ts` "never leaks an actor id into the returned shape". | |
| T-11-92 | 11-22 | Elevation of Privilege | mitigate | CLOSED | `certificate-service.ts:493-496` (`withPermission("certificates.view", () => ({}))`, global scope). Tests "denies a caller without certificates.view" and "denies a cohort-scoped grant" (`certificate-service.test.ts:536-551`). | |
| T-11-93 | 11-22 | Repudiation | mitigate | CLOSED | `certificate-service.ts:522-524` (missing row -> `not-recorded`); `RecentlyIssuedList.tsx:80-87` and `IssuedCertificatesTable.tsx:37-39` render "Not recorded". Test `tests/components/certificate-record.test.tsx:171` (no "Automatic" for an unsourced row). | The audit action names match the writer: `certificate-issuance-service.ts:471`. |
| T-11-94 | 11-22 | Denial of Service | accept | CLOSED | Rationale still true: `certificates/issued/page.tsx:26-28` uses the ids from the (unpaginated) `list({})`; the landing page is capped to 10 ids (`certificates/page.tsx:12,38`). | See "Accepted risks assessed". |
| T-11-SC | 11-22 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-95 | 11-23 | Information Disclosure | mitigate | CLOSED | `src/app/staff/certificates/page.tsx:32-52` (`listPendingIssuance()`, `certificateService.list({})`, `listCertificateIssuanceSources` all global scope; `AuthenticationError`/`AuthorizationError` -> `notFound()`); `certificate-service.ts:415-422` (`withPermission("certificates.view", input.scope ?? {})`). Test `tests/certificates-landing-page.test.ts:133-` (`it.each` maps AuthorizationError from each read to `notFound()`). | Passes. |
| T-11-96 | 11-23 | Information Disclosure | mitigate | CLOSED | `RecentlyIssuedList.tsx:54-77` renders only learnerName, awardTitle, issuedAt, status pill, source label; no actor id, no `revocationReason`. | |
| T-11-97 | 11-23 | Tampering | mitigate | CLOSED | `certificates/page.tsx` exports only `metadata` and the default component (no `dynamic`/`revalidate`); `certificate-actions.ts:36,39,55,62` `revalidatePath("/staff/certificates")`. | |
| T-11-SC | 11-23 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-98 | 11-24 | Repudiation | mitigate | CLOSED | `attendance-service.ts:429-437` passes `actorId` into `recalculateCompletion`; `certificate-issuance-service.ts:762-773` forwards it; `reactToCompletionResults` uses it at `:702-715`. Tests `attendance-service.test.ts:1056,1063`; `certificate-issuance-service.test.ts:1155,1174`. | Lesson-progress path has no human actor and stays SYSTEM. |
| T-11-99 | 11-24 | Tampering | mitigate | CLOSED | `actorId` is `ctx.actor.userId` from `withPermission` (`attendance-service.ts:510,520,621,637`), never client input; `flagCertificateForReview` writes only `reviewFlaggedAt` (`certificate-issuance-service.ts:534-537`). | |
| T-11-100 | 11-24 | Repudiation | mitigate | CLOSED | `flagged` guard `certificate-issuance-service.ts:698`. Tests `certificate-issuance-service.test.ts:1038,1119,1139`. | |
| T-11-101 | 11-24 | Tampering | mitigate | CLOSED | `certificate-issuance-service.ts:694` skips only `COURSE` scope on a Programme cohort; PROGRAMME-scope still flags. Tests `:1066` (COURSE-only flags nothing), `:1091` (PROGRAMME-only flags once), `:1038` (mixed). | |
| T-11-SC | 11-24 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-102 | 11-25 | Elevation of Privilege | mitigate | CLOSED | `certificate-issuance-service.ts:377-380` (`revoked-blocked` for every actor, after the ACTIVE pre-check). Unit `certificate-issuance-service.test.ts:884` (system and staff); real-Postgres undo-then-redo `certificate-lifecycle-guards.integration.test.ts:296-331`. | Integration test read, not run. |
| T-11-103 | 11-25 | Denial of Service | mitigate | CLOSED | `certificate-issuance-service.ts:303-305` (`not-eligible` before any write, `CERTIFICATE_ELIGIBLE_ENROLMENT_STATUSES = ["ACTIVE","COMPLETED"]` at `:229`). Test `certificate-issuance-service.test.ts:855` (WITHDRAWN/PENDING_PAYMENT `reactToCompletionResults` resolves, nothing written). | |
| T-11-104 | 11-25 | Tampering | mitigate | CLOSED | Pre-check applies to staff actors (`certificate-issuance-service.ts:377-380`); queue excludes ACTIVE and REVOKED keys (`certificate-service.ts:359-366`); the action maps `revoked-blocked` to a fixed message (`certificate-actions.ts:44-50`). Tests `certificate-revocation.test.ts:394`, `certificate-service.test.ts:326`, integration `certificate-lifecycle-guards.integration.test.ts:344-363`. | |
| T-11-140 | 11-25 | Denial of Service | mitigate | CLOSED | `certificate-service.ts:667-678` (supersedes every REVOKED row for the enrolment and scope before issuing). Tests `certificate-revocation.test.ts:639`; real-Postgres `certificate-lifecycle-guards.integration.test.ts:391`. | |
| T-11-105 | 11-25 | Information Disclosure | mitigate | CLOSED | `certificate-actions.ts:33-75` (fixed string per outcome; catch returns fixed strings; no `error.message`). Test `certificate-issue-action.test.ts:22-63` (exact messages, 5 distinct). | |
| T-11-SC | 11-25 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-106 | 11-26 | Tampering | mitigate | CLOSED | `tests/certificate-font-asset.test.ts:9-12,45-49` pins SHA-256 `FE8C022F...B614`. Independent `sha256sum assets/fonts/certificate/NotoSans-Regular.ttf` = `fe8c022f...b614` (matches); single `.ttf` in the directory; only commit touching `assets/` is ff5a61e. | Hash independently recomputed. |
| T-11-107 | 11-26 | Repudiation | mitigate | CLOSED | `assets/fonts/certificate/LICENSE.txt` (SIL OFL 1.1) beside the font; `README.md` records the human decision verbatim; `11-26-SUMMARY.md` records the blocking human-action checkpoint resolved by the human. Test `certificate-font-asset.test.ts:52-59` asserts the licence file exists and names the licence. | Source URL is the human's session-scoped Google Fonts "selection" page; disclosed in the README, stable references added and labelled as not the human's words. |
| T-11-108 | 11-26 | Denial of Service | accept | CLOSED | Rationale still true: hash-pinned single file; render failures contained post-commit: `certificate-file-service.ts` `renderAndStoreCertificateFile` try/catch returns `failed`, `ensureCertificateFile`/`settlePendingCertificateFiles` never reject; `runTransactionThenSettleCertificateFiles` swallows settle failure. | See "Accepted risks assessed". |
| T-11-SC | 11-26 | Tampering | mitigate | CLOSED | See global evidence; only a font FILE added (ff5a61e), no package change. | |
| T-11-109 | 11-27 | Tampering | mitigate | CLOSED | `prisma/migrations/20260919120000_enrolment_one_live_per_learner_cohort/migration.sql` `CREATE UNIQUE INDEX enrolment_one_live_per_learner_cohort ... WHERE status IN ('ACTIVE','COMPLETED')`. Real-Postgres `tests/enrolment-live-index.integration.test.ts:77-133` (catalogue, P2002 for duplicate ACTIVE/COMPLETED, `takeSeat` raises `AlreadyEnrolledError`). | Test read, not run; migration applied to Neon per 11-33. |
| T-11-110 | 11-27 | Denial of Service | mitigate | CLOSED | Migration: preflight `DO $$ ... RAISE EXCEPTION` before any DDL; new index created before the old is dropped; DDL transactional. Tests `enrolment-live-index.integration.test.ts:204` (create before drop) and `:238-` (preflight aborts, old index and rows intact). | |
| T-11-111 | 11-27 | Tampering | mitigate | CLOSED | Read the migration: no `DELETE`/`UPDATE`/`TRUNCATE`/`INSERT` outside `--` comments (grep of non-comment lines returns nothing); only `SELECT`, `CREATE UNIQUE INDEX`, `DROP INDEX`. | Verified by grep on the current file (the plan's acceptance check). |
| T-11-112 | 11-27 | Elevation of Privilege | mitigate | CLOSED | `tests/support/pg.ts:107-113` overrides `DATABASE_URL` with the Testcontainers URL; `enrolment-live-index.integration.test.ts` header states it never reads `.env`. Remote apply governed separately (11-33). | Plan deviation: 11-33 records the orchestrator applied the migration to a Neon mirror branch and then the main DB at the user's explicit instruction, after rehearsal and a zero-row diagnostic. This is user-authorised and reviewed, not an unreviewed executor apply; the in-repo control holds. Operational follow-up in "Open threats" (info). |
| T-11-113 | 11-27 | Repudiation | mitigate | CLOSED | `11-DECISIONS.md:7` decision row "CR-05 ... Deferred" with rationale, consequence and revisit trigger (d). | |
| T-11-SC | 11-27 | Tampering | mitigate | CLOSED | See global evidence. | |
| T-11-114 | 11-28 | Information Disclosure | mitigate | CLOSED | Scoping unchanged (T-11-73). Guard test `tests/certificate-slot.test.ts:288` (never surfaces another learner's certificate) plus `enrolment-dashboard-service.test.ts:566`. | |
| T-11-115 | 11-28 | Repudiation | mitigate | CLOSED | `enrolment-dashboard-service.ts:503-522` (`deriveCertificateColumn`: existing certificate decides first, before `hasCompletionRecord`). Tests `certificate-slot.test.ts:73,88,128` (no completion record + flagged/ACTIVE certificate -> flagged/issued). | |
| T-11-116 | 11-28 | Information Disclosure | mitigate | CLOSED | `enrolment-dashboard-service.ts:508-510` returns `{ kind: "revoked" }` only. Tests `certificate-slot.test.ts:97,142` (no `certificateId` key), `enrolment-dashboard-service.test.ts:703` (`toEqual({kind:"revoked"})`). Defence in depth: `certificate-service.ts:451-458` (`getOwnCertificateForDownload` returns `null` unless owner and status ACTIVE); test `certificate-service.test.ts:407`. | |
| T-11-SC | 11-28 | Tampering | mitigate | CLOSED | See global evidence. | |

## Open threats

None. All 60 rows resolve to CLOSED.

Non-blocking observations (not register items, no severity of BLOCKER or WARNING):

- INFO (operational, from 11-33): a Neon branch connection string was pasted into the session transcript when the CR-06 migration was rehearsed. `11-33-SUMMARY.md` already tells the user to delete/reset that branch or rotate its password. Confirm this was done. It does not affect any T-11 mitigation.
- INFO (11-REVIEW WR-06, second half, acknowledged in the `deriveCertificateColumn` doc comment): `enrolment-dashboard-service.ts:930` keys a `Map` by `${enrolmentId}:${scope}`, so if REVOKED and ACTIVE rows coexist (legacy data) the last row wins. This is not a disclosure: the download route only serves the owner's ACTIVE row (`certificate-service.ts:451-458`), and `revoked-blocked` plus Reissue supersede-all now prevent new coexistence. It is a presentation-accuracy issue only.

## Superseded or changed since plan time

- Issuance is database-only with the PDF rendered after commit (11-30/11-31). The plan-time text for T-11-105 to T-11-108 and the 11-19 renderer described in-transaction rendering. Controls verified in the new shape: `certificate-file-service.ts` (best effort, catches everything, logs id and error name only, time-bounded settle) and `certificate-issuance-service.ts:486` (`registerPendingCertificateFile`). T-11-108's stated containment "by plan 11-30" is present.
- Renderer embeds the whole Noto Sans font (`subset: false`, `certificate-pdf-renderer.ts:327-330`, commit c7d8033) and sanitises text (`sanitiseCertificateText`, `:184-200`). T-11-77/79 still hold: `fitImageInBox` and the position tests are unchanged in behaviour; the PDF renderer test set (378-test run) passes.
- Template image `assetKey` is confined to `certificate-template-assets/` (WR-03, 704f79e): `certificate-template-layout.ts:148-170`. This strengthens T-11-78 (the parser is now stricter, not weaker) and the render path.
- Learner download is restricted to the owner's current ACTIVE row (WR-05, 4c6ee07): `certificate-service.ts:451-458`. Strengthens T-11-116 and T-11-74 (a SUPERSEDED or REVOKED certificate is not downloadable).
- REVOKED blocks automatic and queue issuance (11-25, CR-04), and Reissue supersedes every REVOKED row (T-11-140). Verified in current code.
- Dashboard certificate column is `certificateEnabled`-aware (WR-06, 619e9f4): `enrolment-dashboard-service.ts:503-522,880-940`. `deriveCertificateColumn` gained a `not-applicable` branch that returns before promising a certificate but never hides an existing row; the plan-time `not-complete` copy (T-11-76) was replaced by "Your certificate, if this course issues one, is shown in the Certificate section below." (`NextUpCard.tsx:59-63`); the T-11-76 test assertions still pass.
- Migration 20260919120000 was applied to a Neon mirror branch and the main Neon database by the orchestrator at the user's instruction (11-33), a deviation from plan 11-27's "remote apply is a human action". The migration file, its tests, and the rehearsal record are consistent; the DB itself was not inspected (no DB access).

## Accepted risks assessed

| threat_id | plan | recorded rationale | still true today? | outcome |
|---|---|---|---|---|
| T-11-78 | 11-19 | Layout numbers are finite and non-negative (parser); conversion only subtracts from page height; off-page drawing is clipped by the viewer. | Yes. `certificate-template-layout.ts:110-113` (`nonNegativeFinite`) covers x, y, width, height, fontSize, widthPt; the render path always parses (`certificate-file-service.ts`). The stricter `assetKey` confinement (WR-03) tightens it further. | Accepted, CLOSED. |
| T-11-88 | 11-21 | The entry page has no sign-in prompt, nav, or form beyond the reference field. | Yes. `verify-certificate/page.tsx` and `verify/layout.tsx` contain only a brand mark, heading, and `VerifyReferenceForm`. | Accepted, CLOSED. |
| T-11-89 | 11-21 | The form only navigates; enumeration resistance is 128-bit reference entropy. | Yes. `certificate-reference.ts:11` `randomBytes(16)`; the form does no lookup. Note: public verification still has no rate limiting (also accepted at 11-03). | Accepted, CLOSED. |
| T-11-94 | 11-22 | One `IN` query over ids the list already returned; the list is already unpaginated, so no new asymptotic cost. | Yes. All-certificates page still uses the unpaginated `list({})` ids; landing page is capped at 10 (`RECENT_LIMIT`). | Accepted, CLOSED. |
| T-11-108 | 11-26 | Single hash-pinned font parsed by the vetted fontkit; render failures contained post-commit by 11-30. | Yes. Hash independently recomputed and matches the pin; containment present in `certificate-file-service.ts` (`renderAndStoreCertificateFile` catch -> `failed`; settle never rejects; `runTransactionThenSettleCertificateFiles` swallows settle errors). Residual: a corrupt font would make every file render fail, but that is contained and the file is pinned. | Accepted, CLOSED. |

Counts: total 60, closed 60, open 0.

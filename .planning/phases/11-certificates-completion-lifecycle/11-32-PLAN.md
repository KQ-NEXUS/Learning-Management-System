---
phase: 11-certificates-completion-lifecycle
plan: 32
type: execute
wave: 12
depends_on: ["11-27", "11-29", "11-31", "11-34"]
files_modified:
  - tests/certificate-unicode-file.integration.test.ts
  - tests/certificate-lifecycle-guards.integration.test.ts
autonomous: true
gap_closure: true
requirements: [CRD-01, CRD-02, CRD-03, CRD-05, CRD-06]

must_haves:
  truths:
    - "On real Postgres and real object storage, a learner with a Yoruba, Polish or CJK name completes the issuing transaction, the certificate is committed, and its PDF is stored after commit with recoverable text (CR-01)"
    - "On real Postgres, a renderer/storage failure after commit leaves the caller's write, the certificate row and the COMPLETED enrolment intact, and the file is produced later on demand (CR-01b)"
    - "On real Postgres, a WITHDRAWN enrolment satisfying an AUTOMATIC award is not eligible and creates nothing, without throwing (CR-03)"
    - "On real Postgres, after a revoke, automatic issuance is blocked (revoked-blocked), the enrolment stays ACTIVE, and only staff Reissue creates a replacement, which restores COMPLETED (CR-04)"
    - "On real Postgres, the revoke and flag reversals (COMPLETED to ACTIVE) succeed and a duplicate live enrolment is impossible (CR-06 together with the plan 11-27 migration)"
    - "These tests are the real-database counterparts of the unit tests, each able to fail against the pre-fix behaviour"
  artifacts:
    - path: "tests/certificate-unicode-file.integration.test.ts"
      provides: "Unicode names through the real renderer, post-commit file production, failure containment, on-demand recovery"
    - path: "tests/certificate-lifecycle-guards.integration.test.ts"
      provides: "CR-03, CR-04 and CR-06 proven against real Postgres"
  key_links:
    - from: "tests/certificate-unicode-file.integration.test.ts"
      to: "src/server/services/certificate-file-service.ts"
      via: "runTransactionThenSettleCertificateFiles and ensureCertificateFile against the container database"
      pattern: "runTransactionThenSettleCertificateFiles"
---

<objective>
Phase-close proof for the second gap-closure pass: real-Postgres (Testcontainers) and real MinIO integration tests for the critical review findings. Unit tests with fakes cannot prove transaction behaviour (the earlier P2002/25P02 lesson in tests/certificate-concurrency.integration.test.ts), the font/subset embedding, or the partial unique indexes.

Nothing here modifies source code. If a test reveals a defect in earlier plans' work, stop and report it; do not patch source from this plan.

The tests start their own throwaway Postgres via `startTestDatabase()` (DATABASE_URL overridden to the container; the remote Neon database in `.env` is never contacted) and use the local MinIO with the same `S3_*` environment values as tests/certificate-concurrency.integration.test.ts (set before the dynamic imports, per that file's env-before-import hazard note).

Output: two integration test files, plus an optional evidence PDF export for the human visual check in plan 11-33.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-certificates-completion-lifecycle/11-REVIEW.md
@.planning/phases/11-certificates-completion-lifecycle/11-25-SUMMARY.md
@.planning/phases/11-certificates-completion-lifecycle/11-27-SUMMARY.md
@.planning/phases/11-certificates-completion-lifecycle/11-29-SUMMARY.md
@.planning/phases/11-certificates-completion-lifecycle/11-30-SUMMARY.md
@.planning/phases/11-certificates-completion-lifecycle/11-31-SUMMARY.md
@.planning/phases/11-certificates-completion-lifecycle/11-34-SUMMARY.md
@tests/certificate-concurrency.integration.test.ts
@tests/certificate-download.integration.test.ts
@tests/support/pg.ts
@tests/support/cohort-fixtures.ts
@tests/support/pdf-content.ts

<interfaces>
<!-- Extracted from the codebase; exact names to be confirmed from the SUMMARYs of plans 11-25..11-31. -->

- `startTestDatabase()` / `TEST_DB_TIMEOUT_MS` (tests/support/pg.ts); fixtures `seedCohortFixture`, `seedLearnerFixture`, `seedEnrolmentFixture` (tests/support/cohort-fixtures.ts).
- tests/certificate-concurrency.integration.test.ts and tests/certificate-download.integration.test.ts show: setting `S3_*` env at top level, setting `DATABASE_URL` in beforeAll, then dynamic `await import(...)` of the services; building a Course/Programme with a CertificateTemplate and a CompletionRecord row; running `issueCertificateForEnrolment` inside `prisma.$transaction`.
- Service surface after plans 11-25..11-31: `issueCertificateForEnrolment(tx, input, deps)` returning outcomes issued / already-issued / not-enabled / no-template / not-eligible / revoked-blocked; `liveIssuanceDeps` (generateRef, audit, writeEvent); `reactToCompletionResults(tx, args, deps)`; `createCertificateFileService(deps)` and the live `ensureCertificateFile`, `settlePendingCertificateFiles`, `runTransactionThenSettleCertificateFiles`, `registerPendingCertificateFile`; `createCertificateService(...)`/live `revokeCertificate`, `reissueCertificate` (need permission context: reuse the technique the existing certificate integration tests use to call permissioned services, or call `createCertificateService` with a permissive fake `withPermission` and the Prisma delegates).
- `getObjectBytes(key)` and `putGeneratedCertificateObject` from storage-service for reading the stored PDF back; tests/support/pdf-content.ts to read text back out of a PDF (ToUnicode-aware after plan 11-29).
- Real names to use: "Adébáyọ̀ Ṣolá", "Ọlọ́run Ẹlẹ́gbẹ́", "Łukasz Żółć", "François Müller-Ñandú", "山田 太郎", "محمد".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unicode names, post-commit file production and failure containment on real Postgres and MinIO</name>
  <files>tests/certificate-unicode-file.integration.test.ts</files>
  <read_first>
    - tests/certificate-concurrency.integration.test.ts — header hazard notes, env setup, seeding of course/programme/template/completion rows, transaction usage.
    - tests/certificate-download.integration.test.ts — how a stored PDF and the download flow are asserted against MinIO.
    - src/server/services/certificate-file-service.ts — `createCertificateFileService` deps shape (store, renderPdf, putObject, buildKey, resolveAsset, log, timeoutMs) and the live exports, as built in plan 11-30.
    - tests/support/pdf-content.ts — the ToUnicode-aware text reader from plan 11-29.
    - assets/fonts/certificate/README.md — accepted limitations.
  </read_first>
  <behavior>
    Scenario A (per name in the real-names list, it.each): seed a learner with that name, an ACTIVE enrolment in an AUTOMATIC-issuance Course cohort with the default template layout (name, award title, date, reference elements) and a satisfied completion record. Run issuance inside `runTransactionThenSettleCertificateFiles(prisma.$transaction ..., body)` using the live services with `liveIssuanceDeps` and a file service bound to the container database and real MinIO. Assert: the certificate row is ACTIVE, the enrolment is COMPLETED, `storageKey` is non-null after the call returns, the object read back from MinIO starts with %PDF, and for Latin-extended names the text recovered from the PDF equals the NFC-normalised name; for the CJK and Arabic names issuance and file production succeed and the recovered text contains "?" placeholders. Fails on pre-change code (render throws inside the transaction and nothing commits).
    Scenario B, containment: same setup but the file service is given a renderer that throws (and, in a second case, a put that throws). Assert: the wrapper resolves normally (no throw), the certificate row and COMPLETED enrolment ARE committed, `storageKey` is null, and a marker write made in the same transaction body (for example an audit or domain-event row written by the body) is committed too, proving the caller's write survives (CR-01b). Then call `ensureCertificateFile(id)` with a working renderer and assert the key is set and the object exists; call it again and assert the same key and no second object (idempotent).
    Scenario C, timeout containment: a renderer that never resolves with a short `timeoutMs` still lets the wrapper resolve within roughly that bound and the row remains file-less; a later ensure completes it.
    Scenario D, template robustness: a template whose stored layout JSON is invalid still lets issuance commit the row and COMPLETED enrolment (the file step fails contained, storageKey null); an image element with WebP bytes in the resolver renders a PDF without the image.
    Optional evidence: when the environment variable `CERTIFICATE_EVIDENCE_DIR` is set, write the PDFs generated in scenario A for "Ọlọ́run Ẹlẹ́gbẹ́", "Łukasz Żółć" and "山田 太郎" into that directory (skip silently when unset), so plan 11-33's human can open them.
  </behavior>
  <action>
    Write the test file following tests/certificate-concurrency.integration.test.ts's structure exactly for env-before-import, container lifecycle (`TEST_DB_TIMEOUT_MS` on every beforeAll/afterAll), and unique data per case. Use dynamic imports after setting `DATABASE_URL`. Build the file service with `createCertificateFileService` against `prisma as unknown as CertificateFileStore`-shaped delegates from the container client for the failure-injection scenarios, and with the live wiring for Scenario A where possible (if the live wiring is bound to `@/server/db`'s singleton, that singleton reads `DATABASE_URL` set in beforeAll, which is why imports are dynamic). Do NOT modify source. Run once with `CERTIFICATE_EVIDENCE_DIR` set to `.planning/phases/11-certificates-completion-lifecycle/11-32-evidence` (create the directory) and keep those PDFs for the human check; do not commit anything else generated. If any scenario fails because of a source defect, stop and report which plan's work is at fault.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/certificate-unicode-file.integration.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All scenarios pass against a Testcontainers Postgres and the local MinIO; no connection to the `.env` database (the file sets DATABASE_URL from the container, and the SUMMARY states so).
    - Scenario A's pre-fix failure mode is recorded in the SUMMARY by CITING the failing-before-fix unit-level results the earlier plans recorded in their own SUMMARYs (plan 11-29: the WinAnsi encoding error for Yoruba/Polish names; plan 11-30: PDF work inside the caller's transaction and an invalid template layout aborting issuance; plan 11-34: WebP/GIF logo throwing in the renderer). Do NOT run this test in a scratch worktree or checkout of the pre-fix tree: the new APIs (file service, settle wrapper, not-eligible outcomes) do not exist there, so the test could not compile.
    - Scenario B asserts the marker write from the same transaction body is committed while the render fails.
    - Three evidence PDFs exist under `.planning/phases/11-certificates-completion-lifecycle/11-32-evidence/`.
    - `git diff --stat -- src` is empty (no source change in this plan); `git diff package.json package-lock.json` is empty.
  </acceptance_criteria>
  <done>The Unicode rendering, post-commit production and failure-containment guarantees are proven end to end on real infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-03, CR-04 and CR-06 lifecycle guards on real Postgres</name>
  <files>tests/certificate-lifecycle-guards.integration.test.ts</files>
  <read_first>
    - tests/certificate-concurrency.integration.test.ts and tests/certificate-download.integration.test.ts — fixtures and service wiring patterns.
    - tests/enrolment-live-index.integration.test.ts (plan 11-27) — how the widened index is asserted; do not duplicate its index-level cases, extend to the lifecycle interplay.
    - src/server/services/certificate-service.ts — `createCertificateService` deps (`withPermission`, `delegate`, `enrolmentScope`, `pendingStore`, `auditStore`, `runInTransaction`, `issuanceDeps`, `writeEvent`) for building a real-database service with a permissive `withPermission` fake.
    - src/server/services/certificate-issuance-service.ts — `reactToCompletionResults`, `flagCertificateForReview`, outcomes.
    - src/server/services/enrolment-transitions.ts — the state machine.
  </read_first>
  <behavior>
    - CR-03: a WITHDRAWN and a PENDING_PAYMENT enrolment with a satisfied completion record in an AUTOMATIC Course: `reactToCompletionResults` inside a real transaction resolves, creates no certificate row, and leaves the enrolment status unchanged; the same transaction's other writes commit. Fails on pre-change code (IllegalTransitionError aborts it).
    - CR-03 queue: with a MANUAL Course and a satisfied completion record, `listPendingIssuance`-equivalent read (built from `createCertificateService` with the real pendingStore) lists an ACTIVE enrolment and omits the WITHDRAWN one.
    - CR-04 end to end: issue (ACTIVE certificate, enrolment COMPLETED) -> revoke through `revokeCertificate` (certificate REVOKED, enrolment ACTIVE) -> undo then redo simulated by `reactToCompletionResults` with [superseded] then [created] on an AUTOMATIC Course -> still exactly one certificate row, REVOKED, enrolment ACTIVE, no ACTIVE certificate. Fails on pre-change code (a new ACTIVE certificate appears and the enrolment returns to COMPLETED). Then `reissueCertificate` from the REVOKED row creates one ACTIVE certificate linked by supersedesId, the old row is SUPERSEDED and the enrolment is COMPLETED again. Also assert the queue does not list the revoked enrolment.
    - CR-06 interplay: after issuance (enrolment COMPLETED), inserting another ACTIVE enrolment for the same learner and cohort fails with P2002; revoke and flag (`flagCertificateForReview`) both succeed and return the enrolment to ACTIVE; and after that reversal the learner has exactly one live enrolment. Fails on pre-migration schema (the duplicate insert succeeds, and the later reversal fails).
    - Flag path guard: flagging a certificate for an enrolment holding a REVOKED certificate is still a no-op (nothing ACTIVE to flag).
    - Legacy two-REVOKED reissue: seed two REVOKED certificate rows A and B directly for one enrolment and scope (the state today's CR-04 bug could leave behind), then `reissueCertificate` on B succeeds: exactly one ACTIVE row is created with supersedesId B, A and B are both SUPERSEDED, and the enrolment is COMPLETED. Fails without plan 11-25's supersede-all-REVOKED step (Reissue would end in revoked-blocked and roll back).
  </behavior>
  <action>
    Write the test file with the same env-before-import and container discipline as the other integration tests, one container for the file (`TEST_DB_TIMEOUT_MS` on hooks), unique fixtures per case. Use the real `runInTransaction` over the container Prisma client so rollback semantics are genuine. Use `liveIssuanceDeps` (audit is the real `recordAudit` on the global client, which is fine because DATABASE_URL points at the container). Wrap file production with a fake or no-op file service (this file is about lifecycle state, not PDFs; issuance no longer renders inside the transaction, so nothing else is needed). Do NOT modify source. If a case fails because of a source defect, stop and report which plan's work is at fault.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/certificate-lifecycle-guards.integration.test.ts tests/enrolment-live-index.integration.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior bullet has an `it`, and the CR-03, CR-04 and CR-06 cases are documented in the SUMMARY with the pre-fix failure mode.
    - The CR-04 case ends with the reissue restoring COMPLETED and exactly one ACTIVE certificate.
    - `git diff --stat -- src` is empty; `git diff package.json package-lock.json` is empty; no connection to the `.env` database.
  </acceptance_criteria>
  <done>CR-03, CR-04 and CR-06 are proven against a real database including the revoke, undo/redo and reissue sequence and the duplicate-enrolment interplay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> databases and object stores | Tests must touch only disposable containers and the local MinIO |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-134 | Elevation of Privilege | A test reaching the remote Neon database via `.env` | mitigate | DATABASE_URL is set from the Testcontainers URL in beforeAll before any dynamic import; acceptance criteria require the SUMMARY to confirm no `.env` connection |
| T-11-135 | Information Disclosure | Evidence PDFs containing real personal data | mitigate | Only synthetic names from the fixed list are used; evidence is written only when an env var is set |
| T-11-136 | Tampering | Weak tests that pass against buggy code | mitigate | Each critical case records its pre-fix failure mode in the SUMMARY |
| T-11-SC | Tampering | npm installs | mitigate | No install; `git diff package.json package-lock.json` must be empty |
</threat_model>

<verification>
- `powershell.exe -NoProfile -Command "npx vitest run tests/certificate-unicode-file.integration.test.ts tests/certificate-lifecycle-guards.integration.test.ts tests/enrolment-live-index.integration.test.ts tests/certificate-concurrency.integration.test.ts tests/certificate-download.integration.test.ts"` green (Docker/Postgres/MinIO up). Run in the background or file by file; never the whole suite in the foreground.
- `git diff --stat -- src package.json package-lock.json` empty.
</verification>

<success_criteria>
The second-pass criticals are covered by real-infrastructure tests that would fail against the pre-fix behaviour, and a set of evidence PDFs exists for the human visual check.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-32-SUMMARY.md` when done
</output>

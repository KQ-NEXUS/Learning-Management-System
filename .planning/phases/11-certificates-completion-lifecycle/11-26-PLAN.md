---
phase: 11-certificates-completion-lifecycle
plan: 26
type: execute
wave: 1
depends_on: ["11-24"]
files_modified:
  - assets/fonts/certificate/README.md
  - tests/certificate-font-asset.test.ts
autonomous: false
gap_closure: true
requirements: [CRD-03]

must_haves:
  truths:
    - "A human has chosen, downloaded and placed a Unicode-capable, permissively licensed font file in the repository; the executor never picks or downloads a font"
    - "The font's licence text, source URL and SHA-256 are recorded next to the file, and a test pins the file to that hash"
    - "The font covers the Yoruba letters (precomposed e/o/s with dot below, upper and lower case), the combining tone marks, Ł/ł and common Western European accents"
    - "No npm package is added or changed (package.json and package-lock.json are byte-identical)"
  artifacts:
    - path: "assets/fonts/certificate/README.md"
      provides: "Font name, version, source URL, licence name, SHA-256, and the accepted CJK/Arabic limitation, transcribed from the human's reply"
    - path: "tests/certificate-font-asset.test.ts"
      provides: "Hash pin, licence-file presence, and glyph-coverage assertions read through @pdf-lib/fontkit"
  key_links:
    - from: "tests/certificate-font-asset.test.ts"
      to: "assets/fonts/certificate/README.md"
      via: "reads the recorded SHA-256 and compares it with the file on disk"
      pattern: "sha256"
---

<objective>
Supply the font asset that fixes CR-01's root cause (the renderer embeds only Helvetica/WinAnsi and throws on Yoruba, Polish and every non-Latin name).

The `@pdf-lib/fontkit` dependency was already human-vetted and installed in plan 11-02, so no package changes here. What is missing is a bundled font FILE with broad Latin-extended/Yoruba coverage and a licence that allows embedding and redistribution. A new binary asset carries a supply-chain and licence risk exactly like a new package, so the human chooses and supplies it; the executor must not download or pick one.

Purpose: unblock plan 11-29 (Unicode rendering) with a vetted, hash-pinned asset.
Output: the font file, its licence text and provenance README (placed by the human, transcribed by the executor), and a test that pins and inspects the asset.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-certificates-completion-lifecycle/11-REVIEW.md
@.planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md
@src/server/services/certificate-pdf-renderer.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Human chooses, verifies and places the certificate font (licence and provenance gate)</name>
  <action>
    Present the checkpoint below to the human and stop. Do not choose, download, generate or install any font, and do not modify any file, on their behalf; this is a blocking licence and provenance gate for a third-party binary. Record the human's reply verbatim in the plan SUMMARY, then continue with Task 2 only if the reply accepts the limitation.
  </action>
  <verify>
    <human-check>The human's reply gives the font name, version, source URL, licence, SHA-256 and the limitation decision, and exactly one .ttf plus one licence text file exist under assets/fonts/certificate/.</human-check>
  </verify>
  <read_first>
    - .planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md — how the previous human legitimacy decision for pdf-lib/fontkit was recorded (verbatim reply, date).
    - .planning/phases/11-certificates-completion-lifecycle/11-REVIEW.md — CR-01, to see which characters must render.
  </read_first>
  <what-built>Nothing is built yet. Claude has confirmed that the renderer needs a Unicode font (fontkit is already installed and approved) and has NOT chosen or downloaded one. A font file is a third-party binary redistributed with the product, so a human must approve its source and licence before it enters the repository.</what-built>
  <how-to-verify>
    1. Choose one static, regular-weight TrueType font (a single `.ttf`, not a variable font, not WOFF2) with a licence that permits embedding in generated PDFs and redistribution in this repository. The recommended candidate is Noto Sans Regular (SIL Open Font License 1.1) from its official source (https://fonts.google.com/noto/specimen/Noto+Sans or the notofonts GitHub releases). Alternatives with acceptable licences: Source Sans 3 (OFL 1.1), DejaVu Sans (Bitstream Vera / public-domain-style licence). Do not use a font whose licence forbids embedding or redistribution.
    2. Confirm on the official page that the font covers Latin Extended Additional (Yoruba: U+1EB8/U+1EB9 Ẹ ẹ, U+1ECC/U+1ECD Ọ ọ, U+1E62/U+1E63 Ṣ ṣ), combining grave/acute/macron (U+0300, U+0301, U+0304), Ł/ł (U+0141/U+0142) and Western European accents.
    3. Place the file at `assets/fonts/certificate/<FontName>-Regular.ttf` and the licence text file (for OFL: OFL.txt, including the copyright notice) beside it as `assets/fonts/certificate/LICENSE.txt`. Check `git check-ignore -v assets/fonts/certificate/<file>` prints nothing (the file must be committable).
    4. Compute the hash in PowerShell: `Get-FileHash assets/fonts/certificate/<file> -Algorithm SHA256`.
    5. Decide whether the limitation is acceptable: a single Latin-extended font does NOT cover CJK, Arabic or Hebrew scripts. The renderer (plan 11-29) will never throw for such names, but will draw a "?" for each unsupported character. Shipping a CJK/Arabic fallback font (much larger files) is out of scope for this pass. Confirm you accept this, or say that you do not.
    6. Reply with: the font name and version, the exact source URL you downloaded it from, the licence name, the SHA-256, and either "limitation accepted" or "limitation NOT accepted".
  </how-to-verify>
  <resume-signal>Reply with the font name/version, source URL, licence, SHA-256 and "limitation accepted" (or describe issues). Type "blocked" if no acceptable font can be sourced.</resume-signal>
  <acceptance_criteria>
    - The human's reply is recorded verbatim in the plan SUMMARY.
    - Exactly one `.ttf` file and one licence text file exist under assets/fonts/certificate/.
    - If the reply says "limitation NOT accepted" or "blocked", the executor stops and reports; plan 11-29 must not run.
  </acceptance_criteria>
  <done>A human-approved font file and licence text are in the repository with a recorded source and hash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record provenance and pin the asset with a hash and glyph-coverage test</name>
  <files>assets/fonts/certificate/README.md, tests/certificate-font-asset.test.ts</files>
  <read_first>
    - The human's reply from Task 1 (in the SUMMARY / checkpoint response) and the two files under assets/fonts/certificate/.
    - node_modules/@pdf-lib/fontkit — its type declarations and entry, to see how to open a font from bytes (`create`) and how to test glyph presence (`hasGlyphForCodePoint`).
    - tests/certificate-pdf-renderer.test.ts — the existing import and file conventions for renderer-adjacent tests.
  </read_first>
  <behavior>
    - The SHA-256 of the single `.ttf` under assets/fonts/certificate/ equals the value recorded in README.md (tamper/accidental-replacement guard; label it a guard).
    - A licence text file exists and contains the licence name recorded in README.md (case-insensitive substring).
    - Opened through @pdf-lib/fontkit, the font has a glyph for each of: ASCII letters and digits; U+1EB8 U+1EB9 U+1ECC U+1ECD U+1E62 U+1E63; U+0300 U+0301 U+0304; U+0141 U+0142; and é ü ñ ç ö å. The assertion message lists every missing code point.
    - README.md contains the source URL, licence name, version, SHA-256, the date, the human decision text (verbatim) and the accepted CJK/Arabic/Hebrew limitation.
  </behavior>
  <action>
    Write assets/fonts/certificate/README.md from the human's reply (transcribe, do not invent any value; if a value is missing, stop and ask). Include a "Decided by / Date" line in the same style as 11-DECISIONS.md. Then create tests/certificate-font-asset.test.ts covering the four behaviors above. The test must discover the font by listing the directory and requiring exactly one `.ttf` (so it does not hard-code a name the human may have chosen differently), read the recorded hash from README.md by a labelled line, and use `node:crypto` for the digest and `node:fs` for reads. It imports `@pdf-lib/fontkit` (already installed by plan 11-02); it must NOT import `pdf-lib` (invariant 1 scans only src/, but keep the single-importer discipline visible). Add nothing to package.json. Run the test; if the coverage assertion fails, that is a real finding (the chosen font lacks Yoruba glyphs): stop and report to the human rather than weakening the assertion (records CR-01 fix precondition).
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/certificate-font-asset.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff package.json package-lock.json` is empty.
    - The test file asserts all four behaviors and passes.
    - README.md contains the string "SHA-256" and the verbatim human reply.
    - `grep -v '^#' assets/fonts/certificate/README.md | grep -c "http"` is at least 1 (source URL recorded; header lines excluded from the count).
  </acceptance_criteria>
  <done>The font asset is documented, hash-pinned and proven to cover the required glyphs, ready for the renderer plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party binary -> repository | A font file is untrusted input redistributed with the product and parsed by fontkit on every render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-106 | Tampering | Font file replaced or corrupted after approval | mitigate | SHA-256 recorded from the human's reply and pinned by a test |
| T-11-107 | Repudiation | Licence not verified before redistribution | mitigate | Blocking human-action checkpoint; licence text stored beside the file; executor may not choose or download a font |
| T-11-108 | Denial of Service | Malformed font crashing the parser on every render | accept | Single vetted file, pinned by hash, parsed by the already-vetted fontkit; render failures are contained post-commit by plan 11-30 |
| T-11-SC | Tampering | New packages | mitigate | No install; `git diff package.json package-lock.json` must be empty. A font FILE (not a package) is the only addition, gated by the checkpoint above |
</threat_model>

<verification>
- `powershell.exe -NoProfile -Command "npx vitest run tests/certificate-font-asset.test.ts"` green.
- `git diff package.json package-lock.json` empty.
</verification>

<success_criteria>
A human-approved, permissively licensed font with Yoruba/Latin-extended coverage is committed with its licence, provenance and a hash-pinning test.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-26-SUMMARY.md` when done
</output>

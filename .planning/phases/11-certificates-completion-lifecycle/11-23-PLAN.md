---
phase: 11-certificates-completion-lifecycle
plan: 23
type: execute
wave: 8
depends_on: ["11-22"]
files_modified:
  - src/app/staff/certificates/RecentlyIssuedList.tsx
  - src/app/staff/certificates/page.tsx
  - tests/components/recently-issued-list.test.tsx
  - tests/certificates-landing-page.test.ts
  - src/lib/certificate-display-status.ts
  - src/app/staff/certificates/issued/IssuedCertificatesTable.tsx
autonomous: true
gap_closure: true
requirements: [CRD-01, CRD-02]

must_haves:
  truths:
    - "When a learner's certificate is issued automatically, staff opening Certificates see it on the landing page without visiting All certificates (UAT test 8)"
    - "Each recent row shows learner, award, when it was issued, and whether it was issued Automatically or by a named staff member, and links to its detail page"
    - "The pending-issuance queue, its empty state and its manual-issue action are unchanged"
    - "A staff member without the global certificates.view grant still gets the same 404 as before"
  artifacts:
    - path: "src/app/staff/certificates/RecentlyIssuedList.tsx"
      provides: "Presentational Recently issued section"
    - path: "src/app/staff/certificates/page.tsx"
      provides: "Landing page loading pending queue, recent certificates and their sources"
      contains: "listCertificateIssuanceSources"
  key_links:
    - from: "src/app/staff/certificates/page.tsx"
      to: "src/server/services/certificate-service.ts"
      via: "certificateService.list({}) newest 10 plus listCertificateIssuanceSources"
      pattern: "listCertificateIssuanceSources|certificateService"
    - from: "src/app/staff/certificates/RecentlyIssuedList.tsx"
      to: "/staff/certificates/issued/[id]"
      via: "row link to the detail page"
      pattern: "/staff/certificates/issued/"
---

<objective>
Close the landing-page half of UAT test 8 (MINOR): the Certificates nav item lands on the MANUAL-mode pending queue, which says "Nothing awaiting
issuance" once empty, so an automatic issuance leaves no trace where staff are looking.

Purpose: D-03 (automatic issuance, SYSTEM actor) is correct and passes the walkthrough; the gap is visibility. UAT asked for "a Recently issued section or count".
This plan adds the section (using plan 11-22's source lookup) and deliberately does NOT add an email/notification: certificate emails are Phase 13's
scope per 11-CONTEXT (domain events are emitted for Phase 13 to drain). The UAT's third suggestion ("staff-visible notice or audit entry link") is satisfied
by each row's link to the detail page, which already renders the "Issued ... by System (automatic issuance)" fact.

Output: `RecentlyIssuedList`, the landing page wiring, and tests including a page-level test that fails against the current page.
The queue's own empty-state copy ("...under manual issuance") is accurate for the queue and stays; the new section sits below it.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-certificates-completion-lifecycle/11-UAT.md
@.planning/phases/11-certificates-completion-lifecycle/11-22-PLAN.md
@src/app/staff/certificates/page.tsx

<interfaces>
<!-- Contract delivered by plan 11-22, plus existing surfaces. -->

From src/server/services/certificate-service.ts (after 11-22):
- `type IssuanceSource = { kind: "automatic" } | { kind: "staff"; actorName: string | null } | { kind: "not-recorded" }`
- `listCertificateIssuanceSources({ certificateIds: string[] }): Promise<Record<string, IssuanceSource>>` (certificates.view, global scope).
- `certificateService.list({})` returns `CertificateRow[]` (id, learnerName, awardTitle, issuedAt, status, reviewFlaggedAt, verificationRef, scope ...); requires a global certificates.view grant.
- `listPendingIssuance()` unchanged.
- `certificateDisplayStatus(row)` from `@/lib/certificate-display-status` returns "active" | "flagged" | "revoked" | "superseded".

Existing landing page (src/app/staff/certificates/page.tsx): async Server Component; `try { rows = await listPendingIssuance(); } catch (AuthenticationError | AuthorizationError) -> notFound()`; renders the heading with links "All certificates" and "Certificate templates", then `<CertificateQueueTable rows={rows} />`.

Shared UI: `StatusPill({ tone, label })` and `formatTimestamp(date)` from `@/lib/format-timestamp`; tones used elsewhere: success, warning, danger, neutral, accent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecentlyIssuedList presentational section</name>
  <files>src/app/staff/certificates/RecentlyIssuedList.tsx, tests/components/recently-issued-list.test.tsx</files>
  <read_first>
    - src/app/staff/certificates/issued/IssuedCertificatesTable.tsx — status pill tone/label mapping, `formatTimestamp` use, `break-all` on the verification reference (UI-SPEC §4: a partially hidden reference is useless).
    - src/components/primitives/index.ts (or where `StatusPill` is exported) — the exact exports and prop shape.
    - .planning/phases/11-certificates-completion-lifecycle/11-UI-SPEC.md — §0.2 restraint (the single Certificates nav entry stays the only top-level route; this section adds no new route), §4 wrapping rules for long names, §5 colour roles (no raw hex anywhere).
    - tests/components/certificate-slot.test.tsx — how a small presentational component is tested in the components project.
  </read_first>
  <behavior>
    - With rows and a sources record, each row renders learner name, award title, an issued timestamp, an Issued-by label (Automatic / staff name / "Staff member" / "Not recorded") and a link to /staff/certificates/issued/{id}.
    - An automatic row shows a visible "Automatic" indicator that a manual row does not.
    - A flagged or revoked certificate shows its status pill (via certificateDisplayStatus), never a plain green Active.
    - With zero rows it renders the heading and the sentence "No certificates have been issued yet." and no list.
    - A very long learner name and award title wrap (no `truncate`/nowrap class on those cells); no raw hex colour appears in the component.
  </behavior>
  <action>
    Create `RecentlyIssuedList.tsx` as a plain presentational component (no `"use client"`, no data access, no `@/server` runtime import — import `IssuanceSource`
    and `CertificateRow` with `import type` only and take status from `@/lib/certificate-display-status`). Props: `rows: CertificateRow[]` (already newest-first and already
    limited by the caller) and `sources: Record<string, IssuanceSource>`. Render a `section` with an `h2` "Recently issued", a short muted line naming what it shows
    ("Certificates issued most recently, including those issued automatically."), and an unordered list; each `li` is a `Link` (`next/link`) to
    `/staff/certificates/issued/${row.id}` containing the learner name (primary), award title (muted), the `formatTimestamp(row.issuedAt)` value in mono type, the
    status `StatusPill` from `certificateDisplayStatus` using the same tone/label mapping as `IssuedCertificatesTable` (reuse, do not fork — if the mapping is not exported,
    move the two small constant objects into `src/lib/certificate-display-status.ts` alongside the helper and import them from both files; keep the table's existing tests green),
    and the Issued-by label. Render "Automatic" as a small neutral/accent `StatusPill` so it is visually distinct from a staff name. The wrap rule: use `break-words` and no
    truncation on learner and award text. Add a "View all certificates" link to `/staff/certificates/issued` at the section's end. Use only existing colour tokens and classes seen
    in sibling components (no raw hex, no new tokens).

    Tests first in `tests/components/recently-issued-list.test.tsx` (components project) covering every behaviour bullet with fixture rows built the same way the existing
    certificate tests build them; assert the empty state and that a long (81-character) name renders in full. Confirm they fail before the component exists.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/components/recently-issued-list.test.tsx tests/components/certificate-record.test.tsx"</automated>
  </verify>
  <done>RecentlyIssuedList renders recent certificates with a clear Automatic vs staff indicator, status pills that respect the flagged/revoked precedence, working detail links and a tested empty state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the section into the Certificates landing page</name>
  <files>src/app/staff/certificates/page.tsx, tests/certificates-landing-page.test.ts</files>
  <read_first>
    - src/app/staff/certificates/page.tsx — current landing page.
    - src/app/staff/certificates/RecentlyIssuedList.tsx — the Task 1 component.
    - tests/learner-dashboard-page.test.ts — the repo's convention for testing a Server Component page in the node project: mock `next/navigation` and `next/link`, mock the service modules the page imports, invoke the page function directly and use `renderToStaticMarkup`.
    - node_modules/next/dist/docs/ — this repo's Next.js has breaking changes (AGENTS.md); this task only edits an existing Server Component page, but read the guidance on data fetching in Server Components before adding a second awaited call, and follow the same `notFound()` usage the file already has.
  </read_first>
  <behavior>
    - The page renders the pending queue exactly as before AND a "Recently issued" section whose rows come from certificateService.list({}) sorted by issuedAt descending and limited to the newest 10, with sources from listCertificateIssuanceSources called once with exactly those ten ids.
    - When the pending queue is empty and one recent certificate was issued automatically, the markup contains both "Nothing awaiting issuance" and that certificate's learner name and "Automatic" (the UAT test 8 scenario).
    - Only the newest 10 appear when 12 exist, newest first; SUPERSEDED rows are excluded from the recent list (the replacement is what staff should see).
    - When either service call throws AuthorizationError or AuthenticationError the page calls notFound() (same 404 shape as before); other errors propagate.
    - With zero certificates the section shows its empty sentence and the queue still renders.
  </behavior>
  <action>
    Extend `page.tsx` (UAT test 8): keep the `listPendingIssuance()` call and its try/catch, and in the same try block also load
    `certificateService.list({})`, filter out rows whose `status === "SUPERSEDED"`, sort by `issuedAt` descending, `slice(0, 10)`, then call
    `listCertificateIssuanceSources({ certificateIds })` with exactly those ids (skip the call when the list is empty — the service already returns `{}`
    for an empty list, but do not rely on it). Any `AuthenticationError`/`AuthorizationError` from either call keeps mapping to `notFound()`, so a
    staff member lacking the global grant sees the identical 404 they saw before this plan. Render `<RecentlyIssuedList rows={recent} sources={sources} />` below
    `<CertificateQueueTable rows={rows} />`, inside the existing column container. Do not change the heading, the two header links, the queue table or its
    copy (`tests/components/certificate-queue.test.tsx` asserts that copy and must stay green). Do not export `dynamic`/`revalidate`. Update the file's doc
    comment to say the landing shows both the MANUAL queue and recent issuances (including automatic ones).

    Tests first in `tests/certificates-landing-page.test.ts` (node project) mocking `next/navigation` (`notFound` throws a sentinel), `next/link` (plain anchor) and the
    service module (`listPendingIssuance`, `certificateService.list`, `listCertificateIssuanceSources`); cover every behaviour bullet. Confirm the
    UAT-scenario test fails against the current page (which never shows the automatic certificate) before wiring.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/certificates-landing-page.test.ts tests/components/recently-issued-list.test.tsx tests/components/certificate-queue.test.tsx"</automated>
  </verify>
  <done>An automatically issued certificate appears in a Recently issued section on the Certificates landing page, marked Automatic, even while the pending queue shows "Nothing awaiting issuance"; the queue and access denial behave as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff session -> landing page data | Recent list and sources require the global certificates.view grant (same as the pending queue) |
| service -> RSC payload | Only display fields cross into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-95 | Information Disclosure | Landing page revealing certificates to a role that can only see part of the school | mitigate | Both reads use the global-scope certificates.view checks; any denial becomes `notFound()`; page test asserts the 404 path |
| T-11-96 | Information Disclosure | Actor ids/emails or revocation reasons in the recent list | mitigate | Component receives `CertificateRow` but renders only name, award, timestamp, status and source label; source map carries names only (11-22); revocation reason is not rendered here |
| T-11-97 | Tampering | Stale cached landing page hiding a fresh issuance | mitigate | Page is dynamic through its authenticated service reads and exports no static/revalidate config; the queue's server action already `revalidatePath`s |
| T-11-SC | Tampering | npm installs | mitigate | This plan installs nothing; `git diff package.json` must be empty |
</threat_model>

<verification>
- `powershell.exe -NoProfile -Command "npx vitest run tests/certificates-landing-page.test.ts tests/components/recently-issued-list.test.tsx tests/components/certificate-queue.test.tsx tests/components/certificate-record.test.tsx"` green.
- `grep -rnE "#[0-9a-fA-F]{6}" src/app/staff/certificates/RecentlyIssuedList.tsx src/app/staff/certificates/page.tsx` returns nothing.
- `grep -rn "revocationReason" src/app/staff/certificates/RecentlyIssuedList.tsx` returns nothing.
- `powershell.exe -NoProfile -Command "npx tsc --noEmit"` exits 0; `git diff package.json package-lock.json` is empty.
</verification>

<success_criteria>
Staff who open Certificates immediately see automatically issued certificates (labelled Automatic) without hunting through All certificates, while the manual queue
behaves exactly as before. UAT test 8 is closable by a browser re-check.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-23-SUMMARY.md` when done
</output>

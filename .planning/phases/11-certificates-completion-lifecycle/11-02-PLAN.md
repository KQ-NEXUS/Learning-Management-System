---
phase: 11-certificates-completion-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/certificate-pdf-probe.mts
  - .planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md
autonomous: false
requirements: [CRD-03]

must_haves:
  truths:
    - "A human has verified the PDF library's registry listing before it entered the dependency tree"
    - "The server can construct a PDF containing text and an embedded image without any browser binary, via a pure PDF-construction library rather than Puppeteer/Playwright (D-08)"
    - "The permission identifier guarding certificate-template authoring is an explicit, recorded human decision"
  artifacts:
    - path: "package.json"
      provides: "Exactly one PDF-construction library, pinned"
      contains: "pdf-lib"
    - path: "scripts/certificate-pdf-probe.mts"
      provides: "Executable proof the chosen library renders text + image server-side in this runtime"
    - path: ".planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md"
      provides: "Recorded human decisions: PDF library choice, template-authoring permission identifier"
  key_links:
    - from: "scripts/certificate-pdf-probe.mts"
      to: "the installed PDF library"
      via: "direct import, executed under node"
      pattern: "from \"pdf-lib\"|require\\(\"pdfkit\"\\)"
---

<objective>
Clear the two human gates this phase cannot proceed past on its own: (1) the mandatory package
legitimacy approval for a new third-party PDF-construction dependency, and (2) the permission-catalogue
question `11-RESEARCH.md` explicitly refused to answer unilaterally — which identifier guards
certificate-template authoring.

Purpose: `11-PATTERNS.md` marks `certificate-pdf-renderer.ts` "no analog — do not begin this file until
that checkpoint clears." Everything downstream of PDF rendering is blocked here. Grouping both human
gates into one Wave-1 plan means the human is interrupted once, early, not twice mid-phase.
Output: One pinned PDF library, a runnable smoke probe proving it works in this runtime, and a written
decision record.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-certificates-completion-lifecycle/11-CONTEXT.md
@.planning/phases/11-certificates-completion-lifecycle/11-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — PDF-construction library</name>
  <read_first>
    - .planning/phases/11-certificates-completion-lifecycle/11-RESEARCH.md — the `## Package Legitimacy Audit` table (all three candidates returned `[OK]` from slopcheck but remain `[ASSUMED]` by provenance rule) and the `## Standard Stack` table's recommendation rationale.
    - .planning/phases/11-certificates-completion-lifecycle/11-CONTEXT.md — the Claude's Discretion item naming this gate, and the established precedent (`stripe` Phase 6, `lucide-react` Phase 04.1, `@tiptap/extensions` Phase 04.1).
    - package.json — confirm none of the candidates is already present.
  </read_first>
  <action>
    Present both options below to the human and stop. Install nothing, modify no file, and do not
    pick a default on their behalf — this is a blocking package-legitimacy gate, the same one
    `stripe`, `lucide-react` and `@tiptap/extensions` each passed through. Record the answer verbatim
    in `.planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md` (create the file) with
    columns: Decision, Chosen, Rationale, Decided by, Date.
  </action>
  <what-built>
    Nothing yet — this gate precedes any install. Research narrowed the field to two mutually exclusive
    options, both MIT-licensed pure PDF-construction libraries (no headless browser, satisfying D-08):

    - **Option A (research recommendation):** `pdf-lib` (1.17.1, published 2017-09-04, MIT,
      github.com/Hopding/pdf-lib) + `@pdf-lib/fontkit` (2.0.4, published 2018-12-19, MIT,
      github.com/Hopding/fontkit) for custom-font embedding. Explicit x/y coordinate API maps 1:1 onto
      `CertificateElementV1`'s positioned-element model.
    - **Option B:** `pdfkit` (0.20.2, published 2011-07-11, MIT, github.com/foliojs/pdfkit). Older,
      built-in font embedding with no companion package, but a stream/cursor document API that needs a
      mapping layer for absolute positioning.

    `qrcode` is deliberately NOT proposed — the QR dynamic field is a CONTEXT.md discretion item this
    plan set defers rather than takes up (see 11-DECISIONS.md, Task 3). Approving a QR package now would
    be approving scope that no requirement asks for.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/pdf-lib and https://www.npmjs.com/package/@pdf-lib/fontkit
       (Option A), or https://www.npmjs.com/package/pdfkit (Option B).
    2. On each page confirm: weekly download volume is substantial (six figures or more), the linked
       repository is the GitHub org named above, the license reads MIT, and the publish history spans
       years rather than weeks.
    3. Confirm the GitHub repository is not archived and has recent commit or release activity.
    4. State your choice: "Option A" or "Option B". If neither looks acceptable, say so and name what
       you want instead — do not approve by default.
  </how-to-verify>
  <acceptance_criteria>
    - The human has stated exactly one of: "Option A", "Option B", or a rejection with an alternative.
    - Nothing has been installed before this response is recorded (`git diff package.json` is empty at the moment of the checkpoint).
    - This checkpoint is NOT auto-approvable: `workflow.auto_advance` does not apply to package-legitimacy gates.
  </acceptance_criteria>
  <verify>
    <human-check>The human has replied with an option id, and that reply is recorded verbatim in 11-DECISIONS.md.</human-check>
    <automated>test -f .planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md && git diff --quiet package.json</automated>
  </verify>
  <done>A human has chosen exactly one PDF library option after checking its registry listing, and nothing is installed yet.</done>
  <resume-signal>Reply "Option A", "Option B", or describe the alternative you want.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the approved library and prove it renders in this runtime</name>
  <files>package.json, package-lock.json, scripts/certificate-pdf-probe.mts</files>
  <read_first>
    - package.json — read the `dependencies` block to match the project's existing exact-pinning convention (`stripe` was pinned exact at 22.6.1 in Phase 6; follow the same style, not a caret range).
    - scripts/ — list the directory and read one existing `.mts` script end-to-end to match the project's script conventions (shebang/imports/exit codes). If `scripts/` does not exist, create it and follow the repo's tsconfig module resolution.
    - .planning/phases/11-certificates-completion-lifecycle/11-RESEARCH.md — Pattern 4's `CertificateElementV1` shape, so the probe exercises the exact primitives the renderer will need.
  </read_first>
  <action>
    Install ONLY the option the human approved in Task 1, pinned exact (no caret, no tilde):
    `npm install --save-exact pdf-lib@1.17.1 @pdf-lib/fontkit@2.0.4` for Option A, or
    `npm install --save-exact pdfkit@0.20.2` for Option B. Do not install both. Do not install `qrcode`.

    After install, confirm the resolved version in `package-lock.json` matches what the human vetted on
    npmjs.com — a drift between vetted and resolved version invalidates the gate and must be reported,
    not silently accepted (this exact check is recorded in STATE.md as the Phase 04.1 `lucide-react`
    precedent).

    Then write `scripts/certificate-pdf-probe.mts` — a standalone script (not a test) that proves the
    library does, in this Node runtime with no browser binary present, everything
    `certificate-pdf-renderer.ts` will need:
    - create a landscape A4 page
    - draw a text string at an explicit x/y coordinate in a named colour and font size
    - draw a filled/stroked rectangle (the `border` element primitive)
    - embed a PNG image at an explicit x/y/width/height (the `image` element primitive)
    - for Option A only: register fontkit and embed an arbitrary TTF, proving the custom-font path works
    - serialize to a `Uint8Array`/`Buffer` and write it to the OS temp directory, printing the byte
      length and the output path

    The probe generates its own tiny PNG in-memory rather than depending on a fixture file.
    Exit non-zero on any failure.
  </action>
  <verify>
    <automated>npx tsx scripts/certificate-pdf-probe.mts</automated>
  </verify>
  <acceptance_criteria>
    - The probe exits 0 and prints a byte length greater than 1000.
    - The first four bytes of the emitted file are the literal `%PDF` magic header.
    - `node -e "const p=require('./package.json');const d=p.dependencies;console.log(JSON.stringify(d))"` shows the approved package(s) at the exact vetted version with no range prefix.
    - Exactly one PDF library is present: `node -e "const d=require('./package.json').dependencies; const hits=Object.keys(d).filter(k=>['pdf-lib','pdfkit','pdfmake','puppeteer','playwright'].includes(k)); if(hits.length>1||hits.length===0) process.exit(1)"` exits 0.
    - `qrcode` is absent from `package.json` dependencies.
    - No `puppeteer`, `playwright`, or `playwright-core` entry was added to `dependencies` (the `playwright-cli` skill is a test tool, not a production dependency — D-08's rejection stands).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Exactly one approved PDF library is pinned in package.json at the vetted version, and a runnable probe proves text, rectangle, image, and (Option A) custom-font rendering all work server-side.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Which permission guards certificate-template authoring?</name>
  <read_first>
    - src/server/permissions/catalogue.ts — full file, especially the header comment ("Adding an identifier here is a product decision, not an implementation one") and the existing certificate trio: `certificates.view`, `certificates.issue`, `certificates.revoke`.
    - .planning/phases/11-certificates-completion-lifecycle/11-RESEARCH.md — the `## Security Domain` section's "D-09 permission open item", which flags this explicitly as needing human sign-off and names the Elevation-of-Privilege risk of leaving it under-specified.
    - .planning/PROJECT.md — the closed-permission-catalogue constraint.
  </read_first>
  <action>
    Present the decision and its three options below, then stop. Change no source file in this task.
    When the human answers, append their choice, the rationale they gave (or this plan's rationale for
    that option if they gave none), the deciding person and the date to
    `.planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md`, in the same table Task 1
    created. Plan 11-05 Task 1 reads that row and applies it; this task never edits
    `src/server/permissions/catalogue.ts`.
  </action>
  <decision>Which permission identifier authorizes creating, editing, and archiving a `CertificateTemplate` (D-09's template editor)?</decision>
  <context>
    The catalogue is closed by design: three certificate permissions exist, none of which semantically
    means "author a certificate's visual design". Adding a fourth identifier is a PRD §1.3 product
    decision, not an implementation choice — which is why research refused to decide it.

    The risk of getting this wrong: a staff member holding only `certificates.issue` (intended for
    exceptional manual issuance after a correction) would gain the ability to redesign every certificate
    the institution issues.
  </context>
  <options>
    <option id="reuse-issue">
      <name>Reuse `certificates.issue` (research's pragmatic default)</name>
      <pros>No catalogue change, no PRD approval path, ships immediately. The roles plausibly holding template authority (Administrator, Programme Manager) already hold `certificates.issue`.</pros>
      <cons>Conflates "sign off on one learner's credential" with "control the visual identity of every credential the institution issues" — a genuine privilege widening for anyone granted issue-only.</cons>
    </option>
    <option id="new-manage">
      <name>Add `certificates.manage` to the catalogue</name>
      <pros>Clean separation; template authoring becomes independently grantable; matches the `grades.manage` / `tickets.manage` naming already in the catalogue.</pros>
      <cons>Requires the PRD §1.3 catalogue-change approval path, a seed/role-migration touch so default roles still work, and expands this phase's scope by a permission-plumbing task.</cons>
    </option>
    <option id="reuse-revoke">
      <name>Reuse `certificates.revoke`</name>
      <pros>Narrowest existing holder set; revoke is already the "high-consequence certificate authority" permission.</pros>
      <cons>Semantically wrong — revoking a credential and designing one are unrelated acts; would surprise anyone reading the role matrix.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The human selected exactly one option id.
    - The selection, its date, and the rationale are written to `.planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md` alongside Task 1's PDF-library choice, in a table with columns: Decision, Chosen, Rationale, Decided by, Date.
    - If `new-manage` was chosen, 11-DECISIONS.md additionally records that plan 11-05 must add the identifier to `src/server/permissions/catalogue.ts` and update the default-role seed, and that this is an in-scope addition to 11-05 rather than a new plan.
    - `grep -c "certificates.manage" src/server/permissions/catalogue.ts` is 0 at the end of this plan regardless of choice — the catalogue edit, if any, belongs to plan 11-05, not here.
  </acceptance_criteria>
  <verify>
    <human-check>The human named one option id, and 11-DECISIONS.md carries a row recording it.</human-check>
    <automated>grep -cE "reuse-issue|new-manage|reuse-revoke" .planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md</automated>
  </verify>
  <done>The template-authoring permission question has a recorded human answer, and the permission catalogue is still unmodified.</done>
  <resume-signal>Reply with one of: `reuse-issue`, `new-manage`, `reuse-revoke`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dependency tree | An untrusted third-party package enters the server's execution context |
| staff role grant → certificate visual identity | Whoever can author templates controls what every issued credential looks like |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm install of a PDF-construction library | mitigate | Blocking human legitimacy checkpoint (Task 1) verifying registry listing, license, repo and download volume before install; exact-version pin; resolved-version-matches-vetted-version assertion in Task 2 |
| T-11-05 | Elevation of Privilege | Certificate-template authoring permission | mitigate | Explicit human decision (Task 3) recorded in 11-DECISIONS.md; the under-specified fall-through research warned about is structurally prevented because plan 11-05's service cannot be written without reading this record |
| T-11-06 | Tampering | Production dependency surface | mitigate | Task 2's acceptance criteria assert exactly one PDF library and zero browser-automation packages in `dependencies`, closing D-08's rejected Puppeteer/Playwright path against accidental reintroduction |
| T-11-07 | Information Disclosure | Probe output | accept | The probe writes a synthetic PDF containing only literal sample strings to the OS temp directory; no learner data exists at this point in the phase |
</threat_model>

<verification>
- `npx tsx scripts/certificate-pdf-probe.mts` exits 0 and emits a `%PDF`-headed file.
- `npx tsc --noEmit` exits 0.
- `npm test` still green (no dependency added that breaks an existing suite).
- `.planning/phases/11-certificates-completion-lifecycle/11-DECISIONS.md` exists with both decisions recorded.
</verification>

<success_criteria>
Exactly one human-vetted PDF library is pinned and proven to render text, shapes, and images
server-side in this runtime. The template-authoring permission question has a recorded answer that
plan 11-05 can implement without guessing.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-02-SUMMARY.md` when done
</output>

---
phase: 11-certificates-completion-lifecycle
plan: 16
type: execute
wave: 6
depends_on: ["11-06", "11-08", "11-10", "11-12", "11-13", "11-14", "11-15"]
files_modified:
  - tests/certificate-download.integration.test.ts
  - tests/certificate-concurrency.integration.test.ts
  - tests/certificate-phase-invariants.test.ts
  - .planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CRD-01, CRD-02, CRD-03, CRD-04, CRD-05, CRD-06]

must_haves:
  truths:
    - "A certificate PDF written to real object storage is retrievable by its owner through a real presigned URL"
    - "Two simultaneous completion triggers against a real Postgres produce exactly one active certificate"
    - "The phase's structural invariants are enforced by an executable test, not by convention"
    - "A human has seen a certificate issue, download, verify, revoke and reissue in a browser"
  artifacts:
    - path: "tests/certificate-concurrency.integration.test.ts"
      provides: "Real-Postgres proof of the partial unique index closing the duplicate-issuance race"
    - path: "tests/certificate-phase-invariants.test.ts"
      provides: "Executable structural gates for this phase"
    - path: ".planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md"
      provides: "Per-task verification map reconciled against what was actually built"
  key_links:
    - from: "tests/certificate-concurrency.integration.test.ts"
      to: "certificate_one_active_per_enrolment_scope"
      via: "concurrent transactions against a real Postgres exercising the partial unique index"
      pattern: "certificate_one_active_per_enrolment_scope|P2002"
---

<objective>
Close the phase: prove against real infrastructure what the unit suites could only prove against fakes,
fence the phase's structural decisions behind an executable invariants test, reconcile the validation
record, and put a human in front of the working feature.

Purpose: This project's history (Phase 6/06-03, 06-09) records integration tests written but left
`Docker-BLOCKED` and reported as green. The duplicate-issuance race and the real presigned download are
exactly the two things a fake cannot prove. If Docker is unavailable, this plan reports that honestly
rather than claiming coverage.
Output: Two integration suites, one invariants test, a reconciled 11-VALIDATION.md, and a human sign-off.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md
@.planning/phases/11-certificates-completion-lifecycle/11-RESEARCH.md
@.planning/phases/11-certificates-completion-lifecycle/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-infrastructure integration suites — download and concurrency</name>
  <files>tests/certificate-download.integration.test.ts, tests/certificate-concurrency.integration.test.ts</files>
  <read_first>
    - tests/checkout-webhook.integration.test.ts — this project's real-Postgres Testcontainers harness: how the container is started, how the schema is applied, how transactions are isolated between cases, and the teardown. Copy it; do not invent a second harness.
    - tests/ — grep for an existing MinIO/S3 integration test (Phase 10's submission upload suite is the likeliest) and read how real object storage is stood up and addressed.
    - vitest.config.mts — the `--no-file-parallelism` convention and any integration-test include/exclude patterns.
    - src/server/services/certificate-issuance-service.ts — `issueCertificateForEnrolment`'s create-then-render ordering and its P2002 handling, which the concurrency test exercises.
    - prisma/migrations/*_certificates_issuance_mode_and_templates/migration.sql — confirm `certificate_one_active_per_enrolment_scope` is in the applied migration; the concurrency test is meaningless without it.
    - tests/certificate-pdf-renderer.test.ts — the exported full-coverage fixture layout from plan 11-04, reused here so the integration test does not invent a second layout.
    - .planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md — the CRD-03 and Concurrency rows, which name these two file paths and their exact commands.
  </read_first>
  <action>
    Create `tests/certificate-download.integration.test.ts` against real Postgres and real object storage:
    seed a learner, an enrolment, a satisfied `CompletionRecord`, a Course with `AUTOMATIC` mode and the
    seeded template; issue a certificate for real; assert the object exists in storage at the recorded
    `storageKey`; resolve a real presigned URL through `presignCertificateObjectUrl`; fetch it and assert
    the returned bytes begin with `%PDF` and contain the learner's name; then assert a second learner's
    request through `getOwnCertificateForDownload` returns null.

    Create `tests/certificate-concurrency.integration.test.ts`: seed one enrolment and fire two genuinely
    concurrent `issueCertificateForEnrolment` calls in separate transactions against the same real
    Postgres. Assert afterwards that exactly one `Certificate` row exists with `status: "ACTIVE"` for that
    enrolment and scope, and that the losing call returned the `already-issued` outcome rather than
    throwing. Add a second case doing the same for a Programme-scope issuance.

    The concurrency test must use real concurrent transactions — `Promise.all` over two independent
    `$transaction` calls — not two sequential calls. A sequential version proves nothing the unit suite
    did not already prove; the whole point is that Postgres READ COMMITTED lets both transactions observe
    "no row" and only the unique index arbitrates.

    **If Docker or object storage is unavailable in the execution environment:** write both files
    completely and correctly, then report in the plan summary that they were NOT executed and name the
    blocker. Do not mark them green, do not `.skip` them silently, and do not weaken them into fake-backed
    tests to make them run. This project has an explicit history of this exact failure mode, recorded in
    STATE.md against Phase 6.
  </action>
  <verify>
    <automated>npx vitest run tests/certificate-download.integration.test.ts tests/certificate-concurrency.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at the exact paths 11-VALIDATION.md names.
    - The concurrency test issues two calls via `Promise.all` over two separate transactions: `grep -c "Promise.all" tests/certificate-concurrency.integration.test.ts` is at least 1.
    - The concurrency assertion counts `ACTIVE` rows and asserts exactly 1 — not "at least 1", not "no error thrown".
    - The download test fetches the presigned URL and asserts on the real returned bytes, not on the URL string.
    - Neither file contains `.skip` or `.todo`.
    - Either both suites executed and passed, OR the plan summary states explicitly that they did not run, names the blocker (e.g. Docker unavailable), and records the phase as having outstanding real-infrastructure verification. Both outcomes are acceptable; a false green is not.
  </acceptance_criteria>
  <done>The duplicate-issuance race and the real download round trip are covered by honest integration tests whose execution status is reported truthfully.</done>
</task>

<task type="auto">
  <name>Task 2: Phase invariants test and validation reconciliation</name>
  <files>tests/certificate-phase-invariants.test.ts, .planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md, .planning/ROADMAP.md</files>
  <read_first>
    - tests/checkout-phase-invariants.test.ts — Phase 6's TypeScript-compiler-API invariants test, this project's established pattern for turning a phase's structural decisions into an executable gate. It found two real pre-existing violations on its first run; copy its approach, including the directory-prefix exemption mechanism.
    - tests/boundary.test.ts — the closure assertions added in plan 11-10, so the invariants test complements rather than duplicates them.
    - .planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md — the Per-Task Verification Map, whose every row is currently `TBD`/`⬜ pending`.
    - .planning/ROADMAP.md — the Phase 11 entry, its Plans line and Plans list, and the Progress table row.
    - All eleven `11-*-SUMMARY.md` files produced by the prior plans, to reconcile what was planned against what was actually built.
  </read_first>
  <action>
    Create `tests/certificate-phase-invariants.test.ts` asserting this phase's structural decisions
    mechanically, each with a message naming the decision it protects:

    1. **The PDF library has exactly one importer** — `src/server/services/certificate-pdf-renderer.ts`
       (D-08; prevents a second rendering path appearing).
    2. **No browser-automation package is imported anywhere in `src/`** — no `puppeteer`, `playwright`,
       `playwright-core`, or `chromium` (D-08's explicit rejection; the `playwright-cli` skill is a test
       tool and lives outside `src/`).
    3. **`verificationRef` values are generated in exactly one place** — only
       `certificate-reference.ts` produces them; no other file calls `randomUUID` into a field named
       `verificationRef` (Pitfall 1's entropy decision cannot be locally re-derived at lower strength).
    4. **No route file under `src/app/verify/` or `src/app/api/certificates/` exports `dynamic`,
       `revalidate`, or `fetchCache`, or references `'use cache'`** (Pitfall 6 — Next 16 Cache Components
       must never freeze a verification verdict or a presigned URL).
    5. **The public verification service selects exactly four `Certificate` fields** — parse
       `certificate-verification-service.ts`'s Prisma `select` and assert its key set is
       `{status, learnerName, awardTitle, issuedAt}` (CRD-04's disclosure contract).
    6. **`Certificate` is never hard-deleted** — no `certificate.delete`/`deleteMany` call anywhere in
       `src/` (CAT-08, CRD-05).
    7. **`Enrolment.status` is set to `COMPLETED` by exactly one module** —
       `certificate-issuance-service.ts` (D-05).

    Then reconcile `11-VALIDATION.md`: fill in every `Task ID`, `Plan`, `Wave`, `Threat Ref` and `File
    Exists` cell from what was actually built, set each row's status honestly (`✅ green` only for suites
    that actually executed; leave integration rows explicitly marked if Task 1 reported a blocker), tick
    the Validation Sign-Off boxes that are genuinely true, and set `nyquist_compliant: true` in the
    frontmatter only if every task in this phase genuinely carried an automated verify.

    Update `.planning/ROADMAP.md`'s Phase 11 entry: the Plans line to "16 plans across 6 waves", the Plans
    list with each plan's one-line objective grouped by wave, and the Progress table row to 16/16. Do not
    mark the phase Complete here — that is `/gsd:verify-work`'s call, not this plan's.
  </action>
  <verify>
    <automated>npx vitest run tests/certificate-phase-invariants.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/certificate-phase-invariants.test.ts` passes with all seven invariants as separate named cases.
    - Each invariant's failure message names the decision or pitfall it protects (D-number or Pitfall number), so a future failure is self-explaining.
    - Invariants 1, 3 and 7 are exact-set assertions, not "contains" checks.
    - `npm test` (full suite) green.
    - `11-VALIDATION.md` has zero remaining `TBD` cells and zero `⬜ pending` rows whose underlying test actually ran.
    - `11-VALIDATION.md`'s `nyquist_compliant` frontmatter value matches reality — if any task shipped without an automated verify, it stays `false` and the summary says which.
    - `.planning/ROADMAP.md`'s Phase 11 Plans line reads "16 plans" and the Progress table shows 16/16, with the status still "Not started"/"In progress" rather than "Complete".
  </acceptance_criteria>
  <done>Seven structural decisions are executable gates, the validation record reflects what was built, and the roadmap is reconciled without prematurely claiming completion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser walkthrough of the certificate lifecycle</name>
  <read_first>
    - .planning/phases/11-certificates-completion-lifecycle/11-VALIDATION.md — the Manual-Only Verifications table: template-editor canvas mechanics and PDF visual fidelity are explicitly manual, because automated tests can assert byte structure but not visual correctness.
    - .planning/phases/11-certificates-completion-lifecycle/11-UI-SPEC.md — §8's three backstop rows (long learner names on the verify page, the full-entropy reference wrapping without a horizontal scrollbar, and editor responsiveness at high element counts) and the one unresolved long-text row.
    - .claude/skills/playwright-cli/SKILL.md — the project's browser-automation skill, if a scripted walkthrough is preferred over a manual one.
  </read_first>
  <action>
    Hand the ten-step walkthrough below to the human and stop. Do not perform the steps yourself, do not
    infer outcomes from the passing test suite, and do not mark any step passed that was not actually
    performed in a browser. When the results come back, transcribe them step by step into
    `11-16-SUMMARY.md`, and record every failure as a gap for `/gsd:verify-work` rather than fixing it
    inside this checkpoint.
  </action>
  <what-built>
    The full certificate lifecycle is implemented and every automated gate is green:
    - Schema, migration, and the partial unique index closing the duplicate-issuance race
    - A template library and a keyboard-accessible drag/position editor
    - Automatic issuance on completion, and a manual pending-issuance queue
    - PDF generation, object storage, and an access-controlled download
    - A public verification page with three outcomes and minimal disclosure
    - Revoke and reissue with mandatory reasons and a preserved supersede chain
    - Review flags on grade, attendance and completion corrections, with the enrolment reverting to ACTIVE
  </what-built>
  <how-to-verify>
    Work through these in a browser against seeded data. Steps 6-9 are the visual checks no automated test
    can make.

    1. **Template editor** — go to `/staff/certificates/templates`, create a new template. Add a border,
       an image (upload a logo), and all four dynamic fields. Drag each to position. Then, using ONLY the
       keyboard (Tab to an element, arrow keys, Shift+arrow), move an element and delete one. Save.
       Reopen it and confirm every position persisted exactly.
    2. **Issuance mode** — on a Course's edit page, set certificates enabled, issuance mode MANUAL, and
       select your new template. Save.
    3. **Manual queue** — drive a learner to completion on that Course. Confirm they appear at
       `/staff/certificates`. Click "Issue certificate", confirm. Confirm the row disappears.
    4. **Learner download** — sign in as that learner, open `/dashboard`, and download the certificate.
       Open the PDF.
    5. **Public verification** — copy the verification reference off the PDF. Open `/verify` in a private
       window (signed out), type the reference, and confirm it reports valid with exactly three facts and
       no sign-in prompt or catalogue nav. Then try a made-up reference and confirm it reports not-found
       with no other fields at all.
    6. **PDF visual fidelity** — compare the downloaded PDF against the template you designed in step 1.
       Are the elements where you put them? Are the fonts, colours and sizes right? Is the learner's real
       name there, not a placeholder?
    7. **Backstop — long strings** — check that the verification reference wraps rather than overflowing
       on the verify page, the issued list, and the detail page, at both desktop and mobile widths, with
       no horizontal scrollbar. Seed or use a long learner name and a long award title and check the same.
    8. **Backstop — editor at scale** — add roughly 20 elements to a template and confirm keyboard nudge
       and inspector edits stay responsive with no visible input lag.
    9. **Revoke and reissue** — at `/staff/certificates/issued`, open the certificate, revoke it with a
       reason. Confirm: the public verify page now reports revoked and does NOT show your reason; the
       learner's dashboard no longer offers a download; the enrolment is back to ACTIVE. Then reissue it.
       Confirm the new certificate has a different reference, the old detail page links to the new one and
       vice versa, and the old reference now verifies as revoked while the new one verifies as valid.
    10. **CRD-06 flag** — override a released grade for a learner holding an active certificate. Confirm
        the certificate shows "Flagged for review" (not "Active") in the issued list and on its detail
        page, that the learner's dashboard shows the reassuring copy and STILL offers the download, and
        that there is no "Clear flag" button anywhere.

    Report per step: passed, failed with what you saw, or blocked with why. Do not report a step as passed
    if you did not actually perform it — this project's history records fabricated walkthrough results as
    a real cost.
  </how-to-verify>
  <acceptance_criteria>
    - Every one of the ten steps has an explicit outcome recorded in the plan summary: passed, failed (with the observed behavior), or blocked (with the blocker).
    - The three UI-SPEC §8 backstops (steps 7 and 8) are each individually resolved or explicitly recorded as still-unverified with the reason.
    - Any failure found is recorded as a gap for `/gsd:verify-work` rather than fixed silently inside this checkpoint.
    - Steps 6 and 9's visual/behavioral observations are described in the summary in the human's own words, not restated from this plan's text.
  </acceptance_criteria>
  <verify>
    <human-check>All ten walkthrough steps have a recorded outcome (passed / failed with observation / blocked with reason) in 11-16-SUMMARY.md, including the three UI-SPEC §8 backstops.</human-check>
    <automated>npm test</automated>
  </verify>
  <done>A human has driven the certificate lifecycle in a browser and every step has an honest recorded outcome.</done>
  <resume-signal>Reply with the per-step results, or "approved" if all ten passed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test environment → claimed coverage | An unexecuted integration test reported as green is a false security assurance |
| structural convention → future edits | Decisions held only by comment erode; only executable gates survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Duplicate certificate race | mitigate | Task 1's concurrency suite exercises the partial unique index with genuinely concurrent transactions against real Postgres — the one control a fake-backed test cannot validate |
| T-11-70 | Repudiation | False-green integration coverage | mitigate | Task 1's acceptance criteria accept either real execution or an explicit blocked report, and forbid `.skip`/`.todo` and fake-backed weakening. The Phase 6 precedent is named in the action so the executor knows this is a known failure mode here |
| T-11-08 | Information Disclosure | Verification-reference entropy silently downgraded later | mitigate | Invariant 3 asserts references are generated in exactly one module |
| T-11-22 | Information Disclosure | Public verification disclosure widened by a future `select`-less refactor | mitigate | Invariant 5 parses the actual `select` clause and asserts an exact four-key set |
| T-11-24 | Spoofing | A caching directive added later freezing a verification verdict | mitigate | Invariant 4 scans both new route segments for `dynamic`/`revalidate`/`fetchCache`/`'use cache'` |
| T-11-17 | Tampering | A headless-browser rendering path reintroduced | mitigate | Invariants 1 and 2, as exact-set and zero-match assertions |
| T-11-31 | Tampering | A second module learning to write `Enrolment.status = COMPLETED` | mitigate | Invariant 7 asserts exactly one writer (D-05) |
| T-11-20 | Tampering | Hard deletion of a certificate | mitigate | Invariant 6 scans all of `src/` |
| T-11-NFR09 | Accessibility (NFR-09) | Keyboard-only template authoring regressing | mitigate | Walkthrough step 1 requires the human to complete a full authoring pass using only the keyboard; the automated keyboard cases in plan 11-12 are the regression net beneath it |
| T-11-SC | Tampering | npm installs | mitigate | This plan installs nothing; `git diff package.json` must be empty |
</threat_model>

<verification>
- `npm test` (full suite) green.
- `npx vitest run tests/certificate-phase-invariants.test.ts` green with all seven invariants.
- The two integration suites either executed and passed, or their non-execution is explicitly reported with a named blocker.
- `npx next build` clean.
- `npx tsc --noEmit` exits 0.
- `npx eslint .` clean.
- The human walkthrough has a recorded outcome for all ten steps.
</verification>

<success_criteria>
The race and the real download are proven against real infrastructure or honestly reported as unproven.
Seven structural decisions are executable gates. The validation record and roadmap reflect what was
actually built. A human has watched a certificate issue, download, verify, revoke, reissue and flag.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-16-SUMMARY.md` when done
</output>

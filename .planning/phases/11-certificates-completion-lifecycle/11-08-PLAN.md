---
phase: 11-certificates-completion-lifecycle
plan: 08
type: execute
wave: 3
depends_on: ["11-01", "11-05"]
files_modified:
  - src/app/staff/courses/CourseForm.tsx
  - src/app/staff/courses/actions.ts
  - src/app/staff/programmes/ProgrammeForm.tsx
  - src/app/staff/programmes/actions.ts
  - src/server/services/course-service.ts
  - src/server/services/programme-service.ts
  - tests/certificate-settings-form.test.tsx
autonomous: true
requirements: [CRD-01, CRD-02, CRD-03]

must_haves:
  truths:
    - "Staff can choose whether a Course issues certificates automatically or on manual sign-off"
    - "Staff can choose which template a Course or Programme issues"
    - "Only non-archived templates are offered in the picker"
    - "A Course with certificates disabled offers no issuance-mode or template choice"
  artifacts:
    - path: "src/app/staff/courses/CourseForm.tsx"
      provides: "certificateIssuanceMode segmented control + certificateTemplateId select"
      contains: "certificateIssuanceMode"
    - path: "src/app/staff/programmes/ProgrammeForm.tsx"
      provides: "the same two controls for Programmes"
      contains: "certificateIssuanceMode"
  key_links:
    - from: "src/app/staff/courses/actions.ts"
      to: "src/server/services/certificate-template-service.ts"
      via: "listSelectableTemplates populating the picker"
      pattern: "listSelectableTemplates"
---

<objective>
Expose D-02's issuance mode and D-10's template selection on the Course and Programme edit forms, so the
fields plan 11-01 added to the schema become staff-operable rather than database-only.

Purpose: Without this, every Course and Programme stays on the `MANUAL` default forever and always uses
the default template — D-02's "one Course can auto-issue while another Programme requires sign-off" is
unreachable.
Output: Two form surfaces, two action/service field allow-list updates, and a component test.
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-certificates-completion-lifecycle/11-CONTEXT.md
@.planning/phases/11-certificates-completion-lifecycle/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Course certificate settings — issuance mode and template picker</name>
  <files>src/app/staff/courses/CourseForm.tsx, src/app/staff/courses/actions.ts, src/server/services/course-service.ts</files>
  <read_first>
    - src/app/staff/courses/CourseForm.tsx — full file. Find the existing `certificateEnabled` control (it already exists on `Course`, default `false`) and read how the form groups related fields, how `FormField`/`TextInput`/select controls are composed, and how error state is surfaced. The new controls sit adjacent to `certificateEnabled`.
    - src/app/staff/courses/actions.ts — the server action's zod schema and its `.strict()` field allow-list. A new form field that is not added here is silently dropped, which is the most likely way this task ships half-done.
    - src/server/services/course-service.ts — the update path and any field allow-list it applies before the delegate write.
    - src/server/services/certificate-template-service.ts — `listSelectableTemplates()` (plan 11-05), which returns non-archived templates only.
    - src/components/primitives/ResourceForm.tsx — the `FormField`/`TextInput` contract and the 38px / `border-input-border` control treatment (04.1 §7.3, NFR-09 3:1 edge).
    - .planning/phases/11-certificates-completion-lifecycle/11-UI-SPEC.md — §0.1's Course/Programme row, §2's "`certificateIssuanceMode` segmented control, `certificateTemplateId` select — no new form chrome", and §2's note that the segmented control reuses 04.1's restyled native controls rather than a new widget.
    - .planning/phases/11-certificates-completion-lifecycle/11-CONTEXT.md — D-02 and D-10.
  </read_first>
  <action>
    Add two controls to `CourseForm.tsx`, directly beneath the existing `certificateEnabled` control:

    1. **Issuance mode** — label "Certificate issuance", a two-option segmented control with values
       `AUTOMATIC` and `MANUAL`. Option copy: "Automatic — issue as soon as the learner completes" and
       "Manual — a staff member signs off first". Use the existing segmented-control treatment
       `ResourceTable`'s filter already uses for ≤4 options; do not introduce a new widget component.
    2. **Certificate template** — label "Certificate template", a native `<select>` populated from
       `listSelectableTemplates()`, with a first option "Use the default template" mapping to a null
       `certificateTemplateId`. Each option's label is the template `name`, with the default template's
       label suffixed " (default)".

    Both controls render disabled (or hidden, matching however the form already treats dependent fields —
    read it and follow the existing convention rather than inventing one) when `certificateEnabled` is
    false: an issuance mode for a Course that issues nothing is a meaningless choice, and UI-SPEC's
    "nothing here promises a page the product cannot open" restraint applies.

    Then close the plumbing, which is where this kind of change usually fails silently:
    - Add both field names to the zod schema in `src/app/staff/courses/actions.ts`.
      `certificateIssuanceMode` is `z.enum(["AUTOMATIC","MANUAL"])`; `certificateTemplateId` is
      `z.string().min(1).nullable()`. The schema is `.strict()`, so an unlisted field is rejected, not
      passed through.
    - Add both to whatever field allow-list `course-service.ts` applies on update. Verify by reading the
      update path — if it forwards `data` wholesale, no change is needed there, and say so in the summary
      rather than adding a speculative edit.
    - Server-side, reject a `certificateTemplateId` that names an archived template (Pitfall 5: an
      archived template may still be *referenced* by history, but must not be newly *selected*). Resolve
      the submitted id against `listSelectableTemplates()` server-side; do not trust that the client only
      offered valid options.
  </action>
  <verify>
    <automated>npx vitest run tests/certificate-settings-form.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "certificateIssuanceMode" src/app/staff/courses/actions.ts` is at least 1 and the field appears inside the zod schema object, not only in a type.
    - `grep -c "certificateTemplateId" src/app/staff/courses/actions.ts` is at least 1.
    - `grep -c "listSelectableTemplates" src/app/staff/courses/` is at least 1.
    - Submitting an archived template's id through the action is rejected server-side (assert with a test case; the rejection must not depend on the client's option list).
    - `grep -rcE "#[0-9a-fA-F]{6}" src/app/staff/courses/CourseForm.tsx` returns 0.
    - Both new controls carry an associated `<label>` (or `FormField` label) — assert via the component test querying by accessible name, not by class.
    - `npx tsc --noEmit` exits 0.
    - The existing Course form test suite (grep `tests/` for a CourseForm test) is still green.
  </acceptance_criteria>
  <done>A Course's issuance mode and template are staff-editable, persist through the action's strict schema, and an archived template cannot be selected even by a hand-crafted request.</done>
</task>

<task type="auto">
  <name>Task 2: Programme certificate settings — the same two controls</name>
  <files>src/app/staff/programmes/ProgrammeForm.tsx, src/app/staff/programmes/actions.ts, src/server/services/programme-service.ts, tests/certificate-settings-form.test.tsx</files>
  <read_first>
    - src/app/staff/courses/CourseForm.tsx — as changed by Task 1; this task mirrors it. Read the finished Task 1 implementation so the two forms are consistent rather than independently invented.
    - src/app/staff/programmes/ProgrammeForm.tsx — full file, including its existing `certificateEnabled` control (note: `Programme.certificateEnabled` defaults to `true`, unlike Course's `false` — the dependent-field behavior therefore shows these controls by default here).
    - src/app/staff/programmes/actions.ts — its zod schema and `.strict()` allow-list.
    - src/server/services/programme-service.ts — the update path's field handling.
    - .planning/phases/11-certificates-completion-lifecycle/11-UI-SPEC.md — §0.1's Programme row.
  </read_first>
  <action>
    Mirror Task 1 exactly on the Programme side: the same two controls with the same labels and option
    copy, the same dependent-on-`certificateEnabled` behavior, the same zod fields added to
    `src/app/staff/programmes/actions.ts`, the same archived-template server-side rejection.

    Extract the two controls into one shared component (e.g. `src/components/catalogue/CertificateSettingsFields.tsx`)
    consumed by both forms rather than duplicating the markup — `src/components/catalogue/` already holds
    `LessonFormFields.tsx` and `AssessmentFormFields.tsx` as the precedent for exactly this shape of
    shared form-fragment. Refactor Task 1's inline version into it as part of this task, so only one
    implementation of these controls exists at the end of the plan.

    Extend `tests/certificate-settings-form.test.tsx` to cover both consumers: the shared component
    renders both controls, disables them when certificates are off, offers "Use the default template",
    and labels the default template.
  </action>
  <verify>
    <automated>npx vitest run tests/certificate-settings-form.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one file defines the issuance-mode + template controls: `grep -rl "Certificate issuance" src/app/ src/components/` returns exactly one path.
    - Both `CourseForm.tsx` and `ProgrammeForm.tsx` import that shared component.
    - `grep -c "certificateIssuanceMode" src/app/staff/programmes/actions.ts` is at least 1; same for `certificateTemplateId`.
    - The component test asserts: both controls render; both are disabled when `certificateEnabled` is false; the template select's first option maps to a null id; the default template's option label ends with "(default)".
    - `npx tsc --noEmit` exits 0.
    - The existing Programme form test suite is still green.
  </acceptance_criteria>
  <done>Programmes have the same two settings as Courses, from one shared component, with one test file covering both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → catalogue mutation action | Form fields cross a `.strict()` zod boundary into a service update |
| client-offered option list → persisted foreign key | The template id the client submits is not trustworthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-33 | Tampering | `certificateTemplateId` naming an archived template via a hand-crafted request | mitigate | Server-side resolution against `listSelectableTemplates()` in the action, not reliance on the rendered option list (Pitfall 5); asserted by a dedicated test case |
| T-11-34 | Elevation of Privilege | Switching a Course to `AUTOMATIC` without catalogue-edit authority | mitigate | Both fields ride the existing `courses.edit` / `programmes.edit` action and service path — no new endpoint, no new permission, and the existing `withPermission` gate applies unchanged |
| T-11-35 | Tampering | Arbitrary string persisted as an issuance mode | mitigate | `z.enum(["AUTOMATIC","MANUAL"])` in the `.strict()` schema, plus the Prisma enum column from plan 11-01 |
| T-11-36 | Repudiation | Silent change to how an institution issues credentials | mitigate | Both fields flow through the resource factory's existing audit-first update, which records before/after and the actor |
| T-11-SC | Tampering | npm installs | mitigate | This plan installs nothing; `git diff package.json` must be empty |
</threat_model>

<verification>
- `npx vitest run tests/certificate-settings-form.test.tsx` green.
- Existing Course and Programme form/action test suites still green.
- `npx tsc --noEmit` exits 0.
- `npx next build` clean.
</verification>

<success_criteria>
Issuance mode and template are staff-editable on both Courses and Programmes from one shared component,
persist through strict schemas, and reject archived templates server-side.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-08-SUMMARY.md` when done
</output>

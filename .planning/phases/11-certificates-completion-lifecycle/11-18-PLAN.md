---
phase: 11-certificates-completion-lifecycle
plan: 18
type: execute
wave: 8
depends_on: ["11-17"]
files_modified:
  - src/app/(learner)/dashboard/page.tsx
  - src/components/learner/NextUpCard.tsx
  - tests/learner-dashboard-page.test.ts
  - tests/certificate-download.integration.test.ts
autonomous: true
gap_closure: true
requirements: [CRD-03]

must_haves:
  truths:
    - "After issuance the learner's dashboard still shows the course with a Certificate slot containing 'Download certificate' and the verification reference (UAT test 10)"
    - "A COMPLETED card never shows a link the learner cannot open: no Continue-learning, no sessions 'View all', no Results 'View all', no assessment lesson links"
    - "The 'Next up' card for a completed course no longer says certificates are yet to ship (UAT test 19)"
    - "The regression is proven against the status the issuance service really writes, in a real database, not only against a hand-written fixture"
  artifacts:
    - path: "src/app/(learner)/dashboard/page.tsx"
      provides: "COMPLETED-aware EnrolmentSection"
      contains: "enrolmentStatus"
    - path: "src/components/learner/NextUpCard.tsx"
      provides: "Current, accurate copy for the 'complete' variant"
    - path: "tests/certificate-download.integration.test.ts"
      provides: "Issue-for-real then load-the-dashboard round trip on real Postgres"
      contains: "loadLearnerDashboard"
  key_links:
    - from: "src/app/(learner)/dashboard/page.tsx"
      to: "src/components/learner/CertificateSlot.tsx"
      via: "CertificateSlot certificate={card.certificate} rendered for COMPLETED cards"
      pattern: "CertificateSlot"
---

<objective>
Finish UAT test 10 at the surface and close UAT test 19: render the COMPLETED card that plan 11-17 now supplies, fix the
stale "Next up" copy, and prove the whole chain end to end with the status issuance really writes.

Purpose: 11-17 makes the card exist; without this plan the page would still render dead links (lesson, sessions,
results) on a card the learner cannot operate, and the completed-course copy would still promise certificates that
already exist. The integration case is the guard against this class of escape: every earlier test hand-wrote an ACTIVE
enrolment, while the real issuance path writes COMPLETED.

Output: A COMPLETED-aware dashboard section, corrected NextUpCard copy, a page test, and a real-Postgres round-trip test.
Applies decision G-01 from plan 11-17 (COMPLETED is visible, not operable).
</objective>

<execution_context>
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/disuk/Learning-Management-System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-certificates-completion-lifecycle/11-UAT.md
@.planning/phases/11-certificates-completion-lifecycle/11-17-PLAN.md
@src/app/(learner)/dashboard/page.tsx
@src/components/learner/NextUpCard.tsx
@src/components/learner/CertificateSlot.tsx

<interfaces>
<!-- Contract delivered by plan 11-17; consume it, do not re-derive. -->

From src/server/services/enrolment-dashboard-service.ts (after 11-17):
- `LearnerDashboardCard` gains `enrolmentStatus: "ACTIVE" | "COMPLETED"`.
- For a COMPLETED card: `nextAction` is `{ kind: "complete" }`; `assessmentObligations` and `results` are the deferred constants and MUST NOT be rendered; `certificate` is the real `CertificateColumn`
  (`not-complete` | `pending-issuance` | `issued` | `flagged` | `revoked`, where issued/flagged carry certificateId, verificationRef, issuedAt).

From src/components/learner/CertificateSlot.tsx: the issued/flagged branches render a link with href `/api/certificates/{certificateId}/download` and the text "Download certificate".

Existing page-test convention (tests/learner-dashboard-page.test.ts): Page is invoked directly and rendered with `renderToStaticMarkup`; `next/link` is mocked to a plain anchor; `loadLearnerDashboard` is mocked; the `card()` fixture there has NO enrolmentStatus, so the page must treat an absent value as ACTIVE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COMPLETED-aware dashboard section and corrected Next-up copy</name>
  <files>src/app/(learner)/dashboard/page.tsx, src/components/learner/NextUpCard.tsx, tests/learner-dashboard-page.test.ts</files>
  <read_first>
    - src/app/(learner)/dashboard/page.tsx — EnrolmentSection (~L226), AccessNoticeBanner, UpcomingSessionsSection (its "View all" link to /learn/{id}/sessions), AssessmentsCard (lesson links), ResultsCard ("View all" to /learn/{id}/results).
    - src/components/learner/NextUpCard.tsx — the `complete` variant copy (~L59-64).
    - src/components/learner/CertificateSlot.tsx — its five states and the exact download markup.
    - tests/learner-dashboard-page.test.ts — the `card()` fixture and the `renderPage` helper; existing assertions about ACTIVE cards (Assessments/Results/sessions) must keep passing.
    - src/server/services/learner-session-service.ts and src/server/services/learner-results-service.ts headers — confirm both still resolve ownership through ACTIVE-only checks, which is why their links must not render on a COMPLETED card (decision G-01).
    - AGENTS.md is a Next.js-breaking-changes notice: this task changes only server-component markup inside an existing page (no new route, no new API), so no docs read is required, but do not introduce new Next APIs.
  </read_first>
  <behavior>
    - A card with enrolmentStatus "COMPLETED" and certificate { kind: "issued", certificateId: "cert-1", verificationRef: "CERT-ABC", issuedAt } renders a link to /api/certificates/cert-1/download with text "Download certificate" and the text CERT-ABC.
    - The same card renders no "Upcoming sessions", no "Assessments", no "Results" section, no "Continue learning" link, and no access-window banner.
    - It still renders the cohort title, the Next-up card, the progress section and the Support tickets deferred slot.
    - A card with no enrolmentStatus (the legacy fixture) or "ACTIVE" renders exactly what it rendered before.
    - The Next-up "complete" variant contains neither "certificates ship" nor "once certificates" and points to the Certificate section below.
  </behavior>
  <action>
    Applying G-01 (COMPLETED is visible, not operable): in `EnrolmentSection` compute
    `const completed = card.enrolmentStatus === "COMPLETED"` (a strict equality, so an absent value is ACTIVE and every
    existing fixture is unaffected). When `completed`: skip `AccessNoticeBanner` (an access-window notice on a finished
    course is noise and could contradict the certificate), skip `UpcomingSessionsSection` (its "View all" links to
    `/learn/{id}/sessions`, which resolves ownership through ACTIVE-only checks and would 404), render `ProgressSection`
    alone (drop the two-column grid wrapper or let it hold one child), and render in the lower grid only the
    `DeferredSlot` for Support tickets and `CertificateSlot`, omitting `AssessmentsCard` and `ResultsCard` (their links go to
    lesson and results pages that are closed to a COMPLETED enrolment, and their data is deliberately not loaded). Keep the
    ACTIVE branch's markup identical. Add a short comment naming G-01 and explaining why each omission exists.

    In `NextUpCard.tsx` replace the `complete` variant paragraph with: "You've completed everything required here. Your
    certificate, if this course issues one, is shown in the Certificate section below." Keep `&apos;` escaping in the JSX.
    The sentence must be true in every certificate state the slot can show (not-complete-yet, awaiting staff sign-off under
    MANUAL, issued, flagged, revoked), so it must not promise a download. Update the file-level or component comments only
    if they mention certificates shipping.

    Tests, written first in `tests/learner-dashboard-page.test.ts`: (1) a COMPLETED card with an issued certificate asserts
    the download href and text, the verification reference and the omitted sections/links listed above; (2) a card with
    nextAction `complete` asserts the new copy is present and the strings "certificates ship" and "once certificates" are
    absent; (3) an ACTIVE card in the same file still renders Assessments, Results and Upcoming sessions (guards against
    over-hiding). Confirm (1) and (2) fail against the current page before changing it.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/learner-dashboard-page.test.ts tests/components/certificate-slot.test.tsx"</automated>
  </verify>
  <done>A COMPLETED card shows the certificate slot with a working download link and verification reference and no dead links; the Next-up copy no longer mentions certificates shipping; ACTIVE cards render unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Real-Postgres regression — issue for real, then load the dashboard</name>
  <files>tests/certificate-download.integration.test.ts</files>
  <read_first>
    - tests/certificate-download.integration.test.ts — its header (env-var-before-import hazard: it takes NO static import of any app module; app modules come in through one dynamic `await import(...)` block after DATABASE_URL is set), how it seeds learner/cohort/enrolment/completion record/course/template, and how it calls `issueCertificateForEnrolment`.
    - tests/support/pg.ts and tests/support/cohort-fixtures.ts — `startTestDatabase`, `seedCohortFixture`, `seedEnrolmentFixture`, `seedLearnerFixture` signatures.
    - src/server/services/enrolment-dashboard-service.ts — the exported `loadLearnerDashboard(actor)`; it imports `@/server/db`, so it must be pulled in by the same dynamic import block.
    - src/server/services/learner-access.ts — `hasActiveEnrolmentCoveringCourse`.
    - .planning/phases/11-certificates-completion-lifecycle/11-16-SUMMARY.md — Docker/MinIO status and the rule that an unexecuted integration suite must be reported as NOT run, never as green.
  </read_first>
  <action>
    Add one `describe` (or `it` group) to the existing suite, reusing its seeded fixtures and its existing real issuance
    call — do not create a second harness and do not add any static import of an app module. After the existing issuance
    has run for real: (a) read the enrolment row directly and assert its `status` is "COMPLETED" — this line is the proof that
    the fixture status is the one production writes, which is exactly what every earlier unit test got wrong; (b) dynamically
    import `loadLearnerDashboard` in the same import block the file already uses and call it for the owning learner
    (`{ userId }` actor shape used elsewhere in the file); assert exactly one card for that enrolment, `enrolmentStatus`
    "COMPLETED", and `certificate` equal to `{ kind: "issued", certificateId: <the issued id>, verificationRef: <issued ref>, ... }`;
    (c) assert a second, unrelated learner's dashboard contains no card for that enrolment; (d) assert
    `hasActiveEnrolmentCoveringCourse(userId, courseId)` is false for the COMPLETED-only learner, pinning decision G-01
    (visible, not operable) against real rows. Do not weaken any assertion into a fake-backed one and do not add `.skip`/`.todo`.

    Run the suite. If Docker or MinIO is unavailable, write the code completely, do NOT mark it green, and record in the
    plan SUMMARY that this task's suite was not executed and why, per the 11-16 precedent; the unit and page tests in
    11-17 and Task 1 remain the executed proof in that case.
  </action>
  <verify>
    <automated>powershell.exe -NoProfile -Command "npx vitest run tests/certificate-download.integration.test.ts"</automated>
  </verify>
  <done>After a real issuance the learner's dashboard has a COMPLETED card with certificate.kind "issued" and the issued certificate's id, a stranger sees nothing, and the COMPLETED-only learner has no lesson access; or the SUMMARY states plainly that the suite could not run and why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard page -> learner-facing links | A rendered link must never point at a route the viewer's enrolment state cannot open |
| learner -> certificate download | Only the owner's certificate id may be linked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-74 | Information Disclosure | Dashboard card linking another learner's certificate | mitigate | The card's certificate comes from the ownership-scoped batch in 11-17; the integration case asserts a stranger's dashboard has no card |
| T-11-75 | Elevation of Privilege | Dead links inviting lesson/results access on a COMPLETED enrolment | mitigate | Page omits those sections for COMPLETED; the routes themselves still refuse (11-17); page test asserts absence |
| T-11-76 | Repudiation | Learner told certificates are 'coming' when one exists | mitigate | Copy corrected and asserted absent by test |
| T-11-SC | Tampering | npm installs | mitigate | This plan installs nothing; `git diff package.json` must be empty |
</threat_model>

<verification>
- `powershell.exe -NoProfile -Command "npx vitest run tests/learner-dashboard-page.test.ts tests/components/certificate-slot.test.tsx tests/certificate-download-route.test.ts"` green.
- `powershell.exe -NoProfile -Command "npx vitest run tests/certificate-download.integration.test.ts"` green, or reported as not run with the blocker named.
- `grep -rn "certificates ship" src` returns nothing.
- `powershell.exe -NoProfile -Command "npx tsc --noEmit"` exits 0.
</verification>

<success_criteria>
UAT tests 10 and 19 are closable by a browser re-check: a learner with an issued certificate sees the course card with a
Download certificate link and accurate copy. The regression is proven against real issuance output, so an ACTIVE-only
fixture can no longer hide it.
</success_criteria>

<output>
Create `.planning/phases/11-certificates-completion-lifecycle/11-18-SUMMARY.md` when done
</output>

// The verification pages share the public shell (catalogue nav, Sign in / Register), exactly as
// drawn in the mockup — so an employer who lands here can still find their way around.
export { default } from "@/app/(public)/layout";
